# Thirdlight — Status (packets 00–82, gates A–U)

Updated: 2026-09-22 — **M4 execution: packets 63–68 COMPLETE** (63: baseline
audit + reference-device route — [63.md](handoffs/63.md); 64: delivery
contract — [64.md](handoffs/64.md); 65: template contract —
[65.md](handoffs/65.md); 66: engine-kit/build contract —
[66.md](handoffs/66.md); 67: 67-A backup/diagnostics/recovery + 67-B
protocol-only — [67.md](handoffs/67.md); 68: the Gate Q review pack — full
diff inventory + traceability + the fixture audit (checkers green, 4
corrupted edges refused nonzero, 38-code registry consistent) + PROPOSED
decision 0004 — [68.md](handoffs/68.md); no commit made). **Gate Q is the
gate now**: the in-session model review is complete; the owner docket (8
decisions) awaits the owner's ruling; the docs-only promotion + `gate-q.md`
record land on the owner's acceptance; 69 starts on the owner's go under the
standing authorization.** Owner standing authorization for all of M4
recorded in the checkpoint.** Earlier (2026-09-21): M4 plan review ACCEPTED WITH BOUNDED FOLLOW-UPS
(PR-M4-1…PR-M4-6; in-session model review, not independent or owner approval)
and the owner's M4 execution authorization recorded separately
(`handoffs/m4-plan-review.md`); M4 execution started — packet 63 in progress.**
M3 execution complete through packet 62 and Gate P
accepted (in-session): contract pack promoted; packets 38–62 done (Gates
K/L/M/N/O/P accepted, in-session under the no-sub-session host constraint); the
in-container acceptance record is final (docs/acceptance/m3-report.md — B01–B24:
22 PASS, 2 PARTIAL, 0 FAIL). The physical/audible/hardware-GPU subclaims are
UNVERIFIED pending the owner desktop walkthrough (§6 annex). No commit made.** Earlier this date: M3 plan
review accepted with bounded follow-ups (`handoffs/m3-plan-review.md`) and the
owner M3 execution authorization recorded there; then **M3 Gate K ACCEPT WITH
BOUNDED FOLLOW-UPS; bounded repairs applied; docs-only contract promotion
performed; owner review pending**
(`handoffs/gate-k.md`, `handoffs/gate-k-repair.md`,
`handoffs/m3-promotion.md`): the 109 promotable inventory rows are promoted into
`docs/contracts/**` and the two new homes `gameplay.md`/`presentation.md` exist;
PM13 stays rejected. The verdict is the Gate K architectural review — a session
review, **not owner approval and not an independent human review** — recorded
under the owner's M3 execution authorization; the owner's final manual review and
the K-3 export-name confirmation are pending. Packets 44 (project-model v3,
31/31 contract fixtures executed), 45 (commands v3, 7/7 scenario + 3/3
no-change + 11/12 failure cases replayed), 46 (durable v3 workspace and
v2→v3 copy migration on the real filesystem), 49 (game flow and swept
gameplay zones) and 50 (runtime-owned safe respawn) are done; packet 51 is next
and not started.
M2 executed and reviewed end to end (packets 14–37,
Gates E–J recorded; acceptance pending only the owner desktop browser/hardware
walkthrough recorded in docs/acceptance/m2-report.md §8). Consolidated owner
brief: docs/handoffs/m2-owner-review.md. Earlier: 2026-09-18
(Gate B accepted 2026-09-18 with bounded follow-ups
BF-1…BF-5 — docs/fixture consistency only, no source — see
docs/handoffs/gate-b.md; apply via a repair step before packet 08.
Gate A record below: accepted 2026-09-17 with bounded follow-ups
F1–F4 — see Notes; **F1–F4 applied 2026-09-17** (commit `b6422e4`) —
repair recorded in the handoff 03 "Gate A follow-up repair" section;
React ruling and pre-review pass recorded in handoff 03).

Gate discipline (docs/planning/implementation-prompts.md): at each gate, stop
dependent implementation until review findings are resolved. A contract change
during later work reopens the relevant review gate. Unrun packets stay
**pending**; a completed handoff never auto-starts the next packet.

## M4 planning — reviewed 2026-09-21; execution authorized 2026-09-21

The owner reports M3 complete and requested M4 packets plus a final owner review
checklist (planning task, complete). **Plan review accepted with bounded
follow-ups 2026-09-21** (`handoffs/m4-plan-review.md` — in-session model review,
not independent or owner approval; PR-M4-1 applied docs-only, PR-M4-2/3 enforced
at 65/63, PR-M4-4/5/6 recorded; no blockers) and **the owner's M4 execution
authorization (packets 63–82, Gates Q–U, one session, no routine questions;
not an approval of unreviewed artifacts, numeric targets, behavior scope, K-3
or a later milestone) is recorded there separately**. Current M3 completion
evidence is Gate P / `acceptance/m3-report.md`, not the early
`handoffs/m3-owner-review.md` progress brief. The 20 proposed M4 packets
**63–82** and Gates **Q–U** are executed in order below; the plan's gate
dependency rules hold (blocking findings resolve before dependent work).

| Planning task | Status | Deliverable / handoff |
|---|---|---|
| M4 packet planning | complete draft | [Plan](planning/m4-plan.md), [packets 63–82](planning/m4-packets.md), [acceptance matrix](planning/m4-acceptance.md), [final owner checklist](acceptance/m4-owner-checklist.md); [planning handoff](handoffs/m4-planning.md) |
| M4 plan review | **accepted with bounded follow-ups 2026-09-21 (in-session; not owner/independent approval)** | [review](handoffs/m4-plan-review.md) — PR-M4-1…PR-M4-6; execution authorization recorded separately |

## M4 execution (2026-09-21)

| M4 gate | Covers | Status |
|---|---|---|
| Q | 63–68; baseline, exact contracts/fixtures, debt disposition | **CLOSED 2026-09-22** ([m4-promotion.md](handoffs/m4-promotion.md)): in-session model review (2 defects found + repaired in-packet), all 26 rows disposed (25 accepted + 11 no-change adjudications — the 25 includes the 11; 0 rejected/deferred), the 8-item docket ruled under owner pre-approval with conservative defaults (device + CC-55-3b stay owner-pending), docs-only promotion applied to 8 accepted files (project-model, workspace, sessions, commands, presentation, export, dependencies, acceptance/deployment incl. the §5 replacement) + decision 0004 status. Not an independent-reviewer approval; final owner manual review pending. Next: 69 |
| R | 69–71; delivered rendering/motion, query parity | pending |
| S | 72–76; template creation, layout/module separation, engine kit, independent game | pending |
| T | 77–80; recovery/backup, diagnostics, measured budgets; hardware-only partial route per plan §2.6 | pending |
| U | 81–82; integrated acceptance and executable owner runbook | pending |

| ID | M4 packet | Gate | Prerequisite | Status |
|---|---|---|---|---|
| 63 | Current-baseline audit and reference-device measurement path | Q | Plan review + execution authorization (2026-09-21) | **done 2026-09-22** ([63.md](handoffs/63.md)): reproducible M3-carry-forward baseline in a real browser (probe suite `tests/evaluations/m4-baseline/`; raw evidence `docs/acceptance/evidence-m4/63/raw/`): A01/A03/A06–A10, B00/B01/B01b/B03/B04 (held-KeyD motion in real runtime state — closes packet-62 B05), C01–C05/C11/C12 PASS; A05/B04b UNVERIFIED (SwiftShader); **C06/C07 FAIL captured (not repaired)** — the v3 play preview never mounts: D-63-5 (v3 wrapper never acks the bridge handshake ⇒ the editor never sends the snapshot), D-63-6 (v3 wrapper `tl.ready` body fails the §13.5 validator), D-63-4 (editor WS receive-only ⇒ no §5.2 heartbeat, no `play.preview.ready` ⇒ 15 s `preview_timeout` on every play), D-63-9 (the v3 snapshot document cannot carry the `game` block the M3 host requires) + D-63-2/7/8; M3 carry-forward reconciled (CC-55-3 split a/b, K-3 names recorded, P2-B re-verified, M3-GLB captured); stale docs separated (SD-63-1/2); reference-device route + proposed spec recorded (67-B protocol-only at Q; hardware rows UNVERIFIED). Ledger: `planning/m4-contracts/debt-ledger.md`; baseline: `baseline.md`; device: `reference-device.md`. No commit made. Next: **64** (under owner standing authorization)** |
| 64 | Delivered rendering, queries and compatibility repair contract | Q | 63 | **done 2026-09-22** ([64.md](handoffs/64.md)): PROPOSED delivery contract `planning/m4-contracts/delivery.md` + owned diff rows C64-1…C64-8 (six contracts; Gate Q). Decisive framing: D-63-4/5/6/9 are **implementation repairs against already-accepted text** (runtime.md §2 already mandates the v3 `game` snapshot field; sessions.md §13.4/§17.6/§10.2/§5.2 already mandate the bridge behavior) — CCR-63-1/2 refiled, no contract change. True additions: `querySettings` op (explicit/resolved maps, registry order, revision on every read, v1 ⇒ `{}`; the only settings source — kills the default-guess fallback), normative `queryProject` count semantics (P2-B; implementation returns no `content` at all), presentation §41.3.6 rule-7 clarification + non-player neutral pinning, adapter `models` option + closed codes + counters, dependencies §4.2 gltf-loader subpath rows for the two M3 bundles, export §5.2 entry-text owner scope decision (C64-5), runtime/sessions no-change adjudication records. Design of record: model bytes via wrapper → adapter options (no fetch in host/adapter); holder parenting (model root child of the entity holder); single loop (hostFrame → renderFrame → one clamped `update` per role controller); corrupt-vs-degraded L1–L9 closed table; both-host load order + pinned-reimport no-op; built-in-only behavior profile with explicit C14-style refusal (CC-55-3a/b owner decisions at Q). Fixtures `fixtures/m4/delivery/`: 5 GLBs (positive 3-clip + root-motion/skin/undeclared/corrupt negatives), 4 case files, index.json, committed generator + independent checker (no deps/eval/network/three) — checker green, corruption negative control exit 1, `npm test` 179/2237 green (an empty untracked `projects/` dir left by the 63 re-check in `fixtures/m3/storage/project-v3-demo-0003/` was found and removed; it had broken one workspace test via `cpSync`). C rows specified, executed by 69/70/71. No commit made. Next: **65** |
| 65 | Template, initialization, module requirements and layout contract | Q | 63–64 | **done 2026-09-22** ([65.md](handoffs/65.md)): PROPOSED template contract `planning/m4-contracts/templates.md` + owned diff rows C65-1…C65-10 (five contracts; Gate Q). One versioned built-in template (`platformer-starter` v1): directory form (descriptor + v3 base scene + 30-command recipe + 7 self-generated source blobs + NOTICE), three independent digest checks, no archives/symlinks/scripts; exact-match engine-version rule (CCR-65-2). New `createProjectFromTemplate` operator: 5 phases (reserved/blobs/envelope/replayed/published), atomic-mkdir claim + reservation marker, full crash-completion table; partial destinations are not projects (C09 no-ready-partial); identity-matching retry = no-op, else `template_destination_exists`; installed-template removal/replacement rules (created projects unaffected; in-flight ⇒ `template_source_unavailable`). Named schema bump: manifest schemaVersion 2 + required `template` block (only (2,3,3) valid; migration never re-versions — accepted rule unchanged; plain createProject still v1); queryProject carries it with no shape change. Starter replays through the SAME command engine (deterministic `req-<sha256-32hex>` requestIds — accepted shape; N ≤ 128 = retry retention; N ordinary undoable history entries). Finite M4 module registry (3 platformer core + demo + behavior-ref pattern ⇒ `module_unresolved` the C14 explicit refusal); resolution at create/capture/build/post-edit; panel/layout never an input. Panel visibility: frozen 7-panel registry, browser-local per-project localStorage, template seed, safe corruption reset, byte-absence invariant (C06). Admin-scoped creation (new route + admin-only `tl_project_create`; project-scoped tokens cannot create). No new package/dep/bundle (templates/ = engine data). Fixtures `fixtures/m4/templates/`: complete template (recipe = the accepted 26 Beacon Reach commands + 4 additions — courier modelAnimation, decoration prefab, TWO independent instances: closes Gate P F4/B17 + visible animated content), 5 case files, index.json; independent checker re-derives the FULL replay through the real @thirdlight/commands engine (revision 0→30, 20 entities, validateProjectV3) + identity/module/layout cases — green; corruption negative control exit 1; `npm test` 179/2237 green. Engine facts verified: settings stored canonical-alphabetical; origin validation browser|mcp|admin (the proposed `template` kind is rejected pre-promotion — documented `admin` replay substitution, C65-7). CCRs: CCR-65-1 (built-in-only behavior refusal — Q decision), CCR-65-2. C rows specified, executed by 72/73/74/76/77/81. No commit made. Next: **66** |
| 66 | Portable pinned engine kit and independent build contract | Q | 63–65 | **done 2026-09-22** ([66.md](handoffs/66.md)): PROPOSED distribution contract `planning/m4-contracts/distribution.md` + owned diff rows C66-1/2/3 (dependencies/export/workspace; Gate Q). The kit: a local, integrity-indexed distribution of the EXISTING workspace (closed allowlist: 3 root manifests + exact lockfile bytes + 370 unit source files + 8 tooling + 9 contracts + 11 template files + 2 generated data files = 403-row inventory; negative inventory: no node_modules/dist/.git/fixtures/samples/top-level tests/symlinks). Identity = digests, never a fabricated tag/version: engineRef `working-tree` (a dirty tree is never identified by a commit alone) or `commit` (supplementary); `kitDigest` = block digest over the allowlisted source files (the generated data embeds the digest ⇒ inventoried, not a digest input); engineVersion stays 0.1.0. node_modules never vendored (25 machine-dependent @esbuild/<platform> packages) — install = `npm ci --prefix <kit>` (lockfile-authoritative ⇒ transitive floating dep impossible by construction; registry-down = structured `kit_install_unavailable`). C12 boundary exact: browser runtime three/react(19.3.0, editor only)/rapier; node runtime ws (backend bundle) + MCP SDK (mcp bundle); esbuild 0.28.2 = build-only-at-runtime (EXTERNAL in the backend bundle, never a browser member); typescript/vitest dev/test-only. Game owns `game.json` (template provenance + enginePin + documented build entry); the vendored kit is immutable (re-hashed before every build: kit_tampered/kit_lockfile_mismatch/kit_inventory_missing); upgrade = explicit second-kit + re-pin. The public Node build tool `tools/game-build.mjs` (plain node:* — NEW tooling unit edge, CCR-66-2): 8 ordered steps (resolve-from-own-location (never process.cwd()) → verify-kit → verify-pin → npm ci → npm run build (the accepted check-deps/check-boundaries/typecheck/build pipeline) → verified template install → export through the ACCEPTED admin export route (kit backend, THIRDLIGHT_ENGINE_ROOT=<kit>) → verify the static closure). Licenses: UNLICENSED/private ⇒ owner-local only, no grant, no publish; required NOTICE with the lockfile-extracted per-entry license names (kit_notice_missing). Owned diffs: C66-1 dependencies (no new package/dep/pin/lockfile change), C66-2 export (no change — the build entry is the accepted route; the kit satisfies THIRDLIGHT_ENGINE_ROOT by shape; scans unchanged), C66-3 workspace (no change — the game project is a plain workspace instance under the game data root; the 65 operator runs against the kit templates/ set). Fixtures `fixtures/m4/distribution/`: the 403-row inventory (re-derived from the live tree), the working-tree identity, the 9-pin license inventory, 7 pin cases, the 8-step build-tool order + rules + 8 negative-inventory cases; independent checker green (26 sections; its first run caught 3 real checker defects, now fixed); tampered-fixture negative control exit 1; generator --check byte-identical. C08/C12 specified — real isolated install/build/export evidence by 75/76/81 (the enumerated §11 smoke procedure is their specification). CCRs: CCR-66-1 (contracts evidence-path exemption), CCR-66-2 (tooling edge ships with 75). No commit made. Next: **67** |
| 67 | Recovery, diagnostic and measured-budget contracts (67-A/67-B) | Q | 63–66 | **done 2026-09-22** ([67.md](handoffs/67.md)): PROPOSED reliability contract `planning/m4-contracts/reliability.md` + owned diff rows C67-1…C67-5 (workspace/sessions/runtime/dependencies + the deployment.md §5 REPLACEMENT proposal; Gate Q). **67-A fully specified:** consistent backup = the accepted workspace §15 included set (manifest + envelope + every sources/sha256 blob incl. superseded; ownership/staging/derived/recovery/migration/temp excluded — never restored); live-owner refusal before any copy (backup_live_project, the accepted §6.2 liveness test); the new backup-manifest.json (inventory + inventoryDigest + per-project digests/revision/template provenance = the C10 provenance) written LAST (no valid manifest = backup_incomplete, never a backup); ordered read-only verify (backup_truncated / backup_hash_mismatch / backup_blob_missing with superseded flag / backup_ownership_included / backup_path_rejected; orphan blobs = included + informational); retention MANUAL (declared constant, no silent pruning); same-ID restore (verified, EMPTY same-id destination, identity before first write, restore_destination_nonempty never modified) vs new-ID creation (the exact 3-field identity rewrite, revision preserved — a copy, not a migration reset) pinned as DISTINCT. Diagnostic report: GET /api/v1/admin/health + admin MCP row tl_health (≤ 32 KiB, 100/32 list bounds — accepted scan/ring values re-used; redaction by typed envelope — no credentials/absolute paths/content bytes; overflow clips + truncated:true; backendId+ts+engineVersion point-in-time identity, re-fetch on change — the stale-identity rule; asserts nothing about hardware). Fault matrix F1–F8 (accepted machinery + 65 template phases + 66 kit re-hash + 67 backup/restore crashes) under the no-ready-partial (C09) invariant. **67-B protocol-only (numerical table BLOCKED):** named device BLOCKED-owner-pending (reference-device §2 proposal); 3 digest-frozen scenes (S1 committed v3 envelope, S2 committed Beacon Reach capture, S3 template starter final scene = deterministic recipe replay); measurement protocol (environment record incl. GL_RENDERER + warm-cache condition, 10-min warmup, N ≥ 30, median + p95, 7 closed metrics, no-early-box-data); every threshold row BLOCKED — unmeasured (never invented); ratification checkpoint handoffs/67-budget-ratification.md (79 preflight allowed after 76/78, NO scored run until target review/promotion + owner confirmation). CCRs: CCR-67-1 (tools ship with 75/79 tooling), CCR-67-2 (non-performance work proceeds while 67-B awaits hardware). Fixtures `fixtures/m4/reliability/`: the checker EXECUTES all 8 refusal cases through its own reference implementation (each asserts the exact code + originals byte-identical after the refusal — the acceptance obligation; R8 uses a real live pid / a real exited pid), re-derives the backup example + the 3-field transform byte-exactly, re-derives S3 by a second independent engine replay; green; tampered-digest negative control exit 1 (2 detections); generator --check byte-identical; `npm test` 179/2237 green. No commit made. Next: **68** |
| 68 | Contract inventory, fixture audit and Gate Q submission | Q | 63–67 | **done 2026-09-22** ([68.md](handoffs/68.md)): the Gate Q review pack — one internally coherent review pack (docs + fixtures only; no accepted contract/product/lockfile changed). (1) `planning/m4-contracts/contract-diffs.md`: the COMPLETE inventory — 26 diff rows (64: 8, 65: 10, 66: 3, 67: 5; 11 no-change adjudications + 1 replacement proposal), each with author / exact destination (contract + section) / fixture / consumer (implement + evidence) / version-graph effect / PROPOSED status; the totals (38 new error codes, 0 packages/deps/pins/lockfile changes, 1 manifest combination C65-1, 1 bundle-graph subpath C64-6, 3 tooling edges, 2 routes + 3 MCP rows); the fixture→checker map; the debt-ledger dispositions (D-63-2/4/5/6/7/8/9 → 70 implementation repairs; P2-B → 71 + C64-2; M3-GLB → 69; SD-63-1 superseded by C67-5; CCR-63-1/2 refiled by 64 as implementation repairs); the cross-pack RECONCILIATION (10 items explicit: settings order, the origin substitution, the templates/ chain, backup-vs-kit, the admin routes, the provenance-in-backup, the engineVersion, the tooling edges, the distinct error-code names, the CCR-63 refiles); the 8-item PENDING OWNER docket (K-3 export-surface confirmation, behavior scope CC-55-3a/CCR-65-1, CC-55-3b wording, the reference device, the play.preview.ready relay ownership + §18 v3 channels, the origin.kind template value, the CCR-66-1 evidence-path exemption, the C64-5 clerical correction) with proposed defaults — NOT inferred from the M3/M4 execution authorization; the Gate Q protocol (model review ≠ owner approval; repairs in-packet; EXPLICIT docs-only promotion via a future handoffs/gate-q.md; 69 does not start automatically). (2) `planning/m4-contracts/traceability.md`: the 7-hop template chain (field → command (closed 8-op whitelist, no new op) → UI/MCP (admin route + tl_project_create/tl_templates_list; project-scoped tokens cannot create) → storage (5-phase operator, manifest v2, envelope 0→30) → module closure (finite registry, layout never an input) → both hosts (preview + export repair rows) → acceptance (C04–C12 evidence owners 72–81)) + the chain invariants mapped to the packet's 8 failure modes + the C01–C16 owner table (every row has implementation/evidence owners) + the separate-records rule (model reviews ≠ owner approvals; physical UNVERIFIED ≠ product gap; M3 authorization ≠ M4 approval). (3) `fixtures/m4/audit/`: the audit runner (tools/audit.mjs — any failure exits nonzero) + the error-code registry (38 new codes: templates 16 / distribution 8 / reliability 14 / delivery 0 reuse-only; the meaning notes for the deliberately distinct template_path_rejected/kit_path_rejected/backup_path_rejected) + README. AUDIT VERIFIED EXIT 0 (2026-09-22): A — 4/4 checkers green (delivery/templates/distribution/reliability); B — 4/4 DELIBERATELY CORRUPTED edges each exit nonzero (B1 the module edge — a tampered M4 module-registry entry; B2 the destination identity — a flipped GLB byte; B3 the kit digest — a zeroed inventory sha256; B4 the backup/scene inventory — a tampered frozen-scene digest, then regenerate + re-check green); C — the registry consistent (each proposal's closed new-code section set-equal; no code in two sections; delivery reuse-only — the contradictory-errors check). `npm test` 179/2237 green. (4) `docs/decisions/0004-m4-reliability-and-templates.md` — PROPOSED only (11 sections; not binding until the Gate Q record). **Gate Q is the gate now**: the in-session model review of the pack is COMPLETE (this packet); the owner docket awaits the owner's ruling; on acceptance the docs-only promotion applies the accepted rows to docs/contracts/ + handoffs/gate-q.md closes the gate; packet 69 starts on the owner's go (standing authorization). No commit made. Next: **Gate Q (owner) → 69** |
| 69 | Scene model attachment and animation resource ownership | R | Gate Q accepted + promotion | **done 2026-09-22** ([69.md](handoffs/69.md)) — the three-adapter `models` block (C64-4): model entities attach/render through the existing resource/animation owners — `models.ts` (`validateModelsBlock`, `createModelsRealization`, per-instance `cloneInstanceMaterials`; two-phase prepare: wrapper-verified bytes → real-`byteLength` descriptor → `store.load`; the adapter never re-hashes), `adapter.ts` wiring (`models?`/`modelsLoader?` options — root subpath stays loader-free; `modelsSettled()`; diagnostics `models` counters; renderFrame advances live controllers once with a clamped `[0,0.25]` delta — C13 single consumer; `viewFor`: the player gets the committed `playerMotion`, non-player model entities always the neutral motion), `models_config_invalid`/`models_asset_unresolved`, 18 unit tests (109/109 in the package). **In-scope defect repaired: the settle race** — `settleIfComplete` gated on `handle.state() === 'pending'` resolved one microtask early (state non-pending before the attach `.then` ran; observed `{instances:4, animations:0}` vs counters `{instances:6, animations:2}`) — explicit `pendingCount` decremented inside each completion callback before settle + two-asset microtask-race regression test. **Out-of-boundary defect discovered + refiled: the committed template/sample `courier.glb` (digest `d62fc659…`) carries corrupt animation data** (root cause: `samples/beacon-reach/tools/generate-assets.mjs` `tAcc = 3 + samplers.length * 2` miswires the sampler input accessor — every clip's key times are the box vertex indices; the real GLTFLoader parses it, the substrate rejects `asset_clip_invalid`); 69 evidence uses packet-owned GLB bytes of identical structure (same assetIds, clip names/order Idle/Run/Airborne, 4.0 s; `tools/glb-build.mts`, self-checked through the real pinned GLTFLoader) recorded in the fixture index `sourceSubstitution` block; refiled to the sample/template packets (72–74 meet it again). Fixture player-model = a documented deterministic DATA TRANSFORMATION on the replayed template recipe (controller+model is unreachable via the accepted command set; the v3 validator accepts the combination and §2.4 defines it; gated by `validateSceneV3`, digest-pinned, 20 entities rev 31). Integration evidence (`tests/integration/m4-render/`, 15/15 rows PASS ×2 runs; evidence `docs/acceptance/evidence-m4/69/raw/`): R01–R02 fixture byte-identity + digests (defective-source digest recorded); R11–R15 Node real loader/real mixer — settle `{ok, assets 2, instances 6, animations 2, unresolved 0}`, dispose → ownership baseline (releases == loads, double-dispose `alreadyDisposed`), L6 wrong clip name → `animations 1`, L4/L5 unresolved → plain group holder; R21–R28 real browser (headless Chrome, real GLB fetch/verify, real Rapier WASM physics, real WebGL/SwiftShader canvas): settle `{2, 6, 2, 0}`, 8 visible non-blank model/pose PNGs, frames change, **two instances at distinct committed states** (player `run` grounded speed 4.0 AND `airborne` grounded false y≈1.97 over the hazard; non-player always `idle`; steps advance), full physics trajectory (run ≈4 m/s, two full-hold jumps clearing the step-block x∈[6,7] and the hazard zone x∈[10.5,11.3] — the controller's variable-height rule halves vy and clears `airborne` on an early release, so the recorded input holds each jump through the apex; clean stop at x≈14.27, ≈2 m from the platform edge; run state `playing` throughout — no death), C13 rAF peak 1, 3× dispose/recreate cycles with settles identical to the first + counters at baseline + no unhandled rejections, 0 external-origin traffic. The probe drives the engine-level `ActionSource` seam (base-relative to the start step; the simulation steps during `awaitingStart` and Node-side timers overshoot under the headless Chrome's CPU contention — the evidence window is anchored page-side to the simulation timeline). **UNVERIFIED: hardware-GPU rendering (SwiftShader only) and real keyboard/gamepad/audio (no physical devices in the container; D-63-2 remains owned by 70).** Toolchain: `npm test` 180 files/2255 tests green; three-adapter + probe typecheck clean; 0 orphaned headless Chrome; no lockfile/contract/other-package change; no commit (standing authorization). Next: **70** |
| 70 | Both production hosts: visible motion, captured media and failure paths (70-A/70-B) | R | 69 | pending |
| 71 | Authoritative settings reads and query parity | R | 70 | pending |
| 72 | Workspace template creation and starter recipe | S | Gate R accepted | pending |
| 73 | Template lifecycle through backend and MCP (73-A/73-B) | S | 72 | pending |
| 74 | Template UI and separate panel visibility preferences | S | 73 | pending |
| 75 | Local engine-kit build and isolated toolchain | S | 74 | pending |
| 76 | Independent game built from the template | S | 75 | pending |
| 77 | Consistent backup, verification and clean restore | T | Gate S accepted | pending |
| 78 | Bounded diagnostics and actionable failure presentation | T | 77 | pending |
| 79 | Representative desktop budget and lifecycle measurements | T | 78; approved 67-B before scored runs | pending |
| 80 | Evidence-led reliability/performance repair and regression check | T | 79 | pending |
| 81 | Integrated template, independent-game and export acceptance | U | Gate T accepted (or its explicit hardware-only partial verdict) | pending |
| 82 | Reproducible operator runbook and final owner review pack | U | 81 | pending |

## M3 planning (2026-09-19; plan review accepted, Gate K accepted, contracts promoted, implementation not started)

Owner reports M2 complete and requests M3 planning, then authorized **M3
execution** (packets 38–62, Gates K–P, plan review, contract promotion and
bounded repairs) in a separate instruction recorded in
`handoffs/m3-plan-review.md` “Execution authorization”. That authorization is
recorded **separately** from the review verdict and approves no contract,
dependency, acceptance target or M4 work. The historical M2 evidence and its
UNVERIFIED browser/hardware labels remain unchanged. Read the current M2 working
tree, not the M1 HEAD alone.

**Plan review (2026-09-19): ACCEPT WITH BOUNDED FOLLOW-UPS**, repairs applied
docs-only in the same step: PR-1 named owners for the instructions string, the
per-checkpoint safe-spawn reference and the checkpoint activation appearance;
PR-2 packet 39 split into bounded parts 39-A/39-B and post-gate write-back
declared for 58/59 onto 44/48; PR-3 disambiguated the authoring `project.json`
(`schemaVersion` 1) from the runtime-content `manifest.json` (`manifestVersion`
1→2) and added the `manifest 1 + scene 3 + storage 3` combination; PR-4 Gate K
must accept the `dependencies.md` rows for `platformer-game`/`game-host` plus the
“editor UI may not import `game-host`” rule; PR-5 B11 bound to a named checklist;
PR-6 the real verification path is recorded (`m3-plan.md` §5.1) — real headless
Chrome for Testing 151 / SwiftShader WebGL 2, DOM+network+console and
AudioContext decode are executable in-container and must be executed; physical
gamepad, audible output and hardware-GPU stay UNVERIFIED with a required owner
manual annex; PR-7 sample wording. Reviewer: one fresh read-only reviewer
subsession; every cited claim re-verified by the coordinator; **not** owner,
human or independent review.

| Planning task | Status | Deliverable / handoff |
|---|---|---|
| M3 packet planning | complete draft | [Plan](planning/m3-plan.md), [packets 38–62](planning/m3-packets.md), [sample](planning/m3-sample.md), [acceptance](planning/m3-acceptance.md); [planning handoff](handoffs/m3-planning.md) |

| M3 gate | Covers | Status |
|---|---|---|
| Plan review | M3 planning deliverables | **accepted with bounded follow-ups 2026-09-19; PR-1…PR-7 repairs applied docs-only** — [review](handoffs/m3-plan-review.md); no blocking defect; packet 38 unblocked; verdict is the review record only, not owner/independent approval |
| K | 38–43; baseline and exact contracts/fixtures | **Gate K ACCEPT WITH BOUNDED FOLLOW-UPS** ([review](handoffs/gate-k.md)); **bounded repair applied 2026-09-19** ([repair](handoffs/gate-k-repair.md)) — FU-1…FU-7 + B3 applied to the pack (109→**110** inventory rows, 13 repaired/rejected, PM13 the only non-promotable row); **docs-only promotion performed 2026-09-19** ([promotion](handoffs/m3-promotion.md)) — 109 accepted/repaired rows applied to `docs/contracts/{project-model,commands,workspace,runtime,sessions,export,dependencies}.md` in the pack's §6 order plus the two new homes `gameplay.md`/`presentation.md`; PM13 not applied; `fixtures/m3/audit/tools/check-promotion.mjs` 0 with `--corrupt-control` detecting all four removals; packet checkers, M1/M2 fixture checkers and `npm test`/typecheck/check-deps/check-boundaries/build all exit 0; no production code, package or lockfile change, no commit. Verdict is the Gate K session review only, **not owner/independent approval**; owner manual review and the K-3 export-name confirmation are pending. **Packet 44 is unblocked and ready — do not start it automatically.** **Packet-44 contract-gap repair applied 2026-09-19** ([record](handoffs/repair-cc44-3-4.md); docs+fixtures only, no product code, no commit): CC-44-3 fixed by correcting `migration/v2-source/envelope.json` to a genuinely v2-loadable scene (and by verbatim carry the destination), CC-44-4 by the new `workspace.md` §16.5.2 derived-revision reset (`publishedRevision`/`behavior.source.publishedRevision`/`behaviorTrust[].acknowledgedRevision` → 0) with the destination fixture, `index.json` expectations and the checker updated; **the repaired portions `workspace.md` §16.5.1/§16.5.2 and `project-model.md` §12 step 5/§13.2 rule 5/§18.9.2 rule 4/§22.2 rule 4/§23.11 are REOPENED — a fresh architectural re-review of exactly those sections is owed before Gate L acceptance; the repair is a session repair, not an independently reviewed change.** CC-44-1 → 46, CC-44-2 → 47, CC-44-5 → 47 (needs a granted bounded re-open of `packages/project-model/src/errors.ts`), CC-44-6 → 46, all checkpointed at Gate L |
| L | 44–48; durable v3 authoring/content/backend/MCP | **Gate L ACCEPT WITH BOUNDED FOLLOW-UPS (2026-09-19)** ([review](handoffs/gate-l.md) BLOCKED on GR-L-1, then [re-review](handoffs/gate-l-rereview.md) **ACCEPT WITH BOUNDED FOLLOW-UPS — block lifted, no P1 open**); **bounded repair + docs-only promotion applied 2026-09-19** ([record](handoffs/gate-l-repair-promotion.md)): GR-L-1 fixed (the stored `source.publishedRevision` load bound is `>= 0`, matching the already-promoted §12 step 5/§22.2 rule 4 text; new non-vacuous prepared-behavior migration test; the destination loads with `0`), GR-L-2/CC-48-3 fixed (the `queryProject`, full-state and play-snapshot scene projections report the **scene document’s** `schemaVersion` 1/2/3; the five affected frozen `fixtures/commands` files regenerated one line each, `generate-fixtures --check` 73/73), and the accepted CC rows promoted docs-only (CC-44-1 §16.9, CC-44-6 §19.2/§16.6.4, CC-45-1/2/3 the failure fixtures + the packet-45 replay de-special-cased, CC-45-4 §3.1.1, CC-45-6 §5.6, CC-45-8 §5.4, CC-47-1 note, CC-47-4 §41.7.3, CC-47-5 §41.4.5/§41.7.1 as a structural maximum, CC-48-2 §6.3, CC-48-3 §19.x); CC-45-5 discharged by leaving the frozen M2 fixture byte-exact. **Open bounded follow-ups (owners/checkpoints in `acceptance/m3-report.md` §4):** **CC-L-1** (P2, real defect: an animated reimport replaces `roles` but leaves `components.modelAnimation.version` at the old version, so the new bytes are never delivered — `captureContent` pins the old version; owner packet-45 command scope, checkpoint **before packet 53**), **P2-B** (`commands.md` §5.6 documents a `queryProject` content summary no served query returns; owner query scope, before M3 acceptance), plus P3 docs/fixture hygiene (§16.9 names corrected in this step; CC-47-1 note applied; frozen M2 `queries.json` example, `fixtures/m3/contracts/README.md`, the placeholder `modelAnimation.roles` fixtures (packet 53) and the report staleness — all M3-acceptance checkpoints). Post-step toolchain: `npm test` **147 files / 1867 tests** `EXIT=0`, typecheck/check-deps/build `EXIT=0`, check-boundaries 15 pkgs/**297 files/1167 specifiers/0 violations**, M2 checker 34/34, all M3 checkers + corruption controls `EXIT=0`, `fixtures/commands` generator 73/73. Verdicts are session reviews only, **not owner/independent approval**. **Packet 49 is unblocked.** |
| M | 49–51; game flow/respawn/camera | **Gate M ACCEPTED (2026-09-20)** ([review](handoffs/gate-m.md)); packet 51 (follow camera) done — numeric §7.2 fixtures + real-runtime integration green; CC-51-1/CC-51-2 resolved. Session review only, not owner/independent approval |
| N | 52–55; rendering/animation/audio/game shell | **Gate N ACCEPTED (in-session, 2026-09-21)** ([review](handoffs/gate-n.md)); packets 52–55 done; CC-55-1/1b/2 accepted, **CC-55-3 open** (carried to O/P). No sub-session available (host constraint) — reviewed in-session |
| O | 56–60; visual authoring/shared delivery/Play/export | **Gate O ACCEPTED (in-session, 2026-09-21)** ([review](handoffs/gate-o.md)); packets 56–60 done; F1–F6 disclosed, no new CCs, CC-55-3 open. Reviewed in-session (no sub-session — host constraint) |
| P | 61–62; authored sample and integrated acceptance | **Gate P ACCEPTED (in-session, 2026-09-21)** ([review](handoffs/gate-p.md)) — packet 62 done: the in-container M3 acceptance record (`acceptance/m3-report.md`, B01–B24: **22 PASS / 2 PARTIAL / 0 FAIL**) + the PR-6 headless-browser evidence (`evidence-m3/62/`, executed: DOM/HUD, canvas, console/network, resize two-aspects, WAV decode, export loading); the physical/audible/hardware-GPU subclaims are UNVERIFIED (owner annex §6). No new CCs (CC-55-3 open, non-blocking). Reviewed in-session (no sub-session — host constraint). **M3 in-container acceptance complete; M4 only on owner request** |

Each packet refers to its section in [m3-packets.md](planning/m3-packets.md).
Handoffs `docs/handoffs/<ID>.md` are required only when executed; none is created
by planning. A completed packet never automatically starts its successor.

| ID | M3 packet | Gate | Prerequisite | Status |
|---|---|---|---|---|
| 38 | Browser baseline and compatibility probes | K | Plan review accepted + execution authorization | done 2026-09-19 — real local Chrome 151/SwiftShader path established without any install; pinned three/loader/Rapier-WASM/input/audio measured; **P1 C38-1: the accepted preview CSP (`sessions.md` §17.4) blocks Rapier WASM in a real browser** (isolation evidence in `evidence-m3/38/raw`) → packet-42 diff at Gate K; physical gamepad/audible output/hardware GPU UNVERIFIED (see [38.md](handoffs/38.md)); C38-1/C38-2 repaired and promoted 2026-09-19 (`sessions.md` §17.4 now `script-src 'self' 'wasm-unsafe-eval'`; `allow="gamepad"` on every play iframe); next 39, which is now complete and promoted |
| 39 | V3 data/storage/migration/authoring contract | K | 38 report | **promoted on the Gate K review verdict; owner review pending** (proposed 2026-09-19) — parts 39-A/39-B in one step ([39.md](handoffs/39.md)): scene `schemaVersion` 3, envelope `storageVersion` 3, the new legal combination `manifest 1 + scene 3 + storage 3` (authoring `project.json` stays 1; export `manifestVersion` 1→2 stays packet 42's), six new components (`gameZone`/`playerSpawn`/`cameraFollow`/`light`/`surface`/`modelAnimation`) + audio `kind`, the bounded `content.game` block, reference/deletion rules, the v2→v3 copy-migration operator (new identity, revision 0, retry/history reset, resumable marker phases, no in-place upgrade, v1 source refused) and the full create/edit/remove/query command surface with exact change/inverse/no-change semantics. The three PR-1 values are owned with creation paths (320-char `instructions`, checkpoint `safeSpawnId`, checkpoint `activation` slot); authorability table 21/21 rows creatable. 39-A/39-B both completed. Deliverables: `planning/m3-contracts/{model,storage,authoring}.md`, `planning/m3-contracts/diffs/{project-model,commands,workspace}.md`, `fixtures/m3/contracts/**` (36 indexed fixtures + self-contained Node checker). Checks: checker 37 groups `EXIT=0`; two deliberate-corruption negative controls `EXIT=1`; `npm test`/`typecheck`/`check-deps`/`check-boundaries`/`build` all `EXIT=0` (no package/contract/lockfile change). C39-1…C39-7 recorded (v3 version rows; §18.1 rule-3 replacement reserved to packet 41; content key set; commands §2 non-goal wording; workspace §4.5 threshold move `≥3`→`≥4`; `game_config_invalid`/`game_reference_in_use`; Gate K manifest-owner note). Packet-41 placeholders marked in `fixtures/m3/contracts/index.json`. **Docs-only promotion applied 2026-09-19** ([m3-promotion.md](handoffs/m3-promotion.md)); no commit. Next 40 (proposal promoted) |
| 40 | Game flow/trigger/respawn/camera contract | K | 39 | **promoted on the Gate K review verdict; owner review pending** (proposed 2026-09-19) ([40.md](handoffs/40.md)): new contract home `planning/m3-contracts/gameplay.md` (run state machine `awaitingStart→playing→respawning→playing`/`playing→won`/`replay`/terminal-failure≠death, exact 30-step bounded respawn delay with no wall clock, M3-only order `boundary→sample→intent→controller→physics→transform→gameplay→camera→commit`, pure swept upright-capsule vs axis-aligned XY zone predicate over the last completed motion segment with `ZONE_OVERLAP_EPS 1e-9` and precedence death>checkpoint>goal/stable-ID ties, single checkpoint activation per run, the runtime-owned reset transaction R1–R8 with a coherence table and the restricted `PhysicsResetPort` (the diagnostic `reset()` is never a gameplay path), the committed read-only `GameView`/observation bound, camera follow/dead-zone/bounded fixed-step smoothing/authored bounds/frustum clamp at 16:9 and 4:3/hard snap/presentation-only resize/one writer, finite limits and five new error codes) + `planning/m3-contracts/diffs/runtime.md` (`R40-1…R40-16`, new §15) and `fixtures/m3/{gameplay,camera}/**` (12+5 indexed fixtures + `index.json` + self-contained plain-Node `gameplay/tools/check-fixtures.mjs`). Checks: checker 23 groups/124 checks `EXIT=0`; deliberate-corruption negative control `EXIT=1` (13 checks in 7 groups, semantic + digest); packet-39 checker re-run `EXIT=0` (37 groups); `npm test`/`typecheck`/`check-deps`/`check-boundaries`/`build` all `EXIT=0` (no package/contract/lockfile change). B04/B05/B06/B08 PASS for the proposal; B07/B09/B10 PARTIAL; B22 UNVERIFIED (implementation evidence is 49–51/59/62). C40-1…C40-12 recorded (camera-ownership supersession in §12.4, appended phases, v3 snapshot `game` field, camera pose via the camera entity transform, restricted reset ops/forbid diagnostic reset, `lastCommitted`/boundary hook/effective-frame overrides, no new settings key, no `commands.md` §5.4 row, sessions/export + dependencies requests for packet 42, packet-41 view bit, and the `GameView.stepIndex` vs event `stepIndex` definition review). **Docs-only promotion applied 2026-09-19** ([m3-promotion.md](handoffs/m3-promotion.md)); no commit. Next 41 (proposal promoted) |
| 41 | Rendering/animation/audio contract | K | 39–40, 38 evidence | **promoted on the Gate K review verdict; owner review pending** (proposed 2026-09-19) ([41.md](handoffs/41.md)): new contract home `planning/m3-contracts/presentation.md` — one-key/one-fill lighting with the packet-38 executed shadow preset (512/PCFShadowMap) and exact bounded shadow-camera derivation, unsupported-shadow → shadow-off diagnostic vs no-WebGL 2 → structured `render_unsupported`, no pixel/perf promise; three copied primitive preset rows (`applySurfacePreset` = one undoable edit, values not links); rigid animation `AnimationRoleBinding { clipIndex, clipName }` with the exact validation order (container→fields→range→duplicates→name agreement→ambiguity→GLB profile caps 8/64/32/4096/10 000 ms), skins and root-node translation rejected, atomic `publishAsset` reimport (new mapping + bytes, one revision, refusal preserves version), the runtime role selector (committed view, one host update, fixed 0.2 s crossfade, independent mixer/action per instance, no loop/physics writes) and the §18.1 rule-3 replacement; bounded PCM-WAV profile (mono s16 LE 48 kHz, two chunks, `44+dataBytes`, primary cap 192 000 PCM bytes ⟺ 96 000 frames ⟺ 2000 ms ⟺ `sourceBytes ≤ 192044`, stage-1 file bound 196 608), `pcm-wav` recipe + `PcmWavMetrics`, 12-stage inspection with nine new diagnostics and the six-cue cap, hard corrupt-vs-soft sound-off split, and the injected browser audio owner (≤ 8 voices, dedupe by event id, stale cancel, hidden/stop suspend, dispose-once close, local gesture only, no server device/base64); checkpoint activation appearance data + the read-only `GameView.checkpointActive` bit and its lifetime; ownership/disposal table; limits/errors and the code-set destinations. Append-only packet-41 sections added to `planning/m3-contracts/diffs/{project-model,runtime,commands}.md` (PM41-1…-7, R41-1…-4, CMD41-1…-3). Deliverables: `fixtures/m3/media/**` (40 indexed self-generated files: 8 GLBs, 24 WAVs, 7 case files, committed `tools/generate-fixtures.mjs`, self-contained plain-Node `tools/check-fixtures.mjs`, `index.json`, `README.md`, `verification.md`). Checks: media checker 13 groups/85 checks `EXIT=0`; deliberate-corruption negative control `EXIT=1` (8 failures incl. 4 semantic); generator `--check` `EXIT=0`; packet-39 checker 37 groups `EXIT=0`; packet-40 checker 23 groups/124 checks `EXIT=0`; `npm test` 135 files/1701 tests `EXIT=0`; `typecheck`/`check-deps`/`check-boundaries`/`build` all `EXIT=0` (no package/contract/lockfile change). B11/B12/B14 PASS for the proposal, B13 PARTIAL — audibility and all implementation/visual evidence UNVERIFIED (47/52/53/54/57/61–62). C41-1…C41-8 recorded (`GameView.playerMotion`; `publishAsset.animation`; atomic reimport change/inverse + code rows; §18.1 rule-3 replacement; no base64/URL audio + CSP; `game-host` rows; `pcm-wav` recipe/metrics/codes; audio capture confirmation). **Docs-only promotion applied 2026-09-19** ([m3-promotion.md](handoffs/m3-promotion.md)); no commit. Next 42 (proposal promoted) |
| 42 | Host/controls/delivery/dependency contract | K | 39–41 | **promoted on the Gate K review verdict; owner review pending** (proposed 2026-09-19) ([42.md](handoffs/42.md)): new contract home `planning/m3-contracts/delivery.md` — manifest `manifestVersion` 1→2 (runtime-content `manifest.json`; authoring `project.json` stays 1) with the resolved six-key `settings`, the embedded bounded `content.game`, the media identity summary (`cues` + per-entity `animation` roles/profileDigest), `assets[].kind`, and `gameDigest`/`settingsDigest`/`mediaDigest` + `buildId` over the canonical v2 key order; v1 stays readable and is never upgraded in place; the `capturedAt` two-tree reproducibility rule restated with the M3 additions; late-edit/reimport and failed/cancelled-build behaviour (previous output preserved, no mixed bytes). **C35-5 closed**: the captured resolved settings object is passed to `instantiateRuntime` *and* configures the physics port from the same in-memory object, hash-bound, with the B16 defaults-vs-changed `run_speed`/`gravity_y` numeric comparison and the pinned-run-unchanged rule. One shared browser-safe composition entry `createGameHost` (editor UI may not import `game-host`; preview wrapper only) + the exact `dependencies.md` §2/§3/§4.1/§4.2/§4.3 rows for `platformer-game`/`game-host`, and the 58/60 §5.4.1+§17.5 re-measurement duty. Frame/menu control separation (fresh-release, gamepad takeover/denial, exclusive injected mode that cannot race physical controls or fake a trusted gesture, controls with no physics tick, focus/visibility reset); typed control/observe relay + `tl_game_control`/`tl_game_observe` with the run-identity tuple and closed failure set (stale/expired/wrong origin/source/nonce/no-browser/unavailable/timeout), no binary/credential/capability crossing. Static closure: the **C38-1** one-token CSP diff `script-src 'self'` → `script-src 'self' 'wasm-unsafe-eval'` (citing `evidence-m3/38/raw/engine.json` blocked vs `engine-nocsp`/`engine-csp-wasm` allowed), the **C38-2** `allow="gamepad"` requirement extended to every play iframe, MIME/cache rules, non-root relative closure and the export's own meta policy. Owned sessions/export/dependencies diffs + appended `runtime.md` packet-42 rows (R42-1…R42-4; commands.md needs none — C40-8 confirmed) + a C39/C40/C41 routing table. Deliverables: `planning/m3-contracts/{delivery.md,diffs/{sessions,export,dependencies}.md}`, `fixtures/m3/delivery/**` (25 fixtures + index + self-contained plain-Node checker + deliberate-corruption control + README/verification). Checks: delivery checker 16 groups/153 checks `EXIT=0`; corruption control both corruptions `EXIT=1`; packet-39/40/41 checkers `EXIT=0`; `npm test` 135 files/1701 tests, `typecheck`/`check-deps`/`check-boundaries`/`build` all `EXIT=0` (no package/contract/lockfile change). B16/B19/B20/B21/B23 PASS for the proposal; CSP/gamepad runtime behaviour UNVERIFIED (no browser run here). C42-1…C42-5 recorded. **Docs-only promotion applied 2026-09-19** ([m3-promotion.md](handoffs/m3-promotion.md)); no commit. Next 43 (proposal promoted) |
| 43 | Contract integration and fixture audit | K | 38–42 | **promoted on the Gate K review verdict; owner review pending** (proposed 2026-09-19) ([43.md](handoffs/43.md)): consolidated Gate K inventory `planning/m3-contracts/contract-diffs.md` — **110 inventory rows** after the Gate K bounded repair (105 section-diff rows incl. R40-17 + 3 packet-43 reconciliation rows `PM43-1…3` + 2 new-contract rows `NC-1/NC-2`), every status **`open`** (0 accept / 0 reject); each row names author packet, exact destination section, proving fixture(s), consumer packets, version effect, collision/supersession note. §3 routes every C38/C39/C40/C41/C42 request exactly once; §4 records 6 open contradictions; §5 supersedes obsolete M1/M2 non-goal wording (`project-model` §§1/14, `export` §8) for scene/export v3; §7 lists 13 numbered Gate K decisions. `planning/m3-contracts/traceability.md`: feature→command→UI/MCP→runtime→export→acceptance matrix with a **creation** path for all 21 authorability rows, all B01–B24 owners and the three PR-1 values (instructions, checkpoint `safeSpawnId`, checkpoint `activation`). New integrated checker `fixtures/m3/audit/**` + `index.json` + README: runs the four packet checkers as one entry point and adds cross-pack checks (inventory closure; one packet per destination section unless reconciliated; version-table agreement across `model`/`storage`/`workspace` and `manifestVersion`; one owner per new code/component/op/phase/module/artifact; no fixture inventing a rejection code; traceability/creation coverage; superseded-quote resolution). `decisions/0003` extended with a PROPOSED §§2–§5 map (statuses **not started**). Checks: audit `EXIT=0` (8 groups); `--corrupt-control` 3 corruptions each forced non-zero with the expected check (`EXIT=0` overall); packet-39/40/41/42 checkers `EXIT=0` (37/23/13/16 groups); `npm test` 135 files/1701 tests, `typecheck`/`check-deps`/`check-boundaries`/`build` all `EXIT=0`; no accepted-contract/package/lockfile change, no commit. Rows B01–B24 remain **UNVERIFIED** (no product run); browser path exists but physical gamepad/audio/display/hardware-GPU stay UNVERIFIED. C43-1…C43-10 recorded. **Gate K review + bounded repair + docs-only promotion performed 2026-09-19** (110 inventory rows; 109 promoted, PM13 rejected); **packet 44 is ready — do not start it automatically** |
| 44 | V3 model and pure migration | L | Gate K accepted + docs-only promotion | **done 2026-09-19** ([44.md](handoffs/44.md)): `@thirdlight/project-model` implements the promoted v3 data/storage contract — scene `schemaVersion` 3, the v3 authoring envelope (`storageVersion` 3), the six v3 components, the `audio` asset discriminator, the bounded `content.game` block, the §23.10 limits and §23.9 codes, the pure `migrateSceneV3` (verbatim entities/IDs, identity on v3, v1 refused with `no_migration_path`), the §23.8 effective validation order and the extended `captureContent`. Exports: `validateSceneV3`/`normalizeSceneV3`/`parseSceneV3`, `validateContentV3`/`normalizeContentV3`/`canonicalContentV3`/`validateGameConfig`, `validateProjectV3`, `validateEnvelopeV3`/`normalizeEnvelopeV3`/`parseEnvelopeV3`, `composeV3`, `migrateSceneV3`, `captureContent`, `V3_REGISTRY`/`GAME_ZONE_ROLES`/`GAME_ZONE_LIMITS`/`SURFACE_PRESETS`/`COLOR_RE_V3`, the v3 types, `ModelErrorV3`/`ModelResultV3`/`EnvelopeV3Load`; `ERROR_CODES` +8, `LimitName` +8, `KNOWN_VERSIONS.scene` `[1,2,3]`. Evidence: **all 31 committed packet-39 contract fixtures executed through the real implementation and passed** (3 valid envelopes with byte/digest-identical canonical roundtrip, 21 invalid envelopes matching the recorded code/path/reason exactly, 1 catalog, 6 migration) plus 42 v3 tests; `npm test` 136 files/1743 `EXIT=0`; `typecheck`/`check-deps`/`check-boundaries` (288 files/1104 specifiers, 0 violations)/`build` all `EXIT=0`; M2 fixture checker stays **34/34** `EXIT=0`; packet-39/40/41/42/audit checkers and the promotion checker `EXIT=0`. 15 files changed, all under `packages/project-model/src/`; no fixture, contract, package or lockfile change, no commit. B01 PASS (model half); B02/B03 PASS (model half); B12/B14 PARTIAL (model half); B17 UNVERIFIED. CC-44-1…CC-44-6 recorded (v3 envelope entry-point name; §18.5/§18.6 vs packet-41-placeholder fixtures; v2-source fixture carries `cameraFollow`; migration destination violates `publishedRevision ≤ revision`; packet-41 codes/limits not addable by 47; capture version precedence). Next 45 — **do not start it** |
| 45 | Pure game/presentation commands | L | 44 | **done 2026-09-19** ([45.md](handoffs/45.md)): `@thirdlight/commands` implements the promoted v3 command surface on the existing pure history engine — `createEntity` add-capable `components` + `surfacePreset` with the derived-ID prefix rule, `setComponent` add/edit/remove for the six v3 components (role stages 2–4, reference-in-use refusals), `applySurfacePreset`, `setGameConfig` create/partial-edit/remove + `queryGameConfig`, the `queryEntities` component filter, the v3 content counts, and `publishAsset` `kind` + the atomic §8.5.1 `animation` mapping. Evidence: the committed packet-39 command fixtures replayed through the REAL engine — **7/7 scenario messages** (revision, createdId, change, recorded inverse, history), **3/3 no-change** cases and **12/12 failure** cases (11 rejected exactly, F10 a documented fixture/contract divergence that succeeds as a legal partial edit), plus byte-exact final envelope; 19 implementation tests (component creation, illegal deletion/reference, invalid reimport role/kind, exact inverse/redo IDs, stale ordering, failed atomic edit, mixed UI/MCP history, independent presets, immutable inputs) and 2 new vitest files; `npm test` 138 files/**1768** `EXIT=0`; `typecheck`/`check-deps`/`check-boundaries` (292 files/1131 specifiers, 0 violations)/`build` all `EXIT=0`; M2 checker **34/34** and all five M3 checkers `EXIT=0`. Scoped diff: 12 modified + 4 new files, all under `packages/commands/src/` (one additive `ERROR_CODES` expectation update in `api-surface.test.ts`); no fixture, contract, package, lockfile or install change, no commit. B02 PASS (pure command path), B03 PASS, B17 PARTIAL (prefab copy + import/reimport pure paths; real host workflow 46/48), B18 PARTIAL/UNVERIFIED (one revision + one history entry per success; durable envelope 46/48). CC-45-1…CC-45-8 recorded (F1 `game_reference_in_use` path; F8/F9/F12 document-relative `field_*` paths; F10 partial edit; `kind` required on create vs committed M2 fixtures; packet-16 `F18` expected text; `queryProject` four vs eight keys; §8.5.1 `modelAnimation.version` on reimport; packet-47 unreachable animation codes). Next 46 — **do not start it** |
| 46 | Durable v3 workspace and migration-copy | L | 45 | **done 2026-09-19** ([46.md](handoffs/46.md)): `@thirdlight/workspace` is durable at v3 — `ENVELOPE_STORAGE_VERSIONS` `[1,2,3]`, `buildEnvelopeBytesV3` (six-key envelope, six-key content `assets, prefabs, behaviors, settings, behaviorTrust, game`), the §16.4 v3 load branch (envelope six-key set → §16.2 combination before field validation → six-key content via `validateContentV3` (incl. `content_bytes`/`game_bytes`) → v3 scene; `validateProjectV3` cross-block in the session loader) and per-session v3 write through the **same** `W`/§5.3 ack timing (no second mutation path; no `game.json` side-car). New operator `migrateProjectCopyV3(sourceProjectId, newProjectId)` (§16.5): v2 source (v2 registry; v1/v3 refused `migration_version_unsupported`), not live-owned, missing/tampered source blob refused before any write; destination = new identity, manifest 1, scene 2→3 verbatim, content verbatim except the four derived-revision resets, `game: null`, storage 3, revision 0, retry cleared, empty history; marker `storageVersion: 3` with `sourceVersion`/`newVersion` and the four phases (`created`→`manifest`→`blobs`→`envelope`), destination-first/authoritative-last, idempotent/resumable, 120 s bound; refusals (`migration_destination_exists`, `migration_marker_conflict`, `migration_version_unsupported`, `migration_source_invalid`) write nothing and leave both trees byte-identical; the startup scan accepts both marker versions and reports `migration_resume_required` (also for a marker-only destination with no manifest — a bounded scan-order fix). Exports `MigrationResultV3`, `MigrationMarkerV3`, `AnyMigrationMarker` + the service method; the accepted v2 operator and its marker are preserved. Typed `PreparedMediaFacts` (`assetId, kind, version, sourceDigest, sourceByteLength, importRecipe`) and copy-safe verified byte reads (`readBlob` returns an independent copy). Evidence: all 3 valid v3 envelope fixtures load + rebuild byte-identically and all 21 invalid ones are refused with the recorded code; real SIGKILL at the v3 envelope boundary, at each copy marker phase, before/after the destination envelope, stale owner and clean-exit lost ack; disk/write fault, external change, source tamper/missing blob, unsupported old/new mix; source tree hashed identical after success and refusal. New committed fixture set `fixtures/m3/storage/**` (indexed `index.json`, v2/v3 project states, `cases/{durability,migration,media}.json`, self-contained plain-Node checker) — checker `EXIT=0`, `--corrupt-control` 6/6 detected. Commands: `npm test` **142 files / 1803 tests** `EXIT=0`; `typecheck`/`check-deps`/`check-boundaries` (15 pkgs, 294 files, 1148 specifiers, 0 violations)/`build` (4 built) all `EXIT=0`; M2 checker **34/34**; all M3 checkers + controls `EXIT=0`. Scoped diff: 8 modified + 2 new files under `packages/workspace/**`, 2 new under `tests/crash/`, 1 new under `tests/integration/m3-storage/`, new `fixtures/m3/storage/**`; one additive expectation update in `packages/workspace/tests/m2-storage.test.ts` (M3 now knows `storageVersion` 3, so the M2-era `storage-version-3` fixture reaches the §16.2 combination check) and one additive scan-order fix; no contract/lockfile/package change, no commit. B01 PASS (v3 durable + migration; full row 62), B18 PARTIAL/PASS-workspace (one envelope + SIGKILL/lost-ack convergence; SDK/WS transport is 48). CC-44-1 and CC-44-6 answered as section-level diffs in the handoff; CC-46-1 (the packet-39 `migration/v2-source` retry record is not a valid §5.1 result, so the fixture was not v2-pipeline-loadable) is **resolved by the bounded coordinator repair `handoffs/repair-cc46-1.md`**: the stale record is removed, `index.json` digest/bytes and the checker's new source-retry shape check updated (negative control exits 1), and the packet-46 migration test now seeds from the repaired contracts fixture with a new non-vacuous `retryCleared` test; post-repair `npm test` **142 files / 1804 tests** `EXIT=0`, all M3 checkers `EXIT=0`, M2 checker 34/34. Visual/browser and hardware rows remain UNVERIFIED. Next 47 — **do not start it** |
| 47 | WAV and animation import inspection | L | 46 | **done 2026-09-19** ([47.md](handoffs/47.md)): `@thirdlight/asset-pipeline` gains `inspectAudio(bytes, options)` — the pure §41.4 leaf (12 ordered stages, exact RIFF/WAVE header bytes, the §41.4.2 arithmetic, `PcmWavMetrics` in exact key order, the `pcm-wav` recipe `{profile, recipeVersion: 1, toolchain: {asset-pipeline: 0.1.0}}` with **no** `extensions`, the nine §41.7.2 C diagnostics, `audio_pcm_bytes`, bytes-only rejection — no extension/MIME/kind input and no URL/`data:`/compressed form, no I/O/Node built-in/decoder/`three`/network/cache write/asset-ID decision/state mutation, cancellation and timeout through the accepted injected `ImportJobPort` reporting `asset_timeout`) — and the role-aware GLB proposal: `ImportOptions.animation.roles` runs §41.3.2 stages 3–6 against the proposal's real clip list and then A1–A6 in order (8 clips; no `skins`/`JOINTS_0`/`WEIGHTS_0`; 64 channels total / 32 per clip; 4096 sampler input keyframes; 10 000 ms longest clip; no scene-root `translation` channel) carrying the §41.7.2 A fields (`role`/`clipIndex`/`clips`/`roles`/`matches`/`nodeIndex`/`nodeName`), while the accepted M2 proposal and its `gltf-glb` recipe stay byte-unchanged when no profile is requested; `importRecipeDigest` accepts the audio recipe and `importMetadataDigest` the audio proposal on the one canonical-JSON rule. **CC-44-2 resolved:** `project-model`'s `content.ts` audio branch enforces §18.5/§18.6/§41.4.3 on load (exact `pcm-wav` recipe with `extensions` absent, exact `PcmWavMetrics` keys, `sourceByteLength === 44 + pcmBytes`, every §41.4.2 cap and derived value, `limits_exceeded`/`field_value`, never normalized) with the audio types in `types-v3.ts` (`GltfGlbRecipeV3`/`PcmWavRecipe`/`ImportRecipeV3` union, `PcmWavMetrics`, `AssetMetricsV3`) and kind-aware v3 canonicalization, and the three committed packet-39 placeholder audio records (`catalog/audio-asset-record-v3.json`, `envelope/valid/demo-0003-media-v3.json`, `envelope/invalid/cue-kind-mismatch.json`) were regenerated from the real 236-byte `source-preimages/cue-start.wav` (96 frames, `pcmBytes` 192, `durationMs` 2) with `index.json` digests/bytes/notes updated. **CC-44-5 resolved:** `errors.ts` gains exactly the six §41.7.2 A codes and the six §41.7.1 limit names (`ERROR_CODES` +6, `LimitName` +6), with only the packet-sanctioned additive exact-list expectation in `api-behavior.test.ts` updated. **Fixtures:** `fixtures/m3/media/**` extended to **65** self-generated files (16 GLBs incl. the A1–A6 boundary positives `clips-at-cap`/`tracks-at-cap`/`keyframes-at-cap`/`clip-10s` and the `bad-joints`/`bad-skin-root`/`many-keyframes`/`many-tracks-per-clip` negatives; the full §41.4.4 stage/boundary WAV negative set incl. `fact`/`bext`/`cue `/`smpl`/duplicate chunks, RF64/BW64, µ-law/A-law, the exact 196 608-byte source bound, non-WAV bytes, `data:`/URL and gzip; `cue-min` (1 frame), `cue-preimage` (236 B) and a `wav-cases.json` `audioRecord` anchor); the media checker re-derives recipe/`recipeDigest`/`metadataDigest` from the bytes and cross-checks the contracts audio records + index digests, with a new `--corrupt-control` detecting **9/9**. **Evidence:** `tests/integration/m3-media-inspect/**` 22 tests (every WAV stage/boundary, the full profile matrix, role reorder/mismatch/ambiguity, independent digest re-derivation, project-model load/refusal, cancellation, no-network/no-filesystem probes + a source scan); `npm test` **143 files / 1826 tests** `EXIT=0`; `typecheck`/`check-deps`/`check-boundaries` (15 pkgs / 295 files / 1154 specifiers / 0 violations)/`build` (4 built) all `EXIT=0`; M2 checker **34/34**; all M3 checkers `EXIT=0` with their corruption controls (contracts 39 groups, gameplay 23 groups/127 checks, media 13 groups/115 checks, delivery 16/153, storage 9/10, audit/promotion). Scoped diff: 5 modified + 1 new under `packages/asset-pipeline/src/`; 5 modified under `packages/project-model/src/`; `packages/commands/src/m3-commands.test.ts` audio constants brought to the promoted shape; the packet-39 checker's metric key order made kind-aware; the three contracts audio fixtures + `index.json`; `fixtures/m3/media/**`; 1 new integration test. No contract/decision/lockfile/package/install change, no commit. B12 PASS for the pure inspection/validation half (empty/duplicate/oversized chunks, unsupported PCM, role reorder/missing/skin/root motion, caps, cancellation); B14 PASS for the media half (version-bound binding validated against the real clip list). UNVERIFIED: browser decode, audible output and hardware (54/61/62). CC-47-1…CC-47-5 recorded (the three out-of-list edits needed to make the promoted rules real: the `cue-kind-mismatch` placeholder record, the frozen packet-45 test's audio constants, the packet-39 checker's metric key list; the pipeline's copy of the §41.7.2 A codes; and the still-unenforced `audio_cues` cue cap). Next 48 — **do not start it** |
| 48 | Content/API/projection/MCP parity | L | 47 | **done 2026-09-19** ([48.md](handoffs/48.md)): `@thirdlight/protocol` gains the v3 authoring wire and the §20 relay shapes — new `m3.ts` (six-key v3 envelope + six-key `content` block structural validators `validateV3EnvelopeShape`/`validateV3ContentBlock`/`validateV3SceneShape` with deep validity left to `project-model`, `validateFullStateFrame` over the §19.4 bounded projection, `validateChangeFrame`, `validateQueryResultV3`, `parseStageInspectRequest` `{kind?, animation?}`, strict ≤ 4 KiB control request/result and ≤ 16 KiB observation validators with ≤ 32 events and a binary-free assertion, the closed §20 error set, `parseAdminMigrateCopyV3Request`) plus `http.ts` adding `applySurfacePreset`/`setGameConfig` (`MutationOp`) and `queryGameConfig` (`ALL_COMMAND_OPS`) so the router no longer rejects v3 envelopes, `ws-events.ts` adding the promoted §7.1/§7.2 `game.control.request`/`game.observe.request`/`game.control.ack`/`game.observe.ack` rows with builders + strict inbound validation, and `errors.ts` the §20 closed codes + `limits_exceeded` + the five migration codes and a `runId` field. Backend: `createAssetInspector()` is kind/role-aware (`inspectAudio` + `AUDIO_PCM_WAV_TOOLCHAIN` for `{kind:"audio"}`; the §41.3.3 animated profile for `{animation:{roles}}`; the accepted M2 proposal byte-unchanged otherwise) through the additive workspace seam (`StageInspector` optional third `request`, `InspectorRequest`/`ImportedProposal`, `InspectStageOptions.kind/animation` — the only workspace edit), so a real animated reimport reaches §41.3.2 stages 5–7 and §41.3.3 A1–A6 via upload→inspect→`publishAsset`; the additive admin operator route `POST /api/v1/admin/projects/:projectId/migrate-copy-v3` returns the §16.5.2 object and the §16.8 codes with refusals writing nothing; `PlayManager` gains the bounded §20 control/observe relay (one pending relay per play, request-driven timeout, forward-only last-known run identity, teardown on stop/dispose) with the two routes and WS ack routing; MCP gains the real `tl_game_control`/`tl_game_observe` tools + client methods, `tl_command` the v3 ops and `tl_content_upload` `kind`/`animation` — no simulation, no credential/capability/binary crossing, no browser ⇒ the contracted `session_unavailable`. Evidence `tests/integration/m3-content/**` (real fs + real backend process + real WS + real stdio MCP SDK): v3 config/preset edits, two-client stale conflict + convergence, retry `duplicated:true`, undo, binary-free change frames, role-aware animated reimport incl. rejections (mismatch/skin/root-motion/ambiguity) and reordered reimport + undo, **all 33** committed §41.4.4 WAV rejections through the transport, one audio publication, the migration success/exists/version/unknown-source cases + non-admin 401, and the §20 relay (not-presented `session_unavailable`, unknown play, verbatim control/observation, `game_run_stale`, `game_relay_timeout` 503, wrong-session ack dropped, bounded credential-free logs, strict wire shapes). Checks: `npm test` **146 files / 1863 tests** `EXIT=0`; `typecheck`/`check-deps`/`check-boundaries` (15 pkgs, 296 files, 1163 specifiers, 0 violations)/`build` (4 built) all `EXIT=0`; M2 checker **34/34**; all M3 checkers + corruption controls `EXIT=0` (contracts 39 groups, gameplay 23/127, media 13/115 + 9/9, delivery 16/153 + both, storage 9/10 + 6/6, audit/promotion + controls; the contracts/gameplay manual copy+corrupt controls exit 1). Scoped diff: `packages/protocol/src/{http,errors,ws-events,ws-events.test,index}.ts` + new `m3.ts`; `packages/backend/src/{content,backend,play}.ts`; `packages/mcp-adapter/src/{backend-client,tools}.ts`; the workspace inspector seam only (`content-store.ts`/`index.ts`); 5 new integration files. No contract/lockfile/package/install change, no commit. Acceptance (transport scope): **B03/B12/B17/B20 PASS**, **B02 PASS** (the query half was completed by the bounded coordinator repair `handoffs/repair-cc48-1.md`: the workspace dispatch now serves `queryGameConfig` and the `queryEntities` `component` filter, `tl_content_query target="game"` is added, and the same read is asserted over the real MCP stdio client and the real backend HTTP `/commands` route), **B18 PARTIAL** (lost-ack replay over the real process; v3 durability/SIGKILL/external change are packet 46's). Post-repair toolchain: `npm test` **147 files / 1866 tests** `EXIT=0`, `typecheck`/`check-deps`/`build` `EXIT=0`, `check-boundaries` 15 pkgs / **297 files / 1167 specifiers / 0 violations**, M2 checker 34/34 and every M3 checker + control `EXIT=0`. Browser/visual/hardware rows remain **UNVERIFIED** (49+/54/59/61/62). CC-48-1 (workspace query dispatch) **RESOLVED by `handoffs/repair-cc48-1.md`** (workspace dispatch + `tl_content_query target="game"` + tests; no contract change), CC-48-2 (the `migrate-copy-v3` route is not named in sessions.md §6.3), CC-48-3 (full-state `scene.schemaVersion` still reports the manifest's `1`; C35-5 gap) — both for Gate L, and the independent CC-47-5 read recorded (**confirmed structurally unreachable**: `content.game.cues` has five fixed keys and `zone_checkpoint_count_invalid`/`checkpointZones = 1` bounds the single `activation.cueAssetId`, so no valid v3 state can reference 7 distinct audio assets — §41.7.1 itself annotates `6` as "5 cue keys + 1 optional activation cue"). Next Gate L review — **do not perform it here, and do not start 49** |
| 49 | Game flow and swept gameplay zones | M | Gate L accepted | **done 2026-09-20** ([49.md](handoffs/49.md)): `@thirdlight/runtime` carries the committed M3 game session — the `GameSession` run state machine (T1–T8, the run-boundary command consumption after the settle pre-roll, the bounded ≤ 32 event ring, `runSnapshot`/view bookkeeping), the M3 composition validation (v3 scene + `game` block projection, one `gameplay` + exactly one `camera` module, `cameraFollow`, the `owner_mismatch` refusal), the M3 step pipeline (intent/controller/physics/transform/gameplay/camera + the step-end promotion kept at the accepted M2 timing — CC-49-2), the §2.5 effective-frame overrides (neutral frame in `awaitingStart`/`respawning`/`won`; forced `jump: 'none'` at the first live step), the committed `GameView` (published at commit/boundary/fail-stop only, deep-frozen copy), `getGameView`/`gameCommand`/`setViewport` (M1/M2 `game_session_unavailable` guards), the M3 diagnostics (`pendingCommands`, `failure.phase` — CC-49-1) and the T7 fail-stop that retains the last complete committed view (`tick()` stays `{ok: true}`, state `failed`); the new pure `@thirdlight/platformer-game` package ships the stateless `gameplay`-phase module `thirdlight.platformer-game:session` (the §4.2 swept upright-capsule vs axis-aligned XY zone predicate over the runtime's committed motion segments; kill-Y fall; single-activation checkpoints; one-shot goal; the §4.3 precedence) with a types-only edge to `runtime`. Evidence: the five promoted `fixtures/m3/gameplay/run/**` fixtures replayed through the real `GameSession` (states/respawn-timing/events-bound/failure-phases/game-view), 12 runtime-wiring tests (composition table, queue semantics, the effective-frame table, port phase rules, the camera owner write guard, goal, ring eviction, fail-stop, dispose) and 3 real-Rapier course runs (real M2 controller + real session module + test-only camera module: happy path start→checkpoint→goal→won + the T6 replay re-win, the fall death + bounded respawn boundary + the deterministic no-physical-reset re-death, the hazard death + same-step re-death); `npm test` **150 files / 1888 tests** `EXIT=0`; `typecheck` (16 pkgs)/`check-deps`/`check-boundaries` (16 pkgs, 303 files, 1185 specifiers, 0 violations)/`build` (4 built) all `EXIT=0`; M2 checker **34/34**; all M3 checkers + corruption controls `EXIT=0`; M1/M2 frozen traces and fixture bytes unchanged. CC-49-1 (the promoted `failure-phases.json` pins a `phase` the contract's `GameView.failure` lacks — additive optional label added), CC-49-2 (`segment-source.json` pins the M2 step-end promotion timing for M3 steps — kept unchanged, the boundary promotes nothing), CC-49-3 (the pinned `thirdlight.platformer-game:session`/`:camera` module IDs violate the carried M1 registry name regex — the first segment widened to `[a-z0-9-]+`, additive, no test pinned the rejection; proposed contract diff at runtime.md §12.1). The due reset is the queued-request no-op seam only (the physical R1–R8 re-placement is packet 50) — no completed respawn claimed. B04/B08 logic PASS, B05 logic PASS, B06 logic PASS, B09 partial (fail-stop half); all visual/browser/hardware rows UNVERIFIED. Next 50 — **do not start it** |
| 50 | Runtime-owned safe respawn | M | 49 | **done 2026-09-20** ([50.md](handoffs/50.md)): the runtime owns the full R1–R7 reset transaction over the real Rapier `PhysicsResetPort` (packet 49's no-op seam replaced). `runResetTransaction` implements gameplay.md §5.1: R1 resolve the destination (the checkpoint `safeSpawnId` when active, else `spawnId`), R2 verify (inside level, `y ≥ killY`, no hazard overlap — pure reads), R3 clearance probe (the real Rapier `characterClearance` narrow-phase query), R4 physics reset (`clearCharacterMotion` + `placeCharacter` — the §5.2 restricted operations), R5 stage the pose, R6 module `reset()` hooks (the controller zeroes velocity/coyote/jump-buffer/windows + the pending control intent), R7 apply + rebase (`prev := curr`, the player motion segment rebased to a zero-motion segment at the reset pose — the no-render-streak invariant, so the respawn step's §4.2 swept-zone check cannot re-kill the capsule). A reset-phase failure fail-stops with no rollback (`failReset`: cancel driver, `stateName = 'failed'`, record the error, `session.markFailure` with the `reset:<label>` phase, publish the retained view); a test-only `injectResetFault` seam reproduces each R1–R8 signature (pinned by `fixtures/m3/gameplay/run/failure-phases.json`). `@thirdlight/physics-rapier` gains `PhysicsResetPort` (`clearCharacterMotion`/`placeCharacter`/`characterClearance` → `blocked`/`no_support`/`query_failed`); `@thirdlight/platformer` gains the controller `reset(ctx)` hook. Evidence (all real Rapier): `tests/m3-respawn/respawn.test.ts` (10/10) — death-before-checkpoint → respawn at the start spawn (3, 0.91) grounded; death-after-checkpoint → safe spawn (22, 0.91); velocity/windows/render-streak (prev/curr rebase) coherence; repeated deaths (3×); buffered/held jump + pending intent cleared by R6; blocked destination (real R3) → `game_spawn_blocked`/`blocked` at step 13; stop during the delay (step-based counter); injected failure at each reset phase (exact pinned signatures); failed runtime cannot resume (`tick` → `runtime_failed`, run command rejected); snapshot unchanged. `tests/m3-gameplay/course.test.ts` (9/9) real-Rapier course runs. Key decision: the boundary event is published at the boundary, before R1–R7 (gameplay.md §3.2 item 0 + the `check-fixtures.mjs` reference replay + the promoted `failure-phases.json` `fail-reset-R1..R8` expect `runStarted` present), so a reset-phase failure shows the boundary event present + `failed: true` + the session's `state: 'playing'` (T1/T5 set the state before R1) — matching the fixture's expected `state: "playing"`. `npm test` **151 files / 1898 tests** `EXIT=0`; `typecheck` (16 pkgs)/`check-deps`/`check-boundaries` (16 pkgs, 303 files, 1185 specifiers, 0 violations)/`build` (4 built) all `EXIT=0`; M2 checker **34/34**; all M3 checkers + corruption controls `EXIT=0` (gameplay 23/127, contracts 39, media 13/115 + 9/9, delivery 16/153 + both, storage 9/10 + 6/6, audit/promotion + controls, media generate-fixtures 65/65); M1/M2 frozen traces and fixture bytes unchanged (no fixture file modified). No general teleport API or raw physics handle exposed (B09). B06/B07/B09 logic PASS; no reset by HUD/editor/script; all visual/browser/hardware rows UNVERIFIED (the camera half of the reset is a test-only stub; the real camera module is packet 51). Next 51 — **do not start it** |
| 51 | Follow camera | M | 50 | **done 2026-09-20** ([51.md](handoffs/51.md)): `packages/platformer-game/src/camera.ts` (new) implements gameplay.md §7 / runtime.md §12.1 — pure `followCamera` (dead zone → smoothing → per-axis `CAMERA_MAX_STEP = 4` cap, skipped on snap → authored bounds → frustum clamp with centre fallback; no-write below `CAMERA_SNAP_EPS = 1e-9` on both axes — bit-identical to the checker reference; the committed `follow.json`/`resize.json` fixtures replay bit-equal, 7/7) and `createGameCameraModule` (the single camera owner, §3.4/§7.6: writes `position.x/y` only in the `camera` phase + the R6 reset barrier — start/replay → start spawn, respawn → that spawn; Z/quaternion/scale never written; `transformOwners: [cameraId]`; a non-finite viewport — unreachable via `setViewport` — is a no-write + one bounded `behaviorLog('warn', camera_viewport_invalid)`, never a fail-stop; the runtime's initial `{0, 0, DEFAULT_ASPECT}` record is the valid "until setViewport" state). Boundary fix: the `platformer-game → runtime` edge is types-only (dependencies.md §4.1) — the §7.1 constants are defined in-package (`CAMERA_CONSTANTS`); the runtime keeps its own export row (gameplay.md §15). Test-only camera stubs in `tests/m3-respawn/respawn.test.ts` and `tests/m3-gameplay/course.test.ts` replaced by the real module (respawn: zero assertion changes; course: camera asserts rewritten to real semantics; the `m3-runtime.test.ts` guard-isolation stub stays). New `tests/m3-camera/` (23 tests): `follow.test.ts` (pure fixture replay, bit-equal), `module.test.ts` (write semantics/defenses), `runtime.test.ts` (real runtime + real Rapier + real camera: start/replay snaps to the exact frustum-min double; level-edge pins at the exact fixture doubles — right clamp on the walk, left clamp through walk-off-edge + fall + death + respawn snap; 16:9→4:3→2560×720 aspect changes clamp at the exact doubles with physics state identical across resize; short authored bounds dominate the frustum exactly; fall pins Y at the exact frustum min and respawn is a one-step snap, no streak; `setViewport` rejects the committed non-finite set verbatim and retains the previous record; the §3.4 `camera_missing`/`camera_owner multiple`/`owner_mismatch` rejections with the real specs). **CC-51-1 (adjudicate at Gate M):** `checkCameraOwner` in `fixtures/m3/gameplay/tools/check-fixtures.mjs` re-derived the owner check with the case-level `c.cameraId` (always `undefined`) instead of `doc.cameraId` — every single-camera case fell into `owner_mismatch`, masking the later §3.4 checks; the committed `owner.json` expects were aligned to that buggy reference. Fixed the checker (`c.cameraId` → `doc.cameraId`) and repaired the five expects to the reasons the real runtime emits (verified on every case): `owner-gameplay-conflict` → `gameplay_module`, `owner-scene-version` → `scene_version`, `owner-game-null` → `game_config`, `owner-no-camera-follow` → `camera_follow`, `owner-ok` → `{ ok: true }`; `fixtures/m3/camera/index.json` digest updated (owner.json 4768 B, sha256 `1ff2e681…be52e`); corrupt control (revert owner-ok) exit 1. **CC-51-2 (adjudicate at Gate M):** consequence of CC-51-1 — `check-audit [codes]` reports the four now-declared reasons (`gameplay_module`/`scene_version`/`game_config`/`camera_follow`; documented verbatim in runtime.md lines 1040, 1046–1049) as unregistered in the packet-43 registry `fixtures/m3/audit/index.json → reasonAllowlist`; `fixtures/m3/audit/**` is not in packet 51's may-edit set, so requested diff: add the four reasons (until then `check-audit` = 7/8 groups, all others green). `npm test` **154 files / 1921 tests** `EXIT=0` (+3 files / +23); `typecheck` (16 pkgs) / `check-deps` / `check-boundaries` (16 pkgs, 304 files, 1188 specifiers, 0 violations) / `build` (4 built) all `EXIT=0`; M2 checkers all green (contracts 34/34); M3 checkers: gameplay **23 groups / 127 checks**, contracts 39, media 13/115 + 9/9 control, delivery 16/153 + both controls, storage 9/10 + 6/6 control, promotion + its control green, media generate-fixtures 65/65; `check-audit` 7/8 groups (the one expected `[codes]` failure, CC-51-2) and its `--corrupt-control` PASSes all corruptions but exits 1 while the clean tree is red (the control script requires a green clean tree). M1/M2 frozen traces unchanged; fixture bytes unchanged except the two CC-51-1 files. B10 (packet 51's numeric/real-runtime portion) PASS — no second frame driver, no feedback into player physics; camera Y after a physics traversal sits on the dead-zone edge of the solver's settled rest line (tests assert the band, not a float-chain value; X/Z and clamp doubles exact or within the 1e-9 no-write EPS); browser framing at two desktop aspects **UNVERIFIED** (no browser in-container; lands in 59/61–62). Next: **Gate M** (fresh read-only reviewer over 49–51, re-run toolchain, adjudicate CC-51-1/CC-51-2 → `docs/handoffs/gate-m.md`), then 52 — **do not start it** |
| 52 | Lights/shadows/primitive materials | N | Gate M accepted | **done 2026-09-20** ([52.md](handoffs/52.md)): `packages/three-adapter` realizes presentation.md §§41.1/41.2 (M3 only; M1/M2 paths unchanged). New `src/lighting.ts` (pure — no three/DOM/Node/clock): `SHADOW_PROFILE` (the seven §41.1.2 frozen constants 512/PCFShadowMap/0.5/20/2/64/200, deep-frozen, bit-equal to the promoted fixture's `shadow` block), `deriveShadowCamera` (exact §41.1.3: centre `((minX+maxX)/2,(minY+maxY)/2,0)`, `halfExtent = max(Δx/2,Δy/2)+margin`, `far = min(distance+halfExtent+margin, farMax)`, `lightPosition = centre − n·distance` from a derived normalized copy — the document is never rewritten; `-0`→`+0`), `decideShadows` (the §41.1.4 decision in the exact priority the promoted fixture's reference re-derivation uses: no-WebGL2 ⇒ hard `render_unsupported`; author `castShadow:false` ⇒ off/`cast_shadow_false`; probe failure ⇒ off/`shadow_unsupported` (soft, key light only); bounds exceeded ⇒ off/`shadow_bounds_exceeded`; else on+plan; the plan is on every `ok` outcome — the directional position/target derive regardless of shadow state), `SURFACE_PRESETS` (the three §41.2.1 frozen rows — no `project-model` edge ⇒ own copy, single change point per package; test pins it bit-equal to the contract rows, the media checker pins the fixture to the model table), `planSceneLights` (rules 1/2/4: ambient `(color,intensity)` no position dependence; directional + derived position/target with `castShadow` exactly the decided state; ≤1 directional + ≤1 ambient node per realized scene). `createSceneAdapter` (the accepted options surface): v3 detection (`schemaVersion===3` + `game.level`); authored `light` components realized as `THREE.AmbientLight`/`THREE.DirectionalLight` with the derived position/target (always, shadow state or not); the M1 hardcoded pair is v1/v2 only (unchanged); `surface` boxes get one `THREE.MeshStandardMaterial` per placement (component values literal, no preset lookup — value-level independence §41.2.3), no `surface` ⇒ M1 Lambert (unchanged); the hard WebGL-2 gate (a WebGL-1 context for a v3 scene ⇒ `render_unsupported`, baseline.md §1; M1/M2 webgl1 fallback unchanged); the once-per-realized-scene first-render shadow probe (`maxTextureSize ≥ 512` + probe render with `shadowMap.enabled` + `THREE.PCFShadowMap` + 512²/derived camera; failure degrades soft and flips the diagnostic; no shadow-casting light ⇒ `shadowMap` never enabled, no map allocated, rule 5); `SceneAdapterDiagnostics` gains exactly the two §41.1.4 read-only fields `shadows:'on'|'off'` + `shadowReason?` (present iff off; once per realized scene, never per frame; no path/token/device string). `ERROR_CODES` gains `animation_role_unresolved` (§41.7.2 D/§41.7.3 — registered in 52, raised by 53's controller). Root-subpath exports: the lighting surface + types (no new subpath/pin/dependency; loader-free graph unchanged). `tests/browser/m3-render/` (manual host + README, packet-32/37 procedure; the host composes the real packages over a v3 scene and records the B11 named checklist — authored-vs-fixture light values, derived position/target+shadow camera, preset rows + shadow constants adapter-vs-fixture bit-equal, real screenshots shadow-on 16:9+4:3 resize / shadow-off-by-author, the diagnostics, a synthetic context-loss/recovery, 5× create/dispose, value-level material independence; typechecks clean under strict; named `.browser.ts` so vitest never collects it) — **UNVERIFIED in-container** (no browser/GPU). No fixture file modified (the promoted `fixtures/m3/media/render/light-surface-cases.json` is the pinned data the media checker already re-derives; `fixtures/m3/render/**` may-edit dir therefore unused). `npm test` **156 files / 1948 tests** `EXIT=0` (+2 files/+27 vs the post-gate 154/1921); `typecheck` (16 pkgs) / `check-deps` / `check-boundaries` (16 pkgs, 307 files, 1195 specifiers, 0 violations) / `build` (4 built) all `EXIT=0`; M2 checkers all green (contracts 34/34); M3 checkers: gameplay 23/127, contracts 39, media 13/115 + 9/9 control, delivery 16/153 + both controls, storage 9/10 + 6/6 control, audit 8/8 + control (post Gate M CC-51-2), promotion + control green, media generate-fixtures 65/65; M1/M2 frozen traces and all fixture bytes unchanged. B11 (packet 52's realization/verification portion) logic PASS — the named-checklist data the browser run compares is verified in-container (preset rows/shadow constants/authored light rows bit-equal adapter↔fixture; the §41.1.4 table replays the fixture's five `shadowCases` verbatim incl. the hard `no-webgl2`; the §41.1.3 derivation exact on the fixture case; no new pipeline/shader graph/pin); B11 browser half **UNVERIFIED** (57/61–62; the unsupported-shadows fallback and the WebGL-1 hard gate can't be forced in a healthy browser — pinned by fixture + pure tests; no GPU-memory claim from JS object counts). `SHADOW_FAR_MAX=200` is an unreachable safety bound with the frozen constants (raw far ≤ 86 at `halfExtent ≤ 64`) — the `min` is implemented and the formula asserted at the 64 boundary. No contract-change requests. Next 53 — **do not start it** (note: the carried CC-L-1 P2 reimport `modelAnimation.version` defect must be repaired before 53 proceeds) |
| 53 | Rigid runtime animation | N | 52 | **done 2026-09-20** ([53.md](handoffs/53.md)): `packages/three-adapter` adds the approved role selector + bounded crossfade (presentation.md §41.3.6/§41.3.7/§41.9, root subpath, no new subpath/pin). New `src/animation.ts`: `createAnimationRoleController(instance, view)` → `AnimationRoleController` (`setRoles(roles, version)`/`update(deltaSeconds)`/`state()`/`dispose()`); one `THREE.AnimationMixer` per instance (rule 5 independent pose/mixer per copy — no global mixer/shared action/shared clock); one action per role (≤3); **no** rAF/timer/mixer-listener/clock (rule 2 — host-driven, `0 ≤ delta ≤ 0.25` else `preview_invalid`); rule 3 pure `selectAnimationRole` (airborne iff `!grounded`; run iff `speed > RUN_SPEED_EPS=0.05`; else idle) from the host-passed committed view slice (`stepIndex`+`playerMotion`, §41.3.7 — the controller never references the runtime, samples input or writes a transform, rules 1/6); rule 4 exactly `ANIMATION_CROSSFADE_SECONDS=0.2` via `crossFadeTo(fadeIn, 0.2, false)` (first advance full weight no-fade; blending-while-blending retargets — previous incoming becomes outgoing); rule 7 `setRoles` re-checks against the LOADED clips (stale-version guard — authored bytes stay pinned during active play; stage 3 range / stage 5 name / stage 6 ambiguity) → hard `animation_role_unresolved` (the 52-registered code) + bounded message; a rejected mapping installs nothing (static render, `update` ok, zero weights); rule 8 `dispose()` safe mid-blend + idempotent (`alreadyDisposed`), stops/uncaches actions and releases the mixer exactly once; controllers are tracked by their instance — `ModelInstance.dispose()` releases each exactly once (§41.6 mixer row), attach on a disposed instance ⇒ `asset_disposed`. `visual.ts`: `ModelInstance.animationClips()` (the stage 3/5–6 input) + internal WeakMap/WeakSet tracking (not public) + the instance dispose path; `index.ts` root-subpath exports; `test-glb.ts` (test support) multi-clip GLB builder (default output byte-identical). `platformer-game` **unchanged** (the runtime already commits `GameView.playerMotion` per C41-1; §41.3.6/§41.9 place the selector on the three-adapter root). Tests: `animation.test.ts` (24, real three@0.186.0 mixers + real GLB clips: rule 3 boundary table incl. `speed===0.05`⇒idle, rule 4 law, rule 7 re-check table; realized 0.2 s fade matches `t/0.2` within 1e-6, retarget-while-blending, reordered-clip mapping, delta bounds (0/0.25 accepted; −/0.26/NaN/∞ refused), two instances of one resource in different roles with zero cross-talk, holder pose byte-identical across the timeline (no transform writes), state stable between explicit updates (host-driven), dispose-while-blending, instance-dispose-releases-controller + second instance stays usable, attach-on-disposed refusal, re-install clean start) + `tests/m3-animation/roles-real-loader.test.ts` (Node, real `GLTFLoader` port over the REAL committed `courier-roles.glb`/`courier-reordered.glb` digest-verified against the media index: stored-order v1 installs+drives, reordered-v2 installs on reordered bytes (§41.3.4 rule 5), stale stored-order mapping vs reordered bytes refused `animation_role_unresolved` with the stage-5 clipName disagreement named). **Committed real-roles fixture (the Gate L P3 / CC-47-1 obligation)**: new `fixtures/m3/media/roles/roles-cases.json` (digest-pinned; stored-order v1 accepted, reordered v2 accepted, stored-order-vs-reordered rejected `animation_role_mismatch`) — the frozen contract envelopes keep their packet-41 placeholder stubs untouched; `generate-fixtures.mjs` now emits 66 files (all 65 pre-existing reproduce exactly), `check-fixtures.mjs` gained the `roles` group (every verdict re-derived with the same stage 3/5–6 function + component shape pins) and a `roles-case-expectation` corruption control (14 groups/119 checks; control 10/10); `media-inspect.test.ts` pin 9/9→10/10. `tests/browser/m3-animation/` (manual host + README, packet-32/37 procedure; B14 named checklist A1–A9: the committed rows install/refuse in-browser, the 0.2 s law sampled against the realized mixer, blending flag, per-copy independence + committed step index, rendered poses A-airborne-vs-B-idle on one shared resource, no transform writes, dispose-while-blending with the shared resource surviving, three real screenshots, fetched-fixture SHA-256 integrity; typechecks clean strict; `.browser.ts` so vitest never collects it) — **UNVERIFIED in-container** (no browser/GPU). `npm test` **158 files / 1976 tests** `EXIT=0` (+2 files/+28 vs 156/1948); `typecheck` (16 pkgs) / `check-deps` / `check-boundaries` (16 pkgs, 309 files, 1204 specifiers, 0 violations) / `build` (4 built) all `EXIT=0`; M2 checkers all green (contracts 34/34); M3 checkers: gameplay, contracts, media 14/119 + 10/10 control, delivery, storage, audit 8/8 + control, promotion + control, media generate-fixtures 66/66. B14 logic PASS (independent pose/mixer, idle/run/airborne switch, 0.2 s blend duration — realized weights match the contract law within 1e-6, clip reorder with new mapping, missing-role refusal, stale-load refusal, disposal while blending, all over the real three stack; root-motion/skin rejection stays packet 47's `inspectGlb` evidence — the 53 selector has no skeletal surface, **never claimed** skeletal isolation §41.10 non-goal); B14 browser half **UNVERIFIED** (rendered poses/pixels, on-screen crossfade, screenshots — host + procedure shipped). three's mid-fade retarget ramps the re-outgoing action from its nominal weight (three-internal; the contract pins the clean two-action law and names `crossFadeTo(…,false)` — the retarget test pins completion, not the transient). The role controller's mixer is counted by the controller's own idempotence (§41.6 counter row), not the resource ownership ledger (balanced either way). No contract-change requests. Next 54 — **do not start it** (note: from here on no sub-sessions — the vLLM host runs out of KV cache for children; Gates N/O/P + all remaining packet work proceed in this session) |
| 54 | Browser audio owner | N | 53 | **done 2026-09-20** ([54.md](handoffs/54.md)): new `packages/game-host/**` unit (first part only — the `.` audio entry, presentation.md §41.9 "created only in packet 54 after Gate K accepts the dependencies.md rows"; the delivery.md §§3–5 composition `createGameHost`/HUD/controls is packet 55 on the same entry, no stubs). `src/audio.ts`: the injected owner of §41.4.7 — the eight binding interface members exactly as contracted (`registerCue`/`submit`/`unlock`/`setMuted`/`setHidden`/`status`/`dispose`) + closed `GameAudioError` codes (`audio_decode_failed`/`audio_unsupported`/`audio_disposed`/`audio_invalid_bytes`, messages ≤256 log-safe). Rule 1 bytes-in only (no URL/locator/data:/fetch/XHR/<audio>/base64 anywhere in the module; cue events carry assetId only); rule 2 bounded async decode on a **copy** (`bytes.slice()`), a failure (rejected promise OR synchronous platform throw) is a bounded per-cue diagnostic never a throw across the host boundary, bytes never mutated (both test-proven); rule 3 `AUDIO_MAX_VOICES=8` concurrent voices, a new cue at the cap is dropped with one bounded `voice_cap` diagnostic never queued, every voice released on ended/stop exactly once; rule 4 dedupe by `GameCueEvent.id` (at most once per owner), the current `runId`'s ids kept, a runId change clears them (checkpointActivated never refires on re-entry — 40 §4.5); rule 5 a runId change (stop/replay) and `dispose()` bump the epoch and stop every voice, a decode resolving after a run change is **discarded, never played** and a fresh decode of the same asset is re-armed for the current run (the cue is an asset reference; run identity lives on the event, which `submit` re-checks — a stale-run event inside a current-run submit is skipped, never played); rule 6 `setHidden(true)` suspends the owned context (voices keep position — resume continues in place, no fast-forward/replay), `setHidden(false)` attempts `resume()` only when already unlocked, a rejected resume **degrades to `blocked` not an error** and a later gesture retries; rule 7 `unlock()` is the host's real-gesture call (browser wiring checks `event.isTrusted` — this host + 55/62), the owner never unlocks on register/submit, the context is created at the gesture never at load, `submit`/`registerCue` always succeed while sound-off (the game never waits for audio); rule 8 `dispose()` closes exactly the contexts it created once (second call `alreadyDisposed`), closing synchronous w.r.t. public state (after dispose every other method returns `audio_disposed`/`disposed`); rule 9 no Node builtin/DOM in the owner (deterministic with the injected `contextFactory` — reads no clock/timer/global audio state), the entry's guarded `browserContextFactory()` (the only `window` touchpoint) returns null when no Web Audio → `unsupported`/`no_audio_context`. Status ladder (§41.4.6): `unsupported` (no factory / yields null) / `blocked` (pre-gesture autoplay, a rejected resume, or a created-but-closed context ⇒ `no_device`) / `ready` (`muted`,`unlocked` — a hidden-suspended context is a normal paused `ready`) / `disposed`. Additive non-binding surface: `diagnostics()` — the bounded 64-entry drop-oldest ring the contract's failure table names ("one bounded diagnostic is recorded"), codes `audio_decode_failed`/`audio_invalid_bytes`/`audio_unsupported`/`voice_cap`/`stale_work_discarded`/`cue_skipped`/`suspend_failed` (the 8 binding members are exactly the contracted set). `package.json`: **zero dependencies** (the audio entry is self-contained; the §4.1 composition edges are declared when 55 lands them), `exports` = `.` only (the host's DOM/control/audio/composition modules are not subpaths — a wrapper cannot reach past the entry). Tooling: `tools/check-boundaries.mjs` adds `game-host` to the §2 `UNITS` list + `NODE_SIDE_ALLOWED` (`packages:[],external:[],node:[]` — the packet's boundary-checker/table/tests may-edit scope); the workspace link materialized via `npm install` (lockfile diff = exactly the 8-line `packages/game-host` workspace entry, no external deps, no pin changes). Tests: `packages/game-host/src/audio.test.ts` (21, deterministic fake Web Audio graph, no Node builtins: bytes-in validation incl. the 16-asset store cap, decode-on-copy + never-mutated, malformed decode = bounded diagnostic + game continues, the sound-off ladder (pre-gesture `blocked`, no-factory `unsupported`, rejected resume `blocked`+gesture retry, factory-null→unsupported then recovered-environment unlock, created-closed→`no_device`, idempotent unlock = one factory call), hidden-before-unlock (unlocks then re-suspends), the full cue path (source→gain→destination graph, dedupe triple→one source, unregistered asset skipped), the 8-voice cap (9th dropped `voice_cap`, never a 9th source, an `ended` frees the slot), the 64-entry ring, the run change (voices stopped, gated in-flight decodes discarded AND re-armed as new work, the new run's cue plays, a stale-run event inside a current-run submit skipped), zero-event submit, mute (voices stopped, decodes deferred, nothing plays, unmute decodes+plays), hidden (suspend/resume, a rejected visible-resume degrades to `blocked`, a later gesture recovers, no resume before unlock), dispose (one close, voices released, every method disposed, a post-dispose decode discarded), re-registered asset version serves new bytes, determinism (two owners, identical scripts → identical observable sequences)) + `tests/m3-audio/owner-real-cues.test.ts` (Node: the REAL committed `fixtures/m3/media/wav/cue-*.wav` bytes — SHA-256+length verified against the media `index.json` — decoded by a pure re-derivation of the §41.4.4 arithmetic; the five committed cue kinds each sound once at their real PCM durations; a 20-cue jump flood holds the 8-voice cap (exactly 12 bounded `voice_cap` drops, no 9th+ source); mute/unmute/hidden/dispose over the real cues (one close, `audio_disposed` after); the real `wav/rejections/bad-magic.wav` bytes take the malformed-decode path (bounded `audio_decode_failed`, the good cues still sound)) + `tests/browser/m3-audio/` (manual host + README, packet-32/37 procedure; named `.browser.ts` so vitest never collects it; typechecks clean strict; bundles with the pinned esbuild — 21.1 kb `dist/m3-audio-test/harness.js`; the B12/B13 named checklist A1–A11: the committed rows' fixture integrity, sound-off completion before any gesture (zero sources), a REAL local `isTrusted` gesture driving `unlock()`, the five cue kinds realized as real voices (the host's pass-through instrumentation wrapper around the REAL `AudioContext` records each source's buffer duration at `start()` — the wrapper observes, never intercepts), the dedupe triple, the 12-cue flood against the cap, mute/hidden/visible, exactly-one `close()`, and `m3-audio-evidence.png`) — **UNVERIFIED in-container** (no browser/GPU/audio device; audibility by contract §41.4.7 rule 9 / §41.4.8, witnessed on a real device at 54/62). `npm test` **160 files / 2001 tests** `EXIT=0` (+2 files/+25 vs 158/1976); `typecheck` (**17 pkgs**) / `check-deps` / `check-boundaries` (17 pkgs, 312 files, 1208 specifiers, 0 violations) / `build` (4 built) all `EXIT=0`; M2 checkers all green (contracts 34/34); M3 checkers: gameplay, contracts, media 14/119 + 10/10 control, delivery, storage, audit 8/8 + control, promotion + control, media generate-fixtures 66/66. B12/B13 owner/activation (logic) PASS (bounded short cues with honest activation + sound-off behavior: bytes-in registration, the real PCM durations of the committed cue bytes, dedupe by run/event identity, the 8-voice cap under a flood, the stale run/decode cancellation, mute/unmute, hidden/visible suspend/resume, rejected resume→`blocked` never an error, exactly-one context close, the sound-off ladder — all over the real implementation with the real committed fixture bytes; no remote/audio fetch in the owner — no fetch/URL/`data:` surface exists — and no new npm audio library — zero new dependencies); B12/B13 browser/audible half **UNVERIFIED** (the real `AudioContext`, the real gesture reaching `running`, the audible jump/checkpoint/death/goal cues — the host + procedure are shipped; counters and context state prove the graph, not the ear §41.4.8). Limitations: the stale-decode re-arm (rule 5) is documented — the discarded buffer is never played (the binding clause) and a fresh decode of the same asset is re-armed for the current run so a legitimate new-run cue can still sound (without it the first replay would permanently silence in-flight assets until re-registration, inconsistent with §41.5.3 "the checkpoint cue sounds once per run"); a non-string `assetId` maps to the closed `audio_invalid_bytes` (the set has no registration-shape code; the host always passes the typed assetId). No contract-change requests (realizes §41.4.7 exactly; the additive `diagnostics()` read + the §4.1 allow-row encoding are documented, not contract changes; no contract/fixture/pin touched). Next 55 — **do not start it** (Gate N after 55, in-session — no sub-sessions from here on per the host constraint) |
| 55 | Start/completion HUD and game controls | N | 54 | **done 2026-09-20** ([55.md](handoffs/55.md)): the local (in-page) game shell lands in `@thirdlight/game-host` (delivery.md §§3–4, B04/B08/B09/B13/B15) + the input package's approved menu-control seam. `packages/input/src/menu.ts` (new): the pure menu state machine — the §4.1 hard bindings (`Enter`/`Space`/fresh pad button 0 → confirm; `KeyM` → mute), fresh-press-only latches, the §4.2 `consumed(needsRelease)` fresh-release machine, same-press jump suppression (keyboard Space / pad button 0 only — the mapping derives the jump down-transition from the HELD state, so the owner zeroes the held contribution, not just the press latch), `clear('all'|'gamepad')` (focus/visibility vs disconnect slices). `browser.ts`: the owner feeds the controller before the gameplay mapping gate (repeat + editable-target gates still apply; keyup always releases the menu) and exposes the additive `sampleMenu()`/`markConfirmConsumed()` (the binding `detach`/`dispose`/`unavailable`/`attached` unchanged). `packages/game-host/src/host.ts` (new): `createGameHost` — the §3.1 surface (`mount`/`control`/`observe`/`setViewport`/`dispose` — the contract's result shapes, `dispose(): void` idempotent; `GAME_HOST_API_VERSION`/`GAME_CONTROL_ACTIONS`/`GAME_HOST_MESSAGES`/`GameHostConfig`/`GameHostObservation`), the §3.2 single production composition (registry: runtime built-ins + `platformer` + `platformer-game` session/camera; `instantiateRuntime`; environment-default driver — raf in the browser, manual in Node; NO BUILTIN_MODULES selection — the M3 game set only), the between-frames menu/control servicing (onFrame after the step update: `gameCommand('start'|'replay')` queueing — C4/C5 succeed with `movementSteps: 0`; consumption only when the confirm DROVE a menu action — a confirm in play is a no-op and keeps its jump), the committed-view-only cue submission (`cueEventsForView`: committed events → cue kinds; the jump cue DERIVED from the committed `playerMotion` grounded→airborne transition — the closed `GameEventKind` has no jump event; the owner dedupes by id per runId), the sound-status mapping (owner `ready/muted`→`muted|ready`, `blocked`→`blocked`, `unsupported|disposed`→`unavailable`; `gesture: local` iff unlocked), the fail-closed preconditions (v3 + game block + a controller entity; behavior components rejected — CC-55-3). `src/hud.ts` (new): the host-owned HUD (title/objective/instructions/prompt/status + Start/Mute plain buttons; `textContent` ONLY — the malicious-title case renders markup as inert literal text; the buttons drive the same `control()` channel). `src/audio.ts`: additive `liveVoices()` read (`sound.voices`; the 8 binding members unchanged). `package.json` gains the §4.1 edges (runtime/platformer/platformer-game values, input types-only); three-adapter is NOT imported (the structural `HostRenderAdapter` surface — the adapter FACTORY is injected; CC-55-2). Tests: `packages/input` +9 tests (`menu.test.ts` 9 pure, `browser-menu.test.ts` 12 over the REAL owner + fake window — the menu keys, the held-consumed-press no-jump, fresh-press-jumps, the editable/repeat gates, the visibility/blur/disconnect slices; `api-surface.test.ts` updated for the additive exports) + `packages/game-host/src/host.test.ts` (20: mount/HUD text-only + innerHTML-zero audit, double-mount rejection, the no-game-block and malformed-config rejections, the direct + menu-driven start with `movementSteps: 0`, the runtime's own `game_command_invalid (reason: state)` rejection, the in-play no-op confirm (not consumed), mute/unmute routing, `observe()` identity incl. the wrapper's `buildId` (CC-55-1) and the real owner's blocked→ready across unlock, the cue wiring over the injected reader (ArrayBuffer contract shape → owner Uint8Array), the `cueEventsForView` pure mapping incl. the derived jump id, `setViewport` pass-through, the disposal order + seam-gone, the repeated mount reusing the wrapper's input/audio owners, the HUD module) + `tests/m3-shell/host-shell.test.ts` (Node, 7 tests — the full loop over the REAL Rapier port + the REAL `attachBrowserInput` (fake window) + the REAL audio owner decoding the REAL `fixtures/m3/media/wav` cue bytes: C4/C5 title→Enter→start (no phantom motion, spawn position)→checkpoint (`zone-checkpoint`)→goal (`won`)→real cue decode+voice+`sound.voices ≥ 1`→fresh-Enter replay (runId `#1`, counters reset, re-placed at spawn), C1/C2 held-Space no-phantom-jump (12 grounded steps at spawn height) then release-press jumps, C3 fresh pad-press start + held no-jump + D-pad movement + fresh-press jump, C7 pad disconnect (the keyboard jump survives), C9/§4.6 hidden-tab (held D + menu latch clear; a 5s wall stall advances ≤ 8 steps — the bounded catch-up + resync), stale-run identity (replay epoch + the owner's `stale_work_discarded` drop of a stale-run event), repeated attach/detach) + `tests/browser/m3-shell/` (manual host + README — UNVERIFIED; checklist A1–A12; bundles 3.5mb `dist/m3-shell-test/harness.js`). `tools/check-boundaries.mjs`: the game-host row (runtime/platformer/platformer-game `packages` + input `typesOnly`). `npm test` **164 files / 2049 tests** `EXIT=0` (+4 files/+48 vs 160/2001); `typecheck` (17 pkgs) / `check-deps` / `check-boundaries` (17 pkgs, 318 files, 1228 specifiers, 0 violations) / `build` (4 built) all `EXIT=0`; M2 checkers green (contracts 34/34; course/runtime/physics/input exit 0); M3 checkers green (contracts, gameplay, media 14/119 + 10/10, delivery, storage, audit 8/8, promotion). B04 HUD PASS (logic; real-browser DOM + the DOM-vs-canvas screenshot UNVERIFIED — A1/A11), B08/B09 start/win/replay/checkpoint PASS (the real Rapier loop, C4/C5; audibility N/A), B13 sound PASS (committed cue events with REAL fixture bytes, real owner decode + voice, blocked→ready across a local unlock, stale-run drop; AUDIBILITY UNVERIFIED by contract — packet-38 baseline, real device at 55/62), B15 keyboard/controller lifecycle PASS (keyboard-only loop, gamepad takeover/disconnect, repeated mount; PHYSICAL DEVICE UNVERIFIED — A8/A9/A12). Contract-change requests: **CC-55-1** (additive `GameHostConfig.buildId` + `assetPaths` — `GameHostObservation.buildId` is unsatisfiable from the binding config and the host cannot resolve cue bytes: the manifest is wrapper-owned, the host is fetch-free), **CC-55-1b** (additive `GameHost.runtime` read-only seam — the binding surface exposes no frame-advance handle; manual-driver/Node + the 59 bridge need it), **CC-55-2** (additive deviation: `adapter` is the factory `(runtime) => adapter|null` — the three-adapter instance requires the runtime it renders, created inside `mount()`), **CC-55-3** (open: the §3.1 config has no behavior-linking channel; the 55 composition rejects behavior-carrying scenes fail-closed — required by 56/59 if a delivered game carries behaviors). Documented interpretations (not CCs): the jump-cue derivation (committed `playerMotion` — a walk-off cliff also fires it); consumption only on menu action (B15 feel; §4.2 targets consumed menu presses); `BUILTIN_MODULES` registered per §3.2 (not selected; the §4.1 row names three runtime values — minor); `readArtifact` returns `Promise<ArrayBuffer>` (contract shape) wrapped for the owner; `settings` typed `GameplaySettings`; the host never disposes the injected input/audio owners (the §3.1 reuse clause); the host's own visibility→`audio.setHidden` page wiring lands with the 59 preview wrapper (the input owner's visibility handling + the owner-level `setHidden` are the verified halves). No commit made (working tree only). **Gate N ACCEPTED 2026-09-21** ([gate-n.md](handoffs/gate-n.md) — in-session review: fresh toolchain re-run 164/2049 + all M2/M3 checkers + corruption controls; §3.1 surface member-by-member PASS; §4.2 suppression independently re-derived (the mapping derives the jump from the HELD state — the suppression zeroes the held contribution); B4/B6/B7 interpretations adjudicated; findings F1–F5 disclosed (tests/m3-shell scope addition, the boundary-table row, BUILTIN_MODULES as a 4th runtime value per the binding §3.2, the additive input exports, the lockfile workspace links + the existing rapier2d-compat@0.20.0 pin materialized); CC-55-1/1b/2 **accepted**, CC-55-3 **accepted as open** (filed by 56/59 if a delivered game carries behaviors); no contract diffs applied at the gate). Next 56 (Gate N satisfied) |
| 56 | Gameplay/camera authoring UI | O | Gate N accepted | **done 2026-09-21** ([56.md](handoffs/56.md)): `packages/editor` (M3 gameplay authoring — the packet-56 scope: gameplay/config/camera panels + projection + client + imperative zone gizmo/overlay only; no Toolbar/Inspector/Hierarchy edits). New `src/session/gameplay.ts` (pure — Node-testable): the six-key settings registry mirror (commands.md §8.11 bounds + the slide≤climb cross-rule) with `parseSettingsDraft`/`planSetSettings` (touched keys only — partial `setSettings` semantics preserve the rest); the game-config form parse (string bounds 64/160/320 + control chars, the ID syntax on the three references, finite level numbers |v|≤1e6, `minX<maxX`, `minY<maxY`, `killY<maxY`) + the reference preflight (exactly-one controller / camera entity with cameraFollow / spawn / ≥1 goal / ≤1 checkpoint — the §23.8 step-8 role rules mirrored, errors listed verbatim in the panel, no command sent) + `planSetGameConfig` (create = the complete canonical block with `cues` all null and `configVersion:1`; edit = changed top-level fields only, `level` replaced whole when any bound changes; **`cues` never touched — packet 57 owns it**); `planCreateZone`/`planEditZone` (the §23.3.1 bounds (0,1e6]; root `group` with unit scale + identity rotation so `zone_transform_unsupported` cannot trigger; the checkpoint field rules — add/switch-TO carry `safeSpawnId`+`activation` (the default `#1bc8ff`×1.2, `cueAssetId:null`) in the same value, non-checkpoints carry neither; a checkpoint→other role switch is the **two-step remove-then-re-add** (the merge edit cannot drop the checkpoint fields) — `planEditZone.steps[]`); `parseCameraFollowForm`/`planSetCameraFollow` (the §23.3.3 bounds; add = complete value, edit = changed fields). New `src/session/zone-gesture.ts` (pure): move/resize/create gestures on the z=0 game plane; `commandDecisions` 0 during the drag / 1 on a committing release / 0 on cancel (one undo); the bounded `revision_conflict` rebase (≤1 — move re-reads the entity's pose via the injected rebase and re-applies the user's delta at the new revision; resize/create surface a second conflict instead of retrying); a negligible create drag (a click) places the role default size at the anchor; an unplannable checkpoint create (no safe spawn) decides NO command (the UI preflights before arming). New `src/viewport/zone-overlay.ts` (imperative three.js, framework-free): role-colored semi-transparent zone rectangles + wireframe border on z=0, spawn cone markers, the cameraFollow bounds line, the selected zone's SE resize handle, the in-flight preview mesh (all disposed exactly once on sync-remove/`dispose()`); pointer routing — armed tool ⇒ `create` (the anchor is the ray ∩ z=0 plane hit, canvas centre on a miss), resize handle ⇒ `resize`, zone body (topmost raycast) ⇒ `move`, else NOT consumed (the orbit/pick path runs); every pointer position is reported as a game-plane world hit (`screenToGamePlane`). `src/viewport/viewport.ts`: the overlay is consulted FIRST on pointer down (a consumed down drives a zone gesture and skips orbit/pick; a zone-body/resize down also selects the entity); frames/end feed the App; selection drives the handle; `setZoneTool`/`previewZonePose`/`cancelGesture` (a zone gesture takes precedence over the gizmo gesture). New `src/ui/GameplayPanel.tsx` (the four tabs — the zone tools live inside the panel, not the Toolbar): **Game** (title/objective/instructions with `maxLength`, the player/camera/spawn pickers over the entities, level bounds + killY; Save → parse + preflight + `planSetGameConfig` → one `setGameConfig` command; Remove; the "no game config yet" note); **Zones** (zone rows: role select, size X/Y, the checkpoint safe-spawn picker (disabled without a spawn), activation read-only (57 edits it), Save/Delete; Add at origin; "Place in viewport" arms the tool — disabled for a checkpoint without a selected safe spawn; the spawn list with Add/Place; the ≤1-checkpoint warning); **Camera** (entity picker, dead zone x/y, smoothing, bounds; Save → `planSetCameraFollow`; Remove); **Settings** (six bounded fields seeded from the registry defaults + tracked `setSettings` changes; only touched keys submitted; the contract limitation is noted in the UI). `src/session/projection.ts`: `gameZone`/`playerSpawn`/`cameraFollow` hydrate from the full state and converge from `setComponent` change records (add/edit/remove via null); `setGameConfig`/`applySurfacePreset` changes advance the revision without a gap (the block/map live on the client). `src/session/client.ts`: the `content.game` block state (from `queryGameConfig` on EVERY full resync — reopen retains the authored configuration — and from applied `setGameConfig` change records, full `previous`/`next` per §5.3) + the settings map (from applied `setSettings` changes — **no accepted query returns settings VALUES**, `queryProject` carries the `settingsKeys` count only; the panel degrades safely and documents it) + new command wrappers (`setGameConfig`/`setSettings`/`setComponent`/`createGameEntity`/`deleteEntityCommand`) over the ordinary envelope path (original `requestId`, bounded retry, dedupe). `src/ui/App.tsx`: the gesture driver (the base pose is captured from the projection at gesture start; the single commit command is issued at the gesture's expected revision with the bounded rebase — `revision_conflict` on a `setTransform` rebases once via the projection re-read, a second conflict is explained, never retried; the **spawn tool rides the create gesture for position but creates a `playerSpawn` marker**, not a zone — the 56 wiring fix); Esc cancels the active gesture (preview reverts, nothing sent) or clears the armed tool (one-shot after a successful placement); the panel wiring + the backend error display. `editor.css`: the panel styles. `tests/browser/m3-authoring/README.md` (new): the manual real-backend browser procedure (Z1–Z11: placement/move/resize gestures with the zero/one/zero command evidence, the checkpoint two-step, the game-config create/edit/reopen persistence, the invalid-reference/coordinate refusals at parse + preflight with zero WS commands, duplicate roles + illegal deletes (the structured error shown, the failed edit preserves state), the stale-MCP-edit-during-gesture bounded rebase (two-client trace), disconnect/resync + lost-ack retry (the resync re-reads `queryGameConfig`; the unacked command retries once with its original `requestId`, duplicates dedupe), MCP/browser convergence (a second client's `setComponent`/`setGameConfig` converges the open editor without a reload — sessions.md §6.2), undo/redo, `m3-authoring-evidence.png`) — **UNVERIFIED in-container** (no browser; packet 38 baseline §1). Tests: `gameplay.test.ts` (31 — the settings registry table incl. exclusive bounds + the cross-rule against current/defaults, the form parse table, the reference rules, the setGameConfig planning (create complete / edit partial / cues untouched / noop), the zone plans (bounds, the checkpoint rules, the two-step switch, the non-checkpoint reference noop), the camera parse/plan) + `zone-gesture.test.ts` (14 — the 0/1/0 decision law, the bounded rebase (one rebase, second conflict surfaced), the negligible-create default, the unplannable checkpoint noop, the runner sink contract) + the `projection.test.ts` v3 block (5 — hydration, `setComponent` add/edit/remove convergence for the three components, the checkpoint activation edit, the `setGameConfig`/`applySurfacePreset` consumption). `npm test` **166 files / 2101 tests** `EXIT=0` (+2 files / +52 tests vs the post-Gate-N 164/2049); `typecheck` (17 pkgs) / `check-deps` / `check-boundaries` (17 pkgs, 324 files, 1254 specifiers, **0 violations** — the editor's project-model edge stays types-only) / `build` (4 built / 0 skipped) all `EXIT=0`; M2 checker (contracts 34/34) + M3 checkers (gameplay, contracts, media 14 groups, delivery, storage, audit 8/8, promotion) all green; media corruption control 10/10. No fixture bytes changed; no contract/fixture/pin touched. B02/B03/B17 gameplay UI scope: the Node-verified planning/gesture/projection halves PASS (invalid references/coordinates refused pre-command, the duplicate-role/illegal-delete structured errors surfaced with state preserved, the stale-edit bounded rebase, the resync re-read, one undo per gesture, reopen retains the authored game config); the real-backend browser authoring + SDK-edit convergence + rendered overlay + every pixel/pointer statement **UNVERIFIED** (the Z1–Z11 checklist is the owner procedure). Limitations: the settings-values query gap (documented in the panel + above — not a CC, the contract is binding); cue assignment + the checkpoint activation appearance are 57's surface (56 creates the block with null cues + the default activation). No contract-change requests (CC-55-3 stays open from Gate N — no behavior channel was needed here). Next: **57** — **do not start it** |
| 57 | Media/rendering authoring UI | O | 56 | **done 2026-09-21** ([57.md](handoffs/57.md)): `packages/editor` (packet-57 scope: media/light/surface/role controls + imperative viewport integration only; no game-host import — the editor boundary row is unchanged). New `src/session/media.ts` (pure): `validateMediaDrop` (`.glb`→model / `.wav`→audio, 1…32 MiB stage bound — an invalid drop creates no stage/job), `planCueEdit` (the `setGameConfig` partial edit — each present top-level field replaces the WHOLE field, so the picker sends the full merged 5-slot `cues` block; noop if unchanged), `parseLightForm`/`planSetLight` (#hex canonical lowercase, 0–8 intensity, directional requires |v|≤1 per component + norm ≥1e-6, ambient carries neither direction nor castShadow; add=complete, edit=changed fields, type switch=complete), `lightCounts` (the §23.10 1+1 scene limit), `parseSurfaceForm`/`planSetSurface` (two #hex, roughness/metalness 0–1, emissiveIntensity 0–4; add complete / edit changed), `SURFACE_PRESETS` (frozen display mirror — the backend applies the authoritative row), `parseModelAnimationForm` (version 1…max, clipIndex ≥0 int, non-empty clipName, serialized roles ≤4096 B) / `planSetModelAnimation` (assetId fixed to the entity model — §23.8 step-5 conflict noop here), `planAnimatedReimport` (the §8.5.1 `animation:{entityId,roles}`), `parseActivationForm`/`validateActivationCue` (audio-kind preflight) / `planSetActivation` (`setComponent(gameZone,{activation})` partial). New `src/session/preview-audio.ts`: `PreviewAudioOwner` — the packet's "injected owner" = the editor-page preview UTILITY (not the binding §41.4.7 game owner — the boundary row cannot import game-host): RULE 1 gesture (context only via `unlock()`, pre-gesture stays `blocked`, never throws), RULE 2 bounded decode on a copy (failure = one bounded diagnostic), RULE 3 ≤8 voices (at-cap drop `preview_voice_cap`, never queued), RULE 4 one live voice per cue, RULE 5 idempotent dispose (one close); deterministic with the injected `contextFactory`; cue bytes from `client.assetByteResolver()` (the authoring token is that read's credential, never a resource). `projection.ts`: `light`/`surface`/`modelAnimation` on `ProjectedEntity` (type-only project-model) + hydration + `setComponent` convergence. `asset-browser.ts`: `publishArgsFromProposal` gains `kind` (required on create, §A3.5) + the §8.5.1 `animation`; `client.ts`: `uploadAsset` carries the inspect `kind` + a reinspect seam. `viewport.ts`: boxes preview the authored `surface.color`; light entities render as a directional arrow / ambient sphere marker. `ui/MediaPanel.tsx` (new, five tabs — **Cues** (slot pickers over audio assets + unlock/preview status + bounded diagnostics), **Checkpoint** (activation appearance), **Lights** (1+1 create buttons + edit form), **Material** (surface form + three preset buttons → `applySurfacePreset`), **Animation** (version select + three role bindings + the just-inspected clip-names hint)); `AssetBrowser.tsx` (accept `.glb,.wav`; the §8.5.1 **Animation mapping** section — entity picker + three clip selects over the just-inspected names; publish disabled until the mapping is complete); `App.tsx` (media import flow with kind + referencing-entity detection, the reimport role draft, the preview-owner lifecycle + all media command callbacks over the envelope path). `tests/browser/m3-media-authoring/README.md` (new, A1–A9) — **UNVERIFIED in-container** (no browser/GPU/audio device; packet 38 baseline §1). Tests: `media.test.ts` (40 — the drop table, the cue planning, the light parse/plan incl. type switch + partial edit, the surface table + presets, the animation profile + §8.5.1 planning, the activation preflight/planning) + `preview-audio.test.ts` (24 — the five rules over a fake Web Audio graph: gesture gating, one-context creation, dedupe, the 8-voice cap drop, mute, decode failure + re-registration, decode-on-copy isolation, idempotent dispose, unsupported platform, the 64-entry ring) + the M2 `asset-browser.test.ts` updated for the `kind` param (+1). `npm test` **168 files / 2166 tests** `EXIT=0` (+2 files/+65 vs post-56 166/2101); `typecheck` (17 pkgs) / `check-deps` / `check-boundaries` (17 pkgs, 329 files, 1272 specifiers, **0 violations** — the editor↔project-model edge stays types-only) / `build` (4 built / 0 skipped) all `EXIT=0`; M2 checker (contracts 34/34) + M3 checkers (audit 0 failed, promotion PASS, contracts 39 groups, delivery 16/153, gameplay 127, media 14/119, storage 9/10) all green. No fixture bytes changed; no contract/fixture/pin touched. §A8 rows 5/11/13/14/15/16/17/18/21 logic PASS (invalid values refused pre-command at the exact contract bounds; the §8.5.1 all-or-nothing reimport + its undo ride the single publishAsset command; reopen persistence via the existing resync paths); every real-browser/audible/rendered statement **UNVERIFIED** (the A1–A9 checklist is the owner procedure). Limitations: no per-version `clipNames` catalog query (only the inspect proposal carries them — the animation tab accepts free-form bindings, backend stage-3/5 is the authority, the just-inspected names are a hint); preview bytes fetched lazily on first ▶; the preview owner is an editor utility, not the §41.4.7 binding owner (documented). No contract-change requests (CC-55-3 stays open from Gate N — no behavior channel needed here). Next: **58** — **do not start it** |
| 58 | Immutable closure/settings/shared composition | O | 57 | **done 2026-09-21** ([58.md](handoffs/58.md)): the project-model owns the pure **runtime-content manifest v2** derivation and the exporter composes the complete **M3 standalone export** from ONE captured authoring state (the single acknowledged envelope read — delivery.md §2.6); both the preview/play path (59) and the export path share the closure builder + the `game-host` production composition. `packages/project-model/src/manifest-v2.ts` (new, pure): `captureManifestV2` (the 22-key v2 document, self-identifying `buildId`), `validateManifestV2` (strict reader incl. captured-state re-derivation: `media_identity`/`asset_kind_mismatch`/`sceneDigest`), `manifestBuildIdInputV2`, `captureContentViewV3` (reachable kind-tagged assets + resolved six-key settings + frozen `game` + `contentDigest`), `resolveMediaIdentityV3` (cue slots + animation rows), `manifestVersionCompat`, the block-digest primitives (all digests `sha256(JSON(value,2)+"\n")` in the **owning contract's key order**, not sorted) + the M3 engine pins / module table / recipe versions. `src/capture.ts`: `collectAssetRefsV3` made public (was private — the v2 view consumes it). `src/manifest-v2.test.ts` (23 pure). `packages/exporter`: `src/content-closure.ts` `buildContentClosureM3` (the shared M3 closure — one captured v3 scene+content → v2 manifest + declared kind-tagged artifact bytes; **fails closed on source-bearing behaviors** — CC-55-3) + `M3_REQUIRED_MODULE_IDS`; `src/export-bootstrap-m3.ts` (new — the M3 page bootstrap: fetch `manifest.json` (v2, WebCrypto `buildId` verify) + `scene.json` (digest verify) + declared assets (generated switch) → `createGameHost` with manifest `settings`/`game`, the Rapier port (`solver.gravityY = settings.gravity_y`), the scene adapter + the audio owner); `src/export-m3.ts` (new — the `exportProjectM3` pipeline: closure → M3 bundle → reference+Rapier probes → revision re-read → output target → graph check → manifest self-identity → GLB/**WAV** container scans + §5.4 forbidden patterns + relative closure → `meta.json` v2 → atomic publish); `src/graph.ts` `checkBundleGraphM3` (allow `game-host`/`platformer-game`; M3 exporter file = `export-bootstrap-m3.ts` only); `src/export-bundle.ts` `buildM3Bundle` (artifact virtual module only); `src/export-content-scan.ts` `scanWavContainer` (RIFF/WAVE PCM mono/48k/16-bit); `src/export.ts` v3→M3 dispatcher branch. **`export-bootstrap-m2.ts` byte-stable (untouched).** `packages/workspace`: additive **captured-view seam** `readCapturedV3` (workspace.md §16 — the single acknowledged envelope read's v3 `scene`+`content` halves for the closure builder; a `storageVersion` 1/2 project is `version_combination_unsupported`; the v1 view unchanged). Tests: `tests/integration/m3-builds/manifest-v2.test.ts` (16 — independent `node:crypto` digest re-derivation; `captureManifestV2` byte-identical to committed `manifest-v2-example.json`; content-view + media-identity over the real `demo-0003-media-v3` envelope; the strict reader's captured-state re-derivation; version-compat) + `m3-settings-trace.test.ts` (4 — B16: `run_speed` 4→6 ratio 1.5× through the real runtime+controller; `gravity_y` −19.62→−30 a plain dynamic body free-falls `vy=g·N/SIM_HZ` (−30@120/−60@240, no controller clamp) through the real Rapier world; changed `run_speed` changes `settingsDigest`/`contentDigest`/`buildId` while `sceneDigest` is stable; the captured closure pins revision 1) + `m3-closure.test.ts` (6 — B19: the closure derives the v2 manifest from one capture; `blob_missing`/`asset_digest_mismatch`/`behaviors_unsupported`/invalid-content refusals; the revision pin) + `m3-builds.test.ts` (3 — B21: the output tree + declared==emitted + `meta.json` v2 (the manifest block's `gameDigest`/`settingsDigest`/`mediaDigest`) + `scene.json` digest == `manifest.sceneDigest` + GLB/WAV container scans + the graph negative control + the `export_snapshot_mismatch` leaves the previous output byte-untouched). `npm test` **173 files / 2218 tests** `EXIT=0` (+5 files/+52 vs post-57 168/2166); `typecheck` (17 pkgs) / `check-deps` / `check-boundaries` (17 pkgs, **333 files, 1305 specifiers, 0 violations**) / `build` (4 built / 0 skipped) all `EXIT=0`; M2 checker (contracts 34/34) + M3 checkers (delivery 16/153, media 14/119 + 10/10 control, gameplay 23/127, contracts 39, storage 9/10) all green; M1/M2 frozen traces + all fixture bytes unchanged. B16/B19/B21 logic PASS (the settings reach both the physics/controller and the hash-bound v2 manifest; the closure pins one revision; the export is a complete declared==emitted relative closure); every browser/visual/audible statement **UNVERIFIED** (no browser/GPU/audio device in-container — packet-38 baseline §1; the M3 page walkthrough lands in 61/62). Limitations: **GLB model rendering gap** (the shared three-adapter `createSceneAdapter` realizes boxes/lights/surfaces/camera only — model entities become empty Groups; no attach surface; the closure still declares+emits the model bytes; a visible packet-61 player must use a box — a three-adapter model-attach surface is a follow-up, out of 58's may-edit); **§5.4.1 count re-measurement deferred to the 58/60 re-measure** (the pipeline gates on the §5.4 forbidden patterns a/b/c/e/g/i=0 + relative closure + container scans; the exact per-needle counts against the current three+game-host install re-measured at Gate O/P); **M3 export fails closed on source-bearing behaviors** (CC-55-3 open — the sample uses built-in modules only). **Reopened project-model §44 row (Gate L)** — bounded re-review: additive v2 surface only, the v1 path unchanged, the v2 core tested (23 unit) + byte-identical to the committed `manifest-v2-example.json` (16 integration). **Workspace captured-view seam interpretation — flag for Gate O re-review** (the may-edit's "named at K" treated as satisfied by the workspace owning the single acknowledged read + the `captureContentView` public edge; purely additive, no derivation duplicated). No commit made (working tree only). Next: **59** — **do not start it** |
| 59 | Isolated Play/control/observation integration | O | 58 | **done 2026-09-21** ([59.md](handoffs/59.md)): editor Play runs the actual game host from immutable artifacts for v3 projects — one captured input + one production composition for both hosts. The backend builds the v3 play artifact set with the SAME shared M3 closure builder the export uses, and the preview wraps the SAME single shared `createGameHost` (delivery.md §3.2 normative — no second bootstrap/controller/run-state owner). The accepted content/snapshot split (sessions.md §13.2/§17.6) is preserved: **the v3 snapshot (scene) arrives only through the nonce-verified `tl.snapshot` bridge** (the accepted §17.2.1 locator route set has no scene route — a locator `scene.json` read is `path_rejected` 400) and **the content arrives only through the locator** (the manifest v2 `buildId` + declared asset bytes); the manifest's `sceneDigest` is the identity the preview verifies the bridge snapshot against. `packages/backend/src/play-m3.ts` (new): `buildPlayContentM3` — a thin consumer of `buildContentClosureM3` assembling the v3 set (`manifest.json` v2 [opaque bytes to the version-agnostic `PlayContentStore`, carrying `sceneDigest`] + declared kind-tagged assets + the prebuilt M3 bundle as the entry; NO `scene.json`); fails closed on the closure closed set (`blob_missing`/`asset_digest_mismatch`/`export_build_unavailable`/`behaviors_unsupported` [CC-55-3] / over-cap bundle). `backend.ts`: the `startPlay` v3 branch — a v3 project reads the captured state via the `readCapturedV3` seam (revision drift ⇒ `revision_conflict`), reads the **M3** bundle (`readGameBundle('preview-m3.js')`) and builds via `buildPlayContentM3`; the v1/v2 M2 path is unchanged (each branch reads its own bundle — a v3 play does not require the M2 `preview.js`). `packages/editor/src/preview/preview-m3.ts` (new, a SEPARATE entry): `startM3Preview` (bridge-delivered snapshot → fetch the locator `manifest.json` [v2 type gate + WebCrypto `buildId` self-identity + the handshake's expected build] → verify the snapshot scene re-hashes to `manifest.sceneDigest` → compose `createGameHost` with manifest `settings`/`game`, the Rapier port (`solver.gravityY = settings.gravity_y`, one object — C35-5), the scene adapter + input/audio owners) + `bootstrapPreviewM3()` (the locator's `game.js` entry: bridge handshake → nonce-verified `tl.snapshot` → mount → truthful `ready` [the title loaded, NOT gameplay started]). **The M2 `preview-bootstrap.ts` is untouched** (the wrapper is a separate entry, never inlined into the M2 bundle — the binding M2 §5.4.1 bundle-scan test re-passes unchanged). `tools/build.mjs` builds the third browser bundle `preview-m3.ts` → `dist/preview/preview-m3.js` (served as the v3 `game.js`); `tools/build.test.mjs` updated (3 built); `tools/check-boundaries.mjs` gains the `preview-m3.ts` row (the only editor file allowed to import `game-host`, delivery.md §4.3). Tests: `tests/integration/m3-play/m3-play.test.ts` (5, Node — the artifact set [v2 self-identity, `validateManifestV2` captured-state re-derivation, both assets re-hashing to their declared digests, **no `scene.json`**, no behavior artifacts], build determinism, over-cap refusal, CC-55-3 fail-closed, `blob_missing`/`asset_digest_mismatch`) + `play-v3.test.ts` (2, real backend + HTTP on both origins — `POST /play` for the committed v3 `demo-0003` serves the v2 manifest + assets + the M3 bundle and a locator `scene.json` read is `path_rejected` 400; **the pin is honored across a live `setSettings` edit (B19/B20)** — a fresh play adopts the new `settingsDigest`/`buildId`, the pinned play's locator still serves the original frozen build) + `tests/browser/m3-play/README.md` (owner-run P1–P10 — **UNVERIFIED in-container**, no browser/GPU/audio, packet-38 baseline §1). `npm test` **175 files / 2225 tests** `EXIT=0` (+1 file / +2 vs post-58 174/2223); `typecheck` (17 pkgs) / `check-deps` (all exact) / `check-boundaries` (17 pkgs, 335 files, 1319 specifiers, **0 violations**) / `build` (5 built / 0 skipped) all `EXIT=0`; M2 checker (contracts 34/34) + M3 checkers (gameplay 23/127, contracts 39, media 14/119 + control, delivery 16/153, storage 9/10) all green. B04–B16 integrated preview scope (Node-verified halves) + B19/B20 PASS (the v3 play build + shared-composition wiring implemented + route-tested; the scene rides the bridge); every browser/visual/audible statement **UNVERIFIED** (P1–P10 is the owner procedure; no fake browser acknowledgement establishes visual success). Limitations: the v3 scene is NOT a locator artifact (the accepted §17.2.1 route set has no scene route — the scene rides the `tl.snapshot` bridge, matching the M2 design; no protocol refinement — PR-2 flag); the §5 relay (`tl_game_control`/`tl_game_observe`) routes through the accepted HTTP `/play/:id/control|observe` over the selected browser (the accepted packet-48 bridge schema has no game-control/observe types — adding them would reopen the protocol section; the v3 entry uses the accepted bridge surface handshake/snapshot/ready/stop — PR-2 flag); CC-55-3 stays open (a v3 play carrying source-bearing behaviors fails closed `behaviors_unsupported`); the GLB model rendering gap carries (a visible packet-61 player must use a box; `demo-0003` is box-based, 0 media assets); `CapturedV3Read.scene/content` are `unknown` (the backend casts `content` at the single call site — the closure validates strictly). No commit made (working tree only). Next: **60** — **do not start it** (Gate O runs after 60) |
| 60 | Standalone export and production parity | O | 59 | **done 2026-09-21** ([60.md](handoffs/60.md)): the M3 standalone export is proven a complete relative closure that runs from static output with no editor services, and the **58/60 §5.4.1 + §17.5 re-measurement** is recorded against the real three + `game-host` install. This is the **measurement/parity/evidence packet** over the already-built M3 export pipeline (packets 58/59) — **no package source changed**; the deliverables are the binding re-measurement tests, the two-tree determinism + preview/export parity tests, and the owner-run standalone procedure. NEW `tests/integration/m3-export/bundle-scan.test.ts` (3): (1) the reference full-core three (pinned `three@0.186.0`, the §5.3 pinned esbuild option set, entry `import * as THREE from 'three'; console.log(THREE.REVISION);`) re-scans to the §5.4.1 table — **d=3, f=3, h=26, j=3; a/b/c/e/g/i=0** (measured 1,777,856 bytes; the table records 1,777,857 — a 1-byte newline separator, the per-needle counts are exact — binding 3 re-verified); (2) the REAL M3 export bundle (the production `buildM3Bundle` over the shared `buildContentClosureM3` closure, two declared assets) scans to exactly the recorded baseline + applicable rows — **a/b/c/e/g/i=0; d=8 = 3 (three core) + 1 (Rapier) + 1 (`./manifest.json`) + 1 (`./scene.json`) + 2 (one per unique declared asset path); f=3, h=26, j=3** — i.e. **0 contributed by `game-host`** to d/f/h/j and a/b/c/e/g/i, no GLTFLoader subpath in the M3 graph (the export bootstrap imports only the root `createSceneAdapter`, not the `./gltf-loader` port) and no trust notice in the M3 page; no absolute/remote `fetch` literal; (3) production-parity graph — `checkBundleGraphM3` passes (no forbidden module) and the bundle links the SAME shared `game-host` + `platformer-game` composition the preview uses (no editor/backend source). NEW `tests/integration/m3-export/determinism.test.ts` (2): (1) **two-tree determinism (B23)** — two exports of the same captured state (a fixed wall clock, `now: () => 1700000000000`) into two DIFFERENT output trees are **byte-identical** file-for-file (the accepted timestamps `capturedAt`/`exportedAt` are the only time-dependent bytes; the `buildId` is re-derived from the manifest content — stable for the same content; the prior-tree-preserved-on-failed-reexport half is the existing `export_snapshot_mismatch` test); (2) **preview/export parity (B22, Node half)** — the M3 EXPORT manifest and the v3 PLAY manifest for the same capture are the **SAME build** — identical `buildId`/`sceneDigest`/`contentDigest`/`settingsDigest` — both derived by the SAME shared `buildContentClosureM3` from the SAME single captured read, both wrapping the SAME single `createGameHost`; the resolved six-key `settings` reach BOTH hosts (C35-5). NEW `tests/browser/m3-export/README.md` (X1–X7 owner-run standalone: independent static server under a NON-ROOT prefix, backend stopped/unreachable, real browser + physical keyboard + gamepad, the recorded network is fully relative — zero requests to the dead backend/authoring origins; audio owner cue playback; `meta.json` versioned metadata + exact licenses; the two-tree determinism witness) — **UNVERIFIED in-container** (no browser/GPU/audio device — packet-38 baseline §1). Already-pipeline-verified surface re-cited: the `index.html` is a **static loader** (loads `./js/main.js` as a module + the HUD `div`s) — no alternate sample logic; `manifest.json`/`scene.json`/`meta.json` + the `content/sha256/<digest>` assets are all relative and digest-addressed (declared==emitted at the closure level); the export pipeline's §5.4 forbidden-pattern (a/b/c/e/g/i=0) + relative-closure + GLB/WAV container scans run over every emitted file; missing/corrupt media and a rejected hostile source (CC-55-3 fail-closed `behaviors_unsupported`) preserve the previous output. **Verdict on the deviation trigger:** the packet-42 M3 addition holds **exactly** as recorded — `game-host` adds 0 to a/b/c/e/g/i and 0 additional to d/f/h/j; the measured text matches the recorded table + applicable rows, so **no bounded re-review is required and no exception is widened**. `npm test` **177 files / 2230 tests** `EXIT=0` (+2 files / +5 vs post-59 175/2225); `typecheck` (17 pkgs) / `check-deps` (all exact) / `check-boundaries` (17 pkgs, 335 files, 1319 specifiers, **0 violations**) / `build` (5 built / 0 skipped) all `EXIT=0`; M2 checker (contracts 34/34) + M3 checkers (delivery 16/153, media 14/119 + control, gameplay 23/127, contracts 39, storage 9/10) all green. B19/B21 (Node) PASS; B22 Node half PASS (physical keyboard/gamepad + standalone browser **UNVERIFIED** — owner-run); B23 (Node) PASS (byte-identical trees, no new authoring value). Limitations: GLB model rendering gap carries (a visible packet-61 player must use a box); CC-55-3 stays open (no behavior-linking channel — fail-closed); the standalone owner-run evidence (network capture, keyboard/gamepad, audible, two-tree hashes) is the packet-61/62 evidence. No commit made (working tree only). Next: **Gate O (in-session, after 60)** — **do not start 61** until Gate O is recorded |
| 61 | Author Beacon Reach via public workflows | P | Gate O accepted | **done 2026-09-21** ([61.md](handoffs/61.md)): one complete redistributable authored sample (Beacon Reach) — original self-generated content (5 PCM WAV cues + 2 glTF 2.0 GLB models, a deterministic idempotent generator `tools/generate-assets.mjs`, per-asset `provenance.json` SHA-256 digests + an explicit reuse `LICENSE`; no downloaded third-party content / external refs / fonts), a real v3 project (`captured/project.json`, 18 entities / 7 assets) authored through the SAME `@thirdlight/commands applyMutation` the browser + MCP use (in-process — the identical function the backend command route calls; the baseline is the workspace's NEW v3 project, the single default camera the v3 validator requires; the recipe then authors the rest via commands, no direct active-envelope editing), a reproducible command recipe (`recipe/commands.json`, 26 commands — replaying it over the same fresh baseline yields the EXACT captured scene + content, identical `sceneDigest`/`contentDigest`), a **traversable step trace with REAL physics** (`tests/integration/m3-sample/step-trace.test.ts`, 3 tests — the captured layout over the real `platformer` + `runtime` + `physics-rapier` (pinned 0.20.0) + `platformer-game` modules at the ACCEPTED settings, no retuned constants: a fall in the first pit before the checkpoint → death 1 + respawn at the START spawn (X≈3); the full traversal clears the low step, both hazards, both pits, activates the checkpoint once, reaches the beacon (goal); a fall in the second pit after the checkpoint → respawn at the CHECKPOINT spawn (X≈24)), and sample integrity tests (`tests/integration/m3-sample/sample.test.ts`, 4 tests — assets match provenance digests + §41.4 PCM container; regeneration idempotent; recipe replay reproduces the digests; no hardcoded runtime IDs, every reference resolves). Owner-run browser procedure `tests/browser/m3-sample/README.md` (S1–S10) — **UNVERIFIED in-container** (no browser/GPU/audio, packet-38 baseline §1). No engine source edits (all `packages/**` unchanged this segment). `npm test` **179 files / 2237 tests** `EXIT=0` (+2 files / +7 vs post-60 177/2230); `typecheck` (17 pkgs) / `check-deps` (all exact) / `check-boundaries` (17 pkgs, 335 files, 1319 specifiers, **0 violations**) / `build` (5 built / 0 skipped) all `EXIT=0`; M2 `bundle-scan` (binding) 1/1. B02/B03/B17/B24 + B04–B15 in-container halves PASS; UI/MCP-stale-edit/reimport-undo/backup-restore/browser-keyboard-pad/audible/frame-capture **UNVERIFIED** (owner-run). Limitations: GLB rendering gap carries (visible player is a BOX; GLB courier/beacon are imported content not rendered as animated models); in-process capture (the HTTP/MCP transport not exercised — the command path is byte-identical, disclosed); independent decoration prefab copies not created (the sample uses direct model entities; Gate P may require a scoped box-based repair); no new CCs (CC-55-3 stays open). No commit made (working tree only). Next: **62** — **do not start it** (Gate P runs after 62) |
| 62 | Integrated acceptance and owner playthrough | P | 61 | **done 2026-09-21** ([62.md](handoffs/62.md)): COMPLETE in-container — M3 acceptance record (B01–B24: 22 PASS, 2 PARTIAL, 0 FAIL) + PR-6 headless-browser probe executed against the real Beacon Reach export; physical/audio/hardware-GPU/display subclaims UNVERIFIED (owner annex). (Row corrected 2026-09-22 by packet 63 — SD-63-2: it was recorded pending though the packet completed.) |

**Next: packet 62 (Integrated M3 acceptance and owner playthrough). Gate O
ACCEPTED (2026-09-21, in-session, [gate-o.md](handoffs/gate-o.md)) — the
packets 56–60 review is recorded (fresh toolchain 177/2230, all checkers green,
§5.4.1 re-measurement confirmed with NO deviation trigger, PR-2 items accepted
as bounded interpretations, CC-55-3 accepted as open, F1–F6 disclosed). Packet 60
and packet 61 are done (2026-09-21). Gate P (in-session) runs after packet 62.
Do not start M4.**
Gate K accepted the pack with
bounded follow-ups, the bounded repair applied FU-1…FU-7 + B3, and the docs-only
promotion applied the 109 accepted/repaired rows (PM13 rejected) into
`docs/contracts/**`, including the new `gameplay.md`/`presentation.md` homes.
Gate-K-dependent implementation reads the promoted contracts; packets 44–46 are
done and the owner's final manual review and the K-3 export-name confirmation
remain pending. The packet-44 contract-gap repair (2026-09-19,
`handoffs/repair-cc44-3-4.md`) fixed CC-44-3/CC-44-4 docs+fixtures-only, so
`workspace.md` §16.5.1/§16.5.2 and `project-model.md` §12 step 5/§13.2 rule 5/
§18.9.2 rule 4/§22.2 rule 4/§23.11 are **reopened** and owe a fresh
architectural re-review of exactly those sections before Gate L acceptance
(session repair, not an independent review). Open bounded follow-ups with owners
and the Gate L checkpoint are listed in `acceptance/m3-report.md` §4 (CC-44-1 →
46 **answered as a §16.9 diff**, CC-44-2 → 47, CC-44-5 → 47, CC-44-6 → 46
**answered as a §19.2/§16.6 diff**, plus CC-45-1…8). CC-46-1 is **resolved** by
`handoffs/repair-cc46-1.md`. The stale
packet-44 test that pinned the pre-repair contradictory migration destination was
repaired by the coordinator (assertion now expects the repaired contract:
destination loadable, `publishedRevision === 0`); `npm test` is green again at
**138 files / 1768 tests**, with typecheck/check-deps/check-boundaries/build and
all M1/M2/M3 fixture checkers at exit 0. Nothing here
closes M2's 16 UNVERIFIED browser/hardware rows, and no M3 acceptance is claimed.

## M2 planning and pending work

M1 is accepted; its records below are preserved. **M2 planning delivered as a
DRAFT; architectural plan review ACCEPTED 2026-09-18 with bounded follow-ups
BR-1…BR-4 (none blocking) — session architectural review (docs/handoffs/
m2-plan-review.md), explicitly not owner approval and not an independent reviewer
approval.** Packet 14 executed 2026-09-18 (handoff: docs/handoffs/14.md): physics
selection is PROVISIONAL (evidence complete in-container; desktop evidence +
Gate E approval still open). Packet 15 executed 2026-09-18 (handoff:
docs/handoffs/15.md): the content storage/asset-identity/migration contract was
delivered as **PROPOSED** drafts, section-level diffs and byte-exact fixtures
under planning/m2-contracts/ + fixtures/m2/contracts/ (Gate E review pending;
no accepted contract changed). No new contract/dependency/trust decision
is approved by the plan, the review, packet 14 or packet 15.

**Update 2026-09-18:** Gate E accepted all 16 proposal/diff rows and the
bounded follow-ups GE-1/GE-2 (GE-3/GE-4 non-gating) were applied; the explicit
docs-only promotion then applied every accepted row into `docs/contracts/**`
and recorded the owner pin/trust/U-4 decisions in
`docs/decisions/0002-m2-content-and-behavior.md` (approved under the owner
pre-approval tag; final manual review pending). See
`docs/handoffs/m2-promotion.md` and
`docs/acceptance/evidence-m2/promotion/`. The physics selection stays
PROVISIONAL; desktop/browser/gamepad evidence remains UNVERIFIED. Packet 20 was
executed 2026-09-18 (handoff: docs/handoffs/20.md; evidence:
docs/acceptance/evidence-m2/20/); packet 21 was not started at that time.

**M2 FINAL STATE (2026-09-19):** packets 14–37 all `done`; Gates E–J all
recorded — E/F/G/H/I each accepted with bounded follow-ups, their repairs
applied (`m2-promotion.md`, `gate-{f,g,h,i}-repair.md`), the Gate I re-review
**accepted** (`gate-i-rereview.md`), and Gate J **accept with bounded
follow-ups** with the close-out applied (`gate-j.md`, `gate-j-repair.md`).
Tree: 15 packages, 135 test files / 1701 tests, boundaries 15/283/1068
specifiers, fixtures checker 34/34, clean disposable `npm ci` green. Acceptance:
all 24 rows executed at process/filesystem/HTTP/WS/SDK/Node level with **no
FAIL**; 16 rows (A02, A03, A04, A05, A07, A08, A11, A12, A13, A15, A16, A17,
A18, A19, A20, A21) carry an UNVERIFIED browser/hardware half pending the owner
walkthrough — documented, never fabricated. Physics selection stays PROVISIONAL
(decision 0002 §1, owner pre-approval; desktop CPU re-measurement open, BR-2).
Owner review: docs/handoffs/m2-owner-review.md; acceptance record:
docs/acceptance/m2-report.md; evidence: docs/acceptance/evidence-m2/**.
**No further packet is auto-started; M3 planning only on owner request.**

| Planning task | Status | Deliverable / handoff |
|---|---|---|
| M2 packet planning | complete draft — plan review accepted 2026-09-18 | [Plan](planning/m2-plan.md), [packets](planning/m2-packets.md), [acceptance](planning/m2-acceptance.md), [physics evaluation brief](planning/m2-physics.md); [planning handoff](handoffs/m2-planning.md); [plan review](handoffs/m2-plan-review.md) |

| M2 gate | Covers | Status |
|---|---|---|
| Plan review | M2 planning deliverables | **accepted 2026-09-18** — architectural plan review (docs/handoffs/m2-plan-review.md; session review, not owner/independent approval): accept with bounded follow-ups BR-1 (14 probe isolation), BR-2 (CPU reference labeling), BR-3 (gamepad/secure-context topology — owner), BR-4 (15 staging vs workspace.md §7 pause scope note); no blocking findings — packet 14 authorized |
| E | 14–19; exact contracts, evaluated physics and owner decisions | **accepted with bounded follow-ups 2026-09-18; bounded follow-ups applied + accepted-doc promotion performed 2026-09-18** — Gate E review (docs/handoffs/gate-e.md, fresh session from tree + contracts + raw evidence): all 16 proposal/diff rows accepted for docs-only promotion (none rejected); toolchain fresh green (checker 33/33 + non-vacuous negative control, 841/841 tests, typecheck/check-deps/check-boundaries 10/150/514/build exit 0); contracts/packages/tools/lockfile empty diff vs 5b746ee; packet-14 course probe reproduced byte-identically and the Rapier pin/licence/integrity verified against the npm registry; p19 manifest digests and behavior-container hashes independently recomputed; no P1. Bounded follow-ups (docs-only): **GE-1** packet-14 raw evidence artifacts are misplaced under tests/evaluations/m2-physics/docs/acceptance/evidence-m2/14/ instead of docs/acceptance/evidence-m2/14/; **GE-2** promotion must materialize the D19-A unit rename (D18-1/2/3/5/6 `behavior-compiler`/`behaviors` superseded by `behavior-build`/`runtime`) as replacement, not additive, text; GE-3/G-4 non-gating (stale p15 negative-control transcript; approximate unpackedBytes). Physics selection PROVISIONAL and desktop/browser/gamepad UNVERIFIED per owner authorization — not blockers; trust boundary owner-pre-approved pending final manual review; CPU directional (BR-2). Verdict is this record only, not owner/independent approval. **Follow-ups applied 2026-09-18:** GE-1 evidence moved to docs/acceptance/evidence-m2/14/ (probe scripts intact, references verified); GE-2 explicit replacement text in diffs/dependencies.md D19-A + contract-diffs.md §3(d); GE-3 transcript re-recorded (duplicate noted, checker unmodified); GE-4 unpackedBytes labelled approximate. **Docs-only promotion performed 2026-09-18:** all accepted rows applied into docs/contracts/** (project-model §13.1/§13.2 + §18–§22; workspace §4.5/§7.6/§13–§15; commands §3.1.1–§3.1.8 + §8.5–§8.12; runtime §12–§14; sessions §10.5 + §16–§18; export; dependencies incl. the Rapier pin text — no install); decision 0002 §§1–6 approved under the pre-approval tag; post-promotion toolchain green (fixture checker 33/33, inventory 6/6, 841/841 tests, typecheck/check-deps/check-boundaries 10/150/514/build exit 0; promotion-check 11/11); contracts diff non-empty and limited to accepted sections. C19-D7 (fixture `behaviorTrust`) deferred — fixtures preserved. Next: 20 (not started). **Reopened portions repaired 2026-09-19:** the Gate F repair step (GF-1/GF-2/GF-3) edited the promoted Gate E destination sections named in docs/handoffs/gate-f-repair.md; those portions are **re-accepted as of that repair** (owner pre-approval tag; final manual review pending). **Reopened portions re-accepted again 2026-09-19** by the Gate G bounded repair (GG-1/GG-2/GG-3/GG-6/GG-7): commands.md §2/§3.1/§3.1.6/§5.3/§5.4/§8.1/§8.10/§9.1, export.md §5.4.1, dependencies.md §3 + §5 check 4, project-model §18.1/§20.4, sessions.md §9 — see docs/handoffs/gate-g-repair.md. **Reopened portions re-accepted 2026-09-19** by the Gate H bounded repair (R4/R5/R6): runtime.md §8/§12.0/§12.1/§12.2/§12.4/§12.5.1–§12.5.8/§12.6, project-model.md §21.1/§21.6, dependencies.md §3 rows, sessions.md §13.1, and the accepted `platformer.md` §7/§7.4 + `physics.md` §8 text — see docs/handoffs/gate-h-repair.md. **Reopened portions re-accepted 2026-09-19** by the Gate I required-changes repair (R-I-1/R-I-2 + the 29 accepted docs diffs): project-model.md §22.4/§22.4.1/§22.5; workspace.md §13.3.1/§13.5; commands.md §5.4; runtime.md §3.1/§12.1/§12.2/§14.4/§14.5; sessions.md §7.1/§7.2/§17.1.1/§17.5/§19.1/§19.4; export.md §3/§5.2/§5.4/§5.4.1/§6/§7; dependencies.md §3/§4.1/§4.2/§5 check 1; planning `behaviors.md` §5.1/§5.4/§5.5 + m2-acceptance A22 — see docs/handoffs/gate-i-repair.md. |
| F | 20–25; durable content and backend/MCP | **accepted with bounded follow-ups 2026-09-18/19** — Gate F review (docs/handoffs/gate-f.md, fresh session from tree + contracts + raw evidence): required checks re-run green on the current tree (`npm test` 81 files/1131 passed; typecheck/check-deps/check-boundaries 11/187/680/build 4 built/check-fixtures 33/33, all exit 0); durability re-derived by an independent migration probe (v1 source byte-identical after success+refusal, destination byte-equal, marker removed) and the 6 real-SIGKILL crash cases; packet-24 recipe/metadata digests recomputed in python (17/17 sidecar↔GLB↔index hashes identical); packet-25 security negatives re-run 11/11 (real backend + fs + stdio MCP + WS). Contracts/decisions untouched by 20–25 (exactly the 7 promotion contracts + new decision 0002; mtimes/index hashes consistent); M1 fixtures byte-unchanged; workspace remains the sole executor (transport writes nothing). All 22 recorded change requests adjudicated: 4 accept as recorded, 18 accept-with-diff, 0 rejected (C23-1 settled by packet 25). Bounded follow-ups: **GF-1** promote delivery §15 content-service prose into sessions.md and reconcile the assets/jobs/integrity limits (C25-4/C25-1/C25-3); **GF-2** apply the naming/clarification diffs (§12.1/§12.4, §6.1 step 5, §3.1.1/§8.5 `importedAt`, §5.6, §5.4/§20.2, dependencies §3 rows, §18.7.2 step 7, §18.9.3, §10.8/§21.1 controller wording, C23-2); **GF-3** correct fixtures F21 (`model-0006`→`model-0005`) and F23 wording and remove the test workarounds; **GF-4** apply GF-1–GF-3 as the reopened-Gate-E repair and re-accept those sections; **GF-5** resolve the upload-completion `publishBlob` ordering (C25-5, P2-1 — recommend publishing only after successful inspection) before packet 27, not blocking 26; **GF-6** STATUS row-23 count (975 not 967), stale harness comment, job-record bound. No P1/blocker; browser/UI/pixel, physics desktop/gamepad, CPU directional (BR-2) and the trusted-main-thread final manual review remain UNVERIFIED per owner authorization. Verdict is this record only, not independent/owner approval. **Bounded follow-ups GF-1…GF-6 applied 2026-09-19** (docs + fixtures + the GF-5 upload-completion `publishBlob` ordering repair; GF-1/GF-2/GF-3 are the reopened-Gate-E repair and those portions are re-accepted as of the repair — see docs/handoffs/gate-f-repair.md). Next: 26 (not started) |
| G | 26–28; visual content authoring | **accepted with bounded follow-ups 2026-09-19** — Gate G review (docs/handoffs/gate-g.md, fresh session from tree + contracts + raw evidence): required checks re-run green on the current tree (`npm test` 95 files/1269 passed; typecheck/check-deps/check-boundaries 11/215/781/build 4 built/check-fixtures 33/33, all exit 0; GLTFLoader present in the generated editor bundle only, preview loader-free); contracts/decisions untouched by 26–28 exactly at the post-Gate-F state (7 files, +5235/−146 = promotion +5077/−145 + GF repair +158/−1; no decision diff; mtimes before the packet-26 evidence); packet-26 ownership/disposal and no-network claims independently re-derived (own esbuild probe: 15/15, outstanding 0; real-loader tests spy exactly one geometry/material dispose); packet-27 snapping math, round-half-away, clamp, cancel and the zero-during-drag/one-on-release/no-on-cancel discipline re-derived from the raw tests; packet-28 copies-not-linked, one-copy edit, undo/redo and reopen re-run against the accepted fixtures. All 7 recorded change requests adjudicated: 2 accept as recorded, 5 accept-with-diff, 0 rejected — **C27-1** (no accepted command creates `components.model`, so A02/A03 "place two instances" cannot be authored) and **C28-1** (no command creates/edits `components.collider`/`controller`, so A12/journey step 5 cannot be authored) are **milestone-blocking contract gaps** with concrete minimal diffs (extend `createEntity` with `kind:"model"` + `model.asset.assetId`; extend `setComponent` with add/edit/remove of `collider`/`controller`) and must be repaired as a separate bounded step (GG-1/GG-2) before packets 30–32 acceptance/Gate H and Gate J; **C26-2** (export.md §5.4.1 binding 4's exact `https://`=23 cannot hold once GLTFLoader enters the packets 35/36 graphs; measured Δ +12) is a bounded contract repair required before 35/36; C26-1/C26-3/C26-4/C27-2 accepted with bounded notes. No P1 code defect; P2 findings: a stale in-flight preview leak in `model-instances.ts` (GG-4) and overstated packet-27 manifest wording (GG-5). **A02/A03/A12 placement and course authoring and all browser/WebGL/pixel/network claims remain UNMET/UNVERIFIED** (no approved browser harness; U-5 open); packet-37 manual procedures are sufficient and honest. **Packet 29 is unblocked** (runtime scheduling does not depend on the missing command surface); verdict is this record only, not independent/owner approval. **Bounded repairs GG-1…GG-8 applied 2026-09-19** (docs/handoffs/gate-g-repair.md; owner pre-approval tag, final manual review pending): C27-1 closed — `createEntity` gains `kind:"model"` + a resolving `model.asset.assetId` (commands.md §2/§3.1/§5.4/§8.1; commands/editor/fixtures/tests + the place-twice browser-test flow); C28-1 closed — `setComponent` gains `collider`/`controller` add/edit/remove with `previous`/`next: unknown | null` and `restore: previous | null` (commands.md §2/§3.1.6/§5.3/§5.4/§8.10/§9.1; editable Inspector controls); C26-2 export.md §5.4.1 GLTFLoader row + dependencies §5 check 4; C26-1 dependencies §3 `./gltf-loader` row; C26-4 project-model §18.1/§20.4 note; C27-2 sessions.md §9 Rotate row + two-ring rotate helper; GG-4/GG-8 `model-instances.ts` stale-preview disposal + bounded failed-load guard with Node tests; GG-5 packet-27/28 wording corrected with dated repair notes; GG-9 recorded for the packet-37 walkthrough. Toolchain green after the repair: `npm test` 97 files/1277, typecheck/check-deps/check-boundaries 11/217/790/build 4 built/check-fixtures 34/34, all exit 0. Browser/WebGL/pixel/network claims remain UNVERIFIED (no browser; U-5 open). Next: 29 (not started) |
| H | 29–32; input, physics and controller | **accepted with bounded follow-ups 2026-09-19** — Gate H review (docs/handoffs/gate-h.md, fresh session from tree + contracts + raw evidence): required checks re-run green on the current tree (`npm test` 119 files/1562 passed; typecheck 14 packages; check-deps; check-boundaries 14/251/908; build 4 built; contracts checker 34/34; runtime/input/physics/course fixture checkers; targeted real-Rapier suites 29 files/339), and packets 29–32 touched no contract/decision file (contracts = the 7 promotion files +5292/−157 with no mtime after the Gate G repair; decisions diff empty). The installed `@dimforge/rapier2d-compat@0.20.0` version/integrity/licence equal decision 0002 §1 and `runtime`/`platformer` import no concrete physics/input; the ≤8-step/no-phantom-step/one-edge discipline, the post-commit fail-stop, the course rows (apex 1.2196625 recomputed; slope/snap/wall/ceiling/Z-lock/stall), the 100-cycle create/dispose release (62 colliders/61 bodies per cycle) and the parentless/grounding adapter were independently re-derived. All 26 recorded change requests adjudicated: **21 accept-with-diff, 4 accept as recorded, 1 rejected (C32-5)** — the docs-only repairs reopen the Gate E portions named in the record. P1/P2 code defects: **F1** an M2 fail-stop during the settle pre-roll publishes the abandoned step's transform (`runtime.ts` `committed === null` fallback; two probes) — repair before packet 34/Gate J; **F2** lower-index gamepad takeover with an already-held jump button yields a phantom `pressed` (`input.md` §4.4/§5.4) — repair before the packet-37 A11 run; **F3** the accepted O7 fixture (`controllerCount: 2`) is not exercised (the test coerces it to 0; the real code returns `snapshot_invalid`/`controller_count_invalid`); **F4/C30-1** the `input.md` §4–§5 device-binding contract is absent from `docs/contracts` while accepted text cites it normatively; **F5/C32-1** the `min_slope_slide_deg` slide policy is normative behaviour absent from the accepted controller contract (plus the wrong `physics.md` §8 adapter attribution); F6/F7 reporting corrections (snap-beyond wording vs the ~20 free-fall steps actually measured; CPU percentiles vs the committed `06-cpu.json`). Bounded repairs R1–R7 applied 2026-09-19 as one docs-and-test-first change (docs/handoffs/gate-h-repair.md): F1 `committed` initialised at instantiate + first-step fail-stop fixture/regression; F2 active-pad change routes through fresh activation + phantom-press test; F3 O7 repaired to the reachable zero-controller case with a count-2 `snapshot_invalid` scene-validation case and the test coercion removed; F4/C30-1 `input.md` §4–§5 promoted into runtime.md §12.5.4–§12.5.8 (C30-2/C30-6 applied); F5/C32-1 slide policy promoted to platformer.md §7.4 + project-model §21.6 and the physics.md §8 attribution corrected; F6/F7 snap-beyond, CPU-percentile and citation corrections. Post-repair toolchain green: `npm test` 119 files/1564 passed, typecheck/check-deps/check-boundaries 14/251/908/build 4 built, contracts checker 34/34, runtime/input/physics/course checkers, targeted suites 25 files/302. **The reopened Gate E portions named above are re-accepted as of this repair** (owner pre-approval tag; final manual review pending). Physics selection stays PROVISIONAL and the BR-3 secure-context/gamepad topology decision remains owner-open. UNVERIFIED: physical keyboard/gamepad, real tab resume, browser WASM/CSP/static packaging, WebGL/pixels/GPU and browser-side CPU (no browser/hardware in-container; the packet-14/30/32 procedures + BR-3 note were judged sufficient and honest). **No blocker: packet 33 is unblocked** (no reopened portion is on its dependency path); this verdict is the review record only, not owner/independent approval. Next: 33 (not started) |
| I | 33–36; behaviors, isolated play and export | **accepted 2026-09-19 — re-review after R-I-1/R-I-2 repairs** — Gate I review (docs/handoffs/gate-i.md, fresh session from tree + contracts + raw evidence, packets 33–36 only): required checks re-run green on the current tree (`npm test` 133 files/1693 passed; typecheck 15 pkgs; check-deps; check-boundaries 15/283/1068; build 4 built; checker 34/34; packet-domain checkers; targeted real-process suites 9 files/62) and contracts/decisions untouched by 33–36 (7 promotion files, +5456/−160 = the post-Gate-H-repair state exactly; no mtime after the repair; decisions empty). Independently re-derived: 6/6 fresh-process cold builds + sentinel unexecuted, hostile-import rejections, 33/33 container digests vs node:crypto, the `P34_*` VM measurement (3.5→0.149, 6.5→0.279), the play locator negative matrix (13/13 + 10/10 units), the preview/export scans (a/c/d/e/f/g/h/i/j = 0/0/6/0/3/0/38/0/3 and 0/0/0/7/0/3/0/38/0/5, GLTFLoader×37, no credential/`/api/v1`), double-export (only manifest.json+meta.json differ, normalised buildId re-derives) and play-vs-export trace (max |Δ| = 0). **Two P1s: P1-1 `behavior-build/src/canonical.ts` still has the `len ≡ 55 (mod 64)` SHA-256 padding defect that packet 36 repaired only in `project-model` — a valid 951-byte container's publication fails `behavior_publication_unavailable`/`preparation_missing` (real workspace probe); P1-2 accepted `runtime.md` §14.5 effective-input rule is unimplemented (the platformer controller/runtime never read `ctx.intents`), so the committed sample's `speed` has no effect in production Play/export and A15's production claim rests on a test-only consumer module.** All 30 recorded change requests adjudicated: **29 accept-with-diff, 1 accept-as-recorded (C34-6), 0 rejected**; the diffs reopen the named Gate E portions (docs-only promotion, plus C34-1/C34-3/C35-5 paired with P1-2). Bounded follow-ups P2-1…P2-4 (packet-35 scope exceptions → contract text; C36-7 `capturedAt`/A22; packet-36 evidence overwritten by `npm test`; packet-36 `project-model` scope exception + authored `content.settings` not reaching Play). Browser/WebGL/pixels/physical input, the real rendered PNG and the backend-stopped export walkthrough remain UNVERIFIED (no browser/hardware; no fabricated screenshot; packet-37 procedures judged sufficient and honest). Verdict is this record only, not owner/independent approval. **Packet 37 is blocked** until R-I-1/R-I-2 land and are re-verified; next step: apply the two bounded repairs + the 29 docs diffs, then Gate I re-review (no automatic start). **REPAIR APPLIED 2026-09-19** (docs/handoffs/gate-i-repair.md; owner M2 pre-approval tag, final manual review pending): repairs R-I-1/R-I-2 + 29 accepted docs diffs applied 2026-09-19; P2-1…P2-4 disposed (packet-35 exceptions contracted via C35-1/C35-3; C36-7 capture-second text + A22; packet-36 evidence writes gated behind `TL_EVIDENCE_DIR` and the artifacts regenerated; packet-36 `project-model` scope exception recorded and `content.settings` deferral contracted with a proposed diff). Post-repair toolchain green (`npm test` 135 files/1701 passed; typecheck 15 pkgs; check-deps; check-boundaries 15/283/1068; build 4 built; checker 34/34; domain checkers; targeted suites). **RE-REVIEW ACCEPTED 2026-09-19** (docs/handoffs/gate-i-rereview.md, fresh session from tree + contracts + raw evidence; this record only, not owner/independent approval): re-ran the full toolchain green (`npm test` 135 files/1701; typecheck 15 pkgs; check-deps; check-boundaries 15/283/1068; build 4 built; checker 34/34; domain checkers; targeted 15 files/104; 16×178 frozen traces exact) and both P1s independently re-derived — **P1-1 fixed**: `canonical.ts` now pads `ceil((len+9)/64)`, my probe agrees with `node:crypto` on 640 lengths incl. every `len ≡ 55 (mod 64)` class, the pre-repair bundle still reproduced `87e873f6…`/`1066eae3…` for len 55 and the real 951-byte container while the repaired code gives the `node:crypto` `4459c410…`, and the real workspace publishes a 951-byte container end-to-end (7/7); no other hand-rolled SHA-256 remains (project-model/asset-pipeline variants all agree). **P1-2 fixed**: the production `composeExportRuntime` registers `platformerSpec`, whose controller reads `ctx.intents.move ?? ctx.action.moveX` per §14.5 (`ctx.action` stays sampled); my fresh production-composition run gives `xSlow −0.499564501689747` / `xFast 0.7639840019401163` / delta `1.2635485036298633` with zero sampled `moveX`, identical to `evidence-m2/34/effective-input.json`. All 29 accepted docs diffs verified at their destination sections (contracts +5657/−161 = +201/−1 over the pre-review +5456/−160; decisions unchanged, mtime before the repair; only the named files changed). P2-1…P2-4 disposed (C35-1/C35-3 contracted; C36-7 capture-second text + A22; packet-36 evidence gated behind `TL_EVIDENCE_DIR` and byte-unchanged across `npm test`; `content.settings` deferral honestly recorded and matched by the code). Non-gating nits only (packet-34 README stale C34-3 label; export.md §5.4.1 GLTFLoader `h` sub-count wording). Browser/WebGL/pixels/physical input, the real rendered PNG and the backend-stopped export walkthrough remain UNVERIFIED (0 PNGs; no fabricated evidence). **No blockers: packet 37 is unblocked**; next step: packet 37 integrated M2 acceptance + Gate J. |
| J | 37; integrated M2 acceptance | **accepted with bounded follow-ups 2026-09-19** — Gate J review (docs/handoffs/gate-j.md; fresh session from tree + contracts + raw evidence; packet-37 handoff not treated as proof; this record only, not owner/independent approval): packet 37 is docs-only (new `acceptance/m2-report.md`, `evidence-m2/37/**` 52 files, `handoffs/37.md`; modified `acceptance/deployment.md` + STATUS row 37; no packages/fixtures/tests/tools/contract/decision/lockfile change — contracts still exactly the Gate I re-review state 7 files +5657/−161, decisions empty, max contract mtime 10:52 vs the 12:44–12:47 packet-37 writes). Re-run green: repo `npm test` 135 files/1701, typecheck 15 pkgs, check-deps, check-boundaries 15/283/1068/0, build 4 built/0 skipped, fixtures 34/34 (all exit 0; evidence/git-status byte-unchanged by the run); disposable `/tmp` `npm ci` copy (167 pkgs) full suite + all tools + 4-built + 34/34 exit 0 with the repo untouched, and the copy's fresh bundles byte-identical to the deployed `dist/backend/backend.mjs` b3cf2f5a… and `dist/mcp-adapter/mcp.mjs` 53f30550… (hash verified here); **independent journey re-run of the unmodified packet-37 driver against those artifacts on a disposable root = 18/18 PASS, exit 0**; export claims **re-derived** by my own `tests/integration/m2-export` run (9/9; only `meta.json` differs across same-second exports, `buildId` re-derives, trace max\|Δ\|=0 over 120 steps, `failure-injection.json` byte-identical) and A15's production measurement re-derived byte-identically; 8+ A01–A24 spot-check classes agree with the cited artifacts; 0 PNGs (also 0 other images), CPU labelled container/directional (BR-2), no credential/capability leak, U-4 closure verified real (decision 0002 §6.8 + sessions.md §13.7 + export.md §5.4.1 + fail-closed backend), no new contract-change request. **No P1/blocker.** Bounded follow-ups (docs/evidence-only; each underlying claim independently re-derived): **P2-1/GJ-F1** the UNVERIFIED row list is incomplete/inconsistent — A03 (browser images) and A16 (UI evidence) are unmarked and A04 is in §2 but not §4 U-1 (handoff 37/STATUS list 13 rows, §4 lists 14, the complete union is 16); **P2-2/GJ-F2** the driver's A22 reproducibility check hashes the same `outputDir` after both exports so `differing=[]` is vacuous (the report's "same-second real exports differed in nothing at all" leans on it; the claim itself is valid — my fresh m2-export run re-derives it); **P3-1** the journey A20 "each stopped cleanly" wording is not asserted (stop status ignored); **P3-2** the driver `noBrowser` probe is actually the unknown-play case (the real no-browser `session_unavailable` is covered by `packages/backend/src/play.test.ts` + `packages/mcp-adapter/src/mcp.e2e.test.ts`); **P3-3** citation nits (m2-content security tests are 12 not 11; the A20 27/27/0 figure is from `evidence-m2/26/raw/ownership-counters.txt`; `judgement/no-png.txt` embeds an earlier run). **Milestone acceptance is recorded as accepted for the executed process/filesystem/HTTP/WS/SDK/Node scope, with 16 browser/hardware rows UNVERIFIED (A02, A03, A04, A05, A07, A08, A11, A12, A13, A15, A16, A17, A18, A19, A20, A21) and pending the owner walkthrough (`m2-report.md` §8 items 1–8; desktop WebGL 2 + physical keyboard/gamepad, recorded secure context).** Per `m2-acceptance.md` §4 and the owner's 2026-09-18 authorization, M2 milestone completion stays formally pending that walkthrough; the report does not claim completed milestone acceptance. Physics selection PROVISIONAL; BR-3 secure-context/gamepad topology owner-open. **Next step: owner walkthrough → bounded docs/evidence close-out → then, only on owner request, M3 planning; no automatic start, no M3 begun. Bounded close-out applied 2026-09-19** (docs/handoffs/gate-j-repair.md): P2-1 the UNVERIFIED row list is now the complete 16-row union and identical across m2-report §2/§4, handoff 37 and this row (A02, A03, A04, A05, A07, A08, A11, A12, A13, A15, A16, A17, A18, A19, A20, A21; A03 browser images, A04 UI error display, A16 UI evidence each point at their §8 procedure); P2-2 the packet-37 journey A22 check is non-vacuous — it now runs two genuinely separate exports into `exports` and `exports-2` and hashes both trees (identical 7-file sets; differing = `manifest.json`,`meta.json` only; non-timestamp bytes identical; normalising `capturedAt` and re-deriving `buildId` reproduces export 1); P3-1 A20 asserts each stop's HTTP 200 `{ok:true}` **and** the owner-acked `play.stopped` with reason `request` and no `stopUnconfirmed` (6 stops, 0 unconfirmed); P3-2 the driver's `noBrowser` misnomer is now the unknown-play case (real no-browser `session_unavailable` cited to `packages/backend/src/play.test.ts` + `packages/mcp-adapter/src/mcp.e2e.test.ts`); P3-3 citations corrected (m2-content 12 security tests; the 27/27/0 ownership figure from `evidence-m2/26/raw/ownership-counters.txt`; `judgement/no-png.txt` regenerated from the final run). **Verdict unchanged: accept with bounded follow-ups; the 16 browser/hardware rows remain UNVERIFIED and milestone completion stays formally pending the owner walkthrough. Re-run journey 18/18 PASS.** |

Every packet below refers to its numbered section in
[planning/m2-packets.md](planning/m2-packets.md). Required handoffs are
`docs/handoffs/<ID>.md` when executed; none exists yet for these packets.

| ID | M2 packet | Gate | Prerequisite | Status |
|---|---|---|---|---|
| 14 | Browser baseline and bounded physics selection | E | Plan review accepted (met 2026-09-18) | done 2026-09-18 — container evidence complete; selection PROVISIONAL (desktop evidence + Gate E open; see 14.md) |
| 15 | Content storage, asset identities and migration contract | E | 14 report | done 2026-09-18 — PROPOSED drafts (planning/m2-contracts/{content-storage,assets}.md), project-model/workspace diffs and 42 indexed byte-exact fixtures; Gate E review + docs-only promotion performed 2026-09-18 (row E; handoffs/m2-promotion.md) (see 15.md) |
| 16 | Typed edits, prefab and property contracts | E | 15 | done 2026-09-18 — PROPOSED drafts (planning/m2-contracts/{prefabs,properties}.md), commands.md diff + project-model "Packet 16 additions" (packet-15 sections preserved), 5 command fixtures whose scenario the checker replays (two independent instances, one legal initial override, ordinary edits, one-undo whole-subtree removal, exact-ID redo/retry, mixed human/MCP history) plus 24 failure cases + 10 constructed boundaries and bounded content queries; checker 16/16 green, 841/841 tests (one pre-existing concurrency flake recorded), no accepted contract changed; Gate E review + docs-only promotion pending (see 16.md) |
| 17 | Stateful runtime, input and 2.5D physics contract | E | 14 evidence, 15–16 | done 2026-09-18 — PROPOSED drafts (planning/m2-contracts/{input,physics,platformer}.md), runtime.md diff R1–R18 + project-model "Packet 17 additions" (packet-15/16 sections preserved): step-indexed ActionFrame, injected input/physics ports, fixed phase order with write guard, one transform owner, 2.5D XY-only convention, coyote/buffer/release windows, 12-step settle pre-roll, fail-stop with no rollback, static −Z camera, six-key gameplay settings registry; 5 replayed fixture sets (platformer/traces 16 traces × 178 rows, physics/numerics, input/action-sequences, runtime/catchup, platformer/failures 21 cases) re-derived by an independent python3 implementation and by checker groups p17-* (21/21 green, negative control 7 failures); 841/841 tests, typecheck/check-deps/check-boundaries/build exit 0; Rapier 2d-compat 0.20.0 selection per decision 0002 §1 (pin only at packet 31); Gate E review + docs-only promotion pending (see 17.md) |
| 18 | Trusted behavior execution and compilation contract | E | 15–17 | done 2026-09-18 — PROPOSED drafts (`planning/m2-contracts/behaviors.md`), runtime diff R19–R30 + project-model diff P18-A1…P18-A9 (packet-15/16/17 sections preserved) + new `diffs/{dependencies,export}.md`: canonical source-graph container + `sourceDigest`/`manifestDigest`/`outputDigest` binding, static import/dynamic-code taxonomy with an exhaustive validation order, pure `compileBehavior` over supplied bytes with compiler bounds separate from runtime trust, stage → validate/compile → digest-bound prepare → `runCommand` publication with hard-unavailable source publication until packet 33 and the two distinct build failures, `BehaviorSpec` prepare→instantiate→step→dispose, bounded intent API (`ctx.emit`) with duplicate-writer/limit failure semantics, per-instance log ring and flood accounting; trusted-main-thread trust boundary documented prominently with the acknowledgment gate and owner pre-approval tag (**owner pre-approval (autonomous M2 build instruction, 2026-09-18); final manual review pending** — pre-approval, not an independent review); 5 fixture sets (41 source-graph cases + 18 intent step rows + 27 runtime-failure/flood rows + 14 publication state-machine cases + the digest-bound example manifest with a declared numeric property), 16 real source containers with recomputed SHA-256/lengths, checker groups `p18-*` 27/27 green (six-corruption negative control exit 1) with the rules independently re-derived in python3, 841/841 tests, typecheck/check-deps/check-boundaries (10/150/514)/build exit 0; accepted contracts, decisions and the lockfile untouched; Gate E review + docs-only promotion pending (see 18.md) |
| 19 | Delivery, protocol, export and dependency integration | E | 15–18; U-4 disposition | done 2026-09-18 — PROPOSED boundary pack (`planning/m2-contracts/delivery.md` + consolidated `contract-diffs.md`): immutable runtime-content manifest (build identity distinct from `project@revision`; build failure preserves the previous output), authenticated project-scoped immutable-version asset-byte reads (renderer gets no token), read-only play-content locator (capability, TTL 900 s, redaction/exclusion, no listing/traversal, reload never newer), versioned bridge v2 (exact origin/source/nonce kept; no binary in WS state), bounded exclusive-test input relay (step range + snapshot/build reported; no browser ⇒ structured unavailable), format-aware GLB/WASM/JS/closure validation replacing the text scan for binaries, export closure with allowlisted relative artifact reads (replacing M1's one-fetch rule only through the explicit export diff), exact bounds/timeouts and negative matrix; session/gesture snapping diff (0.25 m / 15° / 0.25, scale [0.01,100], 1e-4 quantization, Shift disables one gesture, zero commands during drag/one on release/none on cancel, no persistent setting); U-4 ACCEPTED both (`engineRoot`, §5.4.1 reference-entry) under the owner pre-approval tag, fail-closed; new units/pins proposed per diff (`asset-pipeline`, `input`, `physics-rapier`, `platformer`, `behavior-build`; `@dimforge/rapier2d-compat@0.20.0` lockfile-only at 31; no new GLB pin) with naming/conflict resolutions; decision 0002 §§2–6 added as DRAFT/PROPOSED (nothing approved) and acceptance.md refined with Gate-E-fixed constants; 6 delivery fixtures + 6 new checker groups (33/33 green, negative control 7 failures), inventory check 6/6, 841/841 tests, typecheck/check-deps/check-boundaries (10/150/514)/build exit 0; accepted contracts/decisions/lockfile untouched; no commit; Gate E review + docs-only promotion performed 2026-09-18 (row E; handoffs/m2-promotion.md) (see 19.md) |
| 20 | Model v2 and pure migration | F | Gate E accepted + doc promotion | done 2026-09-18 — explicit M2 v2 types + versioned entry points (`validateSceneV2`/`normalizeSceneV2`/`parseSceneV2`, `validateContent`/`normalizeContent`, `validateProjectV2`, `captureContent`, `resolveGameplaySettings`, `migrateSceneV1ToV2`), extended `ERROR_CODES` + per-document `SCHEMA_VERSIONS_BY_DOCUMENT`/`KNOWN_VERSIONS`; `fixtures/m2/model/**` (30 fixtures + expected.json, valid byte-exact, invalid non-destructive, migration IDs retained) + 43 tests (project-model 147); C19-D7 closed (`behaviorTrust` added to 15 v2 envelope/content fixtures + expected.json + checker key-order assertion); M1 `validateScene`/`parseScene`/`migrate*` pinned to v1 and all M1 fixtures/tests unchanged; checker 33/33, 67 files/884 tests, typecheck/check-deps/check-boundaries (10/156/546)/build exit 0; no commit; contract-change requests C20-1/C20-2 (v2 scene validator + M1→M2 conversion naming) recorded in handoff 20 (see 20.md) |
| 21 | Pure content and property commands | F | 20 | done 2026-09-18 — non-prefab M2 ops on the shared pure history engine (`publishAsset`, `publishBehavior` declaration modes with source mode refused as `behavior_publication_unavailable`, `setBehaviorProperties`, `setComponent`, `setSettings`, `acknowledgeBehaviorTrust`, `deleteEntity` §8.3 step 2b `reference_in_use`) plus bounded `queryAssets`/`queryBehaviors`/`contentCounts`; change/inverse data, no-change over scene+content, one revision/history entry per success, redo invalidation and stale-before-argument ordering; prepared blobs are digest-addressed facts only (no I/O, no workspace mutation); packet-16's committed M1/M2 scenario pairs + 11 reachable failure cases replay byte-exactly and `fixtures/m2/commands/**` indexes a new byte-exact scenario; `npm test` 68 files/916, typecheck/check-deps/check-boundaries (10/161/571)/build exit 0, checker 33/33; one M1 `api-surface.test.ts` assertion extended for the contract-mandated §5.4 `ERROR_CODES` rows, all M1 fixtures byte-unchanged; contract-change requests C21-1…C21-5 recorded; owner pre-approval (autonomous M2 build instruction, 2026-09-18); final manual review pending (see 21.md) |
| 22 | Pure prefab capture and instantiation | F | 21 | done 2026-09-18 — `createPrefab` (immutable definition capture: subtree closure in document order, camera/nesting/external-reference/definition-vocabulary rejection, `prefab_entities`/`prefab_depth`/`prefab_bytes`/`prefabs` bounds) and `instantiatePrefab` (one transaction, deterministic smallest-free IDs in definition document order, every local entity/reference ID remapped exactly once, exact mapping recorded in the result) plus `queryPrefabs`; change data `createPrefab`/`removePrefab`/`instantiatePrefab`, inverse `removePrefab`/`delete`, one-undo subtree removal and exact-ID redo on the shared pure history engine; overrides type-checked with the constraint-rich §8.7.3 presentation; packet-16's M1–M10 scenario replays byte-exactly (M10 modulo `duplicated`) and 22 reachable `prefab-failures.json` cases match; `fixtures/m2/prefabs/**` (expected.json + a two-copy independence/exact-ID-redo scenario); `npm test` 69 files/938, typecheck/check-deps/check-boundaries (10/164/587)/build exit 0, checker 33/33; no project-model edit (C21-5 deferred with a proposed diff), no M1/contract fixture changed; C21-5 deferred + C22-1/C22-2 + fixture defect FD-22-1 recorded; owner pre-approval (autonomous M2 build instruction, 2026-09-18); final manual review pending (see 22.md) |
| 23 | Workspace content publication and migration-copy | F | 22 | done 2026-09-18 — durable M2 envelope/blob operations on the real filesystem: `storageVersion 2` envelope load/write (single atomic envelope; `scene.revision` the only revision), immutable `sources/sha256/<digest>` publication via §5.1 `W` (write-once/verify/no-overwrite), §7.6 staging incl. the BR-4 harness-edit path + blob-temp hygiene, derived-cache paths (regenerable, non-authoritative), verified `readBlob` + `contentIntegrity` (missing/corrupt/tampered/`referenced`/orphan), quota + device-space pre-flight, `captureContentView`, v2 command execution (content+manifest wired as the three-block inputs, commit-time blob verification, dedup before revision/stage lookup), operator `migrateProjectCopy` (new identity, reset-to-zero, marker + phased resume, source byte-identical) and the interrupted-destination scan row; behavior `mode:"source"` stays structurally refused; all repaired M1 ownership/external-change behavior preserved. `fixtures/m2/storage/**` (blobs + byte-exact v2 project + `expected.json` case status) and the accepted M2 envelope/migration fixtures are executed; 27 storage + 4 migration tests and 6 real-subprocess SIGKILL crash tests (blob/envelope boundaries, durable ack + replay). `npm test` 72 files/975, typecheck/check-deps/build exit 0, check-boundaries 10/168/629, checker 33/33; no accepted contract/M1 fixture/lockfile change; only the M1 `storageVersion 2` invalid-fixture expectation moved to the v2 branch (`envelope_invalid`/`field_missing`, bytes unchanged) and the consumer ambient `.d.ts` fs surface extended for typecheck. C23-1 (`inspectStage` needs packet 24) and C23-2 (`captureManifest` absent from the accepted model) recorded; owner pre-approval (autonomous M2 build instruction, 2026-09-18); final manual review pending (see 23.md) |
| 24 | Bounded GLB inspection and import proposals | F | 23 | done 2026-09-18 — new pure leaf `@thirdlight/asset-pipeline` (`inspectGlb`/`prepareImport` over supplied bytes + an injected job port; project-model §18.7/§18.8 in the exact §18.7.2 order: 17 steps, first failing step only, ≤10 diagnostics + true count, §18.6 metrics, the closed §18.8.2 code set, §18.5 `recipeDigest` + a metadata digest; deep-frozen validated data, no I/O / Node builtins / three / GLTFLoader / plugin registry / URL fetch, no cache writes, no asset-ID decisions; caller/workspace commits). 17 self-generated redistribution-safe GLBs under `fixtures/m2/assets/**` (valid tiny mesh+2 materials+clip pair with reordered v2, truncation, bad chunk, BOM/duplicate-key JSON, accessor overflow, remote-buffer and `data:` image URIs, required extension, Draco compression, decoded-image/node/image-count caps, malformed clip, empty model, mime mismatch) + `expected.json` (digests, ordered codes/paths/limits, metrics, recipe/metadata hashes) + base64 sidecars so the I/O-free tests run on the exact committed bytes; 102 packet tests (fixture index 23, adversarial step-by-step rejections 38, limits/job 12, determinism 7, surface/no-network 22). `npm test` 77 files/1077, typecheck/check-deps/build exit 0, check-boundaries 11/183/660, checker 33/33; `npm install` added one workspace link only (lockfile +11 lines); negative boundary probes (`three`/`node:fs`/workspace + a value import of project-model) fail check 1 and were removed; independent python3 re-verification of the recipe digest and all 17 sidecar→`.glb` digests. C24-1 (`prepareImport` absent from dependencies.md §3), C24-2 (§18.7.2 step 7's literal `byteOffset <= 3` rejects every real GLB — glTF 2.0 alignment implemented), C24-3 (`json_chunk_bytes`/`image_bytes` missing from §18.9.3) recorded; browser/UI + transport parts of A02/A04 remain unverified (packets 25–27); `inspectStage` wiring still open (C23-1, now unblocked); owner pre-approval (autonomous M2 build instruction, 2026-09-18); final manual review pending (see 24.md) |
| 25 | Content HTTP services, projection data and MCP parity | F | 24 | done 2026-09-18 — the packet-19 content routes (bounded upload/stage/inspect/discard, assets/integrity/jobs reads), `queryAssets`/`queryPrefabs`/`queryBehaviors` and the M2 mutation ops over `POST /commands`, and authenticated committed asset-byte reads by immutable `(project, asset, version)` (sessions.md §16.1) are wired through a transport that performs NO filesystem write and NO authoritative mutation: it injects the workspace service, constructs the `asset-pipeline` inspector and injects it (workspace owns the staged read via new `inspectStage` + `importRejected`), and publishes the immutable blob as the non-authoritative preparation step at upload completion. Bounded in-memory inspect/publish jobs (30 s/120 s, 2-per-project/4-global concurrency, cancellation/expiry/late-result discard, `job_not_found`/`job_expired`); behavior-build jobs/publication stay structurally unavailable (packet 33, never mocked). Additive versioned protocol additions (content codes/fields, M2 op lists, `checkUploadFrame` reproducing the accepted `upload-bounds.json` U1–U7, binary-free full-state/change guards); M1 client/validators unchanged. Full state gains an additive bounded `content` projection (summaries only); `mutation.applied` never carries bytes. MCP gains `tl_content_query`/`tl_content_upload`/`tl_content_job` and the M2 ops on `tl_command`, proven through the actual stdio MCP process. Integration tests (real backend child process on a disposable root + real fs + real stdio MCP SDK client + real WS) cover import/query/instantiate/edit/retry, browser/MCP parity, projection convergence and the negative security matrix; `npm test` 81 files/1131, typecheck/check-deps/build exit 0, check-boundaries 11/187/680, checker 33/33; no accepted contract/M1 fixture/lockfile change; C25-1…C25-6 recorded (additive `content` field; `publishAsset` `importedAt`; route-level cls mappings; delivery §15 vs commands §4 limit; `publishBlob` trigger; frozen-lockfile dependency declarations); browser/UI parts of A02/A04/A07 remain UNVERIFIED (no browser; packet 37); owner pre-approval (autonomous M2 build instruction, 2026-09-18); final manual review pending (see 25.md) |
| 26 | Shared GLB rendering and asset previews | G | Gate F accepted | done 2026-09-19 — one resource-owner path in `@thirdlight/three-adapter` for model instances and asset inspection: an approved asset-byte resolver (supplied bytes **or** an injected async resolver — never a token/URL/fetch), cancellable async prepared visual resources with stale-completion discarding, whole-GLB model instances (internal hierarchy preserved under one entity; independent transform and animation state), a local material/animation preview controller (play/pause/scrub, instance-local material overrides, host-driven `update(dt)`), explicit ownership counters and idempotent disposal; nothing mutates runtime/authoring state and the M1 instantiate/render/screenshot/diagnostics behaviour is unchanged. The pinned `three@0.186.0` GLTFLoader binding lives on the new `./gltf-loader` subpath so the export/preview bundles stay loader-free, and `tools/check-boundaries.mjs` now restricts `three-adapter` to the approved `three` root + `examples/jsm/loaders/GLTFLoader.js` subpath (negative probe still fails; 1 test added). Real-loader Node tests over self-generated GLB bytes (`test-glb.ts`) prove realization, two-instance independence, play/pause/scrub pose application, cancellation/stale discard, `asset_missing`/`asset_corrupt`/`asset_extension_unsupported`/`asset_image_invalid`/`asset_clip_invalid`, ownership balance 27 allocations/27 releases/0 outstanding over repeated load/reimport/dispose, no-network prepare/preview/screenshot, and lost-context reporting with listener release; `npm test` 85 files/1161 (three-adapter 12→40), typecheck/check-deps/build exit 0, check-boundaries 11/196/710, checker 33/33; no accepted contract/fixture/lockfile/other-package change. Browser/WebGL/pixel checks (mesh+materials render, texture decode, screenshot taint, real context-loss round trip, viewport play/scrub) remain UNVERIFIED (no browser; packet-37 procedure in the manifest). C26-1 (`./gltf-loader` subpath needs a dependencies.md §3 row), C26-2 (export.md §5.4.1 binding 4's exact `https://`=23 cannot hold once GLTFLoader enters the export/preview graphs — measured Δ +12 today; packets 35/36), C26-3 (allowlist duplication from the forbidden `asset-pipeline` edge), C26-4 (clone shares skeletons) recorded; owner pre-approval (autonomous M2 build instruction, 2026-09-18); final manual review pending (see 26.md) |
| 27 | Content browser, placement/reimport and snapping | G | 26 | done 2026-09-19 — editor asset authoring on top of the packet-26 GLB path: pure Node-testable modules (`session/snapping.ts` with the contract's exact increments/rounding/clamps, an extended `session/gesture.ts` with snapping preview + explicit cancel + `GestureRunner` command discipline, additive `session/content-projection.ts`, pure `session/asset-browser.ts` for drop validation/bounded frame-job-import flow/stale-late-result handling/`publishAsset` args, `session/placement.ts` typed whole-model planning) plus browser transport in `session/client.ts` (bounded `queryAssets`, content stage/frame/inspect/job/discard routes, authenticated committed asset-byte read, `publishAsset` through the same command path), the packet-26 consumer `viewport/model-instances.ts`, gizmo accumulated drag + local Shift-disabled snapping, and the React `ui/AssetBrowser.tsx` panel wired in `ui/App.tsx`; `App.tsx` now also pushes the projection into the viewport (M1 never did). `npm test` 91 files/1216, editor 9 files/86, `tests/browser/m2-assets` 1 file/6, typecheck/check-deps/build exit 0, check-boundaries 207 files/746, checker 33/33. Acceptance: import→preview→place-twice→reimport-without-ID/transform-change, failure-preserves-content, stale-job-not-applied, invalid drop, snapping table verified against sessions.md §9, zero-during-drag/one-on-release/none-on-cancel/one-undo, remote-edit-during-drag all Node-verified; **direct placement of a freshly imported asset FAILED/blocked by C27-1** (no accepted command creates a `components.model` entity; module reports `placement_unavailable`, prefab placement via `instantiatePrefab` works) and all browser/WebGL/pixel claims UNVERIFIED (no browser; packet-37 procedure `tests/browser/m2-assets/m2-assets.browser.ts`). C27-2 rotate-axis interpretation recorded. Owner pre-approval (autonomous M2 build instruction, 2026-09-18); final manual review pending (see 27.md) |
| 28 | Prefab and declared-property authoring UI | G | 27 | done 2026-09-19 — editor prefab-copy and typed-property authoring on top of the packet-27 content path: pure Node-testable modules (`session/property-controls.ts` — schema-driven controls for the seven-type vocabulary built ONLY from published declaration data (typed defaults/constraints/errors, invalid numeric/reference input parsing, the whole-values-map `setBehaviorProperties` builder, read-only collider/controller contract shapes); `session/prefab-authoring.ts` — capture preflight + `createPrefab`/`instantiatePrefab` planning (root parent + root transform + declared-property overrides only), override-draft collection, stale-revision re-issue-then-surface and `limits_exceeded` limit/current/max recovery; `session/prefab-projection.ts` — definition/declaration projection advanced by the same `mutation.applied` records, so a definition/declaration change never rewrites a copy) plus additive `setBehaviorProperties` convergence and prefab/component fields in `session/projection.ts`, bounded `queryPrefabs`/`queryBehaviors` (full-resync definition/declaration hydration; reopens rebuild from queries) and additive `MutationResponse` limit/current/max detail in `session/client.ts`+`envelope.ts`, the React `ui/PrefabPanel.tsx` (Create Prefab Definition / Place Copy / initial override; "Copy of <name> — copies are independent"; no apply/revert/variant/link control) and the extended `ui/Inspector.tsx`+`ui/PropertyControls.tsx` wired in `ui/App.tsx`. `npm test` 95 files/1269, editor 12 files/127, `tests/browser/m2-prefabs` 1 file/12, typecheck/check-deps/build exit 0, check-boundaries 215 files/781, checker 33/33. Acceptance Node-verified: accepted-fixture capture/instantiate request bytes, two-copy independence and one-copy edit, one-undo subtree/exact-ID redo, reopen-from-queries, MCP-origin convergence (M1 dedup/gap), override legality incl. unknown declaration/invalid numeric+reference/unsupported structural override, stale-revision re-issue-once-then-surface, instance-limit surfacing, no script evaluation, copies-not-linked. **All browser/WebGL/pixel claims UNVERIFIED** (no browser; packet-37 procedure `tests/browser/m2-prefabs/m2-prefabs.browser.ts`); **collider/controller controls are read-only — C28-1** (no accepted M2 command creates or edits them; proposed diff in the manifest), and `prefab_bytes` stays backend-enforced. Owner pre-approval (autonomous M2 build instruction, 2026-09-18); final manual review pending (see 28.md) |
| 29 | Stateful runtime scheduling and module lifecycle | H | Gate G accepted | done 2026-09-19 — runtime.md §12/§13 M2 module-set lifecycle on the accepted M1 core: canonical phase order (`sample → intent → controller → physics → transform → render`) with a phase-scoped write guard (throwing read-only `curr` outside `transform`, ownership-scoped writes inside it), module `phases`/`excludes`/`requiresPhysicsPort` registration validation, transform-ownership + duplicate-writer + unsupported-combination rejection at instantiate (no partial start, specific `transform_owner_*`/`module_combination_unsupported`/`config_invalid` codes), injected `ActionSource`/`PhysicsPort` interfaces with one `sample(stepIndex)` per executed step and one validated `port.step()`, the 12-step settle pre-roll (no input sampling), and the `failed` fail-stop lifecycle (step abandoned with no transform rollback, last committed state retained for rendering, `failedModuleId`/`failedPhase`/`failedStepIndex` sticky, `runtime_failed` refuses resume, idempotent disposal of modules+port). M1 compatibility preserved exactly: `demo.ts` byte-unchanged, all M1 runtime tests pass unmodified, the frozen §7.1 demo points replay bit-exactly; M1 sets keep the accepted diagnostics key set and no-op-on-throw. New `packages/runtime/src/{actions,ports,guard}.ts`, extended runtime/types/errors/registry/snapshot/index; `fixtures/m2/runtime/**` (index + scheduler/demo/failstop fixtures + plain-Node re-derivation checker), `tests/m2-runtime/**` replays the accepted packet-17 `catchup.json` C1–C7 + O1–O8 and the new fixtures; `npm test` 102 files/1323, typecheck/check-deps/check-boundaries (11/225/832)/build 4 built/contracts checker 34/34 + runtime checker all green; no lockfile/package/contract/M1-fixture change; browser/WebGL and real Rapier behavior UNVERIFIED (packets 30–32; port exercised via an in-repo fake); C29-1…C29-9 recorded (spec port flag; `inputSamples` vs pre-roll; demo exclusion ID; M1/M2 module naming + optional `phases`; `ctx.state`; M2-only diagnostics fields; ownership codes vs §12.4 header; O7 unreachable; §12.0 promotion artifact); owner pre-approval (autonomous M2 build instruction, 2026-09-18); final manual review pending (see 29.md). Next: 30 (not started) |
| 30 | Keyboard and gamepad actions | H | 29 | done 2026-09-19 — new `@thirdlight/input` workspace package (runtime types only): pure `mapRawInput(snapshot, options) → ActionFrame` (keyboard A/D + arrows + Space; standard gamepad axis 0 / D-pad 14–15 / primary face button; 0.2 radial dead zone with linear rescaling and exact-boundary zero; deterministic keyboard → D-pad → stick precedence, never summed; press latch, key-repeat suppression and the press/hold/release phase chain; quantization matching the runtime validator), the single explicit browser listener owner `attachBrowserInput(target, options)` (editable-target suppression without preventDefault, hidden tab/pagehide/blur/target-focus suspension with fresh activation — `held`, never `pressed`, until an observed up→down — hot disconnect and index reuse keyed by index+id, lowest-index active standard pad, structured `input_unavailable` once per attach for absent/insecure/throwing `getGamepads`, keyboard-only degradation, idempotent `detach()`/`dispose()` removing every listener and clearing held state), and the replayable `createStepInputSource` (injectable step-indexed source for tests/replays, separate from the browser attachment). `fixtures/m2/input/**` (9 sequences / 38 raw-snapshot→frame steps + a plain-Node checker with its own independent re-derivation, pins and SHA-256 index) and `tests/browser/m2-input/m2-input.browser.ts` (packet-37 desktop procedure). Toolchain green: `npm test` 106 files/1401 (input 4 files/78), typecheck clean, check-deps OK, check-boundaries 12/235/861 with the disposable negative probe failing 3/3 then removed, build 4 built, contracts checker 34/34 + input checker green. Lockfile delta limited to the `@thirdlight/input` link/entry (+11 lines); no accepted contract/fixture/other-package change. **Physical keyboard, physical controller and real-browser logs UNVERIFIED** (no browser/hardware in-container; BR-3 gamepad/secure-context topology note in the manifest). C30-1…C30-6 recorded (I-5 promotion gap for input.md §§4–5; both-keys-cancel prose vs the accepted packet-17 fixture; `createStepInputSource` absent from the §3 export row; `Math.round` vs half-away-from-zero; preview iframe `allow="gamepad"`; latch during awaiting-release); owner pre-approval (autonomous M2 build instruction, 2026-09-18); final manual review pending (see 30.md). Next: 31 (not started) |
| 31 | Rapier 2D adapter | H | 30; approved physics pin | done 2026-09-19 — new `@thirdlight/physics-rapier` on the approved real pin (`@dimforge/rapier2d-compat@0.20.0`, integrity verified equal to lockfile/registry/decision 0002 §1; lockfile delta 3 hunks/+16 lines: the pin entry + the workspace link only): cancellable async `createPhysicsPort(config, signal?)` (`physics_init_cancelled`/`physics_init_failed` with `reason`), one `World` per game with static box + convex-polygon colliders (`ColliderDesc.convexHull`), the parentless kinematic capsule (`createCharacterController(offset)` + `computeColliderMovement`/`computedMovement`/`setTranslation`, snap 0.1, autostep off), XY-only grounding/support normals from the collision results, bounded diagnostics (library `world.colliders.len()`/`world.bodies.len()` + stall/correction counters), idempotent disposal with a frozen post-dispose snapshot and `physics_port_disposed` for any later world call; the adapter keeps no module-level state and owns no frame driver. `fixtures/m2/physics/**` (frozen packet-14 course re-derived by an independent checker, snap/slope/convex courses, 18 measurement cases, 36 failure cases, tolerances, SHA-256 index) + `tests/m2-physics/**` execute the real library and re-validate every result with the runtime's own `validateCharacterMoveResult`; `tools/check-boundaries.mjs` gains the `physics-rapier → runtime (types) + @dimforge/rapier2d-compat` row and `tools/check-deps.mjs` the pin (+ its pin-table test). `npm test` 113 files/1501, typecheck clean (13 packages), check-deps OK, check-boundaries OK (13/246/895, 0 violations; disposable negative probe 3/3 then removed), build 4 built/0 skipped, contracts checker 34/34 + physics/input/runtime fixture checkers green. Acceptance PASS (real library/Node): floor/wall/ceiling/box-ramp/convex-ramp correction, slope normals + the 44.9/45.0/45.1° threshold (45.1° is the raw-grounded `steepSlope` case), ground snap within/beyond 0.1 m, no Z, 100 create/dispose cycles with per-cycle library counts (62 colliders/61 bodies) and a stale-handle throw, plus cancellation, invalid shape, forbidden transform, correction failure and dispose-during-preparation. Probe browser-IIFE build under the pinned export.md §5.3 flags = 2,191,202 bytes. **UNVERIFIED: real-browser WASM init, CSP and static packaging** (no browser in-container; packet-14 desktop procedure + packet 37). C31-1…C31-5 recorded (the §8 45.1° cos typo; the 0.20.0 one-time 11.3 mm ground-offset push-out absent from `numComputedCollisions()` and outside the 1 mm non-`snapped` allowance — `snapped` now means any bounded ground-contact correction ≤ snap+skin, beyond which the adapter throws `collision_correction_failed`; snap chaining over a 0.2 m drop; no autonomous sliding at the 30° minimum-slide angle; the §8 item 4 parenthetical about a normal-flagged steep grounded) plus two additive notes (extra exported types/`PhysicsPortError`; optional `parentId`/`scale`/`rotation` config fields). No M1 regression. Selection per decision 0002 §1, owner pre-approval (autonomous M2 build instruction, 2026-09-18); final manual review pending (see 31.md). Next: 32 (not started) |
| 32 | 2.5D controller and diagnostic course | H | 31 | done 2026-09-19 — new `@thirdlight/platformer` workspace package (runtime **types-only**): `platformerSpec`/`PLATFORMER_MODULE_ID`/`CONTROLLER_CONSTANTS` — the pure `thirdlight.platformer:controller` module implementing `platformer.md` §7 A–K exactly (single jump, variable height on release, the integer-step coyote (6) and jump-buffer (8) windows, no air/wall jump, no autostep, exact-arrival `approach()`) plus `physics.md` §8 grounding from support normals (a support below the max climb angle is `steepSlope` and treated as not grounded) and the C32-1 `min_slope_slide_deg` slide policy (settings-derived, collision-result-only: a slide-steep support normal or a bounded downward ground-contact correction whose slope is ≥ `tan(min_slide)`, gated on no commanded movement). New `fixtures/m2/course/**` (the frozen packet-14 64-static course + the packet-31 slope/snap courses copied byte-identically, the pre-declared tolerance table, 24 raw input atoms/sequences, 28 diagnostic cases, SHA-256 index, independent plain-Node checker 9/9) and `tests/m2-controller/**` (4 files/35 tests): all 16 accepted packet-17 traces × **178 sampled rows replay exactly** (≤1e-9), and real runs through the real packet-31 Rapier adapter + real packet-30 mapping measure settle/rest 0.9099987, run 3.999369 m/s, 12/8 accel/decel, apex gain **1.2196625** (the §7 contract-order value) vs 1.249 ± 0.05, release 0.5, single jump, wall stop 9.19016/0 penetration, ceiling 3.089910/0 rise after strike, ledge block 0.006568/land 1.309999, 43° climb +2.1209/grounded 1.0, 47° +0.0076 m, the 29.9/30.0/44.9/45.0/45.1° table, slide drift 0/−0.277/−0.144/−0.143/0 m, snap 0.05 absorbed + 0.50 snap-chained (22 steps/max 0.0572), coyote+buffer boundaries, Z locked, snapshot deep-frozen/unchanged, stall 8 executed/592 dropped with no phantom port step and one jump edge, fail-stop retaining the committed state, stop/start state retention; all inside the frozen tolerances. `tests/browser/m2-controller/**` is the manual keyboard+physical-gamepad host (builds under the pinned esbuild, 3,513,230 bytes) for packet 37. Toolchain green: `npm test` 119 files/1562 (baseline 113/1501, no M1 regression), typecheck 14 packages, check-deps OK, check-boundaries 14/251/908 with the disposable negative probe 4/4 then removed, build 4 built/0 skipped, contracts checker 34/34 + physics/input/runtime/course fixture checkers green. **Lockfile delta: 2 hunks/+11 lines — the `@thirdlight/platformer` link/entry only** (two unrelated stale `asset-pipeline` normalizations reverted); no accepted contract/fixture/other-package change. **UNVERIFIED: the actual browser course with a physical keyboard and gamepad, real tab resume, WebGL rendering, real WASM init/CSP and all browser CPU/GPU claims** (no browser/hardware; packet-37 procedure). CPU p99 0.061–0.108 ms for the whole composed fixed step vs the 8.333 ms tick budget — container/directional (BR-2), not a reference-desktop or product-budget claim. Contract-change requests C32-1…C32-5 recorded (slide policy absent from §7; §12.8 runtime constant exports absent; §7.1 coyote prose off by one vs the fixture; T9’s 12 m/s wall approach runway-limited at 8.67 m/s; packet-31’s "19 free-fall steps" snap note vs the measured snap chain). No commit. Owner pre-approval (autonomous M2 build instruction, 2026-09-18); final manual review pending (see 32.md). Next: **Gate H review** (not started) |
| 33 | Immutable behavior builds | I | Gate H accepted | done 2026-09-19 — new `@thirdlight/behavior-build` (Node-side, pure; deps `@thirdlight/project-model@0.1.0` + the already-pinned `esbuild@0.28.2`; lockfile delta = its link/entry + two dependency declarations, +13 lines, two unrelated stale `asset-pipeline` normalizations reverted): strict canonical source-graph container parse + §22.3.3 ordered static analysis (path/order/duplicate/extension/encoding/entry, files/file_bytes/graph_bytes/owned_transforms/imports/import_depth bounds, textual import + dynamic-code scan, POSIX resolution, cycle), the canonical digest-bound `BehaviorManifest`, the §5.5 output scan and `compileBehavior` over in-memory bytes through an esbuild in-memory plugin (`write:false`, `absWorkingDir:"/"`, `format:"esm"`, no execution anywhere — async signature recorded as C33-1). Publication enabled through ONE commit path: `workspace.prepareBehaviorSource` (stage/bytes → trust gate → injected compiler → immutable container blob + derived `prepared.json`, no authoritative change) and `publishBehavior{mode:"source"}` building the record only from those prepared facts (new `PreparedBehaviorSourceFact`, `behaviorPreparerRegistered`, `behavior_declaration_mismatch`); `backend` constructs/injects the compiler and exposes the `publishBehaviorSource` facade on `/services`. `fixtures/m2/behaviors/**` = 2 valid + 31 hostile canonical containers + committed output artifact + `expected.json` + independent plain-Node checker (5/5); `tests/m2-builds/**` 22 unit tests incl. hostile taxonomy, 2-run byte equality, injected compile-failure/timeout/output-bound/output-scan and no-execution sentinels; `tests/integration/m2-builds/**` real-fs publication (6) + 6 fresh-process cold builds under the packet-13 deployed esbuild-external arrangement (6/6 successes, identical digests, 14–17 ms, 0 failures — U-2 not observed; sentinel unexecuted) with transcripts. Toolchain green: `npm test` 123 files/1594 (baseline 119/1562), typecheck 15 pkgs, check-deps, check-boundaries 261 files/959 specifiers/0 violations (4 disposable negative probes failed then removed), build 4 built/0 skipped, packet-18 contracts checker 34/34. M2 behaviour preserved: the declaration path is unchanged; the no-compiler workspace keeps `behavior_publication_unavailable`/`preparer_unavailable` (2 M2 test expectations updated where the placeholder became the real path). Evidence `docs/acceptance/evidence-m2/33/**`. Contract-change requests C33-1…C33-7 (async `compileBehavior`; `absWorkingDir` determinism pin; `behavior_declaration_mismatch` missing from commands.md §5.4; §5.5 scan letters; trust-check order; project-model `parseSourceGraphContainer` absent; declaration-bound limit names). No commit. Owner pre-approval (autonomous M2 build instruction, 2026-09-18); final manual review pending (see 33.md). Next: **34** (not started) |
| 34 | Trusted behavior execution and publication UI | I | 33 | done 2026-09-19 — the approved trusted-main-thread behavior host: `packages/runtime/src/{intents.ts,behavior.ts}` implement runtime.md §14's `BehaviorSpec` lifecycle (prepare→instantiate→step→dispose) for one already-compiled artifact, the validated-intent API (`IntentSet`; runtime-owned cross-module duplicate-writer + per-step cap + the position-axis transform write, instance-owned per-instance caps and the per-instance log ring), the contract fail-stop reasons (`behavior_intent_invalid`/`behavior_transform_forbidden`/`behavior_intent_conflict`/`behavior_intent_limit`/`behavior_step_failed`/`behavior_step_async`/`behavior_state_shared`), the frozen `prepare` object and the create-time `config_invalid` reasons (`behavior_prepare_failed`/`behavior_instantiate_failed`/`behavior_property_invalid`) plus §14.6 `transform_owner_forbidden`/`behavior_ownership_forbidden`; `StepContext` gains the additive `intents`/`emit` fields and `ModuleConfig` the additive `behaviorLog` sink (C34-1/C34-2), diagnostics gain `intentCommitCount`/`logCount`/`logDropped` and `behavior_log` entries in the bounded ring; editor: pure `session/behavior-publication.ts` (normative `BEHAVIOR_TRUST_NOTICE`, `planAcknowledgeTrust`/`planPublishSource`/`planPublishDeclaration`, bounded ≤32 compile diagnostics, staged-edit isolation), `PrefabProjection` keeps the digest-bound `source` and observes `behaviorTrust`, `session/client.ts` stages source through the EXISTING content-stage routes and publishes via the ordinary `publishBehavior` command, and `ui/BehaviorPanel.tsx` renders the trust notice/staging/diagnostics/declaration form (wired in App); `tests/browser/m2-behaviors/behavior-host.test.ts` executes the committed packet-33 compiled example in a bounded `node:vm` context through the real runtime host; `packages/runtime/src/behavior.test.ts` (+25) and `packages/editor/src/session/behavior-publication.test.ts` (+16) cover the lifecycle/intents/ownership/logs/fail-stop/projection rows. `npm test` 126 files/1641 (baseline 123/1594), typecheck 15 pkgs, check-deps, check-boundaries 267 files/986 specifiers/0 violations, build 4 built/0 skipped, contracts checker 34/34 + behavior-build checker OK, browser harness esbuild probe 1 262 091 bytes/53 ms. **No browser**: live-browser execution/UI/pixels UNVERIFIED (`tests/browser/m2-behaviors/README.md` + packet 37); no unbounded-loop test in a browser (documented limitation); the §14.5 controller effective-input wiring needs packet 35 (C34-3) and the editor end-to-end source publish needs a preparation wire route (C34-8). Contract-change requests C34-1…C34-8 (StepContext.intents/emit; ModuleConfig.behaviorLog; §14.5 effective input vs the accepted platformer; `content.behaviorTrust` not exposed by any query; §12.1 inventory row; the unreachable per-instance intent cap; `duplicate_writer` carrying both IDs; the missing prepare route). Evidence `docs/acceptance/evidence-m2/34/**`. No commit. Owner pre-approval (autonomous M2 build instruction, 2026-09-18); final manual review pending (see 34.md). Next: **35** (not started) |
| 35 | Content-aware play delivery and bounded MCP input | I | 34 | done 2026-09-19 — the immutable runtime-content manifest + scoped locator and the bounded MCP input relay are wired end to end, and the production play-preview bundle is composed from runtime/input/platformer/physics-rapier/three+GLTFLoader. Play start builds the §17.1.1 manifest (self-identifying `buildId`, §4.2 option-set digest, sorted assets/behaviors, required engine modules) and publishes one bounded in-memory artifact set (manifest, the prebuilt bundle as `game.js`, one verified read per declared asset path, deterministic recompiles of the pinned behavior outputs verified against the published `outputDigest`); the preview origin serves exactly the §17.2.1 routes with `X-Thirdlight-Digest`/`ETag`/`private, max-age=<remaining>, immutable`/`no-referrer`/the §17.4 CSP, rejecting listings, traversal, undeclared paths and other capabilities (`path_rejected` 400 / `play_locator_invalid` 404), expiring at `PLAY_CONTENT_TTL` (900 s) with the 60 s terminal grace, redacting the capability from errors/logs and never touching the v1 snapshot shape for M1 sets. The M2 bridge is v2 (origin/source/nonce unchanged; `tl.handshake` bridgeVersion/contentId/buildId, `tl.playContent.expect`, `tl.load.progress`, `tl.input.request`/`tl.input.result`, `tl.ready` buildId/contentDigest/stepIndex, `tl.error` phase) with the §17.6 payload bounds; `tl.input.request`/`input.result` WS events carry the bounded relay. `POST …/play/:id/input` (and the real-SDK `tl_input_exercise` tool) runs 1–600 step-indexed semantic frames in exclusive test mode through the real editor WS, returns `appliedFromStep`/`appliedToStep` + `snapshotId`/`buildId`/`inputMode: \"test\"`, and maps limits/timeouts/concurrent relays/unknown plays to structured failures (never a simulated success). Preview: `preview-bootstrap.ts` loads the manifest, verifies the buildId + every declared asset digest, links the behavior outputs and composes runtime + `@thirdlight/input` + platformer + physics-rapier + three/GLTFLoader, reports truthful `tl.load.progress`/`tl.ready` only after manifest+assets+physics+runtime, and holds no token and no `/api/v1` call; `App.tsx` builds the locator iframe src and carries `allow=\"gamepad\"`. The additive packet-35 routes/services: `POST …/content/behaviors/source` (runs the packet-33 facade; no second commit path) and `workspace.readSourceBlob` (verified digest-addressed behavior-container read). Tests `tests/integration/m2-play/**` (real backend child + fs + both origins + WS + real stdio MCP SDK: 13/13 delivery, 1/1 bundle scan) and `packages/backend/src/play-content.test.ts` 10/10; `npm test` 129 files/1672, typecheck 15 pkgs, check-deps, check-boundaries 270 files/1006 specifiers 0 violations, build 4 built/0 skipped, contracts checker 34/34. Measured preview bundle 4 522 584 B: patterns a/c/d/e/f/g/h/j = 0/0/6/0/3/0/38/3 (three core re-derived 3/0/3/26/3; GLTFLoader ×37 +12 `https://`; +1 inert Rapier compat `fetch(`), `three` revision 186, no credential/`/api/v1`/Node leak. **UNVERIFIED: the real browser Play (pinned GLB + behavior, WebGL, ready/progress, course traversal), physical keyboard/gamepad + denied gamepad, tab resume and the rendered PNG** (no browser in-container; packet-35 captured no pixels and substituted no placeholder — packet-37 procedure in `tests/browser/m2-play/README.md`). Contract-change requests C35-1…C35-8 (`readSourceBlob`; manifest behavior row declaration/ownedTransforms/requiredModules; §4.1/§4.2 preview-entry edges + locator dynamic import; behavior-source preparation route; scene `schemaVersion` + `content.settings` absent from the snapshot/bridge; the §5.4.1 preview `fetch(` rule; the Rapier §5.4.1 row; `input.request`/`input.result` absent from §7). Owner pre-approval (autonomous M2 build instruction, 2026-09-18); final manual review pending (see 35.md). Next: **36** (not started) |
| 36 | Standalone M2 content/gameplay export | I | 35 | done 2026-09-19 — the M2 export closure is composed from ONE injected captured state by the shared builder in `exporter` (`buildContentClosure`, also used by the play path — `backend/play-content.ts` is now a thin wrapper: no second manifest/bundle implementation) and published by `exportProjectM2`: manifest.json + `scene.json` (the `sceneDigest` preimage bytes, contract-change request C36-1) + every reachable GLB as `content/sha256/<digest>` (byte-identical to the authoritative blob, GLB-container validated) + every reachable compiled behavior output as `behaviors/<outputDigest>.js` (statically linked into the bundle through esbuild plugins — no dynamic import, no runtime code fetch) + `index.html`/`js/main.js` (the same §5.3 pinned option set; the M2 bootstrap composes runtime/input/platformer/physics-rapier/three+GLTFLoader through the shared `composeExportRuntime`, presents the mandatory §2.3 trust notice before starting, digest-verifies manifest/scene/assets) + `meta.json` v2 (exact versions/licenses/hashes/build identity/outputDigest). Validation: the M2 bundle graph check, the format-aware scans (GLB magic/version/chunk/strict-JSON/`uri` rules, WASM magic/pin, the §5.4 a–j counts with the re-measured three core + GLTFLoader + Rapier compat + the fixed engine trust-notice text, the relative-closure rule), the manifest `buildId` re-derivation, the declared==emitted closure rule, and the revision re-read. Measured (real export of a v2 project with 1 GLB + 1 behavior + a controller course): bundle 4 455 999 B, patterns a–j = 0/0/0/7/0/3/0/38/0/5, GLTFLoader ×37, scanHits 0; three cold builds 351/352/340 ms, 0 failures; two same-input exports byte-identical except `meta.json.exportedAt` (same capture second; `outputDigest`/`buildId` equal); failure isolation hash-verified for a missing blob, a corrupt blob and a hostile (network-import) source refused upstream (`behavior_import_forbidden`); credential/locator-capability/non-GLB negatives rejected; non-root static serving (`/games/<id>@r<rev>/`) with `model/gltf-binary` for the extension-less digest paths and `application/wasm` for `.wasm`; and the fixed 120-step action sequence through the play-locator artifact set vs the export tree with the REAL Rapier adapter gives **max |Δ| = 0** (tolerance 1e-9). Toolchain: `npm test` 133 files/1693 (baseline 129/1672), typecheck 15 pkgs, check-deps, check-boundaries 283 files/1068 specifiers/0 violations, build 4 built/0 skipped, contracts checker 34/34. Lockfile untouched (no new dependency: the shared builder uses existing `project-model`/`protocol` edges; the boundary rows added are bundle-entry overrides). **A P1 defect was found and repaired as a dependency: `project-model/src/sha256.ts` padded `len ≡ 55 (mod 64)` inputs with an extra block, producing wrong digests (it broke the export/play `sceneDigest` agreement) — fixed + NIST/known-answer/`node:crypto` cross-check regression tests.** UNVERIFIED (no browser): the real-browser walkthrough (WebGL rendering, trust-notice gate, WASM/CSP init, keyboard/gamepad, pixels, network panel) — procedure in `docs/acceptance/evidence-m2/36/manifest.md`; no screenshot substituted. Contract-change requests C36-1…C36-7 recorded (scene.json layout gap; manifest owner; outputDigest definition; behaviorTrust read; M2 bundle entry list; §5.4.1 Rapier/notice/scene rows; `capturedAt` digest-input contradiction). Owner pre-approval (autonomous M2 build instruction, 2026-09-18); final manual review pending (see 36.md). Next: **Gate I review** (not started) |
| 37 | Integrated M2 acceptance and deployment documentation | J | Gate I accepted | done 2026-09-19 — **Gate J bounded close-out applied 2026-09-19** (docs/handoffs/gate-j-repair.md; docs/evidence-only, no product/contract/decision change) — integrated M2 acceptance executed against the **real deployed artifacts** (`dist/backend/backend.mjs` sha256 `b3cf2f5a…`, `dist/mcp-adapter/mcp.mjs` sha256 `53f30550…`) on disposable data/export roots + real HTTP/WS/stdio-MCP-SDK: the packet-37 journey passed **18/18** checks (operator migration-copy byte-exact to the accepted fixture with the source byte-identical; M1 v1 edit + unknown-`storageVersion` refusal without rewrite; GLB import/two placements/reimport v2 with stable IDs+transforms/undo→v1/redo→v2/malformed+external-URI+required-extension+decoded-limit rejection with unchanged envelope+revision; prefab capture/two copies/legal override/invalid-capture atomicity/MCP typed edit + `revision_conflict` + WS convergence; behavior declaration→trust→compile→publish with `behavior_import_forbidden` hostile refusal and the old publication retained; Play locator/pinning during a live change/fresh Play adopting the new build/security negatives/MCP `tl_input_exercise` applied steps 100–102 with `play_not_found` for unknown plays/5 start-stop cycles whose stops are each 200 + owner-acked (`play.stopped`, no `stopUnconfirmed`); real `SIGKILL` → structured `stale_ownership` → admin takeover → lost-ack replay `duplicated:true` with the acknowledged entity durable; source tamper+missing detection/derived-cache deletion recoverable/source-backup restore → fresh Play 200; two exports into distinct disposable roots with exact declared==emitted closure and a real two-root tree diff (differing only `manifest.json`/`meta.json` timestamp carriers; non-timestamp bytes identical; `buildId` re-derives), failure isolation hash-verified, independent static server under a non-root prefix with `model/gltf-binary` and backends-stopped serving, 0 external requests; pins `three 0.186.0`/`esbuild 0.28.2`/`typescript 5.9.3`/`vitest 5.0.1`/`ws 8.21.3`/`rapier2d-compat 0.20.0`/`mcp-sdk 1.30.0`). Toolchain green on the repo (`npm test` 135 files/1701 passed; typecheck 15 pkgs; check-deps; check-boundaries 15/283/1068/0; build 4 built/0 skipped; contracts checker 34/34; domain checkers; targeted real-process suites content 25/builds 11/play 14/export 9, runtime 20, physics 61, controller 35, builds 26, crash 6, crash-recovery 3, browser-harness 27) and in a **disposable `npm ci` copy in /tmp** (167 pkgs; full suite 135/1701; typecheck/check-deps/check-boundaries/build 4-built/fixtures 34/34 all exit 0; repo lockfile/node_modules untouched). Deliverables: `docs/acceptance/m2-report.md` (A01–A24 matrix, §3 journey, U-1…U-5 individually, honesty statement, limitations, owner checklist), `docs/acceptance/evidence-m2/37/**` (replayable `journey/run.mjs`, results/JSON/transcripts, `suites/**`, `clean-install/**`, `manifest.md`, `judgement/no-png.txt`), M2 additions to `docs/acceptance/deployment.md` (§8 M2 setup + binding trust acknowledgment + BR-3 secure-context/gamepad topology; §9 source-backup classification; §10 supplied-vs-performed) and `docs/handoffs/37.md`. **A01/A06/A09/A10/A14/A22/A23/A24 PASS at process/Node level; A02/A03/A04/A05/A07/A08/A11/A12/A13/A15/A16/A17/A18/A19/A20/A21 PASS at process/API/Node level with their browser/hardware halves UNVERIFIED (16-row union); no row FAILED; 0 PNGs (no fabricated screenshot/browser/gamepad evidence).** U-1 remains partially resolved (owner browser/keyboard/gamepad checklist); U-2 not reproduced (16 cold builds total, none failed); U-3 unchanged/documented; U-4 CLOSED (both M1 requests accepted, decision 0002 §6.8); U-5 withdrawn/moot unless the owner adopts browser automation. Physics selection stays PROVISIONAL. No new contract-change request; packets 33–36 requests all disposed by Gate I (29 accept-with-diff applied). No source/contract/fixture/lockfile change, no commit. Owner actions to close the UNVERIFIED rows are listed in `m2-report.md` §8. **Gate J review performed 2026-09-19 (`docs/handoffs/gate-j.md`, accept with bounded follow-ups, no P1); bounded close-out applied 2026-09-19 (`docs/handoffs/gate-j-repair.md`). Next: owner walkthrough (`m2-report.md` §8); after that, stop — M3 is a separately requested planning task, only on owner request.** |

## Review gates (M1 historical record)

| Gate | Covers | Status |
|------|--------|--------|
| A | 00–03 | **accepted 2026-09-17** — architectural review: accept with bounded follow-ups (F1–F4, Notes); apply via the "Repair prompt" before packet 04 — **F1–F4 applied 2026-09-17** (handoff 03 "Gate A follow-up repair") |
| B | 04–07 | **accepted 2026-09-18** — architectural review (docs/handoffs/gate-b.md): accept with bounded follow-ups BF-1…BF-5 (consistency fixes: docs + fixture regeneration only, no source) — apply via a separate repair step **before packet 08** with the small docs re-checks noted per item; no blocking findings |
| C | 08–12 | **accepted 2026-09-18** (single-session mode, same session as impl post-compact) — architectural review (docs/handoffs/gate-c.md): ACCEPT with bounded follow-ups CF-1…CF-4 (CF-1 browser visual/WebGL UNVERIFIED — settled by packet 13's browser-verification step; CF-2/CF-3 documented contract-change requests — sessions.md §13.7 optional `engineRoot`, export.md §5.4.1 reference-entry interpretation under pinned esbuild — awaiting owner disposition, implemented fail-closed; CF-4 open playwright pin request from packet 08) — no blockers for packet 13 |
| D | 13 | **accepted 2026-09-18** — Gate D review (docs/handoffs/gate-d.md, fresh session, single pass from tree + contracts + raw evidence): all 14/14 step claims re-derived from the raw outputs and matching (md5/sha256 re-hashes, sizes re-measured, reproducibility re-diffed, scans re-run, MCP-origin events re-read); toolchain fresh green (841/841 ×2, boundaries 10/150/514, build 4 built); contracts untouched (empty diff vs e9e52d7); tree clean; no P1; **1 P2 (FD-1: m1-acceptance.md §1 steps 6/7 expected revision r4→r5 — documentation-only)** ⇒ **ACCEPT with bounded follow-up FD-1; M1 accepted** (stop — no automatic M2). FD-1 applied 2026-09-18 (docs-only repair commit after a1ca75d: m1-acceptance.md steps 6/7 r4→r5 + revision note; tree clean). Owner real-browser observations recorded 2026-09-18 (m1-report v0.2 §1B: export-page demo run CONFIRMED verbatim on the code-identical bundle, no backend; editor attach + Play round trip + two UI edits confirmed at transport level) — U-1 partially resolved; residual pixel items UNVERIFIED |

## Packets

| ID | Packet | Gate | Prerequisite | Status | Handoff |
|----|--------|------|--------------|--------|---------|
| 00 | Repository intake and environment | A | — | done (2026-09-16); Gate A accepted 2026-09-17 | docs/handoffs/00.md |
| 01 | Project data contract | A | 00 | done; review corrections v0.2 (2026-09-17); Gate A accepted 2026-09-17 | docs/handoffs/01.md |
| 02 | Commands, persistence, and conflict contract | A | 01 | done; contracts + fixtures v0.1 (2026-09-17); Gate A accepted 2026-09-17 (follow-up F4 touches commands.md/workspace.md) | docs/handoffs/02.md |
| 03 | Runtime, session, export, and dependency contracts | A | 02 | done; contracts v0.1 + M1 acceptance plan (2026-09-17); Gate A pre-review pass 2026-09-17; Gate A accepted 2026-09-17 (follow-ups F1–F3 touch this packet's documents) | docs/handoffs/03.md |
| 04 | Minimal toolchain and dependency checks | B | Gate A accepted | re-repair review 2026-09-17 (docs/handoffs/04-rereview2.md): **accepted** — packet 04 complete, 141/141 tests, no new P1 findings (2 non-gating P2 observations recorded); **Gate B accepted 2026-09-18** (gate-b.md; N1/N2 accounted: N1 limitation line + N2 carried forward, both in the BF set) | docs/handoffs/04.md |
| 05 | Project model implementation | B | 04 | implemented 2026-09-17; review 2026-09-17 (docs/handoffs/05-review.md): **accepted** — 234/234 (real tree + clean npm ci copy), 113 independent probes pass, no P1 findings (4 non-gating P2 observations recorded); **Gate B accepted 2026-09-18** (gate-b.md; N1 → BF-4, N2/N3 carried forward, N4 → BF-5) | docs/handoffs/05.md |
| 06 | Pure commands and history | B | 05 | review 2026-09-17 (docs/handoffs/06-review.md): changes still required — 1 P1 (pipeline order vs §6.1 step 4), 3 non-gating P2s; repair (round 1) 2026-09-17 `b15786a`; **re-review 2026-09-18 (docs/handoffs/06-rereview.md): accepted** — F1 closed (independent 21-probe round incl. new ordering probes; 386/386 real tree + clean `npm ci` copy; fixtures 71/71); F2–F4 carried forward as recorded P2s (no code); **Gate B accepted 2026-09-18** (gate-b.md; hint-drift contract-change request → BF-1, F2–F4 carried forward) | docs/handoffs/06.md |
| 07 | Durable workspace service | B | 06 | **Follow-up commit review 2026-09-18: changes required before packet 08** — 17 findings (including ownership/data-preservation failures and an ownership-contract blocker); see docs/reviews/2026-09-18-commits.md and docs/handoffs/2026-09-18-commit-review.md. Reopen packet 07/Gate B for repair/re-review; prior acceptance below is historical. implemented 2026-09-18 (`be58767`, handoff `11ae4d3`, test fix `afd1467`): `packages/workspace` on the real FS (full §6.1 pipeline, ownership/takeover/release, durable retry records, atomic W + verification, recovery snapshots, scan/§8.3 completion, external-change pause/resolve); 52 new real tests (fixture-pinned scenarios 01–05/08/09, fault injection, 3 real-SIGKILL crash tests); **review 2026-09-18 (docs/handoffs/07-review.md): accepted** — 438/438 real tree + clean `npm ci` copy, 92 independent probes (exit 0), no P1 findings (5 non-gating P2s recorded); **Gate B accepted 2026-09-18** (gate-b.md; F1 → BF-2/BF-5, F2 → BF-3, F3/F5 carried forward, F4 → BF-5; requests (1)/(7) → BF-2/BF-1) — 2026-09-18 repair campaign in progress: approved contract diff applied to workspace.md (`dabfcff`), source repairs pending per docs/handoffs/07-repair-2026-09-18.md — repair group A1 (R10/R17) applied `7b4645a`; repair group A2 (R11/R12/R13) applied `aa03976`; repair groups B1 (R14) e9ebe6e + B2 (R15) applied `6b04dff`; repair groups B1–B3 (R14/R15/R7) applied (B3 `30d4015`); group C (R1/R2/R6) applied `4f31707`; group D (R3/R16) applied `0550a1d`; group E1 (R9) applied `9f9eff2`; group E2 (R4/R5) applied `6f2e48f`; group E3 (R8 + L1) applied `2f7f1c2` (+ `52f78aa` holder-null residual, spot-check); docs follow-ups (BF) applied `1584236`; commands (O1/O2) applied `69b1a17`; independent packet 07 re-review ACCEPTED (P-rereview1 `d19b3e6`); Gate B re-review round 1 CHANGES REQUIRED (`861f6b3`): G1–G3 + §3 layout line applied `1e9e910`; Gate B re-review round 2 (final) ACCEPTED (GateB-rereview2 `d142324`) — gate re-accepted 2026-09-18 | docs/handoffs/07.md |
| 08 | Runtime and three.js adapter | C | Gate B accepted | implemented 2026-09-18 `83e0250` (BF-1…BF-5 repair step applied before start: `1584236` et al., Gate B re-accepted `d142324`): `@thirdlight/runtime` (snapshot isolation, rAF/manual lifecycle, fixed-step + bounded catch-up 8, demo, module registry, interp, diagnostics ring 32) + `@thirdlight/three-adapter` (scene sync, WebGL render, screenshot, diagnostics; never throws); three@0.186.0 pinned + installed; 629/629 tests (3 runs), check-deps 8→6, check-boundaries + negative probes OK, 5 independent esbuild-bundle probes PASS; **browser visual verification UNVERIFIED** (no root: libnspr4/libnss3 missing; evidence docs/handoffs/evidence-08/); contract-change request (not applied): playwright pin missing from dependencies.md §7 — **independent packet review 2026-09-18 (docs/handoffs/08-review.md, single-session review pass from tree+contracts+fresh probes, round 1 of max 3): ACCEPTED** — 2 P2 findings found + fixed in-round with regression tests (`98f3b1a`: F1 `adapterError` value export beyond the §3 surface row removed; F2 `captureScreenshot(maxWidth)` fail-fast validation + Node-observable test); no P1; m1-acceptance §2.1 packet-08 checks all verified or carried (bundle content scan clean: 0 hits on all 11 patterns in the runtime bundle); 630/630 ×2, tools green; interpretation decisions re-verified (step ordinal / module_error cadence / capped alpha 0 / renderBackend null) | docs/handoffs/08.md
| 09 | Backend API and live session transport | C | 08 | implemented 2026-09-18 (single-session takeover): `@thirdlight/protocol` (pure wire shapes — strict JSON pass-1 parser self-contained (types-only edge), §7 WS catalog validators + frame bounds, §11.2 error codes/status, HTTP payload validators, §13.5 bridge allowlist validators, ID syntax) + `@thirdlight/backend` (HTTP/WS transport + `/services` surface: establish/re-attach/full-state, single-use wsToken TTL, origin allowlist + bad_origin, commands route delegating mutations to workspace `runCommand` (sole executor) + queries to the workspace query path with raw commands.md results (`revision_conflict` carries `currentRevision`), `mutation.applied` any-origin, play lifecycle (active→presented→stopping→stopped; preview_failed/preview_timeout/expired/session_lost), screenshot/diagnostics relay (relayId-routed, `relay_failed` + `cause`, maxWidth 256–2048), admin ops (create/release/takeover/accept/discard), separate-origin preview template + static serving (traversal-contained), bounded logs 128 + frame bounds 64KiB/1.5MiB/1MiB, 10/60s protocol-error tolerance; local ambient decls — no `@types/node`/`@types/ws`; no headless hosting / no server GPU). ws@8.21.3 installed (exact pin; the only new dep). Interpretation decisions 1–8 recorded in handoff. 77 protocol + 69 backend tests; full 59 files / 776 passed (×2); check-deps / check-boundaries (7 pkgs, 115 files, 423 specifiers) / build all exit 0. **Browser visual UNVERIFIED** (no browser in container — same constraint as packet 08; the preview page + bridge wiring is packet 10, the first browser-verified surface).  **independent review 2026-09-18 (docs/handoffs/09-review.md, single-session pass from tree+contracts+fresh probes, round 1 of max 3): ACCEPTED** — no P1; 0 forbidden-pattern hits in the esbuild protocol bundle (purity); strict pass-1 JSON 10/10 fresh edge cases; 16/16 fresh live transport assertions (origin/token single-use+replay 1008/re-attach 1000 detached/command apply+duplicate-retry/revision_conflict+currentRevision/query passthrough/preview template credential-free); fresh play relay chain (play.started full snapshot → present → screenshot relay relayId-routed carrying snapshotId+revision+dataUrl+width+height → stop relay + play.stopped reason request) all PASS; the only round deltas were review-probe field-name/shape errors on my side (backend matched the contract; no source change); 146/146 (protocol+backend), 776 full, tools exit 0; browser visual UNVERIFIED carried to packet 10 (first browser surface) |
| 10 | Minimal visual editor and isolated play | C | 09 | implemented 2026-09-18 (single-session takeover): new `@thirdlight/editor` package — React + @types/react + @types/react-dom installed (exact §7 pins 19.3.0/19.3.0; editor-scoped only, decision 0001 §10). TWO LAYERS: (a) PURE Node-testable logic — `session/projection` (projected browser state: hydrate from full state, `mutation.applied` apply with requestId dedup + stale-resync gap rule, document order, ChangeData create/setTransform/delete/restore), `session/gesture` (local no-traffic preview, exactly ONE undoable commit, ≤1 auto-rebase on `revision_conflict` else surfaced), `session/envelope` (requestId idempotency, duplicate-response detect, lost-ack retry decision), `preview/bridge` (postMessage bridge: exact `event.origin` + trusted-source checks, nonce handshake, allowlisted validation, bounded drop counters, never `targetOrigin:"*"`); (b) BROWSER — `session/client` (WS authoring transport: establish/upgrade/full-resync/mutation.applied/play/command lost-ack retry+conflict), `viewport/viewport` (imperative three.js scene/camera/renderer + orbit + picking + projection sync), `viewport/gizmo` (translate/rotate/scale local preview, one commit), `ui/{App,Hierarchy,Inspector,Toolbar,StatusBar}` (React panels), `preview/preview-bootstrap` (separate-origin preview: runtime+adapter+demo+bridge relays, NO credentials), `index.tsx` entry. esbuild TSX loader build now emits `dist/editor/main.js` + `dist/editor/index.html` (build-injected `window.__thirdlightEditor={v,projectId,previewOrigin,authoringToken}`) + `dist/preview/preview.js`; the preview page carries only `{v,authoringOrigin}` (no credentials). 37 editor tests (projection 8, gesture 7, envelope 11, bridge 11); full 63 files / 813 passed; check-deps / check-boundaries (8 pkgs, 134 files, 470 specifiers) / build all exit 0. **Bundle credential scan: preview has NO Bearer//api/v1//__thirdlightEditor/authoringToken** (the only fetch(/XMLHttpRequest tokens are dormant three.js loader code — the preview's own code makes ZERO backend calls); editor bundle has no Node builtins. **Browser visual/WebGL UNVERIFIED** (no browser in container — packet-08 libnspr4/libnss3/no-root constraint carries forward) with exact manual steps in the handoff. **independent review 2026-09-18 (docs/handoffs/10-review.md, round 1 of max 3, single-session from tree+contracts+fresh probes): ACCEPTED** — no P1/no P2; 15/15 fresh public-API probes (gesture no-traffic+one-commit+≤1-rebase, projection hydrate/dedup/gap-resync, bridge no-`*`-origin+origin/source/nonce drops+allowlist); fresh preview-bundle credential scan 0/0/0/0/0 (Bearer//api/v1///__thirdlightEditor/authoringToken/new WebSocket); fresh editor-bundle Node-builtin scan 0; full 828 (with probes) / 813 committed, check-deps/check-boundaries/build exit 0; the only round deltas were review-probe ID/snapshot shape errors on my side (bridge correctly defers to the strict protocol validator — no editor change); browser visual/WebGL UNVERIFIED carried to packet 13/Gate C | — |
| 11 | MCP adapter for the external harness (stdio; 7 tools → backend /api/v1; real-SDK e2e; stale/no-browser fail) | C | 10 | done | 11.md |
| 12 | Standalone export (`exporter` pkg + admin route; review round 1: ACCEPTED, P2 dep-hygiene fixed) | C | 11 | done 2026-09-18 | 12.md |
| 13 | M1 integrated acceptance and local deployment (14/14 steps PASS at protocol/HTTP/WS/process level on the real deployment bundle; browser-visual UNVERIFIED — report §5 manual steps; deployment config + docs supplied & locally performed (disposable); 3 bounded defects fixed) | D | Gate C accepted | done 2026-09-18 | 13.md |

## Notes

- Owner ruling 2026-09-17: **React 19.3.0** (`react`, `react-dom`,
  `@types/react`, `@types/react-dom` — all registry-verified 2026-09-17) is
  the M1 editor UI framework — panels only; three.js viewport/gizmo/picking
  stays imperative and framework-free. Supersedes decision 0001's "no UI
  framework" proposal (recorded as decision 0001 §10). Implementation-prompts
  pack bumped to v0.1.1 with packets 04/10 clarified. No other stack changes;
  nothing installed yet (first install is packet 04).
- Packet 00 open items carried forward: TypeScript line RESOLVED — owner
  confirmed 5.9.3 (decision 0001 §3/§8). The one-time root action for
  `/etc/thirdlight` is withdrawn (data root moved to user home, 2026-09-16).
- Git (owner decision, 2026-09-16): engine repository is under version
  control with a public GitHub remote; game data root stays out of the repo.
- Owner rulings recorded 2026-09-16: no containerization (Docker/Podman)
  — the harness runs inside a Proxmox LXC and deployment is process-level;
  game project data lives in `/home/dadmin/thirdlight/projects/<project-id>/`
  (user home; first ruling had placed it in `/etc/thirdlight`, withdrawn the
  same day to avoid root permissions) — decision 0001 §6 (revised).
- No implementation has started. No dependencies installed. No services
  deployed. Git: initial commit + public remote created 2026-09-16 (owner
  decision; see handoff 00 revision note).
- Packet 01 defined `docs/contracts/project-model.md` (schemaVersion 1:
  manifest + single scene; Transform/box/camera; Y-up right-handed meters,
  2.5D plane XY with depth Z) plus `fixtures/project-model/` with a
  machine-readable `expected.json` index.
- **Gate A review (2026-09-17, architectural review of the M0 contract
  pack, packets 00–03): verdict — accept with bounded follow-ups.**
  Independently re-verified: both fixture verification suites re-run
  (project-model: 20 index entries / 22 JSON parses / 1 intentional syntax
  rejection; commands `--check`: 71 files byte-identical, digests +
  canonical stability; cross-language SHA-256 recomputation; scenario disk
  invariants), all stack pins against the npm registry, handoff-00
  environment facts and commit provenance, and the handoff-03 pre-review
  fixes (one-shot `play.started`, `clientInfo.kind` pin, session code
  mapping, dependencies edge/export table, scan pattern j, demo-math test
  points, project-model §12.1 declarations). Bounded follow-ups (consistency
  fixes, not new scope; apply via the "Repair prompt" **before packet 04** —
  **applied 2026-09-17**; handoff 03 "Gate A follow-up repair" section;
  both fixture verification suites re-run OK):
  - **F1 — export/preview scan vs pinned `three@0.186.0` (verified, larger
    than the handoff-03 risk note):** a default esbuild 0.28.2 bundle of the
    pinned three contains `fetch(` ×3 (two are real fetch calls in three's
    loader code), `http://` ×3, `https://` ×23, `process.` ×27, and
    `XMLHttpRequest` ×3 (inert strings/comments; `node:`, `__dirname`,
    `WebSocket`, `file://` absent). As written, export.md §5.4 patterns
    d/f/h/j and dependencies.md §5 check 4 (packet 10, not only 12) fail on
    the first preview/export build. Repair: a recorded-exception table in
    export.md §5.4 bound to the pinned version/hash, restate §5.3's
    single-fetch rule as "exactly one fetch initiated by engine code
    (bootstrap `./snapshot.json`)", apply the same table in dependencies.md
    §5 check 4, pin the exact esbuild option set for all three bundles
    (verified: default `format` is IIFE even for ESM entries — export.md
    §5.3/§7 must state it), and extend the handoff-03 risk note to patterns
    d/f/j and to packet 10.
  - **F2 — m1-acceptance §2.1 demo-math test points:** stepIndexes 0/120/240/
    360 do not yield the stated exact values under the runtime.md §7.1
    formula (`(stepIndex+1)` offset: x(120)=x0+0.499957, x(240)=x0−0.00654).
    Exact points: 119 (x0+A), 239 (x0), 359 (x0−A), 479 (x0). One-line fix
    in m1-acceptance.md; runtime.md unchanged.
  - **F3 — sessions.md §5.1 attach `workspace` shape:** the clean shape
    `{writePaused:false, pendingChange:null}` contradicts commands.md §5.6's
    clean shape `{writePaused:false}` and the committed scenario fixtures;
    the "exactly the commands.md §5.6 object" claim is false. Align
    sessions.md §5.1 with commands.md §5.6 + fixtures (drop
    `pendingChange: null`).
  - **F4 — `project_unavailable.reason` scope:** the workspace.md §4.3 load
    pipeline can surface project-model codes (`encoding_invalid`,
    `json_parse_error`, `duplicate_key`, `field_type`,
    `manifest_scene_mismatch`) as `reason`; the "(a workspace.md §11 code)"
    parenthetical (commands.md §5.4, sessions.md §11.3) is too narrow —
    one-sentence clarification.
  Later improvements (non-gating): no byte-exact fixtures for session/
  bridge/runtime-snapshot payloads (check map covers; small fixture set may
  be added in packet 09); handoffs 01/02 lack final commit IDs (9d1241a,
  21dd09e); `history_empty` cls `unavailable` with "retry later" guidance
  misfits a non-transient condition (commands.md §5.4/§5.5); charter
  first-release items (snapping, lights/shadows, material presets) have no
  explicit post-M1 milestone assignment — carry into the M2 planning prompt.
  **Evidence limits (docs-only gate):** no implementation exists; the
  acceptance record establishes contract self-consistency (modulo F1–F4),
  fixture self-consistency, stack-pin registry truth, environment facts, and
  the F1 scan measurement (throwaway /tmp install of the pinned
  esbuild/three). Durability under real crash/power loss, `/proc` liveness,
  runtime/browser behavior, and the M1 scenario remain unestablished (as
  honestly recorded in the handoffs). No reviewer approval is claimed beyond
  this record.