# Thirdlight — Status

Updated 2026-09-22. Goal and scope: `docs/architecture/charter.md`.
Defect list: `docs/audit-2026-09-22.md` (D-numbers below).

Rule: a phase item is **done** only when it works in a real browser, has an
automated test at the boundary it fixes (Playwright for UI), and the owner
has looked at it where it is visual/audible.

## Phases

| Phase | Goal | Fixes | Status |
|---|---|---|---|
| 0 | Commit the work, archive the packet process, reset docs | — | done |
| 1 | The M1 authoring loop works in a real browser | D1, D4–D8, D14, undo UI | done 2026-09-22 — `npm run test:e2e` covers open/create/undo/redo/gizmo/reload/restart/MCP/token; owner look pending |
| 2 | One current schema; every feature reachable through the product | D2, D3, D9, D22–D26 | done 2026-09-22 — new projects are v3 with starter lights; Play/export work for plain scenes (scene mode) and via the backend route; e2e covers Play pixels and export served statically with the backend stopped. Loading Beacon Reach through the backend moved to phase 4; removing v1/v2 code paths is D21 debt |
| 3 | Charter first-release editor features | D15, D16, D27 | done 2026-09-22 — rename/reparent (`updateEntity`), typed transforms, shortcuts, per-axis gizmo + click-pick, Problems tab (backend problems log), external-edit detection with load/keep banner, isolated asset preview, runtime-faithful viewport (hierarchy, box size/color). Not done: multi-select, duplicate, sibling reorder |
| 4 | Beacon Reach is honestly playable in editor and export | D10–D13, D20, D28–D30 | done 2026-09-22 — templates via `POST /admin/projects {template}`; visible animated courier; goal cue; checkpoint glow; fixed GLB; Play works for games in the editor; scripts run in the game (Play + export); e2e plays it to the goal through the relays and drives it by keyboard. Owner played it to the goal in the editor through the reverse proxy on 2026-09-23 |
| 5 | MCP parity with charter §7 | D18 | done 2026-09-22 — `tl_inspect target=selection` (editor selection reported over WS), `tl_diagnostics` without a play returns the problems log (import/compile failures now recorded), `tl_play_start` accepts an explicit `sessionId`, MCP-started Play presents in the editor and an MCP stop tears it down; `dist/mcp-adapter/mcp.mjs` starts without an env flag. `tests/e2e/mcp.e2e.ts` drives the real stdio server against a live editor |
| 6 | Real M4: templates, independent project, one-command start, backup | D17, D19 | done 2026-09-22 — one owner token (`THIRDLIGHT_OWNER_TOKEN`); project picker + new-project-from-template screen; D17 (modules derived from the game block, referenced content, behavior requirements and template declarations; unresolved ⇒ `module_unresolved` at creation/Play/export, the game host refuses a module it cannot provide); `npm start` (`tools/start.mjs`); `tools/backup.mjs` create/verify/restore; an independent game with an engine pin (version + commit + lockfile digest), then `tools/game.mjs`, since replaced by folder projects and `tools/project.mjs` (phase 10); `docs/deployment.md` matches these. e2e: `projects`, `start`, `backup`. Runs as a systemd service behind the owner's reverse proxy (`deploy/thirdlight.service`); owner created a project from the template and played it, 2026-09-23 |
| 7 | Editor layout like Unity: docks, menu bar, tiles | — | done 2026-09-23 — hierarchy left, Scene/Game centre, inspector right, bottom dock with draggable remembered splitters; a menu bar (File: new/open project, Export game… with zip download, project settings, reload; Edit: undo/redo/duplicate/delete/snapping; GameObject: empty/box/camera/lights/spawn/zones/model/prefab; Component: add/remove collider, controller, spawn, camera follow, light, zone; Window; Help: shortcuts/about); one camera and one light per type enforced in the menu; Assets/Prefabs/Behaviors as tile grids. e2e: `layout`, `menus`. Owner look pending |
| 7b | Editor look: theme, logo, favicon, icons | — | done 2026-09-23 — token palette with separated surfaces and an amber brand accent; SVG mark + favicon; viewport placeholders are icon billboards (camera frustum, light direction, spawn, empty), pickable; 13 icons generated with Qwen-Image (`packages/editor/public/icons`, alpha fringe cleaned by `tools/png-alpha-clean.py`) used in the viewport, hierarchy, tiles and toolbar. e2e: `placeholders`, `layout` checks icons load. Owner look pending |
| 8 | Debt: one schema version, split `backend.ts`, drop hand-written Node types and `createTestBackend` from production code, drop unused deps | D21 | mostly done 2026-09-23 — test backend in a test-only subpath, date-named tests renamed; `backend.ts` split into route modules (2,624 → ~1,200 lines); v1/v2 product paths removed (M1/M2 Play/export, M2 preview bundle, migrate route; archived under `archive/removed-v1-v2/`); `@types/node`/`@types/ws` replace the ambient stubs. Remaining: the v1/v2 model code in workspace/commands/project-model/runtime, still loadable but unreachable from the product; removing it means regenerating the `fixtures/commands` contract corpus in v3 (owner decision pending) |
| 9 | Towards a real platformer (owner list, 2026-09-23; to be specified one item at a time): multiple scenes/levels; real art assets + animations from Blender; a full gameplay loop; save system; skybox editor; material editor incl. global wind, post-processing, fog volumes; light baking; an input editor window; better scene placeholders for lights/cameras/spawns with transparent icons from the Qwen-Image service (`~/projects/qwenimage`, workstation 10.0.10.145) | — | not started |
| 10 | Projects in the game's own repo: a registry in the data root maps project id → folder; a `thirdlight.json` marker at the game root; picker/admin routes to open or create a project in a server folder and to unregister; the MCP adapter finds the project from its working folder (one global MCP entry); `tools/game.mjs` folded into the same concept; Sprout moved into `~/projects/sprout` | — | done 2026-09-23 (owner look pending; service restarted on this build) — registry + marker, picker create/open/remove, admin routes, MCP finds the project from its folder, `tools/project.mjs` (replaces `game.mjs`), backup/restore `--folder`, engine pin warnings; e2e `folder-projects`. Sprout now lives in `~/projects/sprout` (registered; old data-root copy moved to `~/thirdlight/removed/`); one user-scope MCP entry. Assets referenced in place (option B, 2026-09-23): optional `sourcePath` on asset versions, verified reads, "from project folder" picker + MCP `projectPath`, changed/missing files in Problems with re-import, Play/export copy the bytes; e2e `folder-assets`. Backups of a folder project copy the whole game folder (owner decision 2026-09-23). Importer widened (owner go-ahead 2026-09-23): WebP/material/transform/quantization extensions, meshopt/Draco/KTX2 compression, and FBX converted to GLB by headless Blender; e2e `import-extensions`, `import-fbx` |
| 11 | Play without the owner's browser: with no editor connected, the backend or MCP adapter runs its own headless Chromium session so `tl_play_start`, `tl_screenshot`, `tl_input_exercise` and `tl_game_observe` work; a connected owner browser still takes priority | — | not started (planned only) |
| 12 | Foundations before phase 9 (owner, 2026-09-23; do before 9 and 11), in this order: (a) hierarchy: folder objects (organisation only, no transform, children keep world positions), collapsible tree, sibling reorder and drop-into-folder by drag, multi-select; folders pass down visible/locked/tags/static to their subtree, stored on the folder in the scene (no side file); (b) tags: a project tag registry of up to 32 named tags with fixed bit indices, a 32-bit mask per object (effective mask = own OR ancestor folders), queryable by scripts; (c) additive scenes: several scenes per project, one file each, loaded and unloaded at runtime (start set + script API + a scene-exit zone), shared assets/prefabs/scripts, project-unique entity ids, editor shows several open scenes with one active; designed so a scene can later be loaded at an offset (streaming, generated dungeons). Existing v3 projects (Sprout) migrate | — | planned (owner decisions 2026-09-23) |
| — | Found in the owner's first real run | D32 | fixed 2026-09-23 — digests fall back to pure SHA-256 outside secure contexts; the editor no longer hashes uploads (the backend's digest is used). `tests/e2e/insecure-context.e2e.ts` plays without WebCrypto and runs an export served on the LAN address |
| — | Debt, only when touching the files anyway | D21 | ongoing |

## Notes

- Tests: `npm test` (unit/integration), `npm run build && npm run test:e2e`
  (Playwright, real backend + Chromium).
- Run it: `npm start` (see `docs/deployment.md`). One owner token
  (`~/thirdlight/owner-token`) for the browser, MCP and admin routes.
- The editor shows a banner when scene files change on disk (polled every
  1.5 s while an editor is connected); `/projects/:id/external/(accept|discard)`
  resolves it with the project token. `/projects/:id/problems` is the bounded
  problems log (also pushed as `problems.added`).
- Dead-owner reclaim is automatic now; the admin `takeover` route remains for
  the rare case where the reclaim fails.

- Deployment is process-level inside the Proxmox LXC; no containers (owner
  ruling, `docs/environment.md`). Project data: `/home/dadmin/thirdlight/projects/`.
- No existing project data needs to be preserved (owner, 2026-09-22).
