# Phase 18 — Material node graph

Goal: materials can be authored as node graphs in a centre tab (phase 16
framework), compiled to TSL (phase 17), rendering the same in the Scene view,
Play and exports on WebGPU and WebGL2. The shader-type materials of 9.4
stay and become built-in graph templates. (Planned earlier as phase 13;
moved after the renderer phase by the owner, 2026-09-24.) Read
`docs/roadmap.md` (principles) first.

## 1. Decisions

- A material is either a shader-type material (9.4) or a graph material;
  "Convert to graph" turns a shader-type material into an equivalent graph.
- Graphs compile to TSL in the browser (editor and runtime) from data; no
  generated code is stored; the exported game carries the graph data.

## 2. Work items

### 18.0 Data

- `content.materials[]` gains `graph?: { nodes, edges, groups, comments,
  outputs }` (a material with a graph ignores `shader`/`params`), exposed
  parameters (`params` with type, default, range, visibility public/private
  like 15.4) that objects and per-object material mappings may override,
  texture references as asset refs. Validation: node kinds from the
  catalogue, port types, no cycles, bounded size (≤ 512 nodes), every
  output connected or defaulted. Commands: node/edge/group edits as
  commands with undo (the framework's command set, generic over graph
  kinds).

### 18.1 Node catalogue (generic, not demo-shaped)

- Inputs: constants (float, vec2–4, colour), exposed parameters, time,
  UV0/UV1, vertex colour, position/normal (object, world, view), view
  direction, camera distance, screen UV, instance index/ID, global wind.
- Maths: add/sub/mul/div, dot/cross, normalize, length, lerp, clamp,
  saturate, smoothstep, step, min/max, abs, floor/fract, sin/cos, pow,
  remap, split/combine, swizzle, one-minus.
- Textures: sample 2D (with UV and sampler settings), normal map, triplanar,
  flipbook; noise (value, gradient, Voronoi), gradients.
- Utility: fresnel, rim, posterize, dither, world-aligned UV, parallax
  (simple), vertex displacement (vertex stage output), alpha clip.
- Outputs: PBR (base colour, metalness, roughness, normal, emissive, AO,
  opacity/alpha clip), unlit, vertex offset; flags (double-sided,
  transparent, shadow casting).
- Sub-graphs (reusable node groups stored as their own content records).

### 18.2 Editor

- The Material tab: the graph, a live preview sphere/plane/model pane with
  the environment, exposed-parameter list; the Inspector shows the selected
  node; the Materials tab lists materials and opens tabs; errors per node.
- Built-in templates generated from the 9.4 shader types (standard,
  foliage wind, world-aligned kit, unlit, water) — a converted material
  looks the same as before (pixel test).

### 18.3 Runtime and export

- The material library compiles graphs to TSL node materials with a cache
  keyed by the graph's canonical digest; the export carries graphs in the
  manifest; per-object overrides of exposed parameters.

### 18.4 Tests and wrap-up

- Unit: compile each node kind; graph validation. e2e: build a graph
  (texture × tint + fresnel emissive), assign it, see it in the Scene view,
  Play and the export (pixel checks); convert a shader-type material and
  compare pixels. Docs and STATUS row 18 (and row 13 points here).

## 3. Progress

| Item | Status | Commits |
|---|---|---|
| 18.0 data | done 2026-09-25 | 936074f |
| 18.1 node catalogue | done 2026-09-25 | 936074f |
| 18.2 editor | done 2026-09-25 | 35cd2fb |
| 18.3 runtime and export | todo | |
| 18.4 tests and wrap-up | todo | |

## 4. Decision log

- 2026-09-25 (18.0): data — `content.materials[]` gains optional `graph` (a generic `GraphData`: nodes, edges, groups, comments; graph kind `material`) and `parameters` (the exposed parameters), both appended after the 9.4 fields in the canonical form, so every shader material keeps its exact bytes. The plan's "`outputs`" are the output nodes of the graph (PBR / Unlit / Vertex offset) with the render flags as their fields — not a separate key (one place for them, edited like any node). Why: the generic graph format stays the one every graph kind uses.
- 2026-09-25 (18.0): the exposed parameters are `material.parameters` (`{key, type: float|vec2|vec3|vec4|color|texture, default, min?, max?, visibility?, label?, group?, tooltip?}`, ≤ 64, list order kept), not `params`: `params` stays the shader's overrides, which the renderer reads and which a graph material keeps as its fallback. Visibility follows 15.4 (public omitted from the canonical form). Parameters without a graph are allowed (inert) so a graph can be removed and brought back.
- 2026-09-25 (18.0): a graph material keeps `shader`/`params`/`textures`. Until the graph compiler (18.3) the runtime closure strips `graph` and `parameters` (`materialsForRuntime`, like `animatorsForRuntime`) and the editor's Scene-view material library gets the shader part only, so the renderer draws the shader fallback (and a graph edit never rebuilds viewport materials); 18.3 carries graphs into the manifest. No renderer code was touched (17.1 is in flight).
- 2026-09-25 (18.0): commands — owner kind `material` in `GRAPH_OWNERS` (owner id = materialId; a material without a graph has none — `setMaterial` adds one): `graphEdit` changes are recorded as the ops (a move in a 512-node graph stays a small record) and undone with the inverse ops; the editor's client advances `materials[i].graph` from them (full resync when they do not fit). The adapter also applies the material rule (a Parameter node names a declared parameter) so a refusal names it. New graph materials, conversions and parameter edits are one `setMaterial` each.
- 2026-09-25 (18.0): validation — catalogue types/fields, port types (a value never feeds a texture input), no cycles, ≤ 512 nodes, at most one surface output (`exclusive` tag) and one vertex offset, Parameter nodes name declared parameters, texture fields and texture parameters name texture assets (content-level, generic over any node field with `asset: 'texture'`). "Every output connected or defaulted" holds by construction: every non-texture input of every material node has a `default` (a constant or a built-in source: uv0/uv1, positions, normals, view directions, screen UV, time — checked by the catalogue test); a texture input falls back to its node's texture field. Missing a surface output is a diagnostic (the PBR output is `required`; the Unlit output satisfies it through the tag), not a refusal, so a graph can be rebuilt step by step.
- 2026-09-25 (18.0): per-object overrides = a new component `materialParams` `{ <materialId>: { <key>: value } }` next to the `materials` mapping (scene entities and prefabs, requires a model/box/instances), not a changed mapping value: every mapping consumer (three-adapter, viewport, bake, preview) reads mapping values as material ids and must not change during 17.x. The project rule (only public parameters of graph materials, values fitting the declaration) runs in `composeV4`, i.e. after every command in the workspace; the command checks the shape. The Inspector's Materials section lists the public parameters of the graph materials the object uses (own mapping or its model's default mapping). Descriptors: the component, `parameters` and `graph` (json, read-only: edited with graph edits).
- 2026-09-25 (18.1): catalogue — two graph kinds as data in `project-model/src/material-graph-kinds.ts`: `material` (owner `material`) and `material-function`; port types float/vec2/vec3/vec4/texture; every value width converts to every other implicitly (splat, truncate, pad 0 / w = 1 — Shader Graph's rules), texture never. Maths nodes carry a `type` field (`auto` default | a width). Node ids: the plan's list, plus Colour ramp beside Gradient (the plan's "gradients": a UV gradient shape and a two-colour ramp); "instance index/ID" is one Instance index node. Why: generic, any genre; the TSL compile of each node is 18.3.
- 2026-09-25 (18.1): framework — data-dependent ports, generic and data-only, in project-model `graph.ts` and the editor's copy (`editor/src/graph/model.ts`, parity in `tests/material-graph-parity.test.ts`): `GraphPortDef.typeFrom {field, lookup? | map? | byLength?}` (a type from node data: an external declaration through the validation context such as a material parameter, a value map, a string's length such as a swizzle mask, or `auto` = the widest wire in by the field's option order), `GraphNodeDef.portsFrom {field, kind}` (a sub-graph call's ports = the interface of the standalone graph the field names), `GraphKindDef.interface` (which nodes are a callable graph's inputs/outputs; port id = the interface node's id so renaming keeps wires; ordered by position), `GraphNodeDef.exclusive`, field type `color`, string `pattern`, string `asset`, port `default`, and a `GraphContext {graph(kind, id), lookup(name, value)}` passed to validation and to the editor (`GraphEditor`/`GraphInspector` prop `portContext`). Every geometry/connection helper takes an optional `PortsOf`. This is the 16.1-deferred "ports that depend on node data".
- 2026-09-25 (18.1): sub-graphs are standalone graph documents of kind `material-function` in `content.graphs[]` (16.1's generic records: `setGraph`/`deleteGraph`/`graphEdit {owner: {kind: "graph"}}`, the Graphs list, the `graph` document tab), not a new `content.materialFunctions[]`. Why: no second record type, op set or tab for what is a graph of a kind; visual-script functions (19) can do the same. Rules: calls between graph documents may not form a cycle (checked across `content.graphs`); a function edit or delete that breaks a caller (a wired port gone, a call to a deleted function) is refused naming the caller (the graph adapter re-validates the callers; the resulting-state check would refuse it too). Functions are not exported yet (18.3 with the graphs).
- 2026-09-25 (18.2 prep, logged here): the Material tab = name bar, a note (17.4 has not landed: "Preview arrives with the WebGPU renderer (phase 17.4) and the graph compiler (phase 18.3)…"), the exposed-parameter list on the left, the GraphEditor; the right dock shows GraphInspector for the selection. "Convert to graph" (editor, pure, `editor/src/session/material-graph.ts`) rebuilds standard and unlit materials (colour × map, roughness/metalness × ORM g/b, AO = lerp(1, ORM r, intensity), normal map × scale, emissive × intensity × map, tiling/offset UV, opacity × map alpha when not opaque, alpha clip, double-sided/transparent flags — as three.js's standard material does; unit-tested to validate in the backend); on a model a shader material also inherits the file's own textures, which a graph does not contain. Foliage/kit/water are disabled with the reason: they become 18.2's built-in templates with pixel tests. Pixel equivalence is unverified until 18.3 renders graphs.
- 2026-09-25 (run): 18.0/18.1 (data and catalogue) were built and merged while phase 17.2–17.5 were still in progress; they touch no renderer code (graph materials render with their shader fallback until 18.3).
- 2026-09-25 (18.3): compiler — `three-adapter/src/material-graph.ts` compiles a graph (and the material functions it calls, inlined per call) from data to TSL: a PBR output fills a `MeshStandardNodeMaterial` (`colorNode` = vec4(base colour, 1), `metalnessNode`, `roughnessNode`, `normalNode` = `TBNViewMatrix × tangent normal` — what three's normal map does after decoding —, `emissiveNode`, `aoNode`, `opacityNode`, `alphaTestNode`), Unlit a `MeshBasicNodeMaterial`, Vertex offset the `positionNode` (world space through `modelWorldMatrixInverse`). Unwired normal/emissive/AO/opacity/alpha clip leave three's defaults (null nodes), so an empty PBR output is three's standard material. Nodes feeding the Vertex offset compile for the vertex stage (positions, normals and the wind from the undisplaced vertex, `modelWorldMatrix × positionLocal` as the 17.2 foliage port does); pixel-only inputs (screen UV, tangent view direction, Dither) there give a warning and a stand-in. Why: one data-driven path for editor and runtime, no stored code (§1).
- 2026-09-25 (18.3): the compiler keeps its own copy of the catalogue's ports/types/defaults (`COMPILER_NODES`, `COMPILER_FIELD_DEFAULTS`) and of the port-type rules (`resolveMaterialGraphPorts`: auto widths, swizzle masks, parameter types, function interfaces), because three-adapter may not import project-model; `tests/material-graph-compile-parity.test.ts` checks both kinds' every node against the catalogue and the resolution against `resolveGraphPorts`.
- 2026-09-25 (18.3): node semantics where the catalogue left room — Fresnel `(1 − saturate(|n̂·v̂|))^power` (abs: back faces of double-sided surfaces too); Rim `smoothstep(1 − width, 1 − width + max(softness, 1e-4), 1 − |n̂·v̂|)`; Noise gradient = MaterialX Perlin `mx_noise_float` × 0.5 + 0.5, value = smoothed cell noise, Voronoi = F1 distance over the 3 × 3 cells with the nearest cell's random value as `cell`; Gradient angular = atan2 / 2π + 0.5; Colour ramp constant = step at 0.5; Dither = ordered Bayer (fract-recursion form) threshold + half a step, `step(threshold, in)`; Triplanar blend weights |n|^sharpness normalized; Flipbook frames left→right, top→bottom (UV v = 0 is the image's top row, flipY off); Parallax `uv + v.xy / (v.z + 0.42) × height × scale` (the common simple form); World-aligned UV = the plane's coordinates × scale; a sampling node without a texture reads white (a warning); `normalize` of a float is its sign, `length` its absolute value (WGSL has no scalar normalize).
- 2026-09-25 (18.3): catalogue additions (generic): **Object position** (the object's origin in world space; in an instance set the instance's own, through 17.2's `instanceOrigin`) — per-piece effects need it (the kit shader's world-X UV per piece is not expressible with the surface position); **Vertex colour** gains `absent: white | zero` (default white = a neutral tint; zero with alpha 1 = COLOR_0 as data, where a mesh without it must not move — the 9.4 foliage convention). The Global wind node's strength includes the gusts travelling across the world at the surface's world X (the 9.4/17.2 foliage formula), direction is the normalized ground direction ((1, 0, 0) when zero).
- 2026-09-25 (18.3): cache and overrides — the library compiles one shared node material per canonical digest (FNV-1a of the canonical text of the graph's node types, data and wires — not positions, groups, comments or reroutes — the parameters and every reached function with its interface order; the full text is compared on a hit, so a digest collision cannot hand out a wrong material). Materials with identical compile inputs share one; unused digests are dropped after a material change. Exposed parameters are uniforms; a public one reads the drawn object's override per object (`onObjectUpdate`, the object group is per render object on both backends) from `mesh.userData.__tlMaterialParams[digest]`, which `apply(root, mapping, overrides)` sets from the `materialParams` component — the shared material never changes (9.4 rule; checked in pixels: one material object, two colours). A texture override cannot be a per-object uniform, so it compiles a variant (its parameters' defaults replaced → another digest). Known edge: two different material ids with byte-identical graphs on one mesh share the per-object values (they are the same material).
- 2026-09-25 (18.3): "Casts shadows" off is per mesh while it wears the material (`castShadow` false; the object's own flag is kept under `__tlMaterialNoShadow` and restored, and the adapter's shadow flags respect it) — three has no per-material shadow-cast switch. The graph's emissive is added to the material's own `emissive` (black by default) so the selection tint and the checkpoint glow (17.2, per-mesh copies) still show on graph materials. Textures: asset ids resolved through the host's loader (the content closure's bytes in Play/export), prepared per sampler (colour space, wrap, nearest keeps no mipmaps; linear keeps the texture's own filtering); one still loading compiles as its fallback and the graphs that waited recompile when it arrives.
- 2026-09-25 (18.3): export — `materialsForRuntime` now keeps a graph material's graph and parameters (graphs as `graphForRuntime`: nodes with positions — a function's ports are ordered by them — and wires, no groups/comments/reroutes/collapsed); the manifest gains the optional key `materialFunctions` (after `materials`; the functions the graphs reach, transitively; validated as graph documents of kind material-function, materials validated against them); the buildId key lists in the exporter, the export bootstrap and the Play preview gained it. The capture's asset refs gained graph texture fields, texture parameter defaults, called functions' textures and objects' texture overrides, so every texture a graph uses travels with the game. The export scan passes (the compiler has no URL, fetch or `node:` text).
- 2026-09-25 (18.3): tests — unit: every node kind compiles in both stages without a GPU, slots/flags, problems, auto typing, function inlining, the per-object uniform, digests (positions ignored), the library's sharing/variants/shadow flag/texture recompile (`three-adapter/src/material-graph.test.ts`), the compile-parity test, the runtime view and function closure (`project-model/src/material-graph.test.ts`). e2e (both projects): `material-graph-render.e2e.ts` — every kind compiles and draws on WebGL 2 and WebGPU (no shader error), and pixel values: unlit constant exact (255, 128, 0), nearest texels, parameter default vs object override on one shared material, fresnel rim (centre 19, edge 116), world vertex offset; `material-graph-play.e2e.ts` — texture × tint (through a material function) + fresnel emissive on two boxes, one overriding the tint, seen in the Scene view, Play and the static export (green/blue/magenta pixel counts; manifest carries the graph and the function).
- 2026-09-25 (18.2): templates — "Convert to graph" (editor, pure, `session/material-graph.ts`) now builds all five shader types and `templateMaterial(shader)` makes a new graph material from a type's defaults (the Materials tab's template menu next to "+ new graph material"). Each template repeats its 17.2 TSL shader term by term with catalogue nodes: foliage = the wind as a world-space Vertex offset (Vertex colour with `absent: zero`, Global wind, R² × bend, sway/flutter sines, the 0.35 sag) + the translucency added to the emissive, double-sided; kit = colour/normal/ORM UVs + (Object position X ÷ UV period, 0), AO and emission on the unshifted UV, the macro normal on UV1 (clamped, as the shader's clone) whiteout-blended then × normal scale; water = normal map on UV × wave scale + flow × Time, base colour = lerp(shallow, colour, saturate(fresnel(view-space geometry normal, view direction) × 1.5)), transparent. Wind, kit and water values become public exposed parameters (a key the material already declares stays a constant). Nodes are laid out in columns by their distance to the output.
- 2026-09-25 (18.2): unset shader values convert to what the renderer draws with on a box, not the schema defaults the 18.2-prep converter used: roughness 1, emissive intensity 1, normal scale 1 (three's `MeshStandardNodeMaterial` defaults, which the shader path keeps for unset params); water keeps its own 0.1 roughness / 0.8 opacity. Why: "a converted material looks the same as before" — the Inspector's displayed defaults (0.8 roughness, intensity 0) differ from what a box draws (a pre-existing inconsistency, not changed here; changing the renderer would move every project's look). On a model the shader also started from the file's values, which a graph cannot contain (documented).
- 2026-09-25 (18.2): pixel test of the templates = the 17.2 shader-parity harness with `graph=1` (every material of the five cases converted by the editor's own converter, compiled by 18.3): measured against the frozen references with the 17.2 rule — WebGL 2: standard/foliage/kit mean 0.00, unlit 0.71, water 0.76 (the same as the shader-type path's known sRGB/blend difference); WebGPU: standard/kit 0.00, foliage 0.00 (0.02 % pixels > 32 on blade edges), unlit 0.71, water 0.76.
- 2026-09-25 (18.2): preview — `editor/src/viewport/material-preview.ts`: its own renderer from the factory (the editor's backend choice), its own material library (the same compile), the project environment through `createEnvironmentRenderer` (key light direction fixed; the project wind drives wind graphs), an orbit camera; shapes sphere/plane/cube or a model of the project (a new `ModelInstance` from the editor's prepared resources, disposed on change). The model shape is the first model asset unless one is picked. The canvas reports `data-tl-renderer` and a frame count for tests.
- 2026-09-25 (18.2): problems — the Material tab's graph shows the compiler's problems as node badges (GraphEditor `extraProblems`, computed without loading anything: `materialGraphProblems` with the project's texture ids), and the Problems tab lists every graph material's rule and compile problems (`materialId` on the issue; a click opens the tab and focuses the node — the focus survives the tab switch). Not done: problems of material functions compiled inside a material are reported on the call node ("in the function: …"), not inside the function's own tab.
- 2026-09-25 (18.2/18.4 tests): `material-preview.e2e.ts` (both projects: preview on sphere, plane, cube and a model; a red colour sky behind it; a graph edit shows; the template menu, Convert for foliage/kit/water, a compile warning on its node and in Problems, click-through), `shader-parity.e2e.ts` graph cases (both projects), `material-graph-parity.test.ts` (every shader type converts and validates).
- 2026-09-25 (18.2): full suite found `inspector.e2e.ts` sending an edit before the editor had applied the previous one (a revision conflict, 3 of 4 runs with the heavier editor bundle under load); the test now waits for the editor's revision before the next edit (the 17.3 environment-test fix), what it checks is unchanged. `perf-harness.e2e.ts` (21.1) fails on main too: it still asks for the archived `legacy` renderer (17.4) — not touched here.
