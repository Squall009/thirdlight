# Handoff 18 — Trusted behavior execution and compilation contract

Outcome: packet 18 is **PROPOSED — pending Gate E** material only. No accepted
contract, decision, package, tool or lockfile changed; no install, no commit
(HEAD `5b746ee`). Owner disposition for the trusted-main-thread boundary,
recorded verbatim: **owner pre-approval (autonomous M2 build instruction,
2026-09-18); final manual review pending** — a pre-approval, not an independent
review. `behaviors.md` §2 states normatively that behavior code runs trusted in
the preview/game main thread with **no hard runtime timeout and no hostile-code
sandbox**; §2.4 records that a rejection means a separate execution-boundary
design packet, not a packet-18 workaround.

Scoped diff (no commit): new `m2-contracts/behaviors.md`,
`diffs/{dependencies,export}.md`, `fixtures/m2/contracts/behaviors/**` (5 fixtures,
16 real source containers, containers.json, one labelled stand-in output),
`evidence-m2/18/**`, `handoffs/18.md`; appended `diffs/runtime.md` R19–R30,
`diffs/project-model.md` P18-A1–A9, `constructed-cases.md` §C11,
`verification.md` §§3b/4b; extended `expected.json` and `check-fixtures.mjs`
(`p18-*`); `STATUS.md` packet-18 row only.

Commands (actual): checker `27/27, 0 problems, exit 0` (was 21);
`python3 …/18/independent-recompute.py` OK (second implementation); negative
control (6 corrupted values) exit 1 across all six p18 groups + `code-registry`;
`npm test` 841/841 (66 files); `typecheck` exit 0; `check-deps` OK;
`check-boundaries` OK (10/150/514); `build` exit 0 (4 built). `git diff --stat HEAD` for `docs/contracts docs/decisions package.json
package-lock.json packages tools` is empty; decision 0002 sha256 `2d6aa6c9…`,
lockfile `a71f4c0b…`.

Acceptance: PASS as proposed/pinned/fixture-replayed — no server-side
evaluation/build hooks; mandatory stage → validate/compile → digest-bound prepare
→ `runCommand`; source publication advances the revision and a failed compile
cannot replace a good publication; publication unavailable until packet 33 (hard
`unavailable`, no `args.source` write path), distinct from a later build failure
that preserves the last good artifact; play/export share the pinned pipeline;
compiler bounds separate from runtime trust; acknowledgment gate; all required
failure classes replayed. UNVERIFIED: nothing executed — no compiler, host,
command, bundle or UI exists; no timing or browser claim.

Limitations: all claims are contract text plus replayed fixtures; the example
output is an opaque stand-in; the compile timeout is cooperative and every bound
is a proposal; `behavior_build_failed`/`preparation_missing` are specified, not
observed; worker isolation is out of scope.

Contract-change requests (not applied): C18-1 content-storage/assets
(`behavior-source` profile, `prepareBehaviorSource`, derived cache, trust); C18-2 commands (`acknowledgeBehaviorTrust`, `preparation_missing`, trust
step); C18-3 platformer (module row, `ctx.intents`, effective-input rule — §7 text
unchanged, so packet 17's 178 rows stay exact with an empty `IntentSet`); C18-4
properties §5.2/§15; C18-5 export §5/§6; C18-6 dependencies (two units, no new
pin); C18-7 runtime §7.3/§10; C18-8 packet-15 envelopes gain `"behaviorTrust"`.

Next packet: **19** — not started.
