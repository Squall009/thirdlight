# Packet 17 — Stateful runtime, input and 2.5D physics contract

2026-09-18 (UTC) · packet 17, **Gate E** (prereq: packet 14 evidence, 15–16, met).
Contract drafting only: no code, no commit, no install. Owner line in every
draft: *owner pre-approval (autonomous M2 build instruction, 2026-09-18); final
manual review pending.*

**selection per decision 0002 §1, owner pre-approval** — `@dimforge/rapier2d-compat`
exactly 0.20.0, kinematic character controller (parentless collider loop,
`enableSnapToGround`), pin only at packet 31.

## Outcome

**PROPOSED fixed-step gameplay contract delivered; nothing accepted.** Step-indexed
`ActionFrame` (phase enum, press latch, dead-zone rescaling, digital/stick arbitration,
focus/text-field/hidden-tab/disconnect clearing, `awaitingRelease` fresh activation,
exact same-engine replay); injected input/physics ports with the fixed `sample → intent →
controller → physics → transform → render` order and a phase-scoped write guard; one
transform owner per entity; 2.5D XY-only convention (Z/rotation/scale never written);
exact controller order, coyote (6) / buffer (8) / release (0.5) windows, 12-step settle
pre-roll; fail-stop to `failed` with no rollback; static −Z camera; the six-key settings
registry filling packet 16's placeholder.

## Scoped diff (no commit)

New: `docs/planning/m2-contracts/{input,physics,platformer}.md`, `diffs/runtime.md`
(R1–R18), `fixtures/m2/contracts/{input,physics,platformer,runtime}/**`,
`evidence-m2/17/**`, this handoff. Modified: `diffs/project-model.md` (§"Packet 17
additions"; packets 15/16 preserved), `check-fixtures.mjs` (`p17-*`), `expected.json`
(+5 entries, +18 codes), `verification.md`, `constructed-cases.md` §C10, STATUS row.
Unchanged: `docs/contracts`, `docs/decisions`, packages, lockfile (`98bf340a…`).

## Commands run (actual)

- checker → `check OK: 21 check group(s) passed, 0 problem(s)` (p17: numerics 34
  constants/8 slope rows/10 settings/20 validation; traces 16/178 rows/48 derived;
  input 15+5; catchup 7+8; failures 21).
- negative control (5 corrupted values in a copy) → `check FAILED, 7 problem(s)`, exit 1.
- `python3 independent-recompute.py --verify` → OK on all five fixtures.
- `npm test` 841/841 (66 files); `typecheck`, `check-deps`, `check-boundaries`
  (10/150/514), `build` (4 built) all exit 0.

## Acceptance criteria

ActionFrame/ports/phases/ownership/fail-stop, packet-14-derived tolerances, table-driven
numerics (incl. seam and snap), neutral/fresh-resume + replay tolerances + no phantom
steps, safe-restart-only failure rule, camera, ownership model: **PASS (contract +
replayed fixtures)**.
Unverified: real Rapier/browser/gamepad evidence, seams/ramps under the real adapter,
desktop CPU.

## Limitations

Traces replay a scripted analytic port (flat ground, axis-aligned statics, authored
seam/snap geometry), not Rapier; accel/decel, release factor and windows are
contract-selected (labeled); slope rows are normal arithmetic, not engine data.

## Contract-change requests

(1) Packet 16: `properties.md` §9/§12's empty settings registry is superseded by the six
keys (P17-A11, at promotion). (2) Packet 19: `dependencies.md` §3/§4.1/§4.2/§5/§6 rows for
`input`, `platformer`, `physics-rapier` (runtime diff R18).

## Next

**Packet 18 — Trusted behavior execution and compilation contract.** Not started.
