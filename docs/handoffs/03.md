# Packet 03 Handoff — Runtime, Session, Export, and Dependency Contracts

Date: 2026-09-17 · Gate A · Status: **complete, awaiting Gate A review
(ready for review, not approved)**

Housekeeping (before packet work): the uncommitted 2026-09-17 owner ruling
(React as the M1 editor UI framework) was reviewed — the diff covered
exactly the three stated files (`docs/decisions/0001-stack-and-deployment.md`,
`docs/planning/implementation-prompts.md` v0.1.1, `docs/STATUS.md`) and
nothing else. It was committed **alone** as `df69e3c` ("Decision 0001
revision: React as M1 editor UI framework (owner ruling 2026-09-17)").
Nothing was pushed.

## Outcome

The M1 architecture contract pack is finished (packets 00–03 → Gate A).
Four new normative contracts + the M1 acceptance plan, all version 0.1
pending Gate A, consistent with project-model v0.2 / commands v0.1 /
workspace v0.1 as-is (**no earlier contract was modified — and none
required changes**):

- **`docs/contracts/runtime.md` v0.1** — the runtime core contract: the
  **runtime snapshot** as the only input (`snapshotId = <projectId>@r<rev>`,
  normalized scene, re-validated and deep-frozen at `instantiateRuntime`);
  the `instantiated → running ⇄ stopped → disposed` lifecycle with exact
  transitions and error codes; the **separate mutable simulation state**
  (deep copy + `prev`/`curr` step pairs, `simTime = stepIndex / fixedStepHz`,
  determinism rules); **transform ownership** (authoring transform =
  backend, runtime transform = runtime state, render transform =
  read-only derived interpolation — no write-back, single owner per
  transform); **fixed 120 Hz steps with bounded catch-up**
  (`MAX_CATCHUP_STEPS = 8`, drop-and-resync anchor rule, module
  step isolation with rollback); the **interpolation policy** (component
  lerp + sign-aligned slerp on normalized *derived* quaternions, per
  project-model §10.1); the **built-in moving-box demonstration** with
  exact deterministic math (`x0 + 0.5·sin(2π·(n+1)/(120·4))`, all boxes,
  camera static); **structured diagnostics** (bounded 32-entry error ring,
  stable codes, adapter block reporting the selected WebGL 2 backend); and
  the normative non-goals — **no user scripts** (execution boundary
  deferred), no input, no physics, no graphs. Runtime imports
  `project-model` only: no filesystem, backend, editor, or MCP dependency.
- **`docs/contracts/sessions.md` v0.1** — the browser/backend session
  contract: authoring origin `O_A` vs **separate preview origin `O_P`**
  (two backend listeners; the preview listener serves only the static
  preview page — **no API, no credentials, ever**); exact ID/token syntaxes
  (`sess-`/`conn-`/`play-` + 32 hex, single-use 60 s `wsToken`, `relay-`);
  project-scoped bearer + exact-origin allowlist policy shapes (packet 09
  implements); the session lifecycle (establish/re-attach with **full
  state**, one active authoring session per project, `wsToken`-authed WS,
  heartbeats); command transport over HTTP (commands.md envelopes
  unchanged) + `mutation.applied` for browser/AI convergence; the
  **exhaustive WS event catalog**; the **gap rule + full-replacement
  resync** and **reconnect after dropped ack** (HTTP acks ⇒ commands.md
  replay semantics; WS re-attach ⇒ full state); **gesture preview/commit**
  (zero server traffic mid-gesture, one `setTransform` on release,
  bounded conflict recovery that explains — never silently loses — a
  failed edit); **play sessions** (start requires the registered browser
  owner, frozen snapshot revision, `active → presented → stopping →
  stopped` machine, **play/screenshot/diagnostics routed to the explicitly
  selected browser; no browser ⇒ structured `session_unavailable`**,
  screenshot responses always carry `snapshotId` + `revision`); **bounded
  logs/errors** (128-entry session log ring, stable codes, payload/timeout
  constants); and the **checked message bridge**: exact-origin/source
  validation (never `"*"`), the handshake sequence, and the exhaustive
  `tl.*` allowlist, plus dev/deploy origin configuration.
- **`docs/contracts/export.md` v0.1** — the standalone export: input is one
  immutable snapshot read through the workspace service (admin operation,
  not MCP in M1); the static output tree
  (`index.html`, `js/main.js`, `snapshot.json`, `meta.json`) with
  **relative paths only**; the bundle is the **same runtime as play mode**
  with an exact import graph (metafile-verified, no editor/server/MCP/node
  modules), a single relative `fetch(./snapshot.json)`, and a **10-pattern
  forbidden-content scan** (no authoring URLs, no `/api/v1/`, no Node
  imports, no MCP endpoints, no second network mechanism, no credentials —
  no silent exceptions);
  `meta.json` records engine + dependency versions; the 6-code validation
  pipeline with no-partial-artifact semantics; and the **reproducibility
  scope stated exactly** (byte-identical `snapshot.json`/bundle, `meta.json`
  equal except `exportedAt`). One small supported scene — not a general
  asset build system.
- **`docs/contracts/dependencies.md` v0.1** — the module/dependency
  contract: the 10 units (create **only when implemented**), each unit's
  public `exports` surface, the **allowed import-edge tables** in both
  planes (node-side per package + the exact editor/preview/export bundle
  graphs), the forbidden edges (`runtime → three`, `mcp-adapter → backend`
  except `/services`, `editor → workspace`, no hidden globals, no duplicate
  mutation path — `workspace.runCommand` is the sole executor), the
  **enforceable boundary checks** (static import-graph check, negative
  fixture proof, esbuild metafile bundle checks, content scan, strict
  typecheck, exact-pin `check-deps`), the **narrow module registration
  mechanism** (compile-time simulation-module registry; M1 contents =
  exactly `thirdlight.demo:box-motion`; deliberately unable to load
  untrusted code), and the **recorded M1 stack per decision 0001 §3
  including the 2026-09-17 ruling** (React 19.3.0 + types, `editor` only;
  esbuild TSX loader; **React is the only permitted UI framework, scoped to
  editor panels; `three-adapter`/`runtime` framework-free; no web framework
  anywhere**).
- **`docs/planning/m1-acceptance.md` v0.1** — the **exact M1 acceptance
  scenario** (14-step integrated loop on a disposable project: create →
  author → gizmo gesture with zero mid-gesture traffic → undo/redo →
  backend restart survives → isolated play with displayed snapshot revision
  → stop/repeat disposal → MCP edit converges in the browser → stale edit
  rejected → screenshot with revision + no-browser structured
  unavailable → export verified against an independent static server with
  the backend stopped, reproducibility diff → external-change +
  disconnect/resync failure modes), the **requirement → future-check map**
  linking every normative requirement of all six contracts to the packet,
  method, and evidence that proves it, and the **explicit not-M1 list**
  (input simulation, physics, graphs, user scripts, play write-back,
  prefabs/assets, rename/reorder, collaboration, WebGPU, headless
  rendering, general asset builds, backups/mobile/multiplayer/RPG).

## Commands run and results

Documentation-only packet (no implementation exists yet, per the packet
boundary):

- `git status` / `git diff` — reviewed the uncommitted ruling diff (3
  files, all React-ruling content; nothing else).
- `git add docs/STATUS.md docs/decisions/0001-stack-and-deployment.md
  docs/planning/implementation-prompts.md` + `git commit` → `df69e3c`
  (3 files, +70/−14). `git status` clean after.
- Read set: `docs/contracts/{project-model,commands,workspace}.md`,
  `docs/decisions/0001-stack-and-deployment.md` (post-ruling),
  `docs/planning/implementation-prompts.md` (v0.1.1, packet 03),
  `docs/architecture/charter.md`, `docs/environment.md`, `docs/handoffs/02.md`.
- Cross-consistency pass over the five new documents (reason codes,
  snapshot source in the bridge, bundle-graph rows, cross-references) —
  corrections applied pre-commit.

No build/test commands apply: no packages, toolchain, or implementation
exist yet (first install is packet 04, after Gate A acceptance).

## Acceptance criteria (packet 03)

| Criterion | Result |
|---|---|
| Runtime: instantiate/start/stop/dispose; immutable snapshot input; separate mutable state; transform ownership; fixed steps with bounded catch-up; interpolation policy; structured diagnostics; no fs/backend/editor/MCP dependency; built-in demo; scripts deferred | passed (runtime.md §2–§10) |
| Sessions: exact request/event shapes; connection/session IDs; authoring/play revisions; snapshot resync; gesture preview/commit; reconnect after dropped ack; bounded logs/errors; play/screenshot routing to selected browser with structured no-browser result; no credentials in preview; separate-origin checked bridge + handshake + allowlist; authenticated authoring access; dev/deploy origin config | passed (sessions.md §2–§13) |
| Export: same runtime as play; immutable snapshot + dependency versions; static relative files; no authoring URLs/credentials/MCP/Node imports/service dependency; validation failures; reproducibility scope; one small supported scene | passed (export.md §2–§8) |
| Dependencies: public exports + allowed edges for the 10 units; create-only-when-implemented; enforceable boundary checks; narrow module registration; recorded M1 stack incl. the 2026-09-17 React ruling (editor-only React, framework-free three-adapter/runtime, no web framework) | passed (dependencies.md §2–§7) |
| Exact M1 acceptance scenario linking each requirement to a future check; later tools (input simulation, physics, graphs) documented as not-M1 | passed (m1-acceptance §1–§3) |
| Changes to earlier contracts proposed separately | not needed — none proposed |
| Handoff 03 finished; Gate A marked ready for review, **not approved**; stop | done (this document + STATUS) |

**Unverified (honest bounds):** no implementation exists (packets 04–13),
so every check in m1-acceptance §2 is a future check, not a result. No
browser was involved in this packet (none needed — documentation only).
Nothing here claims runtime behavior.

## Limitations

- All five documents are v0.1 pending Gate A; the §"Change rules" sections
  bind future packets after acceptance.
- sessions.md fixes *shapes and constants*; token issuance UX, the exact
  LAN topology/ports, and TLS/proxy details are packet 09/13 facts
  (decision 0001 §7/§8 open items).
- The M1 constants (120 Hz, catch-up 8, 128-record/128-log rings, the
  timeout table in sessions.md §11.5, demo A=0.5 m/T=4.0 s) are recorded
  choices, reviewable at Gate A.
- The `?play=<playSessionId>` preview-URL parameter is documented as
  non-credential-bearing (the preview origin has no API); Gate A may
  request a different preview-session binding.

## Contract-change requests

**None.** project-model v0.2, commands v0.1, and workspace v0.1 required no
modification and blocked nothing; no proposed diffs.

## Changed files (this packet's scoped commit)

- new: `docs/contracts/runtime.md`, `docs/contracts/sessions.md`,
  `docs/contracts/export.md`, `docs/contracts/dependencies.md`,
  `docs/planning/m1-acceptance.md`, `docs/handoffs/03.md`
- modified: `docs/STATUS.md` (packet 03 row + Gate A row "ready for
  review" + the one-line "Updated" freshness fix reflecting the same
  change)
- (separate housekeeping commit `df69e3c`: the 2026-09-17 React ruling —
  not part of this packet's commit)

## Gate A pre-review findings (2026-09-17) — reviewed and fixed

A full cross-document review of the packet 03 pack (all six contracts +
m1-acceptance, checked against the packets 01–02 contracts, decision 0001,
the charter, and the environment report) found and fixed, before Gate A:

- **sessions.md** — §2 cross-refs off by one (injected config is §13.2;
  bridge rules span §13.3–§13.5); `clientInfo.kind` now pinned to exactly
  `"browser"` (only M1 session kind; MCP/admin never establish sessions);
  the attach `workspace` field is now exactly the commands.md §5.6 query
  object (`pendingChange: null` when clean; `pauseReason` + `pendingChange`
  when paused); new **one-shot `play.started` delivery rule** (§7.1: held in
  the owner session's pending queue across a temporary WS drop, dropped if
  the play stops first) with §13.4 step 1 requiring the editor to hold the
  retained snapshot before the handshake (15 s present timeout bounds it)
  — closes a path that otherwise forced `preview_timeout`; §12 step 2 +
  §11.3 now map "play exists but not yet `presented`" to
  `session_unavailable` (unknown/stopped ⇒ `play_not_found`); §13.3 defines
  the editor's bridge `targetOrigin` as the origin part of `playBase` from
  the play-start result (configured `previewOrigin`, no trailing slash);
  §10.1/§10.2/§12 §13.x refs corrected.
- **dependencies.md** — §3 `runtime` public surface now lists
  `registerSimulationModule` (used normatively in §6's sample but missing
  from the export table); §4.1 `backend` allowed edges now include
  `project-model` (types only — the snapshot document, sessions.md §10.1),
  matching the existing note and boundary check 1.
- **export.md** — §4 states the bundle build's pipeline position
  (between steps 1 and 2; defects surface at 4–5); §5.4 gains pattern j
  (`XMLHttpRequest`/`WebSocket` substrings) so the scan fully covers the
  §5.3 "no other network mechanism" rule (10 patterns; m1-acceptance
  §2.2/§2.3 updated); dynamic `import()` enforcement (metafile graph check
  + pattern h) is stated.
- **m1-acceptance.md** — §2.1 demo-math test points were garbled ("143/4",
  "288/4"); corrected to stepIndexes 0, 120 (quarter period, x = x0 + A),
  240 (half period, x = x0), 360 (x = x0 − A) per runtime.md §7.1
  (SIM_HZ·T = 480 steps per period).
- **Contract diff (earlier document, recorded per AGENTS.md):**
  **project-model.md v0.2** — §12.1 now declares `serializeCanonical(doc) →
  Uint8Array` (the §12.2 canonical byte form), `ERROR_CODES` (§12.6), and
  `KNOWN_VERSIONS` (`[1]`), with a revision note in the header. Rationale:
  dependencies.md §3 names all three as the package's public surface citing
  §12.1, but §12.1 never declared them — packet 05 would have had an
  underspecified API and boundary check 1 no rule for them. No behavior
  change; the declaration matches §12.2/§12.6 as already written.

**Known risk flagged for Gate A (not changed):** export.md §5.4 pattern h
(absolute URL literals) is deliberately strict — if a pinned dependency
(e.g. `three@0.186.0`) carries URL literals in comments, the export fails
and the §5.4 recorded-exception path applies (reviewed contract change with
a documented exception; no silent weakening). Packet 12 should expect this.

All documents remain v0.x **pending Gate A acceptance** — this review is a
pre-acceptance fix pass, not an approval. Nothing here claims Gate A
acceptance.

## Next packet (not started)

**Gate A architectural review** of packets 00–03 (bring: charter, all six
contracts, m1-acceptance.md, handoffs 00–03, environment report). Packet
04 (minimal toolchain and dependency checks) starts only after Gate A
acceptance is recorded in docs/STATUS.md. Not started here.