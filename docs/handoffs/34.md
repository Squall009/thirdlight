# Packet 34 — Trusted behavior execution and publication UI

Outcome: a published behavior's declared numeric property measurably changes
play state in the Node test host. Owner pre-approval (autonomous M2 build
instruction, 2026-09-18); final manual review pending. No commit.

## Scoped diff (no commit; the tree already carried uncommitted packets 20–33)

New: `packages/runtime/src/{intents.ts,behavior.ts,behavior.test.ts}`;
`packages/editor/src/session/behavior-publication.ts`(+test),
`packages/editor/src/ui/BehaviorPanel.tsx`;
`tests/browser/m2-behaviors/{behavior-host.test.ts,m2-behaviors.browser.ts,README.md}`;
`docs/acceptance/evidence-m2/34/**`.
Modified: `packages/runtime/src/{runtime.ts,types.ts,index.ts}`,
`packages/editor/src/session/{client.ts,prefab-projection.ts}`,
`packages/editor/src/ui/App.tsx`, `packages/editor/src/editor.css`,
`docs/STATUS.md` (packet-34 row only).

## Commands and actual results

`npm test` 126 files / 1641 passed (baseline 123/1594) · `typecheck` 15 pkgs
exit 0 · `check-deps` exit 0 · `check-boundaries` 267 files/986 specifiers,
0 violations · `npm run build` 4 built/0 skipped · contracts checker 34/34 ·
behavior-build checker 5/5 · `tests/browser/m2-behaviors` 9/9 ·
`packages/runtime` 106/106 · `packages/editor` 143/143. Browser harness esbuild
probe 1 262 091 bytes/53 ms.

**Measured (real compiled artifact, `node:vm`):** `speed 3.5 → x=0.149`,
`speed 6.5 → x=0.279` over the same 40 steps (52 intents incl. pre-roll);
`outputDigest` and both snapshots byte-identical before/after. Fail-stop entry
`module_error`/`behavior_step_failed`, one entry, message ≤256. Log flood
`logCount 208 / logDropped 312 / ring 32`. Two writers ⇒ `duplicate_writer`;
invalid intent ⇒ `behavior_intent_invalid`; new value ⇒ fresh instance only.

## Acceptance

Passed (Node): property→measured behavior after fresh instantiation; source
snapshot/digests unchanged; exceptions fail-stop with bounded diagnostics;
invalid intents/ownership/duplicates rejected; log flood bounded; staged edits
don't touch the play pin or published revision; trust notice text explicit.
**Unverified (live browser):** real ESM blob-URL artifact load, WebGL/pixel
measurement, `BehaviorPanel` render, browser WebCrypto, end-to-end source
publish (no preparation wire route). Procedure:
`tests/browser/m2-behaviors/README.md` (packet 37). No unbounded-loop test
exists; the no-preemption limitation stays documented.

## Contract-change requests (C34-1…C34-8)

`StepContext.intents`/`emit`; `ModuleConfig.behaviorLog`; §14.5 effective input
needs the accepted platformer (packet 35); `content.behaviorTrust` exposes no
query; §12.1 inventory row says behavior phases `["intent"]`/no owners; the
per-instance intent cap is structurally unreachable; `duplicate_writer` should
carry both IDs; no source-preparation route. Diffs in the evidence manifest.

## Next

Packet 35 (Content-aware play delivery and bounded MCP input) — not started.
