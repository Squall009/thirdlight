# Packet 07 — repair campaign (2026-09-18 owner review R1–R17 + O1/O2)

**Authority:** `docs/reviews/2026-09-18-commits.md` (owner-requested review of
the packet 07 commit set — findings R1–R17 + O1/O2) and its *Suggested
orchestration work order* (step 1: record a contract decision for R9's
exclusivity mechanism and R1/R3's unknown-I/O / evidence-failure states;
step 2: the repair groups).
**State:** packet 07 / Gate B reopened for repair/re-review (work-order step
1). The 2026-09-18 acceptance recorded in `docs/handoffs/07.md` is historical.
This file accumulates the campaign's repair sections; source-repair sections
follow as the work-order step-2 groups land.

## 0. Contract application (2026-09-18)

**What was applied.** The accepted contract diff from
`docs/handoffs/2026-09-18-contract-request.md` (sections 4 + 7.4 — 19
Before/After hunks + 3 insertions), applied byte-for-byte from the request
file to `docs/contracts/workspace.md` only (no source, test, fixture, or
other contract changed):

- **4.1** → §6.2 open-time evaluation rows `absent` / `released` /
  own-record (claim-file gate; self-reclaim norm; non-ENOENT read-failure
  clause)
- **4.2** → §6.2 liveness block: new "Liveness and the exclusion gate" bullet
- **4.3** → §6.3 full replacement: epoch-scoped `O_CREAT|O_EXCL` claim file
  (`.thirdlight/claim-<e>`), 6-step claim, pre-record verification (N1),
  orphan-recovery rule, superseded-epoch cleanup, single-winner norm,
  residual-state clause (N2)
- **4.4** → §6.4 `takeoverWorkspace` procedure step 3 (gate + cleanup)
- **4.5** → §6.4 residual split-brain bullet (O_EXCL bound; bypassing-actor
  path)
- **4.6** → §6.5 lifecycle (claim-file lifecycle; "a file, not an fd")
- **4.7a** → §9 step 1 effect list (release unlinks the owner's own claim
  file; no partial release; crash residue `released@e + claim-e`)
- **4.7b** → §9 step 2 "holds no claim file" wording
- **4.8** → §11: stuck-state note; `ownership_conflict` row reworded; new
  `claim_inconsistent` row inserted immediately after it; `holder` shape
  line gains the `or null` clause
- **7.4(a)** → §5.5 G1.3 (durable snapshot = precondition of every
  destructive resolution; unknown-never-absent; ENOENT exclusion)
- **7.4(b)** → §7.2 steps 1–2 (non-ENOENT read failure ⇒
  `paused-unreadable`; byte-binding; `paused-snapshot-failed`) + step-4
  `snapshotState` shape
- **7.4(c)** → §7.2 invalid-bytes paragraph (evidence precondition binds both
  resolutions)
- **7.4(d)** → §7.3 accept/discard preconditions gain `snapshotState: "ok"`;
  new refusal bullet after the `no_pending_change` bullet (re-read-then-
  resolve rule incl. ENOENT unblock)
- **7.4(e)** → §7.4 pruning order + pending-change exemption (R16 contract
  side)
- **7.4(f)** → §11 ops-table accept/discard failure-code cells + two new
  error rows `external_change_unreadable` / `external_change_evidence_missing`
  inserted after the `external_change_invalid` row

**Approval chain** (as recorded; nothing re-approved here): request
`af4881c` (R9, §6 exclusivity) + `893a2fd` (R1/R3 state addendum) →
independent review, changes required + repair round 1 (a) `8ed35ad`
(`docs/handoffs/2026-09-18-contract-review.md` F1–F3) → repair round 1
(section 7) `1dc2129` (F4–F7) → rereview accepted with bounded follow-ups
N1/N2 `5f7ec28` (`docs/handoffs/2026-09-18-contract-rereview1.md`) → N1/N2
applied `1211149`.

**Application method.** A script extracted the 23 (§4) and 18 (§7.4) fenced
blocks from the request, verified all 19 Before blocks + insertion anchors
unique-exact against `workspace.md` at `1211149`, and applied the 22 steps in
work-order sequence with a per-step safety net (the next hunk's target must
still occur exactly once after each replacement). The first run's 3
insertions were missing their trailing line separators (detected by
line-level inspection before any commit); the file was restored from a
pre-application backup, the applier fixed, and the clean run is what is
committed.

**Verification (real outputs; contract is docs-only).**

- **(a)** GONE — 0 matches each: `concurrent claimers converge`,
  `without any lock file or unlink`, `holds no lock`,
  `oldest pruned to keep 16`, `on re-read mismatch re-evaluate (a concurrent
  claimer won)`. KEPT — `lockEpoch` ×14, `state: "released"` ×3,
  `stale_ownership` ×9, `takeoverWorkspace` ×4. NEW — present:
  `claim_inconsistent` ×9, `external_change_unreadable` ×7,
  `external_change_evidence_missing` ×7, `Pre-record verification` ×1 (§6.3
  step-3 heading), `superseded-epoch cleanup` ×8, `paused-unreadable` ×5,
  `paused-snapshot-failed` ×4. The three new error codes appear exactly once
  as §11 rows (lines 935/939/940); the repeated occurrences are normative
  cross-references inside the accepted After texts.
- **(b)** python: all 19 After blocks + 3 insertion blocks present exactly
  once in the applied file (extracted from the request, 3-/4-backtick aware).
- **(c)** §6.2 table: 5 rows before → 5 after (absent, released, own-record,
  owned+live, owned+dead), 3 cells each (backtick-aware count — the new rows
  contain a literal `|` inside inline code `O_CREAT|O_EXCL`). §11 error-code
  table: 12 → 15 rows, 2 cells each, no malformed rows; new rows placed
  immediately after `ownership_conflict` / `external_change_invalid`.
  Ops table: 5 rows × 4 cells; accept/discard failure cells enumerate the new
  codes.
- **(d)** `grep -n 'lock'`: every non-`lockEpoch` occurrence verdict **ok** —
  "retry block" ×5 (unrelated), "blocked/block/unblocked" (project write-
  pause mechanism, incl. new text at lines 454/582/736), and one
  "session-lifetime lock fd" at line 611 inside the accepted 4.6 After
  (rejected-alternative contrast). No `flock`, no capital-`Lock`, no
  "lock file", no "holds no lock". No stale reference found outside hunk
  scope; the §3 gaps below are recorded for re-review, not rewritten.
- **(e)** `npm test`: `Test Files  27 passed (27)` / `Tests  438 passed
  (438)` / `Duration 21.29s` — suite stays 438/438 (no test pins the changed
  contract text).
- **(f)** `node fixtures/commands/tools/generate-fixtures.mjs --check`:
  `check OK: 71 files byte-identical, digests + canonical stability
  verified`, exit 0.

**Residual notes (for re-review; nothing rewritten — outside the accepted
diff's scope).**

1. §3 layout block does not list the new `.thirdlight/claim-<e>` artifact
   (the accepted diff leaves §3 untouched; the location is normative in
   §6.3). Documentation gap, not a contradiction.
2. §3's snapshot note "at most 16 kept, oldest pruned" predates the §7.4(e)
   exemption nuance — still true (≤ 16 kept; pruning removes only
   non-exempt, oldest-first), flagged for re-review.
3. The new §6.2 rows contain a literal `|` inside inline code
   `O_CREAT|O_EXCL` exactly as in the accepted After text; some Markdown
   renderers may display those rows with an extra column. Content is
   normative as written.

**Limitations.** The contract is now ahead of the implementation: the source
still exhibits R1–R17 (rename-based claim without an exclusive gate, zero-
byte "snapshot" on unreadable bytes, snapshot-less discard, 16-oldest
pruning, …) until the work-order step-2 repair groups land against the
public `WriteOps` seam. The request's §5/§7.5 mandatory tests must be added
and pass before Gate B can be re-accepted. No source behavior was changed by
this step.

**Commit.** `dabfcff` — "Apply accepted 2026-09-18 contract diff:
workspace.md §6 O_EXCL claim-file exclusivity + §5.5/§7 evidence-failure
states + §9/§11 rows (docs only)" (266 insertions, 41 deletions,
`docs/contracts/workspace.md` only).