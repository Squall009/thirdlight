# Packet 23 — Workspace content publication and migration-copy workflow

Owner pre-approval: **owner pre-approval (autonomous M2 build instruction,
2026-09-18); final manual review pending.** No commit made.

## Outcome

The real filesystem now performs the accepted M2 content storage: `storageVersion
2` envelope load/write (single atomic envelope; `scene.revision` the only
revision), immutable `sources/sha256/<digest>` publication via the exact §5.1
`W` (write-once, verify, never overwrite), the §7.6 staging area including the
BR-4 harness-edit path, derived-cache paths, verified `readBlob`,
`contentIntegrity` (missing/corrupt/tampered/`referenced`/orphan), quota and
device-space pre-flight, `captureContentView`, v2 command execution (content +
manifest wired as the three-block validation inputs; commit-time blob
verification; dedup before any revision/stage lookup), and operator
`migrateProjectCopy` (new identity, reset-to-zero, marker + phased resume,
source byte-identical). Behavior `mode:"source"` stays refused structurally.
All repaired M1 ownership/external-change behavior is preserved.

## Scoped diff (no commit)

New: `packages/workspace/src/{content-store,migration}.ts`,
`packages/workspace/tests/{m2-storage,m2-migration}.test.ts`,
`tests/crash/m2-child.ts`, `tests/m2-crash.test.ts`, `fixtures/m2/storage/**`,
`docs/acceptance/evidence-m2/23/**`. Modified:
`packages/workspace/src/{envelope,errors,session,service,types,index,isolate,node-ambient.d}.ts`,
`packages/workspace/tests/envelope.test.ts` (the M1 `storageVersion 2`
fixture now enters the v2 branch — `envelope_invalid`/`field_missing`; the
fixture bytes are unchanged), and the consumer ambient `.d.ts` files
(`backend`, `exporter`, `mcp-adapter`) that mirror the workspace `node:fs`
surface for typecheck. No accepted contract, M1 fixture, lockfile or
`node_modules` change.

## Commands and actual results

`npm test` 72 files/**975 passed** · `typecheck`/`check-deps`/`build` exit 0 ·
`check-boundaries` 10/168/629 exit 0 · `check-fixtures.mjs` 33/33 exit 0.
Packet tests: storage 27, migration 4, real-SIGKILL crash 6 (all passed).

## Acceptance

Passed: SIGKILL before/after blob publication and before/after envelope
replacement; fsync/ENOSPC/full-disk/device-space/quota failures; second owner;
symlink escape; tampering; missing blob; expired stage + lost-ack replay;
concurrent stale reimport; migration success/failure/interrupted resume +
byte-identical source; envelopes/blobs byte-exact; orphan blobs reported;
derived deletion harmless; undo/redo/retry pins readable after restart;
M1 regression green. Unverified: power-loss beyond fsync ordering.

## Limitations / contract-change requests

- **C23-1:** `inspectStage`/`ImportProposal` needs packet 24's `asset-pipeline`;
  not exposed (no placeholder).
- **C23-2:** dependencies.md §3.5's `captureManifest` is absent from the
  accepted project-model exports.
- Interpretations: `publishBlob.source` = `{stageId}|{bytes}`; `referenced` =
  version is `currentVersion`; v1 `captureContentView` is unavailable
  (`version_combination_unsupported`); `frame_bytes` is transport-only.

## Next

**Packet 24 (Bounded GLB inspection and import proposals) — not started.**
