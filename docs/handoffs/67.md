# Packet 67 — Recovery, diagnostic and measured-budget contracts

## Outcome

**COMPLETE (two bounded parts, one handoff, per packet boundary; docs +
fixtures only).** 67-A (consistent backup inventory/verify/restore, the
diagnostic report envelope, the recovery fault matrix) is fully specified
with executed refusal fixtures; 67-B (the measured budget for plan §2.6)
freezes the **protocol only** — the named device is BLOCKED on the owner
decision and every numerical threshold is explicitly `BLOCKED — unmeasured`
(never invented; no representative hardware in this container). No product
source, accepted contract or lockfile was changed. C09–C11/C13 are specified
here; their real evidence is the post-ratification 79 runs + 75/76/77/80/81.

## Contract of record (`planning/m4-contracts/reliability.md`)

- **67-A.1 Consistent backup** (§1): the backup is exactly the accepted
  workspace.md §15 included set (manifest + envelope + every
  `sources/sha256/<digest>` file, incl. superseded; staging/derived/
  ownership/recovery/migration/temp excluded). A live-owner project is
  refused before any copy (`backup_live_project`, bytes untouched — the
  accepted §6.2 liveness test; released/stopped/dead-owner proceed). The new
  operator data file `backup-manifest.json` (inventory + block
  `inventoryDigest`, per-project digests + revision + template provenance —
  C10) is written **last**: a directory without a valid manifest is
  `backup_incomplete`, never a backup. `verify` (ordered, read-only):
  truncation / bad hash / missing-or-superseded catalog blob /
  ownership-included / path-escape, each with its exact code; orphan blobs
  (on-disk, unnamed by the catalog) are included + reported informational.
  **Retention is manual** (declared constant — no silent source/backup
  pruning anywhere).
- **67-A.2 Restore vs create** (§2): **same-ID restore** (verified set into
  a clean EMPTY same-`projectId` destination; identity checked before the
  first write; a nonempty destination is refused — `restore_destination_
  nonempty` — and never modified; no ownership is restored; the accepted
  post-restore verification applies) and **new-ID creation** (the EXACT
  3-field identity rewrite — manifest `id`, envelope `projectId`, directory
  name — and nothing else: revision/history preserved, NOT a migration
  reset) are pinned as **distinct** operations.
- **67-A.3 Diagnostic report** (§3): `GET /api/v1/admin/health` + the
  admin-scoped MCP row `tl_health` (C67-2) — a bounded (≤ 32 KiB; 100/32
  list bounds — the accepted scan/ring values re-used), redacted (no
  credentials, no absolute paths, no content bytes — the envelope is typed
  so an unredacted value cannot be represented; overflow clips + sets
  `truncated: true`), point-in-time snapshot tagged `backendId` + `ts` +
  `engineVersion` (consumers MUST re-fetch on any `backendId` change — the
  stale-identity rule); it asserts nothing about hardware.
- **67-A.4 Fault matrix** (§4): F1–F8 — the accepted machinery (atomic
  envelope write, orphan claim, write faults, blob rot) + the 65 template
  phases + the 66 kit re-hash + the 67 backup/restore crashes; every row:
  state / detection / recovery / originals, under the **no-ready-partial
  (C09)** invariant.
- **67-B Budget protocol** (§5–§8): the named device (BLOCKED — owner
  pending, the reference-device.md §2 proposal), 3 **digest-frozen scenes**
  (S1 the committed v3 envelope, S2 the committed Beacon Reach capture, S3
  the template starter's final scene — a deterministic recipe replay), the
  measurement protocol (environment record incl. `GL_RENDERER` +
  warm-cache condition, 10-min warmup, N ≥ 30, median + p95, 7 closed
  metrics, no-early-box-data rule) and the threshold table with **every row
  `BLOCKED — unmeasured`**. Ratification checkpoint:
  `handoffs/67-budget-ratification.md` — 79 may run its preflight after
  76/78 but **no scored run until target review/promotion + owner
  confirmation**.

## Owned diffs (Gate Q)

C67-1 workspace (no change — the tools execute the accepted §15
classification offline; no new operation/route/class/code), C67-2 sessions
(one admin route + one MCP read row + the 32 KiB/100/32 bound rows), C67-3
runtime (no change — the report aggregates the accepted §8 diagnostics),
C67-4 dependencies (no new package/dependency — plain-Node tooling edge,
CCR-67-1), **C67-5 the `deployment.md` §5 REPLACEMENT** (`diffs/
deployment.md`: the raw whole-tree `tar` — which copies the §15-excluded
ownership/staging/derived classes and carries no verification — is replaced
by the inventory-based, verified procedure; the takeover note is preserved).

## Fixtures + checker (verified)

The checker **executes** all 8 refusal cases through its own reference
implementation of the §1–§2 procedures against temp trees (each asserts the
exact code **and** that the originals are byte-identical after the refusal —
the acceptance obligation; the R8 live-owner leg uses a real live pid, the
dead-owner leg a real exited pid), re-derives the backup example and the
3-field transform byte-exactly, and re-derives S3 by a **second independent
engine replay** (generator and checker agree). Commands: generator write +
`--check` (7 files byte-identical); checker — **OK** (7 sections; the first
run caught a relative-path bug and three test-harness digest-capture-order
bugs, all fixed); tampered-digest negative control — **exit 1** (two
detections), then regenerated to green. `npm test` — 179/2237, EXIT 0.

## Acceptance criteria (packet 67)

- **PASS (specified + fixture-verified):** every packet-67 failure mode has
  its exact code (live project, truncated backup, missing/superseded blob,
  bad hash, ownership included, nonempty destination, symlink/path escape,
  disk/write fault, crash before final publication, stale diagnostic
  identity, secret/log overflow — impossible by construction, invalid scene
  / inadequate hardware — the 79 refusal codes); the fixtures prove
  refusals retain originals (executed); the proposed targets trace to
  measurements + the owner device choice (all blocked, none invented); Q
  records that independent non-performance work proceeds while 67-B awaits
  hardware (CCR-67-2).
- **UNVERIFIED (by design):** the C10/C11/C13 real evidence — the
  post-ratification scored 79 runs on the owner device, the 75/76/77/81
  integrated evidence.

## Limitations

- PROPOSED text; nothing promoted (Gate Q). The backup/restore/budget tools
  ship with the packet-75/79 tooling (CCR-67-1) — the fixture's reference
  implementation proves the spec, not the product.
- The `deployment.md` §5 replacement applies only on the owner's Gate Q
  acceptance (it is an acceptance document, not a promoted contract).
- 67-B's numerical table resumes only after the owner device + ratification;
  the blocked rows are the frozen shape.

## Next packet

**68 — Contract inventory, fixture audit and Gate Q submission** (depends on
63–67; the Gate Q gate) — under the owner standing authorization for all of
M4.