# Packet 37 — Integrated M2 acceptance and bounded deployment documentation

Owner pre-approval: **owner pre-approval (autonomous M2 build instruction,
2026-09-18); final manual review pending.** No git commit was made.

## Outcome

M2 was exercised end to end against the **real deployed artifacts**
(`dist/backend/backend.mjs`, `dist/mcp-adapter/mcp.mjs`) on disposable data and
export roots, plus a real clean-install copy. The integrated journey passed
**18/18** process/HTTP/WS/SDK checks; `docs/acceptance/m2-report.md` records the
full A01–A24 matrix, the §3 journey, U-1…U-5, the honesty statement and the
owner checklist.

## Scoped diff (no commit)

New: `docs/acceptance/m2-report.md`; `docs/acceptance/evidence-m2/37/**`
(`journey/run.mjs` + results/transcript/JSON, `suites/**`, `clean-install/**`,
`judgement/no-png.txt`, `manifest.md`). Modified:
`docs/acceptance/deployment.md` (§8–§10: M2 setup, trust acknowledgment,
secure-context/gamepad topology, source-backup classification, supplied vs
performed); `docs/handoffs/37.md`; the packet-37 row of `docs/STATUS.md`. No
source, contract, fixture, lockfile or `node_modules` change.

## Commands and actual results

- `npm test` **135 files / 1701 passed**; `typecheck` exit 0 (15 pkgs);
  `check-deps` exit 0; `check-boundaries` OK 15/283/1068/0;
  `build` 4 built/0 skipped; `check-fixtures.mjs` 34/34.
- Integration suites: m2-content 25, m2-builds 11, m2-play 14, m2-export 9;
  runtime 20, physics 61, controller 35, builds 26, crash 6, crash-recovery 3,
  browser-harness 27.
- Journey: **18/18 PASS** (migration copy byte-exact; M1 v1 + malformed
  refusal; import/reimport/undo/redo; prefab + override + MCP stale conflict;
  behavior trust/compile + hostile refusal; Play pinning/security/MCP relay;
  SIGKILL + stale ownership + takeover + lost-ack `duplicated:true`;
  tamper/missing/backup restore; double export + failure isolation +
  backends-stopped static serving under a non-root path). Re-run 2026-09-19 in
  the Gate J close-out with the corrected checks: A22 now exports into two
  **distinct disposable export roots** and hashes both trees (differing =
  `manifest.json`,`meta.json`; non-timestamp bytes identical; `buildId`
  re-derives), and A20 asserts each stop's 200 `{ok:true}` plus the owner-ack
  `play.stopped` (`stopUnconfirmed` absent). See `docs/handoffs/gate-j-repair.md`.
- Clean install in a disposable `/tmp` copy: `npm ci` (167 pkgs) + full suite
  135/1701 + all tools + 4-built + fixtures 34/34, all exit 0; repo untouched.
- `find docs/acceptance/evidence-m2 -iname '*.png'` → empty.

## Acceptance rows

**PASS at process/Node level with no browser/hardware half**: A01, A06, A09,
A10, A14, A22, A23, A24.

**PASS at process/API/Node level with a browser/hardware half UNVERIFIED**
(16-row union; each row's recorded procedure is in `m2-report.md` §8 items 1–8):
A02, A03, A04, A05, A07, A08, A11, A12, A13, A15, A16, A17, A18, A19, A20,
A21. Procedures: `tests/browser/m2-input/m2-input.browser.ts` (A11);
`tests/browser/m2-controller/m2-controller.browser.ts` (A12/A13);
`tests/browser/m2-assets/m2-assets.browser.ts` (A02/A03/A04/A08);
`tests/browser/m2-prefabs/m2-prefabs.browser.ts` (A05/A07);
`tests/browser/m2-behaviors/README.md` + `m2-behaviors.browser.ts` (A15/A16);
`tests/browser/m2-play/README.md` (A17/A19/A20);
`docs/acceptance/evidence-m2/36/manifest.md` §9 (A21);
`docs/acceptance/evidence-m2/14/manifest.md` desktop procedure (physics/CPU).

**No row FAILED.** No screenshot, browser log, gamepad report or performance
figure was fabricated.

## Limitations

No browser/GPU/keyboard/gamepad in-container: all browser-visual, physical
input, tab-resume and real-PNG evidence is UNVERIFIED for the 16 rows above
(owner checklist, `m2-report.md` §8). Physics selection remains PROVISIONAL
(packet-14 desktop evidence).

## Contract-change requests

None new. Packets 33–36 requests were all disposed by Gate I
(29 accept-with-diff applied, 1 accept-as-recorded, 0 rejected).

## Next step

**Gate J review — not started.** Do not start M3.
