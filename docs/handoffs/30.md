# Packet 30 — Keyboard and gamepad actions

**Outcome.** New `@thirdlight/input` package: the pure `mapRawInput(snapshot,
options) → ActionFrame` mapping (keyboard A/D + arrows + Space; standard gamepad
axis 0 / D-pad 14–15 / face button 0; 0.2 radial dead zone; keyboard → D-pad →
stick precedence, no summation; press latch and jump phase chain), the single
explicit browser listener owner `attachBrowserInput(target, options)`
(text-field suppression, hidden tab/pagehide/blur suspension with fresh
activation, hot disconnect, index reuse, structured unavailable state,
idempotent `detach()`/`dispose()`), and the replayable `createStepInputSource`.

**Scoped diff (no commit).** New `packages/input/**` (package.json with the
exact `@thirdlight/runtime@0.1.0` dep and `.` export, tsconfig, `src/{index,
types,mapping,browser,step-source}.ts`, 4 test files / 78 tests); new
`fixtures/m2/input/**` (9 sequences / 38 steps, `index.json`, Node checker); new
`tests/browser/m2-input/m2-input.browser.ts` (packet-37 procedure); new
`docs/acceptance/evidence-m2/30/**`; `tools/check-boundaries.mjs` (UNITS +
`NODE_SIDE_ALLOWED.input`, `runtime` types-only); this handoff + STATUS row 30.
**Lockfile delta: 2 hunks, +11 lines — the `node_modules/@thirdlight/input` link
and the `packages/input` entry only.** No dependency or pin changed; `npm
install` also wanted two unrelated stale-entry normalizations from the
uncommitted packet-24/25 work, reverted to keep the delta limited (see the
manifest).

**Commands and actual results.** `npx vitest run packages/input` 4 files / 78
passed. `npm test` 106 files / 1401 passed. `npm run typecheck` clean (12
packages). `npm run check-deps` OK. `npm run check-boundaries` OK — 12 packages
/ 235 files / 861 specifiers, 0 violations; the disposable negative probe
(`three`, runtime value import, `node:fs`) failed 3/3 and was removed.
`npm run build` 4 built / 0 skipped. Contracts checker 34/34; input fixture
checker green.

**Acceptance.** PASS (Node): exact frames for every mapping/edge case (dead
zone, press/hold/release, key repeat, latch, simultaneous sources,
non-standard/absent device, determinism). PASS (Node, injected fake
DOM/gamepad): suppression, suspension/fresh activation, hot disconnect, index
reuse, blocked/insecure/absent API, listener + held-state cleanup. **UNVERIFIED:
physical keyboard, physical controller and real browser logs** (A11's hardware
half; A13's real tab-resume) — no browser/hardware here; the named desktop
procedure and the BR-3 gamepad-topology note are in the manifest. No
synthetic-gamepad-only claim.

**Limitations.** The device-binding rules are pinned to the Gate-E-accepted
`input.md` because the I-5 promotion left them out of `docs/contracts` (C30-1).
Physics stays PROVISIONAL; selection per decision 0002 §1, owner pre-approval.

**Contract-change requests.** C30-1 promotion gap; C30-2 both-keys-cancel prose
vs the accepted packet-17 fixture; C30-3 `createStepInputSource` absent from the
export row; C30-4 `Math.round` vs half-away-from-zero; C30-5 preview iframe
`allow="gamepad"`; C30-6 latch during awaiting-release (manifest).

**Next packet:** 31 — Rapier 2D adapter (not started).
