# Packet 25 — Content HTTP services, projection data and MCP parity

Owner pre-approval: **owner pre-approval (autonomous M2 build instruction,
2026-09-18); final manual review pending.** No commit.

## Outcome

The UI-facing transport and the **actual stdio MCP** can query and publish M2
content through the real backend: the packet-19 content routes, the M2 mutation
and content-query ops over `POST …/commands`, and authenticated committed
asset-byte reads by immutable `(project, asset, version)` (sessions.md §16.1).
The transport performs **no filesystem writes and no authoritative mutation**:
it injects the workspace service, constructs the `asset-pipeline` inspector and
injects it (the workspace owns the staged read), and publishes the immutable
blob only as the non-authoritative preparation step. Behavior-build
jobs/publication stay unavailable (packet 33), never mocked. The full state
gained an additive bounded `content` projection; `mutation.applied` is
binary-guarded.

## Scoped diff

New: `protocol/src/content.{ts,test.ts}`, `backend/src/content.{ts,test.ts}`,
`tests/integration/m2-content/**`, `docs/acceptance/evidence-m2/25/**`, this
handoff. Modified: `protocol` errors/http/index; `workspace` content-store/
errors/service/session/types/index + manifest; `backend` backend.ts/ambient +
manifest; `mcp-adapter` tools/backend-client/ambient; `check-boundaries.mjs`;
the packet-25 `STATUS.md` row. No accepted contract, M1 fixture, lockfile or
`node_modules` change.

## Commands run

`npm test` 81 files/**1131 passed** · new tests 54 (protocol 21, backend 9,
integration flow 13, security 11) · `typecheck`/`check-deps`/`build` exit 0 ·
`check-boundaries` 11/187/680 exit 0 · `check-fixtures.mjs` 33/33. Integration
tests spawn a **real backend process** on a disposable root + real fs + real
**stdio MCP** SDK client + real WS.

## Acceptance

Passed: import/query/instantiate/edit/retry flow; browser-origin and
MCP-origin identical results/errors; bounded job errors; projection
convergence with no bytes in frames; and the negative security matrix (auth,
origin, traversal, staged reads, framing, disconnect, restart replay, lost
stage). **Unverified:** all browser/UI parts (A02/A04/A07 UI) — no browser in
this container; packet-37 procedure.

## Limitations / contract-change requests

- **C25-1** sessions.md §5.1 lacks a `content` field; added additively (v2 only).
- **C25-2** `publishAsset` args require `importedAt`, absent from commands.md §3.1.1.
- **C25-3** workspace `blob_corrupt`/`asset_not_found` classes are `validation`,
  but the packet-19 table/§16.1 require 500/404; mapped at the route.
- **C25-4** delivery §15 asset `limit` 200 vs commands.md §4 128 → 128 used.
- **C25-5** no accepted route triggers `publishBlob`; upload completion performs
  it (preparation); a reviewed flow diff may be wanted.
- **C25-6** the two `asset-pipeline` declarations need a lockfile link update
  this packet forbids; checks are green via the existing symlink.
- Transport-observable mid-request cancellation of a synchronous bounded
  inspection is not possible (late results are unit-tested instead).

## Next

**Gate F review — not started.**
