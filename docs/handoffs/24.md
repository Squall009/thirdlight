# Packet 24 — Bounded GLB inspection and import proposals

Owner pre-approval: **owner pre-approval (autonomous M2 build instruction,
2026-09-18); final manual review pending.** No commit made.

## Outcome

New pure leaf `@thirdlight/asset-pipeline`. `inspectGlb(bytes, options)` and
`prepareImport(bytes, {…, job})` implement project-model.md §18.7/§18.8 in the
exact §18.7.2 order (17 steps, first failing step only, ≤10 diagnostics plus the
true count, §18.6 metrics, the closed §18.8.2 code set, the §18.5 `recipeDigest`
and a metadata digest). Output is deep-frozen validated data: no I/O, no Node
builtins, no `three`/GLTFLoader/plugin registry, no URL fetch (remote and `data:`
URIs are rejected, never resolved), no cache writes, no asset-ID decisions.
Caller/workspace commits; the importer cannot mutate authoring state. 17
self-generated GLBs under `fixtures/m2/assets/**`; 102 new tests.

## Scoped diff (no commit)

New: `packages/asset-pipeline/**` (package.json, tsconfig, 11 src, 5 test files),
`fixtures/m2/assets/**` (17 GLBs, index, generator), `docs/acceptance/evidence-m2/24/**`,
this handoff. Modified: `tools/check-boundaries.mjs` (UNITS plus one
`NODE_SIDE_ALLOWED` row, project-model **types-only**), the packet-24 row of
`docs/STATUS.md`, and `package-lock.json` **+11 lines = the new workspace link
only**. No other package, contract or M1 fixture touched; no dependency change.

## Commands and actual results

`npm test` 77 files/**1077 passed** · asset-pipeline 5 files/**102 passed** ·
`typecheck` exit 0 · `check-deps` OK, no drift · `check-boundaries` 11/183/660
exit 0 · `build` exit 0 (4 built) · `check-fixtures.mjs` 33/33 · `npm ls` clean.
Negative boundary probe: `three`, `node:fs`, workspace and a value import of
project-model all fail check 1 (probes removed). Independent python3 check:
recipe digest and all 17 sidecar→`.glb` digests match.

## Acceptance

Passed: valid tiny mesh/material/clip fixtures with deterministic recipe and
metadata hashes (two runs byte-identical); every rejection class (truncation, bad
chunk/JSON, accessor overflow, external/data URI, required extension,
compression, decoded caps, malformed clip, timeout, cancellation) with ordered
codes, paths and limits; rejected input produces no state; no network or plugin
execution. Unverified: browser/UI and transport parts of A02/A04 (packets 25–27).

## Limitations / contract-change requests

- **C24-1…C24-3** (details and proposed diffs in the evidence manifest):
  §3 lacks the packet's `prepareImport`; §18.7.2 step 7's literal
  "`byteOffset ≤ 3`" rejects every real GLB (the glTF 2.0 alignment rule was
  implemented); §18.9.3's limit enum lacks `json_chunk_bytes`/`image_bytes`.
- workspace.md's byte-cap rows say `limits_exceeded` while §18.7.2 steps 1/4 say
  `asset_size_exceeded`/`asset_json_invalid`; the normative codes were used.
- `inspectStage` wiring and the unreachable geometry caps: see the manifest.

## Next

**Packet 25 (Content HTTP services, projection data and MCP parity) — not started.**
