# Packet 26 — Shared GLB rendering and asset previews

Owner pre-approval: **owner pre-approval (autonomous M2 build instruction,
2026-09-18); final manual review pending.** No commit.

## Outcome

One resource-owner path in `@thirdlight/three-adapter` for instances and
inspection: an approved asset-byte resolver (supplied bytes **or** an
injected async resolver — never a token/URL/fetch), cancellable async prepared
visual resources that discard stale completions, whole-GLB model instances
(preserved hierarchy under one entity, independent transform + animation state),
a local material/animation preview controller (play/pause/scrub,
instance-local material overrides, host-driven `update(dt)`, no clock or render
loop), explicit ownership counters and idempotent disposal. No second scene-mutation engine: M1
instantiate/render/screenshot/diagnostics behaviour is intact and the helpers
never touch runtime or authoring state. The pinned `three@0.186.0` GLTFLoader
binding lives on the new `./gltf-loader` subpath (so the export/preview bundles
stay loader-free), and `tools/check-boundaries.mjs` restricts `three-adapter` to
the approved `three` root + GLTFLoader subpath (other modules still fail —
negative probe recorded).

## Scoped diff (no commit)

New: `packages/three-adapter/src/{ownership,visual,gltf-loader}.ts`,
`{visual-resource,preview-controller,gltf-loader,context-loss}.test.ts`,
`{test-glb,test-visual}.ts`, `docs/acceptance/evidence-m2/26/**`, this handoff.
Modified: `three-adapter` `{package.json (exports + description), src/adapter.ts,
src/errors.ts, src/index.ts}`, `tools/check-boundaries.{mjs,test.mjs}`, the
packet-26 `STATUS.md` row. No contract, fixture, lockfile or other package
change.

## Commands run (actual)

`npm test` 85 files/**1161 passed** (three-adapter 12 → **40**) ·
`typecheck` exit 0 · `check-deps` exit 0 (no new dependency/pin) ·
`check-boundaries` 11/196/710 exit 0 · `build` `4 built, 0 skipped` exit 0 ·
`check-fixtures.mjs` 33/33. Transcripts in `docs/acceptance/evidence-m2/26/`.

## Acceptance

**Passed (Node; real three.js objects + the real pinned GLTFLoader):** whole-GLB
realization (real scene graph, `Spin` clip); two instances independent in
transform and animation state with shared geometry/material; play/pause/scrub
applies the pose; cancellation and stale-completion discard; the five failure
codes mapped structurally (no throw); ownership balance 27/27/0 over repeated
load/reimport/dispose, per-kind; double dispose releases nothing twice; lost
context reported and listeners released; no network access on the prepare/
preview/screenshot paths.
**Unverified (browser; packet-37 procedure in the manifest):** pixels/
materials, texture decode, screenshot taint, context-loss round trip, viewport
play/scrub.

## Limitations / contract-change requests

C26-1 the `./gltf-loader` subpath needs a `dependencies.md` §3 row. C26-2
`export.md` §5.4.1 binding 4's exact `https://` count (23) cannot hold once
GLTFLoader enters the export/preview graphs (dependencies §4.2, packets 35/36);
measured Δ +12; table extension proposed. C26-3 the M2 extension allowlist is
duplicated defensively (forbidden `asset-pipeline` edge).
C26-4 `Object3D.clone(true)` shares skeletons (skeletal per-instance animation
not claimed). No editor wiring: the M1 `ProjectedEntity` has no model kind yet,
so packet 27 consumes these helpers instead of unwired viewport code.

## Next packet

**27 — Content browser, placement/reimport and snapping. Not started.**
