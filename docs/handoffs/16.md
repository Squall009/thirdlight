# Packet 16 — Typed edits, prefab and property contracts

2026-09-18 (UTC) · packet 16, **Gate E** (prereq: packet 15, met). Contract
drafting only: no code, no commit. Owner line in every draft: *owner
pre-approval (autonomous M2 build instruction, 2026-09-18); final manual review
pending.*

## Outcome

**PROPOSED command/query/history/projection contract delivered; nothing
accepted.** Ops: `publishAsset`, `createPrefab`, `instantiatePrefab`,
`publishBehavior`, `setBehaviorProperties`, `setComponent`, `setSettings`, plus
bounded `queryAssets`/`queryPrefabs`/`queryBehaviors`. Prefabs are **materialized
copy-on-instantiation** (immutable definitions, no nesting/variants/link): capture
excludes the required camera and external entity references; instantiation
allocates IDs deterministically (smallest free `<derivedPrefix>-NNNN`, definition
document order), remaps internal references once, records the mapping durably,
creates the subtree in one transaction, and redoes/retries the exact IDs. Declared
properties are bounded typed data (defaults, no code evaluation); declaration
updates validate every existing use and never drop values. Behavior **source**
publication is **unavailable** until packet 33's digest-bound preparation record.

## Scoped diff (no commit)

New: `docs/planning/m2-contracts/{prefabs.md,properties.md}`, `diffs/commands.md`,
`fixtures/m2/contracts/commands/**`, `docs/acceptance/evidence-m2/16/**`,
`docs/handoffs/16.md`. Modified: `check-fixtures.mjs` (+`p16-*` checks),
`expected.json` (+5 entries, +21 codes), `constructed-cases.md` §C9,
`diffs/project-model.md` (§"Packet 16 additions"; packet-15 sections untouched),
`docs/STATUS.md` (packet-16 row). Unchanged: contracts, decisions, packages,
lockfile.

## Commands run (actual)

- checker → `check OK: 16 check group(s) passed, 0 problem(s)`.
- negative control (corrupted digest/content block/M4 mapping) → `check FAILED,
  6 problem(s)`.
- independent `python3` recompute (preimages, view digest, ID allocation/remap) →
  all match.
- `npm test` run 1 → 1 failed/840 (`ownership-claim-2026-09-18` child `EEXIST`
  race); isolated re-runs ×2 green; run 2 → 841/841. typecheck, check-deps,
  check-boundaries, build exit 0 (4 built).
- `git status --porcelain docs/contracts package.json package-lock.json` → empty.

## Acceptance criteria

Two independent instances, initial override, ordinary edits, one-undo subtree
removal, exact-ID redo/retry, mixed human/MCP history: **PASS** (fixtures +
replay). Atomic-failure matrix, field default/type/range and
compatibility-no-data-loss: **PASS as contract text + declarative cases**
(execution is Gate F's). Copy semantics in API/UI terms: **PASS**. Unverified:
behavioral claims, bounds (Gate E pins), settings success (packet 17's registry).

## Limitations

Fixtures are declarative/replayed, not executed implementations; `setSettings`
can only reject until packet 17 pins keys; source publication is absent by design;
the recorded concurrency-test flake is pre-existing.

## Contract-change requests

(1) `publishAsset` reconciles packet 15's delegated `createAssetVersion` for
packet 19. (2) Packet 15's empty-container text is superseded; update in the
promotion pass (committed envelopes stay valid). (3) `dependencies.md` §3 rows →
packet 19.

## Next

**Packet 17 — Stateful runtime, input and 2.5D physics contract.** Not started.
