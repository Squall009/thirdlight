# Packet 36 — Standalone M2 content/gameplay export

Outcome: the M2 export closure (shared runtime + input + platformer +
physics-rapier + three-adapter/GLTFLoader + statically linked compiled
behaviors + every reachable GLB + canonical manifest/scene/meta v2) is
composed from ONE injected captured state by the shared `buildContentClosure`
(also used by the play path — no second manifest/bundle implementation),
format-aware validated and published atomically. Physics selection: Rapier 2D
compat 0.20.0 — selection per decision 0002 §1, owner pre-approval
(autonomous M2 build instruction, 2026-09-18); final manual review pending. No
commit.

## Scoped diff (no commit)

New: `packages/project-model/src/{manifest.ts,sha256-length.test.ts}`;
`packages/exporter/src/{content-closure,export-m2,export-bundle,export-composition,export-bootstrap-m2,export-page,export-content-scan,export-io,export-types,scene-read}.ts`,
`export-m2.test.ts`; `tests/integration/m2-export/**`;
`docs/acceptance/evidence-m2/36/**`.
Modified: `packages/exporter/src/{export,graph,errors,index,ambient.d.ts,tsconfig.json}`;
`project-model/src/{index,sha256}.ts` (P1 repair);
`backend/src/{play-content,backend}.ts`; `tools/check-boundaries.mjs`;
`docs/acceptance/deployment.md` §7.2; `docs/STATUS.md` row 36.

## Commands and actual results

`npm test` **133 files / 1693 passed** (packet-35 baseline 129/1672);
typecheck exit 0 (15 pkgs); check-deps exit 0; check-boundaries OK
(283 files / 1068 specifiers / 0 violations); build 4 built / 0 skipped;
fixture checker 34/34. Packet suites: exporter M2 9/9, integration 7/7,
sha256 cross-check 2/2. Measured: export bundle 4 455 999 B, patterns a–j =
0/0/0/7/0/3/0/38/0/5, GLTFLoader ×37, scanHits 0; cold builds 351/352/340 ms,
0 failures; double export byte-identical except `meta.json.exportedAt`;
play/export trace **max |Δ| = 0** over 120 steps (tolerance 1e-9).

## Acceptance criteria

Passed (real process/fs/HTTP/publication/esbuild/Rapier): closure
completeness; exact versions/licenses/hashes + build identity in `meta.json`
v2; double-export reproducibility; failure isolation (missing/corrupt blob,
hostile source) with hash-verified untouched output; credential,
locator-capability and non-GLB probes rejected; non-root static serving with
`.glb`/`.wasm` MIME; trace agreement.
**Unverified (no browser):** browser walkthrough — WebGL rendering,
trust-notice gate, WASM/CSP init, keyboard/gamepad, pixels, network panel
(procedure: evidence manifest §9; nothing fabricated).

## Limitations

`capturedAt` is in the promoted `buildId` preimage, so a wall-clock re-export
in a later second changes `manifest.json`/`buildId` (C36-7); full
byte-identity is proven with a fixed clock (unit) and same-second real exports.
No separate WASM artifact is emitted (validator unit-tested only).

## Contract-change requests

C36-1 (`scene.json` absent from §3/§17.5), C36-2 (manifest owner
project-model vs protocol), C36-3 (`outputDigest` undefined), C36-4
(behaviorTrust read absent), C36-5 (M2 bundle entry file list), C36-6 (§5.4.1
rows: Rapier compat, engine trust-notice text, scene read; behaviors static),
C36-7 (`capturedAt` digest-input contradiction). Diffs in the evidence
manifest §10.

## Bounded defect repair (P1, dependency)

`project-model/src/sha256.ts` added an extra padding block for
`len ≡ 55 (mod 64)`, yielding wrong digests (it broke export/play
`sceneDigest` agreement). Repaired + NIST/known-answer/`node:crypto`
cross-check tests; fixtures still 34/34.

## Next

**Gate I review** (not started).
