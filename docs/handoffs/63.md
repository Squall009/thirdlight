# Packet 63 — Current-baseline audit and reference-device measurement path

## Outcome

**COMPLETE (capture-only, per packet boundary).** The packet-63 probe suite
(`tests/evaluations/m4-baseline/`) ran the built editor + isolated preview +
standalone sample in a real headless browser and produced the reproducible
M3-carry-forward baseline with raw evidence. No product source, package/
lockfile, accepted contract or historical verdict was changed. All
acceptance failures were **captured, not repaired**.

- Row matrix (final): A01/A03/A06–A10 PASS; A02/A04/A11 INFO; A05
  UNVERIFIED; B00/B01/B01b/B03/B04 PASS (B04 held-KeyD moves the player in
  real interpolated state, Δx 2.6901 — closes packet-62 B05); B02/B05 INFO;
  B04b UNVERIFIED; C01–C05 PASS; C03b/C13 INFO; **C06/C07 FAIL**; C08–C10
  UNVERIFIED; C11/C12 PASS. Full detail: `planning/m4-contracts/baseline.md`.
- **Root cause of the connected-preview failure chain (v3 play), captured
  with live evidence (`c-bridge.json`, `c-06-failure.json`, session logs):**
  - **D-63-5** — the v3 preview wrapper (`preview-m3.ts:272–276`) never acks
    the bridge handshake (M2 wrapper does, `preview-bootstrap.ts:565`); the
    editor gates the snapshot on the ack (`App.tsx:570`) → the `tl.snapshot`
    is never delivered → the M3 host never mounts (C06/C07/C09). Transport
    proven healthy (control `tl.ping`/`tl.pong` round-trip); a probe
    handshake (validator-OK with the real 43-char contentId / 64-hex
    buildId) also got no ack; a probe snapshot was accepted at the Bridge
    nonce gate and reached `startM3Preview`.
  - **D-63-6** — the v3 wrapper's `tl.ready` body
    (`preview-m3.ts:289`, empty `contentDigest`) fails the §13.5 validator
    (verified in Node) → `tl.ready` would be silently dropped even on a
    successful mount.
  - **D-63-4** (incl. merged D-63-3) — the editor WS client is
    receive-only (zero `.send(` in the editor package) → no §5.2 heartbeat
    and no `play.preview.ready` → the backend stops **every** play at the
    15 s `preview_timeout` (sessionLog: `registered` → `started` →
    `preview_timeout` at +15 s in every run).
  - **D-63-9** — the v3 play snapshot document (runtime.md §2 4-key shape;
    `bridge.ts:124–127` allowlist) cannot carry the `content.game` block the
    M3 host requires (`snapshot.ts:173` normalizes absent→null;
    `runtime.ts:593` fails `config_invalid`/`game_config`;
    `host.ts:451` fail-closed) → v3 play cannot mount even after D-63-5/6.
  - **D-63-2** — preview/export canvas not focusable as shipped (no
    tabindex; A02/B02/C08); **D-63-7** — client drops `playContent` on the
    retained `play.started` (masked by `App.tsx:315–317`); **D-63-8** —
    export wrapper HUD stale after mount.
  Full chain with sources/evidence/owners: `planning/m4-contracts/debt-ledger.md`.
- **M3 carry-forward reconciled:** CC-55-3 split into CC-55-3a (behavior-
  linking gap, accepted open at Gate N; code truth `host.ts:480` /
  `content-closure.ts:506`) and CC-55-3b (HUD/status wording diff) — neither
  record claimed authoritative by recency. K-3 export symbol names recorded
  for owner confirmation. P2-B re-verified live: `queryProject` omits the
  `content` summary (and thus the v3 `game/zones/spawns/audioAssets`
  counts). M3-GLB model-attach gap captured (A11).
- **Reference-device route established** (`planning/m4-contracts/
  reference-device.md`): proposed reference-desktop spec (owner decision),
  67-B protocol-only freeze at Q with the numerical table explicitly
  deferred, and the exact blocked prerequisites. SwiftShader is never a
  hardware-GPU claim; C11/hardware rows UNVERIFIED.

## Changed files (this packet; no commit made — owner rule: no commits in
M4 execution; scoped diff summary)

- new `tests/evaluations/m4-baseline/` — `run.mts` (orchestrator:
  env/toolchain record, capability table E02, phases A–C, `summary.json`,
  active-handle diagnostic + hard exit; `TL_M463_EVIDENCE_DIR/
  TL_M463_PHASES/TL_M463_NO_BUILD` overrides), `phases-ab.mts`, `phase-c.mts`
  (real backend + real editor + real Play; CDP Network capture; bridge
  transcript + control probes; session-log dumps), `lib/frames.mjs`
  (cross-origin frame-context eval), `probes/composition-entry.ts`.
- new `docs/planning/m4-contracts/{debt-ledger,baseline,reference-device}.md`.
- new `docs/acceptance/evidence-m4/63/raw/*` (22 files).
- `docs/STATUS.md` — row 62 corrected to complete (SD-63-2) + row 63 updated.
- (carried from the pre-compaction session, same scope:
  `docs/handoffs/m4-plan-review.md`, `docs/handoffs/m4-execution-checkpoint.md`,
  PR-M4-1 bullet in `docs/planning/m4-packets.md`.)

## Commands run (results)

- `npm test` → EXIT 0 (179 workers isolate) at audit start; `npm run build` (in-suite) → exit 0.
- `TL_M463_EVIDENCE_DIR=docs/acceptance/evidence-m4/63/raw npx tsx
  tests/evaluations/m4-baseline/run.mts` → EXIT 0; row matrix above
  (started 2026-09-22T05:16Z).
- Node validator checks vs `packages/protocol/src/bridge.ts` → handshake ok (real IDs),
  wrapper `tl.ready` body rejected (`contentDigest must be 64 lowercase hex`),
  ack ok, snapshot ok.
- `openWorkspaceService({root}).query({op:'queryProject'})` on the v3 fixture → ok,
  `content` field absent (P2-B re-verified).
- The C05 409 `session_conflict` storm (mid-packet) root-caused to orphaned
  headless Chrome from interrupted runs (harness `browser.close()` skipped by
  early returns) — fixed in the probe (`finally`); clean environment → C05
  PASS. Harness hygiene, not a product defect.

## Acceptance criteria

- PASS: C01; executable probes with raw logs/canvas + state at known frames
  (B04 step/x samples); capability table (E02); baseline timing samples;
  ledger rows with source/evidence/status/owner/disposition; stale docs
  separated (SD-63-1/2); CC-55-3 reconciled without recency authority; K-3
  names recorded; failures captured not repaired.
- UNVERIFIED (owner annex, hardware): A05/B04b/C10 canvas-identity motion
  (SwiftShader); C08–C10 preview interaction (frame torn down by the 15 s
  timeout); audio audibility (B05); physical input devices.
- FAIL (captured as defects, ledger §2): C06/C07 (preview host never
  mounts) via D-63-5/6/9; the 15 s `preview_timeout` via D-63-4.

## Limitations

- Single-container baseline: software GL, no physical devices, loopback
  only; timing samples are not budget data (67-B rule).
- D-63-9's live null-game rejection is code-verified (not yet observed with
  the real snapshot — the probe snapshot failed earlier at the scene-digest
  check, as expected for a fake scene).
- C03b recorded one re-establish cycle (status bar not stably
  connected-without-error within 60 s); the Play click still succeeded
  (C05 PASS) — the client re-establish behavior is recorded, not repaired.
- Reviews herein are in-session model review, not owner/independent approval.

## Contract-change requests

- **CCR-63-1 (D-63-9):** extend the v3 snapshot document (runtime.md §2) +
  the `tl.snapshot` allowlist (sessions.md §17.2.1) with the frozen
  `content.game` block; include it in the backend's retained `play.started`
  snapshot; add it to the preview's snapshot verification. Owner decision at
  Gate Q.
- **CCR-63-2 (D-63-5/6):** reconcile the v3 preview bridge behavior with
  sessions.md §13.4/§17.2.1 (handshake ack + truthful ready identity tuple +
  §18 channels) — repair scope, owner decision (packet 70 / 64).
- CC-55-3a/b: owner scope decision at Gate Q (behavior-linking config diff
  vs defer; wording fix vs close).

## Exact next packet

**Packet 64 — Delivered rendering, queries and compatibility repair
contract** (`docs/planning/m4-packets.md` §64). It consumes this baseline:
the D-63-* defect lines (especially P2-B query parity inputs and the
snapshot/bridge CCRs) are its contract inputs; the ledger's owners column
tracks what defers to 69/70/71/77. Do not start 64 from this handoff alone
— the owner's execution authorization for 64 is separate.