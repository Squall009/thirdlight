# Packet 20 — Model v2 and pure migration

Owner pre-approval: **owner pre-approval (autonomous M2 build instruction,
2026-09-18); final manual review pending.** No commit made.

## Outcome

M2 logical data is validated and canonicalized with M1 compatibility intact.
Added explicit M2 types and versioned entry points: `validateSceneV2` /
`normalizeSceneV2` / `parseSceneV2` (all §10.5–§10.8, §21 components,
registry order, limits, physics-transform rules), `validateContent` /
`normalizeContent` (§18 assets/metrics/recipes, §20 prefabs/declarations,
§20.9/§21.4 settings, §22.2 source records, §22.5 trust),
`validateProjectV2` (§13.1 three-block composition), `captureContent`
(§19), `resolveGameplaySettings`, `M2_SETTINGS_KEYS`, and the pure
`migrateSceneV1ToV2`. `ERROR_CODES` is the full §12.6 set;
`SCHEMA_VERSIONS_BY_DOCUMENT`/`KNOWN_VERSIONS` are per-document.
C19-D7 closed: every v2 envelope/content fixture gained
`behaviorTrust: { entries: [] }`, expected.json notes updated, and the
packet-16 content key-order assertion extended.

## Scoped diff (no commit)

New: `types-v2.ts`, `scene-v2.ts`, `content.ts`, `project-v2.ts`,
`sha256.ts`, `m2-model.test.ts`; `fixtures/m2/model/**` (30 fixtures +
expected.json). Modified: `errors.ts`, `validate.ts`, `normalize.ts`,
`parse-api.ts`, `migrate.ts`, `index.ts`, `test-fixtures.ts`,
`api-behavior.test.ts` (two constant assertions). C19-D7: 15
`fixtures/m2/contracts/**` files, `expected.json`,
`tools/check-fixtures.mjs`. Evidence `docs/acceptance/evidence-m2/20/**`;
STATUS row 20.

## Commands and actual results

`check-fixtures.mjs` 33/33 exit 0 · `npm test` 67 files/884 passed (was
841) · `npm run typecheck` exit 0 · `check-deps` exit 0 ·
`check-boundaries` 10 pkgs/156 files/546 specifiers exit 0 · `build` 4
built exit 0. Project-model 147 tests (104 M1 + 43 new).

## Acceptance

Passed: valid fixtures round-trip byte-exactly; invalid fixtures fail with
exact codes and retained bytes; migration retains sceneId/revision/entity
IDs and never mutates input; M1 data fixtures/behavior unchanged; no
I/O/three.js. Unverified: nothing packet-20-scoped.

## Limitations / contract-change requests

- **C20-1:** contract §12.1 implies `validateScene`/`parseScene` know scene
  `[1,2]`, but packet 20 must keep `fixtures/project-model/invalid/
  unsupported-version.json` + `mixed-schema-versions` and the M1 tests
  byte-identical. Implemented as the versioned entry points above; §8
  supports interchange = v1. Proposed diff: name `validateSceneV2`/
  `normalizeSceneV2`/`parseSceneV2` in §12.1.
- **C20-2:** `migrateScene(v1doc, 2)` still returns `no_migration_path`
  (M1 test pinned); the conversion is `migrateSceneV1ToV2`. Proposed diff:
  name that entry point in §12.4.
- Controller: implemented as **at most one** (§17's "packet-15/16 v2 scene
  stays valid (no controller)"; physics fixture V19 zero-controller →
  runtime `config_invalid`).
- `normalizeSceneV2` preserves behavior-values key order; declaration-order
  enforcement is the cross-block check. Envelope load is packet 23's.

## Next

**Packet 21 (Pure content and property commands) — not started.**
