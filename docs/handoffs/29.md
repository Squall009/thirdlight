# Packet 29 — Stateful runtime scheduling and module lifecycle

**Outcome:** the runtime now composes M2 module sets safely. Added the
canonical phase order `sample → intent → controller → physics → transform →
render`, phase-scoped write guards, transform-ownership and
unsupported-combination rejection at instantiate (no partial start), the
12-step settle pre-roll, one action sample per executed fixed step, injected
action/physics ports (no concrete adapter import), and the fail-stop lifecycle
(`failed` is terminal; no transform rollback; fresh restart only). Accepted M1
sets keep the accepted M1 semantics byte-for-byte: `demo.ts` is unchanged, all
M1 runtime tests pass unmodified, and the frozen §7.1 demo points replay
bit-exactly.

**Scoped diff (no commit):** modified `packages/runtime/src/{runtime,types,
errors,registry,snapshot,index}.ts`; new `packages/runtime/src/{actions,ports,
guard,m2-helpers}.ts`; new tests `m2-phases`, `m2-failstop`, `m2-scheduler`,
`m2-actions`; new `fixtures/m2/runtime/**` (index + 3 fixtures + Node checker +
README); new `tests/m2-runtime/runtime-fixtures.test.ts`; new
`docs/acceptance/evidence-m2/29/**`; this handoff + the STATUS row. No
lockfile/package/dependency change.

**Commands and actual results:** `npm test` 102 files / 1323 passed;
`npm run typecheck` clean (11 packages); `npm run check-deps` exit 0;
`npm run check-boundaries` 11 packages / 225 files / 832 specifiers, 0
violations; `npm run build` 4 built / 0 skipped;
`node fixtures/m2/contracts/tools/check-fixtures.mjs` 34/34 groups;
`node fixtures/m2/runtime/tools/check-runtime-fixtures.mjs` all checks pass.

**Acceptance:** PASS — phase order, duplicate writer, module order mismatch,
unsupported combination, throw-after-mutation fail-stop (only last committed
state observable, no resume), catch-up ≤8 with no phantom/duplicate sampling,
edge consumed once, idempotent teardown, restart-after-failure, M1 regression
(M1 sets and the demo). UNVERIFIED — browser/WebGL and real Rapier behavior
(packets 30–32); the port is exercised through a deterministic in-repo fake.

**Limitations:** "initialize cancellation" covers the runtime's own settle
pre-roll (cancellable by stop/dispose before the first frame); async
`createPhysicsPort(config, signal)` cancellation is packet 31.

**Contract-change requests (details in the manifest):** C29-1
`requiresPhysicsPort` spec field; C29-2 `inputSamples` ≠ `stepIndex` (pre-roll
samples nothing); C29-3 hard-coded platformer ID for the demo exclusion;
C29-4 `SimulationModule` name/`phases` optionality for frozen M1 compat;
C29-5 `ctx.state` write target; C29-6 M2-only diagnostics fields; C29-7
ownership error codes vs the §12.4 header; C29-8 O7 unreachable >1 controller;
C29-9 §12.0 promotion artifact.

**Next packet:** 30 — Keyboard and gamepad actions (not started).
