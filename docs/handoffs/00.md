# Handoff 00 — Repository intake and environment

Date: 2026-09-16 (UTC) · Executor: pi coding agent 0.85.0 session inside
pi-web 1.202608.2 on this server · Packet: 00 (planning only).

## Outcome

Planning workspace initialized. Environment measured and recorded; confirmed
decisions restated; TypeScript recorded as adopted-recommended (not a prior
explicit confirmation); minimal Node/browser/workspace stack proposed with
registry-verified versions; MCP transport to the external harness proposed
against the harness's documented (absence of built-in) MCP capability;
two-container shared-mount deployment proposed but not deployed. No editor
implementation exists. No dependencies installed. No version changed (owner
instruction respected).

## Files changed

Created:

- `AGENTS.md` — supplied persistent harness instruction verbatim (extracted
  from `docs/planning/implementation-prompts.md` lines 31–73) with a 4-line
  provenance header.
- `docs/STATUS.md` — packets 00–13 and gates A–D; 00 done, 01–13 pending.
- `docs/environment.md` — measured environment, verified versions, unknowns.
- `docs/decisions/0001-stack-and-deployment.md` — confirmed decisions,
  adopted defaults, proposed stack/deployment/MCP transport, open items.
- `docs/handoffs/00.md` — this file.

Copied (md5-verified identical; originals at the repo root preserved):

- `threejs-editor-charter-and-architecture-v0.1.md` → `docs/architecture/charter.md`
- `thirdlight-implementation-prompts-v0.1.md` → `docs/planning/implementation-prompts.md`

Note: `docs/architecture/charter.md` was absent at the documented path at the
start of packet 00; the charter content was present at the repo root and has
been read in full (16.9 KB). This is the "missing input" the packet asked to
report — resolved by copy, original untouched.

## Commands run and results (abridged; full outputs not reproduced)

- `uname -a`, `/etc/os-release`, `nproc`, `/proc/cpuinfo`, `free -h`,
  `df -h` → Ubuntu 26.04.1 LTS, kernel 7.0.2-2-pve (Proxmox guest), x86_64,
  i5-12600H (6 CPUs visible), 16 GiB RAM, 7.6 GiB free on `/`.
- Tool probes (`command -v` + `--version`) → node v22.22.1, npm 9.2.0,
  corepack 0.24.0, git 2.53.0, python3 3.14.4, make 4.4.1; no
  pnpm/yarn/bun/deno/docker/podman/docker-compose/ffmpeg.
- `npm ping` → PONG ~126 ms (registry reachable).
- `npm view` (metadata only, no installs) → typescript 7.0.2 latest /
  5.9.3 latest-5.x, three 0.186.0, @types/three 0.186.0,
  @modelcontextprotocol/sdk 1.30.0, esbuild 0.28.2, vitest 5.0.1, ws 8.21.3.
- `lspci`/`/dev/dri`/`nvidia-smi` → no usable GPU in this environment.
- Harness docs read (installed package): pi-coding-agent 0.85.0 README +
  `docs/usage.md` → "intentionally does not include built-in MCP";
  pi-web 1.202608.2 README + `docs/config.md` → per-user services,
  reverse-proxy deployment, allowed-hosts config.
- `npm search` (metadata) → MCP-client extension candidates for pi:
  pi-mcp-adapter@2.34.0, pi-mcp-extension@1.5.0 (not installed).
- No git operations, no package installs, no file deletions or moves
  (copies only).

## Acceptance criteria

| Criterion | Result |
|-----------|--------|
| Charter read; missing input reported if absent | passed — was absent at the documented path; read from repo root; copied to `docs/architecture/charter.md` (original preserved) |
| AGENTS.md from supplied instruction | passed — verbatim block + provenance header |
| docs/STATUS.md with packets 00–13, gates A–D, unrun tasks pending | passed |
| docs/environment.md | passed |
| docs/decisions/0001-stack-and-deployment.md | passed |
| Actual OS/CPU, Node/package-manager/container tools, workspace paths, repo structure determined | passed — see environment.md §1; docker/podman not visible inside the harness → host container tooling marked unknown |
| Host mount path not inferred from a container path; inaccessible host info marked unknown | passed — environment.md §3 |
| No environment variables or secrets printed | passed — none printed |
| Confirmed facts / proposed defaults / unknowns distinguishable | passed — STATUS.md, environment.md §1 vs §3, decision 0001 §1 vs §3 vs §8 |
| Confirmed decisions recorded (name, 2.5D side-scroller, desktop + keyboard/controller, self-hosting, shared working copy, standalone Web export) | passed — decision 0001 §1 |
| TypeScript recorded as recommended, not previously explicitly confirmed | passed — decision 0001 §2 |
| Minimal Node backend / browser UI / package workspace proposed | passed — decision 0001 §3–4 |
| Compatible toolchain versions from official sources, evidence + uncertainties recorded, no installs | passed — environment.md §2; TS line left to owner |
| No physics/advanced-graphics library selected | passed — decision 0001 §3, §9 |
| One active authoring session per project + external harness; MCP transport stated; harness capability verified, not guessed | passed — decision 0001 §5 (harness docs state no built-in MCP; extension-based client proposed) |
| Two-container shared-mount deployment proposed; nothing deployed | passed — decision 0001 §6 |
| Remote desktop-browser access documented; WebGPU secure-context need noted | passed — decision 0001 §7 |
| Existing files preserved | passed — the two root v0.1 documents untouched (md5-verified copies made) |
| No implementation begun | passed — no code, no installs, no services, no git init |

## Limitations

- Host-side facts (mount path, full core count, host container runtime, LAN
  topology) are unknown from inside this environment and must be confirmed by
  the owner before packet 13 (decision 0001 §8).
- TypeScript line is undecided (5.9.3 recommended vs 7.0.2 native); packet 04
  must not pin it before owner confirmation.
- npm 9.2.0 is older than the npm bundled with Node 22.x; used as-is.
- No git repository exists yet; handoff carries this file list instead of a
  commit ID.
- MCP extension choice and transport are proposals; packet 11 must validate
  against the chosen extension's official documentation.

## Contract-change requests

None.

## Next packet

01 — Project data contract. Not started, per gate discipline.

## Post-handoff revision (2026-09-16, owner)

Owner rulings supersede two proposals recorded above: (1) no container
deployment — the two-container proposal is withdrawn; deployment is
process-level inside this LXC; (2) game project data root — first stated as
`/etc/thirdlight/projects/<project-id>/`, then withdrawn the same day and
moved to `/home/dadmin/thirdlight/projects/<project-id>/` (user home) to
avoid root permissions; directory created empty, owner `dadmin`.
See decision 0001 §5–6 (revised).

Further owner decisions 2026-09-16:
- TypeScript **5.9.3** confirmed for M1 (decision 0001 §3/§8).
- `git init` + public GitHub remote for the engine repository confirmed;
  initial commit and push completed 2026-09-16 (commit ID recorded here:
  see `git log`). Game project data stays outside the repository.