# Packet 49 — Committed game flow and swept gameplay zones

**Outcome: done (logic scope).** The runtime now carries the committed M3 game
session — run state machine, run-boundary command consumption, the §2.5
effective-frame overrides, the committed `GameView`, bounded event ring and
M3 fail-stop — and the new pure `@thirdlight/platformer-game` package ships the
stateless `gameplay`-phase session module (`thirdlight.platformer-game:session`)
with the swept upright-capsule vs axis-aligned XY zone predicate over the
runtime's committed motion segments. Tests run the real controller + real
Rapier + real session module on a flat test course (happy path, fall death,
hazard death). The due reset is the **queued-request no-op seam** only: run-state
bookkeeping, events and the boundary are real; the physical re-placement
(R1–R8 over `PhysicsResetPort`) is packet 50. No completed respawn is claimed.

## Changed files (scoped diff, no commit)

- `packages/runtime/src/`: `types.ts` (M3 phase order, `SimulationPhaseModule`,
  M3 types incl. `GameSessionPort`/`GameView`, M3 `Runtime` API, diagnostics),
  `ports.ts` (`CharacterClearanceResult`, `PhysicsResetPort`),
  `snapshot.ts` (v3 scene + `game` block validation/projection),
  `game-session.ts` (new: the `GameSession` run state machine + view builder),
  `runtime.ts` (M3 composition validation, step pipeline, boundary,
  `getGameView`/`gameCommand`/`setViewport`, fail-stop T7, diagnostics),
  `intents.ts` (phase validation widened to M3 order), `index.ts` (exports),
  `registry.ts` (**CC-49-3** regex, see below), `m3-runtime.test.ts` (new, 12).
- `packages/platformer-game/` (new package: `session.ts`, `zones.ts`,
  `constants.ts`, `index.ts`, manifest/tsconfig).
- `tests/m3-gameplay/` (new: `course.test.ts` 3 real-Rapier course runs;
  `game-session.test.ts` 6 fixture-driven state-machine replays — moved out of
  `packages/runtime/src/` so Node built-ins stay out of the runtime package).
- `tools/check-boundaries.mjs` (+ its test): `platformer-game` unit row
  (types-only edge to `runtime`).
- `package-lock.json`: `npm install` re-run — the pre-existing
  `packages/platformer-game` lockfile entry was missing its `name` field, which
  `npm ls` flagged invalid; the entry now matches the workspace.
- `docs/STATUS.md` (this row).

## Commands and results

| Command | Exit | Result |
|---|---|---|
| `npm test` | 0 | **150 files / 1888 tests** (was 147/1866) |
| `npm run typecheck` | 0 | 16 packages |
| `npm run check-deps` | 0 | green |
| `npm run check-boundaries` | 0 | 16 pkgs / **303 files / 1185 specifiers**, 0 violations |
| `npm run build` | 0 | 4 built, 0 skipped |
| `node fixtures/m2/contracts/tools/check-fixtures.mjs` | 0 | **34/34** |
| M3 `contracts` | 0 | 39 groups |
| M3 `gameplay` | 0 | 23 groups / 127 checks |
| M3 `media` (+`--corrupt-control`) | 0 | 13/115; 9/9 detected |
| M3 `delivery` (+control) | 0 | 16/153; both corruptions exit 1 |
| M3 `storage` (+control) | 0 | 9/10; 6/6 detected |
| M3 `audit`/`promotion` (+controls) | 0 | all checks; corruptions rejected |
| M3 media `generate-fixtures.mjs --check` | 0 | 65 files reproduce exactly |
| M2 input/course/runtime/behaviors/physics checkers | 0 | all green |

M1/M2 frozen traces and M2/M3 fixture bytes are unchanged (no fixture file
modified this session; the moved `game-session.test.ts` only reads the promoted
`fixtures/m3/gameplay/run/**` fixtures).

## Acceptance (B04/B05/B06/B08 logic; no DOM, renderer or second movement solver)

- **B04 (logic): PARTIAL.** `awaitingStart` gating (no effective motion before
  Start, no phantom jump at the first live step — the `held-jump.json` table
  replayed through the real runtime), terminal `won` freeze, `replay` restart.
  Title image/controls/sound status and keyboard/gamepad start are 55/61–62.
- **B05 (logic): PASS.** Real Rapier + real M2 controller motion on the test
  course (flat rest 0.91, run-speed band, grounded); M2 traces byte-unchanged.
  Human traversal 61–62.
- **B06 (logic): PASS.** Fall and hazard deaths emit exactly one `died` event
  (cause + zone id), the swept §4.2 predicate catches the crossing at the
  committed step, precedence/ties per the promoted fixtures (23/127 checker
  groups + 6 fixture-driven state-machine replays against the real
  `GameSession`). Visible death demonstration 61.
- **B08 (logic): PASS.** Checkpoint activates once (re-crossing emits nothing),
  goal wins once and freezes play, `replay` clears checkpoint/deaths/goal and
  restarts (T6) — all asserted through the committed view on real physics.
  Images/inputs 55/61–62.
- **B09 (fail-stop, partial):** a `gameplay`-phase violation or port commit
  failure fail-stops with the last complete committed view retained (T7;
  `failure.phase` label), `tick()` stays `{ok:true}`, state `failed`.
  Full reset-phase failure matrix is 50/59/62.
- **UNVERIFIED (honest rows):** all visual/browser/hardware rows (title/checkpoint/goal
  images, physical inputs, audible cues, hardware GPU/display). No DOM, no
  renderer, no browser run this packet.

## Limitations

- The respawn boundary is bookkeeping-only (the no-op seam); with no physical
  reset the capsule re-deterministically dies where it died (asserted in
  course B/C as the expected packet-49 behavior).
- Camera math is the real packet-51 module; the tests use a test-only
  camera-phase module (owner write, viewport read, authored-depth invariant).
- `game-host` tool registration remains out of scope (packet 55).

## Contract-change requests (none applied to contracts; implementation-side resolution recorded)

- **CC-49-1:** the promoted `failure-phases.json` pins a `phase` on every
  failure record while the contract's `GameView.failure` is
  `{code, reason?, stepIndex}`. Implementation adds the optional `phase`
  label (additive).
- **CC-49-2:** `segment-source.json` pins `state.prev` during step n at the
  end of step n−2 (the accepted M2 step-end promotion timing) for M3 steps;
  the §3.2(a)/(b) reading (boundary promotes `prev := curr` after reset
  writes) would shift the streak. Resolution: M2 step-end promotion kept
  unchanged; the boundary performs no promotion (packet 50's reset writes
  land in the step's `curr`).
- **CC-49-3:** the promoted contracts pin the module IDs
  `thirdlight.platformer-game:session`/`:camera` (gameplay.md, runtime.md
  §12.1 inventory, delivery.md catalog, the package) while the carried M1
  registry name syntax `^thirdlight\.[a-z0-9]+:[a-z0-9-]+$` rejects them.
  The module ID is the specific pin; the registry regex's first segment was
  widened to `[a-z0-9-]+` (additive: every M1/M2 name still matches; no test
  pinned the rejection). Proposed contract diff: same widening in
  runtime.md §12.1.

**Next packet: 50 (runtime-owned safe respawn) — do not start it.**