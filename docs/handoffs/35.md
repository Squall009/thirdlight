# Packet 35 — Content-aware play delivery and bounded MCP input

Outcome: production isolated Play is composed from the immutable play build +
scoped locator, and the bounded MCP input relay runs through the real SDK.
Selection per decision 0002 §1, owner pre-approval. Owner pre-approval
(autonomous M2 build instruction, 2026-09-18); final manual review pending. No
commit.
## Scoped diff (no commit; the tree already carried uncommitted packets 20–34)

New: `packages/protocol/src/delivery.ts`;
`packages/backend/src/play-content.ts`(+test); `tests/integration/m2-play/**`;
`tests/browser/m2-play/README.md`; `docs/acceptance/evidence-m2/35/**`.
Modified: `packages/protocol/src/{index,ids,errors,bridge,ws-events}.ts`(+2
tests); `packages/backend/src/{backend,play,static.test}.ts`;
`packages/editor/src/preview/{bridge,preview-bootstrap}.ts`(+bridge test),
`packages/editor/src/session/client.ts`, `packages/editor/src/ui/App.tsx`,
`packages/editor/tsconfig.json`; `packages/mcp-adapter/src/{tools,backend-client}.ts`;
`packages/workspace/src/{content-store,types,service,index}.ts`;
`tools/check-boundaries.mjs`; `docs/STATUS.md` (packet-35 row only).

## Commands and actual results

`npm test` 129 files / 1672 passed (baseline 126/1641); `npm run typecheck` 15
packages exit 0; `npm run check-deps` exit 0; `npm run check-boundaries` 270
files / 1006 specifiers, 0 violations; `npm run build` 4 built / 0 skipped;
`node fixtures/m2/contracts/tools/check-fixtures.mjs` 34/34. Packet-35 suites:
`tests/integration/m2-play/play-delivery.test.ts` 13/13 (real backend child +
fs + HTTP on both origins + WS + real stdio MCP SDK);
`bundle-scan.test.ts` 1/1; `packages/backend/src/play-content.test.ts` 10/10.
Two M1 bridge tests, the §7 catalog test and the preview-template test were
updated to the v2 contract (sessions.md §13.5/§17.6).
Measured: preview bundle 4 522 584 B, patterns a/c/d/e/f/g/h/j =
0/0/6/0/3/0/38/3 (reference three core 3/0/3/26/3 re-derived; GLTFLoader ×37,
+12 `https://`; +1 inert Rapier `fetch(`); `three` revision 186.

## Acceptance

Passed (real process/FS/HTTP/WS/SDK): manifest/`buildId` identity, locator
routes + digests + TTL/grace/caps, traversal/listing/cross-capability rejection,
redaction, pins frozen through a mid-play publication, fresh play adopting the
new build, MCP relay provenance/limits/timeouts/no-simulated-success, repeated
start/stop + disconnect + restart, bundle credential/API scan.
**Unverified (no browser):** live browser Play (pinned GLB + behavior, WebGL,
ready/progress), course traversal, physical keyboard/gamepad + denied gamepad,
tab resume, and the rendered PNG — packet-35 substituted no placeholder image;
procedure in `tests/browser/m2-play/README.md` (packet 37).

## Contract-change requests

C35-1 `readSourceBlob`; C35-2 manifest behavior row needs
declaration/ownedTransforms/requiredModules; C35-3 §4.1/§4.2 preview-entry
edges + locator dynamic import; C35-4 behavior-source preparation route; C35-5
scene `schemaVersion` + `content.settings` absent from the snapshot/bridge;
C35-6 preview `fetch(` rule; C35-7 Rapier §5.4.1 row; C35-8
`input.request`/`input.result` absent from §7. Diffs in the evidence manifest.

## Scope exception (recorded)

Three edits fall outside the may-edit list: `packages/workspace` (one additive,
verified, digest-addressed public read — C35-1), `tools/check-boundaries.mjs`
(§4.2 preview entry + one recorded computed-dynamic-import exception — C35-3)
and `packages/editor/tsconfig.json` (`esnext.disposable`, for the Rapier d.ts).

## Next

Packet 36 (Standalone M2 content/gameplay export) — not started.
