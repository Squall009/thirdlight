/**
 * GENERATED by tools/gen-behavior-api.mjs from @thirdlight/runtime's
 * BehaviorContext (phase 16.3). Do not edit: change the runtime types and run
 * `node tools/gen-behavior-api.mjs` (a unit test fails when this file drifts).
 */
import type { BehaviorApiMember } from './behavior-api-types';

/** The behavior API typings (`declare module '@thirdlight/runtime'`). */
export const BEHAVIOR_API_DTS: string = "// The Thirdlight behavior API (generated from @thirdlight/runtime; read-only).\n// In a script: import type { BehaviorContext } from '@thirdlight/runtime';\ndeclare module '@thirdlight/runtime' {\n  /** What a behavior's `step(state, ctx)` receives each fixed step (once per phase it runs in). */\n  export interface BehaviorContext {\n    /**\n     * The behavior's id.\n     * @graphNode Script id\n     */\n    readonly behaviorId: string;\n    /**\n     * The entity carrying this script instance.\n     * @graphNode This object\n     */\n    readonly entityId: string;\n    /** The fixed step counter of the run. */\n    readonly stepIndex: number;\n    /** The phase being stepped: `'intent'`, then `'transform'` (only with owned transforms). */\n    readonly phase: SimulationPhase;\n    /** The instance's property values (declaration defaults with the object's overrides). */\n    readonly properties: BehaviorProperties;\n    /** The step's sampled input frame (the same for every phase of the step). */\n    readonly action: ActionFrame;\n    /** The intents committed so far in this step. */\n    readonly intents: IntentSet;\n    /** The game's gameplay settings. */\n    readonly settings: Readonly<GameplaySettings>;\n    /** Physics queries for this step. */\n    readonly physics: PhysicsStepClient;\n    /** Find entities by tag. */\n    readonly tags: BehaviorTagQuery;\n    /** Read-only positions of the loaded entities. */\n    readonly world: BehaviorWorldView;\n    /** Load and switch scenes (projects with a scene catalog). */\n    readonly scenes?: BehaviorSceneControl;\n    /** The step's input actions by name. */\n    readonly input: BehaviorInputView;\n    /** An entity's animator (`ctx.animator(id)?.set('speed', 1)`), or null when it has none. */\n    readonly animator?: (entityId: string) => BehaviorAnimatorHandle | null;\n    /**\n     * Last step's clip events and the enter/exit events of the triggers this instance owns.\n     * @graphNode skip the event nodes (On trigger, On animator event) read them one by one\n     */\n    readonly events?: readonly (AnimatorEventRecord | TriggerEventRecord)[];\n    /** Named timers of this instance, counted in fixed steps. */\n    readonly timers: BehaviorTimers;\n    /** Signals (seen one step after they are emitted). */\n    readonly signals?: BehaviorSignals;\n    /** Phase 19.1: messages to other scripts, with a value (seen one step after they are sent). */\n    readonly messages?: BehaviorMessages;\n    /** The run's counters, the player's health and object visibility. */\n    readonly game?: BehaviorGameState;\n    /** Play sounds (presentation only, never part of the simulation). */\n    readonly audio?: BehaviorAudio;\n    /** Values kept in the player's save. */\n    readonly save?: BehaviorSave;\n    /**\n     * Copy a project prefab into the running game; returns the new root id (or null at an engine limit).\n     * @graphNode Spawn prefab\n     * @graphLabel prefabId prefab\n     */\n    readonly spawn?: (\n      prefabId: string,\n      options: {\n        /** Where the copy's root goes: [x, y] (keeping the root's authored z) or [x, y, z]. */\n        position: readonly number[];\n        /**\n         * The root's rotation as a quaternion [x, y, z, w].\n         * @graphType list\n         */\n        rotation?: readonly number[];\n        /** The root's scale: one number or [x, y, z]. */\n        scale?: number | readonly number[];\n      },\n    ) => string | null;\n    /**\n     * Remove a spawned entity at the next step boundary.\n     * @graphNode Destroy spawned\n     */\n    readonly destroy?: (entityId: string) => boolean;\n    /** Commit one intent (a transform/pose of an owned entity, or a gameplay intent). */\n    emit(intent: BehaviorIntent): void;\n    /**\n     * Write to the play log (`'info' | 'warn' | 'error'`).\n     * @graphNode skip the Log node (debug.log) writes any value as text\n     */\n    log(level: BehaviorLogLevel, message: string): void;\n  }\n\n  /**\n   * A behavior module's `export default`: only `step` is required; it must be\n   * synchronous and return nothing. State is per entity (from `instantiate`).\n   */\n  export interface BehaviorSpec<State = unknown, Prepared = unknown> {\n    prepare?(cfg: BehaviorPrepareConfig): Prepared;\n    instantiate?(prepared: Prepared, inst: BehaviorInstanceInfo): State;\n    step(state: State, ctx: BehaviorContext): void;\n    dispose?(prepared: Prepared, state: State): void;\n  }\n\n  /** What `prepare(cfg)` receives (once per run, before any instance). */\n  export interface BehaviorPrepareConfig {\n    readonly behaviorId: string;\n    readonly sourceDigest: string;\n    readonly declaration: PropertyDeclaration;\n    readonly enginePins: readonly BehaviorEnginePin[];\n  }\n\n  /** What `instantiate(prepared, inst)` receives (once per entity carrying the behavior). */\n  export interface BehaviorInstanceInfo {\n    readonly entityId: string;\n    readonly properties: BehaviorProperties;\n    readonly tags: BehaviorTagQuery;\n  }\n\n  /**\n   * The canonical simulation phase order (runtime.md §12.1; M3 adds the two\n   * appended values `gameplay`/`camera` — `gameplay.md` §3.1). Every accepted\n   * M2 phase list stays a prefix of this order and remains valid unchanged.\n   */\n  export type SimulationPhase = 'intent' | 'controller' | 'transform' | 'gameplay' | 'camera';\n\n  /** The declared-property value map fed to one behavior instance (§14.3). */\n  export type BehaviorProperties = Readonly<Record<string, PropertyValue>>;\n\n  /** One quantized, self-describing action frame (runtime.md §12.5). */\n  export interface ActionFrame {\n    /** Integer, `0 ≤ v ≤ 2^53−1` — the executed fixed-step index. */\n    stepIndex: number;\n    /** Finite, `−1 ≤ v ≤ 1`, quantized to 1e-4 (`round(v·1e4)/1e4`). */\n    moveX: number;\n    jump: JumpPhase;\n    /**\n     * Phase 9.8, optional: every named input action this step — `v` its value\n     * (a button 0/1, an axis −1..1 after its processors), `x`/`y` for a 2D\n     * axis, `p` the button phase. Absent: only move and jump exist.\n     */\n    actions?: Readonly<Record<string, ActionValue>>;\n  }\n\n  /** The runtime's per-step intent set (runtime.md §14.5). */\n  export interface IntentSet {\n    readonly stepIndex: number;\n    /** The committed `control_move` (quantized) or `null`. */\n    readonly move: number | null;\n    /** The committed `control_jump` or `null`. */\n    readonly jump: JumpPhase | null;\n    /** The module ID that committed `move`, or `null`. */\n    readonly moveWriter: string | null;\n    readonly jumpWriter: string | null;\n    /** Committed transform writes, in commit order. */\n    readonly transformWrites: readonly IntentTransformWrite[];\n    /** Phase 9.9: an upward speed the runtime gives the controller this step (a stomp or a hit). */\n    readonly bounce?: number;\n  }\n\n  /** The six-key M2 gameplay settings registry value (§21.4/§21.5). */\n  export interface GameplaySettings {\n    gravity_y: number;\n    run_speed: number;\n    jump_velocity: number;\n    max_fall_speed: number;\n    max_slope_climb_deg: number;\n    min_slope_slide_deg: number;\n    /** Phase 15.3: engine settings, present only when the project sets them (absent: the engine default). */\n    fixed_step_hz?: number;\n    audio_voices?: number;\n    music_fade_s?: number;\n    animation_crossfade_s?: number;\n    /** Phase 17.1: the renderer backend (0 WebGL legacy, 1 auto, 2 WebGPU, 3 WebGL 2; absent: 0). */\n    render_backend?: number;\n  }\n\n  /** The restricted view handed to modules in `StepContext` (physics.md §5). */\n  export interface PhysicsStepClient {\n    /**\n     * Controller phase only; a second stage for one entity is `duplicate_move`.\n     * @graphNode skip scripts never run in the controller phase\n     */\n    stageCharacterMove(entityId: string, delta: Vec2): void;\n    /**\n     * The last completed step's result, or `undefined` before the first step.\n     * @graphPure\n     * @graphNode Character result\n     */\n    characterResult(entityId: string): CharacterMoveResult | undefined;\n    /**\n     * Phase 9.9: a ray against the level's colliders (bounded per step; null when nothing is hit).\n     * @graphNode Raycast\n     * @graphDefault direction [1, 0, 0]\n     * @graphDefault maxDistance 10\n     */\n    raycast?(origin: Vec2, direction: Vec2, maxDistance: number): RaycastHit | null;\n    /**\n     * Phase 9.9: the entities whose colliders overlap a box (center, half extents) — counted with the rays.\n     * @graphNode Overlap box\n     * @graphDefault half [0.5, 0.5, 0]\n     */\n    overlapBox?(center: Vec2, half: Vec2): string[];\n    /**\n     * Phase 9.9: the entities whose colliders overlap a circle — counted with the rays.\n     * @graphNode Overlap circle\n     * @graphDefault radius 0.5\n     */\n    overlapCircle?(center: Vec2, radius: number): string[];\n  }\n\n  /**\n   * Phase 12 (b): what a behavior script can ask about tags (`ctx.tags`, in\n   * `instantiate` and every `step`). Built once when the scene loads from the\n   * effective masks (own mask OR every folder above's); calls never allocate\n   * per frame beyond the first query of a given mask.\n   */\n  export interface BehaviorTagQuery {\n    /**\n     * The mask of the named tags (names ignore case). Throws on an unknown name.\n     * @graphPure\n     * @graphNode Tag mask\n     * @graphLabel names tags (comma separated)\n     */\n    mask(...names: string[]): number;\n    /**\n     * The entity's effective tag mask (0 for an unknown entity).\n     * @graphPure\n     * @graphNode Tags of\n     */\n    of(entityId: string): number;\n    /**\n     * Whether the entity carries any (default) or all of the mask's bits.\n     * @graphPure\n     * @graphNode Has tags\n     */\n    has(entityId: string, mask: number, match?: 'any' | 'all'): boolean;\n    /**\n     * The entities carrying any (default) or all of the mask's bits, in scene order.\n     * @graphPure\n     * @graphNode Find by tags\n     */\n    query(mask: number, match?: 'any' | 'all'): readonly string[];\n  }\n\n  /** Phase 12 (c): a read-only view of entity transforms (`ctx.world`). */\n  export interface BehaviorWorldView {\n    /**\n     * The entity's current transform (this step so far), or `undefined` when it is not loaded.\n     * @graphPure\n     * @graphNode Transform of\n     */\n    transform(entityId: string): Readonly<{ position: readonly [number, number, number]; rotation: readonly [number, number, number, number]; scale: readonly [number, number, number] }> | undefined;\n  }\n\n  /** Phase 12 (c): the scene API a behavior script reaches as `ctx.scenes`. */\n  export interface BehaviorSceneControl {\n    /**\n     * Request a load; it completes at a later step boundary. Loading or loaded ⇒ no-op.\n     * @graphNode Load scene\n     */\n    load(sceneId: string, options?: SceneLoadOptions): void;\n    /**\n     * Request an unload at the next step boundary. Unloaded ⇒ no-op.\n     * @graphNode Unload scene\n     */\n    unload(sceneId: string): void;\n    /**\n     * Where a scene is in its load cycle.\n     * @graphPure\n     * @graphNode Scene status\n     */\n    status(sceneId: string): SceneStatus;\n    /**\n     * The loaded scene ids, in load order.\n     * @graphPure\n     * @graphNode Loaded scenes\n     */\n    loaded(): readonly string[];\n  }\n\n  /**\n   * Phase 9.8: `ctx.input` — the step's input actions by name. Without named\n   * actions in the frame, `move` and `jump` still answer from the frame.\n   */\n  export interface BehaviorInputView {\n    /**\n     * A button 0/1, a 1D axis −1..1, a 2D axis's length; 0 for an unknown name.\n     * @graphPure\n     * @graphNode Input value\n     * @graphLabel name action\n     */\n    value(name: string): number;\n    /**\n     * A 2D axis as [x, y] ([value, 0] for others).\n     * @graphPure\n     * @graphNode Input vector\n     * @graphLabel name action\n     */\n    vector(name: string): [number, number];\n    /**\n     * Pressed in this step.\n     * @graphPure\n     * @graphNode Input pressed\n     * @graphLabel name action\n     */\n    pressed(name: string): boolean;\n    /**\n     * Released in this step.\n     * @graphPure\n     * @graphNode Input released\n     * @graphLabel name action\n     */\n    released(name: string): boolean;\n    /**\n     * Down this step (pressed or held).\n     * @graphPure\n     * @graphNode Input held\n     * @graphLabel name action\n     */\n    held(name: string): boolean;\n  }\n\n  /** Phase 9.7: one entity's animator, as a script sees it. */\n  export interface BehaviorAnimatorHandle {\n    /**\n     * Set a float/int/bool parameter; false for an unknown name or a wrong type.\n     * @graphNode Set animator parameter\n     * @graphLabel name parameter\n     */\n    set(name: string, value: number | boolean): boolean;\n    /**\n     * Set a trigger (it resets when a transition uses it).\n     * @graphNode Set animator trigger\n     * @graphLabel name trigger\n     */\n    trigger(name: string): boolean;\n    /**\n     * A parameter's value (undefined for an unknown name).\n     * @graphPure\n     * @graphNode Animator parameter\n     * @graphLabel name parameter\n     */\n    get(name: string): number | boolean | undefined;\n    /**\n     * The current state's name (of the base layer, or of override layer `layer` — 1 is the first; phase 14.6).\n     * @graphPure\n     * @graphNode Animator state\n     */\n    state(layer?: number): string;\n  }\n\n  /** Phase 9.7: a clip event an animator passed. */\n  export interface AnimatorEventRecord {\n    readonly entityId: string;\n    readonly name: string;\n    readonly clip: string;\n    /** The step in which the clip passed the event. */\n    readonly stepIndex: number;\n  }\n\n  /**\n   * Phase 14.2: one `ctx.events` entry for a trigger a script owns — the player\n   * entered (`enter`) or left (`exit`) it in `stepIndex` (scripts see it in the\n   * next step, like signals). A script owns the triggers on its own entity, on\n   * the entity's descendants, and those named by its entityRef properties.\n   */\n  export interface TriggerEventRecord {\n    readonly type: 'enter' | 'exit';\n    /** The trigger's entity id. */\n    readonly trigger: string;\n    readonly stepIndex: number;\n  }\n\n  /**\n   * Phase 14.2: `ctx.timers` — named timers of one script instance, counted in\n   * fixed steps (deterministic: `seconds × fixedStepHz` rounded, at least one\n   * step). At most `MAX_TIMERS_PER_INSTANCE` (64) run per instance; a new run\n   * (start, replay, a level switch) clears them.\n   */\n  export interface BehaviorTimers {\n    /**\n     * Fire once, `seconds` from this step. Returns `false` (and changes\n     * nothing) when a one-shot timer of that name and length is already\n     * running — a script may call it every step; a different length restarts it.\n     * @graphNode Start timer\n     * @graphLabel name timer\n     * @graphDefault seconds 1\n     */\n    after(name: string, seconds: number): boolean;\n    /**\n     * Fire every `seconds`, the first time `seconds` from this step. Returns\n     * `false` (and changes nothing) when a repeating timer of that name and\n     * period is already running.\n     * @graphNode Start repeating timer\n     * @graphLabel name timer\n     * @graphDefault seconds 1\n     */\n    every(name: string, seconds: number): boolean;\n    /**\n     * True in the step the timer fires (in every phase of that step).\n     * @graphPure\n     * @graphNode Timer fired\n     * @graphLabel name timer\n     */\n    fired(name: string): boolean;\n    /**\n     * Stop a timer; `false` when none of that name was running.\n     * @graphNode Cancel timer\n     * @graphLabel name timer\n     */\n    cancel(name: string): boolean;\n  }\n\n  /** Phase 9.9: `ctx.signals`. */\n  export interface BehaviorSignals {\n    /**\n     * Send a named signal; switches, doors and scripts see it in the next step.\n     * @graphNode Emit signal\n     * @graphLabel name signal\n     */\n    emit(name: string): void;\n    /**\n     * Emitted in the previous step (by a switch, a trigger or a script).\n     * @graphPure\n     * @graphNode Signal received\n     * @graphLabel name signal\n     */\n    on(name: string): boolean;\n  }\n\n  /**\n   * Phase 19.1: `ctx.messages` — named messages between scripts with an\n   * optional value, to every script or to the scripts of one entity. Like\n   * signals, a message sent in a step is seen in the next step (in send order);\n   * at most `MAX_MESSAGES_PER_STEP` (256) are sent per step and a new run\n   * clears them.\n   */\n  export interface BehaviorMessages {\n    /**\n     * Send a message (name: 1–64 letters, digits or _ . : -) with an optional\n     * value (a number, text of at most 256 characters or true/false) to every\n     * script, or only to the scripts on entity `target`. `false` when it is\n     * refused (a bad name or value, or the step's limit).\n     * @graphNode Send message\n     * @graphLabel name message\n     * @graphLabel target to entity (empty: every script)\n     */\n    send(name: string, value?: number | string | boolean, target?: string): boolean;\n    /**\n     * The messages of that name sent in the previous step to every script or to this entity, in send order.\n     * @graphPure\n     * @graphNode Messages received\n     * @graphLabel name message\n     */\n    received(name: string): readonly BehaviorMessage[];\n  }\n\n  /** Phase 9.9: `ctx.game`. */\n  export interface BehaviorGameState {\n    /**\n     * The current value of one of the run's counters (0 when it was never added to).\n     * @graphPure\n     * @graphNode Counter value\n     * @graphLabel name counter\n     */\n    counter(name: string): number;\n    /**\n     * Add to one of the run's counters (coins, keys, anything you name); the HUD and score rules read them.\n     * @graphNode Add to counter\n     * @graphLabel name counter\n     * @graphDefault amount 1\n     */\n    add(name: string, amount: number): void;\n    /**\n     * The player's health, or null when the game has none.\n     * @graphPure\n     * @graphNode Player health\n     */\n    health(): { current: number; max: number } | null;\n    /**\n     * Show or hide an entity (and its children) until the next run; it still collides and triggers.\n     * @graphNode Set visible\n     * @graphDefault visible true\n     */\n    setVisible(entityId: string, visible: boolean): void;\n  }\n\n  /** Phase 9.10: `ctx.audio`. */\n  export interface BehaviorAudio {\n    /**\n     * Play an audio asset once (volume 0–1).\n     * @graphNode Play sound\n     * @graphLabel assetId sound\n     * @graphAsset assetId audio\n     * @graphDefault volume 1\n     */\n    play(assetId: string, options?: { volume?: number }): void;\n  }\n\n  /** Phase 9.11: `ctx.save` — values a script keeps in the player's save (≤ 64 keys, ≤ 4 KB each as JSON). */\n  export interface BehaviorSave {\n    /**\n     * The value kept under `key` (undefined when there is none).\n     * @graphPure\n     * @graphNode Saved value\n     */\n    get(key: string): unknown;\n    /**\n     * Keep a value under `key`; `false` when it does not fit (a bad key, 64 keys, 4 KB).\n     * @graphNode Save value\n     */\n    set(key: string, value: unknown): boolean;\n    /**\n     * Forget the value under `key`.\n     * @graphNode Remove saved value\n     */\n    remove(key: string): void;\n    /**\n     * The keys of the kept values.\n     * @graphPure\n     * @graphNode Saved keys\n     */\n    keys(): string[];\n  }\n\n  export type BehaviorIntent = ControlMoveIntent | ControlJumpIntent | TransformIntent | PoseIntent | RespawnIntent;\n\n  /** The `ctx.log` levels (runtime.md §14.3). */\n  export type BehaviorLogLevel = 'info' | 'warn' | 'error';\n\n  export interface PropertyDeclaration {\n    properties: DeclaredProperty[];\n  }\n\n  /** One compiled engine pin the manifest carries (read-only here). */\n  export interface BehaviorEnginePin {\n    id: string;\n    version: string;\n    apiVersion: number;\n  }\n\n  /** Seven-type property value vocabulary (§20.5). */\n  export type PropertyValue =\n    | number\n    | boolean\n    | string\n    | [number, number, number]\n    | null;\n\n  /** The four jump phases (input.md §2). */\n  export type JumpPhase = 'none' | 'pressed' | 'held' | 'released';\n\n  /** Phase 9.8: one input action's value in a step. */\n  export interface ActionValue {\n    readonly v: number;\n    readonly x?: number;\n    readonly y?: number;\n    readonly p: JumpPhase;\n  }\n\n  /** One committed transform write in commit order (§14.5). */\n  export interface IntentTransformWrite {\n    moduleId: string;\n    entityId: string;\n    position: { x?: number; y?: number; z?: number };\n  }\n\n  /**\n   * The injected physics port — runtime.md §12.6 (promoted from `physics.md`\n   * §5). The concrete adapter (`physics-rapier`, packet 31) implements this\n   * surface; the runtime core never imports it. The runtime owns the instance\n   * and hands modules only the restricted `PhysicsStepClient`.\n   *\n   * These are pure types plus the runtime's strict result validation: no\n   * concrete physics library, no Node built-ins, no I/O.\n   */\n  export interface Vec2 {\n    x: number;\n    y: number;\n  }\n\n  /** The port's per-step character result (physics.md §5). */\n  export interface CharacterMoveResult {\n    requested: Vec2;\n    applied: Vec2;\n    position: Vec2;\n    grounded: boolean;\n    supportNormal: Vec2;\n    contacts: { ground: boolean; wall: boolean; head: boolean; steepSlope: boolean };\n    snapped: boolean;\n    /** Phase 9.9: the collider entity the character stands on (grounded), when the port knows it. */\n    groundEntityId?: string | null;\n    /** Phase 9.13: how far a moving (kinematic) body moved into the character this step; the correction may exceed the request by this much. */\n    kinematicSlack?: number;\n  }\n\n  /** Phase 9.9: a ray hit (`raycast`). */\n  export interface RaycastHit {\n    entityId: string;\n    distance: number;\n    normal: Vec2;\n  }\n\n  /**\n   * Phase 12 (c): options of one scene load. `at` offsets the scene's root\n   * entities (world units). Loads are keyed by scene id today; the batch shape\n   * leaves room for keyed, repeated (instanced) loads of one scene later.\n   */\n  export interface SceneLoadOptions {\n    at?: readonly [number, number, number];\n  }\n\n  /** Phase 12 (c): where a scene is in its load cycle. */\n  export type SceneStatus = 'unloaded' | 'loading' | 'loaded';\n\n  /** Phase 19.1: one message a script sent (`ctx.messages`). */\n  export interface BehaviorMessage {\n    readonly name: string;\n    /** The payload (a number, text or true/false), or null when none was sent. */\n    readonly value: number | string | boolean | null;\n    /** The entity whose script sent it. */\n    readonly from: string;\n    /** The step it was sent in (scripts see it in the next step). */\n    readonly stepIndex: number;\n  }\n\n  /**\n   * `−1 ≤ value ≤ 1`, quantized at commit (§14.4).\n   * @graphNode Control move\n   * @graphPhase intent\n   */\n  export interface ControlMoveIntent {\n    kind: 'control_move';\n    value: number;\n  }\n\n  /**\n   * One `JumpPhase` value (§14.4).\n   * @graphNode Control jump\n   * @graphPhase intent\n   */\n  export interface ControlJumpIntent {\n    kind: 'control_jump';\n    value: JumpPhase;\n  }\n\n  /**\n   * A position write on ONE owned entity axis set (§14.4/§14.6).\n   * @graphNode Move object\n   * @graphPhase transform\n   */\n  export interface TransformIntent {\n    kind: 'transform';\n    entityId: string;\n    position: { x?: number; y?: number; z?: number };\n  }\n\n  /**\n   * Phase 9.9 (wrap-up): an owned entity's rotation (degrees: yaw about +Y,\n   * pitch about +X, roll about +Z, applied yaw · pitch · roll; missing axes are\n   * 0) and/or scale (one number, or [x, y, z]) — transform phase only, visual\n   * (colliders keep their shape). Fields in the order kind, entityId,\n   * rotation, scale; at least one of rotation and scale.\n   * @graphNode Pose object\n   * @graphPhase transform\n   */\n  export interface PoseIntent {\n    kind: 'pose';\n    entityId: string;\n    rotation?: { yaw?: number; pitch?: number; roll?: number };\n    scale?: number | [number, number, number];\n  }\n\n  /**\n   * Phase 12 (c): kill the player (intent phase; ignored unless the run is playing).\n   * @graphNode Respawn player\n   * @graphPhase intent\n   */\n  export interface RespawnIntent {\n    kind: 'respawn';\n  }\n\n  export interface DeclaredProperty {\n    key: string;\n    label: string;\n    type: PropertyType;\n    default: PropertyValue;\n    min?: number;\n    max?: number;\n    step?: number;\n    maxLength?: number;\n    values?: string[];\n    bounds?: PropertyBounds;\n    /**\n     * Phase 15.4: who may see and set the property. `public` (the default; the\n     * canonical form omits it) is shown in the Inspector of every object\n     * carrying the behavior and overridable per object; `private` is neither\n     * shown nor overridable — the script always reads `default`.\n     */\n    visibility?: PropertyVisibility;\n    /** Phase 15.4: the Inspector section the property is listed in (1–64 characters). */\n    group?: string;\n    /** Phase 15.4: a heading shown above the property in the Inspector (1–64 characters). */\n    header?: string;\n    /** Phase 15.4: the hover help of the property's field (1–256 characters). */\n    tooltip?: string;\n  }\n\n  export type PropertyType =\n    | 'number'\n    | 'boolean'\n    | 'string'\n    | 'enum'\n    | 'vec3'\n    | 'entityRef'\n    | 'assetRef';\n\n  export interface PropertyBounds {\n    min: Vec3;\n    max: Vec3;\n  }\n\n  /** Phase 15.4: declared-property visibility (Unity-like public/private). */\n  export type PropertyVisibility = 'public' | 'private';\n\n  /** Three finite numbers (meters), canonical order per §10. */\n  export type Vec3 = [number, number, number];\n}\n";

/** Type key → members (completion walks `type` / `returns` keys). */
export const BEHAVIOR_API_TYPES: Readonly<Record<string, readonly BehaviorApiMember[]>> = {
  "ActionFrame": [
    {
      "name": "stepIndex",
      "kind": "property",
      "detail": "number",
      "doc": "Integer, `0 ≤ v ≤ 2^53−1` — the executed fixed-step index."
    },
    {
      "name": "moveX",
      "kind": "property",
      "detail": "number",
      "doc": "Finite, `−1 ≤ v ≤ 1`, quantized to 1e-4 (`round(v·1e4)/1e4`)."
    },
    {
      "name": "jump",
      "kind": "property",
      "detail": "JumpPhase"
    },
    {
      "name": "actions",
      "kind": "property",
      "detail": "Readonly<Record<string, ActionValue>> | undefined",
      "optional": true,
      "doc": "Phase 9.8, optional: every named input action this step — `v` its value (a button 0/1, an axis −1..1 after its processors), `x`/`y` for a 2D axis, `p` the button phase. Absent: only move and jump exist."
    }
  ],
  "BehaviorAnimatorHandle": [
    {
      "name": "set",
      "kind": "method",
      "detail": "(name: string, value: number | boolean): boolean",
      "doc": "Set a float/int/bool parameter; false for an unknown name or a wrong type."
    },
    {
      "name": "trigger",
      "kind": "method",
      "detail": "(name: string): boolean",
      "doc": "Set a trigger (it resets when a transition uses it)."
    },
    {
      "name": "get",
      "kind": "method",
      "detail": "(name: string): number | boolean | undefined",
      "doc": "A parameter's value (undefined for an unknown name)."
    },
    {
      "name": "state",
      "kind": "method",
      "detail": "(layer?: number | undefined): string",
      "doc": "The current state's name (of the base layer, or of override layer `layer` — 1 is the first; phase 14.6)."
    }
  ],
  "BehaviorAudio": [
    {
      "name": "play",
      "kind": "method",
      "detail": "(assetId: string, options?: { volume?: number | undefined; } | undefined): void",
      "doc": "Play an audio asset once (volume 0–1)."
    }
  ],
  "BehaviorContext": [
    {
      "name": "behaviorId",
      "kind": "property",
      "detail": "string",
      "doc": "The behavior's id."
    },
    {
      "name": "entityId",
      "kind": "property",
      "detail": "string",
      "doc": "The entity carrying this script instance."
    },
    {
      "name": "stepIndex",
      "kind": "property",
      "detail": "number",
      "doc": "The fixed step counter of the run."
    },
    {
      "name": "phase",
      "kind": "property",
      "detail": "SimulationPhase",
      "doc": "The phase being stepped: `'intent'`, then `'transform'` (only with owned transforms)."
    },
    {
      "name": "properties",
      "kind": "property",
      "detail": "Readonly<Record<string, PropertyValue>>",
      "doc": "The instance's property values (declaration defaults with the object's overrides)."
    },
    {
      "name": "action",
      "kind": "property",
      "detail": "ActionFrame",
      "type": "ActionFrame",
      "doc": "The step's sampled input frame (the same for every phase of the step)."
    },
    {
      "name": "intents",
      "kind": "property",
      "detail": "IntentSet",
      "type": "IntentSet",
      "doc": "The intents committed so far in this step."
    },
    {
      "name": "settings",
      "kind": "property",
      "detail": "Readonly<GameplaySettings>",
      "type": "Readonly<GameplaySettings>",
      "doc": "The game's gameplay settings."
    },
    {
      "name": "physics",
      "kind": "property",
      "detail": "PhysicsStepClient",
      "type": "PhysicsStepClient",
      "doc": "Physics queries for this step."
    },
    {
      "name": "tags",
      "kind": "property",
      "detail": "BehaviorTagQuery",
      "type": "BehaviorTagQuery",
      "doc": "Find entities by tag."
    },
    {
      "name": "world",
      "kind": "property",
      "detail": "BehaviorWorldView",
      "type": "BehaviorWorldView",
      "doc": "Read-only positions of the loaded entities."
    },
    {
      "name": "scenes",
      "kind": "property",
      "detail": "BehaviorSceneControl | undefined",
      "type": "BehaviorSceneControl",
      "optional": true,
      "doc": "Load and switch scenes (projects with a scene catalog)."
    },
    {
      "name": "input",
      "kind": "property",
      "detail": "BehaviorInputView",
      "type": "BehaviorInputView",
      "doc": "The step's input actions by name."
    },
    {
      "name": "animator",
      "kind": "method",
      "detail": "(entityId: string): BehaviorAnimatorHandle | null",
      "returns": "BehaviorAnimatorHandle",
      "optional": true,
      "doc": "An entity's animator (`ctx.animator(id)?.set('speed', 1)`), or null when it has none."
    },
    {
      "name": "events",
      "kind": "property",
      "detail": "readonly (AnimatorEventRecord | TriggerEventRecord)[] | undefined",
      "optional": true,
      "doc": "Last step's clip events and the enter/exit events of the triggers this instance owns."
    },
    {
      "name": "timers",
      "kind": "property",
      "detail": "BehaviorTimers",
      "type": "BehaviorTimers",
      "doc": "Named timers of this instance, counted in fixed steps."
    },
    {
      "name": "signals",
      "kind": "property",
      "detail": "BehaviorSignals | undefined",
      "type": "BehaviorSignals",
      "optional": true,
      "doc": "Signals (seen one step after they are emitted)."
    },
    {
      "name": "messages",
      "kind": "property",
      "detail": "BehaviorMessages | undefined",
      "type": "BehaviorMessages",
      "optional": true,
      "doc": "Phase 19.1: messages to other scripts, with a value (seen one step after they are sent)."
    },
    {
      "name": "game",
      "kind": "property",
      "detail": "BehaviorGameState | undefined",
      "type": "BehaviorGameState",
      "optional": true,
      "doc": "The run's counters, the player's health and object visibility."
    },
    {
      "name": "audio",
      "kind": "property",
      "detail": "BehaviorAudio | undefined",
      "type": "BehaviorAudio",
      "optional": true,
      "doc": "Play sounds (presentation only, never part of the simulation)."
    },
    {
      "name": "save",
      "kind": "property",
      "detail": "BehaviorSave | undefined",
      "type": "BehaviorSave",
      "optional": true,
      "doc": "Values kept in the player's save."
    },
    {
      "name": "spawn",
      "kind": "method",
      "detail": "(prefabId: string, options: { position: readonly number[]; rotation?: readonly number[] | undefined; scale?: number | readonly number[] | undefined; }): string | null",
      "optional": true,
      "doc": "Copy a project prefab into the running game; returns the new root id (or null at an engine limit)."
    },
    {
      "name": "destroy",
      "kind": "method",
      "detail": "(entityId: string): boolean",
      "optional": true,
      "doc": "Remove a spawned entity at the next step boundary."
    },
    {
      "name": "emit",
      "kind": "method",
      "detail": "(intent: BehaviorIntent): void",
      "doc": "Commit one intent (a transform/pose of an owned entity, or a gameplay intent)."
    },
    {
      "name": "log",
      "kind": "method",
      "detail": "(level: BehaviorLogLevel, message: string): void",
      "doc": "Write to the play log (`'info' | 'warn' | 'error'`)."
    }
  ],
  "BehaviorGameState": [
    {
      "name": "counter",
      "kind": "method",
      "detail": "(name: string): number",
      "doc": "The current value of one of the run's counters (0 when it was never added to)."
    },
    {
      "name": "add",
      "kind": "method",
      "detail": "(name: string, amount: number): void",
      "doc": "Add to one of the run's counters (coins, keys, anything you name); the HUD and score rules read them."
    },
    {
      "name": "health",
      "kind": "method",
      "detail": "(): { current: number; max: number; } | null",
      "returns": "BehaviorGameState.health()",
      "doc": "The player's health, or null when the game has none."
    },
    {
      "name": "setVisible",
      "kind": "method",
      "detail": "(entityId: string, visible: boolean): void",
      "doc": "Show or hide an entity (and its children) until the next run; it still collides and triggers."
    }
  ],
  "BehaviorGameState.health()": [
    {
      "name": "current",
      "kind": "property",
      "detail": "number"
    },
    {
      "name": "max",
      "kind": "property",
      "detail": "number"
    }
  ],
  "BehaviorInputView": [
    {
      "name": "value",
      "kind": "method",
      "detail": "(name: string): number",
      "doc": "A button 0/1, a 1D axis −1..1, a 2D axis's length; 0 for an unknown name."
    },
    {
      "name": "vector",
      "kind": "method",
      "detail": "(name: string): [number, number]",
      "doc": "A 2D axis as [x, y] ([value, 0] for others)."
    },
    {
      "name": "pressed",
      "kind": "method",
      "detail": "(name: string): boolean",
      "doc": "Pressed in this step."
    },
    {
      "name": "released",
      "kind": "method",
      "detail": "(name: string): boolean",
      "doc": "Released in this step."
    },
    {
      "name": "held",
      "kind": "method",
      "detail": "(name: string): boolean",
      "doc": "Down this step (pressed or held)."
    }
  ],
  "BehaviorInstanceInfo": [
    {
      "name": "entityId",
      "kind": "property",
      "detail": "string"
    },
    {
      "name": "properties",
      "kind": "property",
      "detail": "Readonly<Record<string, PropertyValue>>"
    },
    {
      "name": "tags",
      "kind": "property",
      "detail": "BehaviorTagQuery",
      "type": "BehaviorTagQuery"
    }
  ],
  "BehaviorMessages": [
    {
      "name": "send",
      "kind": "method",
      "detail": "(name: string, value?: string | number | boolean | undefined, target?: string | undefined): boolean",
      "doc": "Send a message (name: 1–64 letters, digits or _ . : -) with an optional value (a number, text of at most 256 characters or true/false) to every script, or only to the scripts on entity `target`. `false` when it is refused (a bad name or val"
    },
    {
      "name": "received",
      "kind": "method",
      "detail": "(name: string): readonly BehaviorMessage[]",
      "doc": "The messages of that name sent in the previous step to every script or to this entity, in send order."
    }
  ],
  "BehaviorPrepareConfig": [
    {
      "name": "behaviorId",
      "kind": "property",
      "detail": "string"
    },
    {
      "name": "sourceDigest",
      "kind": "property",
      "detail": "string"
    },
    {
      "name": "declaration",
      "kind": "property",
      "detail": "PropertyDeclaration",
      "type": "PropertyDeclaration"
    },
    {
      "name": "enginePins",
      "kind": "property",
      "detail": "readonly BehaviorEnginePin[]"
    }
  ],
  "BehaviorSave": [
    {
      "name": "get",
      "kind": "method",
      "detail": "(key: string): unknown",
      "doc": "The value kept under `key` (undefined when there is none)."
    },
    {
      "name": "set",
      "kind": "method",
      "detail": "(key: string, value: unknown): boolean",
      "doc": "Keep a value under `key`; `false` when it does not fit (a bad key, 64 keys, 4 KB)."
    },
    {
      "name": "remove",
      "kind": "method",
      "detail": "(key: string): void",
      "doc": "Forget the value under `key`."
    },
    {
      "name": "keys",
      "kind": "method",
      "detail": "(): string[]",
      "doc": "The keys of the kept values."
    }
  ],
  "BehaviorSceneControl": [
    {
      "name": "load",
      "kind": "method",
      "detail": "(sceneId: string, options?: SceneLoadOptions | undefined): void",
      "doc": "Request a load; it completes at a later step boundary. Loading or loaded ⇒ no-op."
    },
    {
      "name": "unload",
      "kind": "method",
      "detail": "(sceneId: string): void",
      "doc": "Request an unload at the next step boundary. Unloaded ⇒ no-op."
    },
    {
      "name": "status",
      "kind": "method",
      "detail": "(sceneId: string): SceneStatus",
      "doc": "Where a scene is in its load cycle."
    },
    {
      "name": "loaded",
      "kind": "method",
      "detail": "(): readonly string[]",
      "doc": "The loaded scene ids, in load order."
    }
  ],
  "BehaviorSignals": [
    {
      "name": "emit",
      "kind": "method",
      "detail": "(name: string): void",
      "doc": "Send a named signal; switches, doors and scripts see it in the next step."
    },
    {
      "name": "on",
      "kind": "method",
      "detail": "(name: string): boolean",
      "doc": "Emitted in the previous step (by a switch, a trigger or a script)."
    }
  ],
  "BehaviorTagQuery": [
    {
      "name": "mask",
      "kind": "method",
      "detail": "(...names: string[]): number",
      "doc": "The mask of the named tags (names ignore case). Throws on an unknown name."
    },
    {
      "name": "of",
      "kind": "method",
      "detail": "(entityId: string): number",
      "doc": "The entity's effective tag mask (0 for an unknown entity)."
    },
    {
      "name": "has",
      "kind": "method",
      "detail": "(entityId: string, mask: number, match?: 'any' | 'all' | undefined): boolean",
      "doc": "Whether the entity carries any (default) or all of the mask's bits."
    },
    {
      "name": "query",
      "kind": "method",
      "detail": "(mask: number, match?: 'any' | 'all' | undefined): readonly string[]",
      "doc": "The entities carrying any (default) or all of the mask's bits, in scene order."
    }
  ],
  "BehaviorTimers": [
    {
      "name": "after",
      "kind": "method",
      "detail": "(name: string, seconds: number): boolean",
      "doc": "Fire once, `seconds` from this step. Returns `false` (and changes nothing) when a one-shot timer of that name and length is already running — a script may call it every step; a different length restarts it."
    },
    {
      "name": "every",
      "kind": "method",
      "detail": "(name: string, seconds: number): boolean",
      "doc": "Fire every `seconds`, the first time `seconds` from this step. Returns `false` (and changes nothing) when a repeating timer of that name and period is already running."
    },
    {
      "name": "fired",
      "kind": "method",
      "detail": "(name: string): boolean",
      "doc": "True in the step the timer fires (in every phase of that step)."
    },
    {
      "name": "cancel",
      "kind": "method",
      "detail": "(name: string): boolean",
      "doc": "Stop a timer; `false` when none of that name was running."
    }
  ],
  "BehaviorWorldView": [
    {
      "name": "transform",
      "kind": "method",
      "detail": "(entityId: string): Readonly<{ position: readonly [number, number, number]; rotation: readonly [number, number, number, number]; scale: readonly [number, number, number]; }> | undefined",
      "returns": "Readonly<{ position: readonly [number, number, number]; rotation: readonly [number, number, number, number]; scale: readonly [number, number, number]; }>",
      "doc": "The entity's current transform (this step so far), or `undefined` when it is not loaded."
    }
  ],
  "CharacterMoveResult": [
    {
      "name": "requested",
      "kind": "property",
      "detail": "Vec2",
      "type": "Vec2"
    },
    {
      "name": "applied",
      "kind": "property",
      "detail": "Vec2",
      "type": "Vec2"
    },
    {
      "name": "position",
      "kind": "property",
      "detail": "Vec2",
      "type": "Vec2"
    },
    {
      "name": "grounded",
      "kind": "property",
      "detail": "boolean"
    },
    {
      "name": "supportNormal",
      "kind": "property",
      "detail": "Vec2",
      "type": "Vec2"
    },
    {
      "name": "contacts",
      "kind": "property",
      "detail": "{ ground: boolean; wall: boolean; head: boolean; steepSlope: boolean; }",
      "type": "CharacterMoveResult.contacts"
    },
    {
      "name": "snapped",
      "kind": "property",
      "detail": "boolean"
    },
    {
      "name": "groundEntityId",
      "kind": "property",
      "detail": "string | null | undefined",
      "optional": true,
      "doc": "Phase 9.9: the collider entity the character stands on (grounded), when the port knows it."
    },
    {
      "name": "kinematicSlack",
      "kind": "property",
      "detail": "number | undefined",
      "optional": true,
      "doc": "Phase 9.13: how far a moving (kinematic) body moved into the character this step; the correction may exceed the request by this much."
    }
  ],
  "CharacterMoveResult.contacts": [
    {
      "name": "ground",
      "kind": "property",
      "detail": "boolean"
    },
    {
      "name": "wall",
      "kind": "property",
      "detail": "boolean"
    },
    {
      "name": "head",
      "kind": "property",
      "detail": "boolean"
    },
    {
      "name": "steepSlope",
      "kind": "property",
      "detail": "boolean"
    }
  ],
  "IntentSet": [
    {
      "name": "stepIndex",
      "kind": "property",
      "detail": "number"
    },
    {
      "name": "move",
      "kind": "property",
      "detail": "number | null",
      "doc": "The committed `control_move` (quantized) or `null`."
    },
    {
      "name": "jump",
      "kind": "property",
      "detail": "JumpPhase | null",
      "doc": "The committed `control_jump` or `null`."
    },
    {
      "name": "moveWriter",
      "kind": "property",
      "detail": "string | null",
      "doc": "The module ID that committed `move`, or `null`."
    },
    {
      "name": "jumpWriter",
      "kind": "property",
      "detail": "string | null"
    },
    {
      "name": "transformWrites",
      "kind": "property",
      "detail": "readonly IntentTransformWrite[]",
      "doc": "Committed transform writes, in commit order."
    },
    {
      "name": "bounce",
      "kind": "property",
      "detail": "number | undefined",
      "optional": true,
      "doc": "Phase 9.9: an upward speed the runtime gives the controller this step (a stomp or a hit)."
    }
  ],
  "PhysicsStepClient": [
    {
      "name": "stageCharacterMove",
      "kind": "method",
      "detail": "(entityId: string, delta: Vec2): void",
      "doc": "Controller phase only; a second stage for one entity is `duplicate_move`."
    },
    {
      "name": "characterResult",
      "kind": "method",
      "detail": "(entityId: string): CharacterMoveResult | undefined",
      "returns": "CharacterMoveResult",
      "doc": "The last completed step's result, or `undefined` before the first step."
    },
    {
      "name": "raycast",
      "kind": "method",
      "detail": "(origin: Vec2, direction: Vec2, maxDistance: number): RaycastHit | null",
      "returns": "RaycastHit",
      "optional": true,
      "doc": "Phase 9.9: a ray against the level's colliders (bounded per step; null when nothing is hit)."
    },
    {
      "name": "overlapBox",
      "kind": "method",
      "detail": "(center: Vec2, half: Vec2): string[]",
      "optional": true,
      "doc": "Phase 9.9: the entities whose colliders overlap a box (center, half extents) — counted with the rays."
    },
    {
      "name": "overlapCircle",
      "kind": "method",
      "detail": "(center: Vec2, radius: number): string[]",
      "optional": true,
      "doc": "Phase 9.9: the entities whose colliders overlap a circle — counted with the rays."
    }
  ],
  "PropertyDeclaration": [
    {
      "name": "properties",
      "kind": "property",
      "detail": "DeclaredProperty[]"
    }
  ],
  "RaycastHit": [
    {
      "name": "entityId",
      "kind": "property",
      "detail": "string"
    },
    {
      "name": "distance",
      "kind": "property",
      "detail": "number"
    },
    {
      "name": "normal",
      "kind": "property",
      "detail": "Vec2",
      "type": "Vec2"
    }
  ],
  "Readonly<GameplaySettings>": [
    {
      "name": "gravity_y",
      "kind": "property",
      "detail": "number"
    },
    {
      "name": "run_speed",
      "kind": "property",
      "detail": "number"
    },
    {
      "name": "jump_velocity",
      "kind": "property",
      "detail": "number"
    },
    {
      "name": "max_fall_speed",
      "kind": "property",
      "detail": "number"
    },
    {
      "name": "max_slope_climb_deg",
      "kind": "property",
      "detail": "number"
    },
    {
      "name": "min_slope_slide_deg",
      "kind": "property",
      "detail": "number"
    },
    {
      "name": "fixed_step_hz",
      "kind": "property",
      "detail": "number | undefined",
      "optional": true,
      "doc": "Phase 15.3: engine settings, present only when the project sets them (absent: the engine default)."
    },
    {
      "name": "audio_voices",
      "kind": "property",
      "detail": "number | undefined",
      "optional": true
    },
    {
      "name": "music_fade_s",
      "kind": "property",
      "detail": "number | undefined",
      "optional": true
    },
    {
      "name": "animation_crossfade_s",
      "kind": "property",
      "detail": "number | undefined",
      "optional": true
    },
    {
      "name": "render_backend",
      "kind": "property",
      "detail": "number | undefined",
      "optional": true,
      "doc": "Phase 17.1: the renderer backend (0 WebGL legacy, 1 auto, 2 WebGPU, 3 WebGL 2; absent: 0)."
    }
  ],
  "Readonly<{ position: readonly [number, number, number]; rotation: readonly [number, number, number, number]; scale: readonly [number, number, number]; }>": [
    {
      "name": "position",
      "kind": "property",
      "detail": "readonly [number, number, number]"
    },
    {
      "name": "rotation",
      "kind": "property",
      "detail": "readonly [number, number, number, number]"
    },
    {
      "name": "scale",
      "kind": "property",
      "detail": "readonly [number, number, number]"
    }
  ],
  "Vec2": [
    {
      "name": "x",
      "kind": "property",
      "detail": "number"
    },
    {
      "name": "y",
      "kind": "property",
      "detail": "number"
    }
  ]
};
