# Packet 07 independent re-review, round 1 — R1–R17 + O1/O2 + L1 on the repaired tree

**Date:** 2026-09-18 (UTC). **Reviewer role:** independent code reviewer (review-only; no source, contract, fixture, test, or STATUS edits; the only file created/committed is this document).

**Scope.** Verify the packet 07 repair campaign (review baseline `be59d15` through HEAD) against the findings of `docs/reviews/2026-09-18-commits.md` (R1–R17, O1/O2), the R9 contract amendment (`dabfcff`, applied per `docs/handoffs/2026-09-18-contract-request.md`), and the orchestrator's spot-check finding L1 (false-dead liveness, fixed in E3). Authority for correctness: the **amended** contracts (`docs/contracts/workspace.md` §5.5/§6/§7/§9/§10/§11, `docs/contracts/commands.md` §3/§5.2) — pre-amendment expectations in the retained probes are not failures.

**Tree.** HEAD `10332ba` ("Orchestration: O1+O2 child … spot-checked pass"), working tree clean, `origin/main` `87394e3`, **88 commits unpushed**. node v22.22.1, user `dadmin` (unprivileged).

**Method.** (1) Read the documents in the prescribed order. (2) Located each fix in the tree and confirmed the claimed commit contains it (`git show --stat` scoped lookups). (3) Produced **my own probe evidence** against the real public API: esbuild-bundled `packages/workspace/src/index.ts` and `packages/commands/src/index.ts` into `/tmp/tl-p07/`, disposable `mkdtemp` roots, `backendId` = `tb-`+32 hex, `requestId` = `req-`+32 hex. Real-permission faults (chmod 000) for the unreadable/EACCES classes; `WriteOps` fault seams for write-failure classes; a `writeAll`/`renameFile` seam interleave for the R9 claim race; the repo's real two-process claim suite run via vitest. (4) Re-ran both retained review probes and re-ran the two retained cases the `.ts` probe's crash prevented, standalone. (5) Suite + delta-scope + BF-closure + claim-primitive conformance checks. Probe artifacts live under `/tmp/tl-p07/` (`probe-main.mjs`, `probe-commands.mjs`, `probe-retained.mjs`, logs `main5.log`, `retained-ts.log`, `retained-mjs.log`); they are described below and not required to be retained.

Known probe-bug conventions honored: public results may be deep-frozen; many workspace errors surface as `project_unavailable { reason: <real code> }`; on-disk envelope = `{ storageVersion, type: 'authoring-state', projectId, scene: {…}, retry: {…} }`.

---

## Per-finding verdicts

| Finding | Verdict | Evidence (mine) |
|---|---|---|
| R1 | **FIXED** | probe `R1-unreadable-fail-closed` (23/23 run): chmod-000 foreign ⇒ `external_change_unreadable`, `snapshotState:'unreadable'`, `externalHash:null`, **no** snapshot artifact; accept/discard refused; after 0644 discard re-reads + re-establishes + snapshots **in the same call**; envelope = LKG bytes exactly; the one snapshot's content byte-identical to the foreign bytes |
| R2 | **FIXED** | probe `R2-retry-never-overwrites-foreign`: faulted rename + foreign write between attempts ⇒ `external` carrying the **real** foreign bytes; target NOT overwritten; exactly one snapshot = the real foreign bytes |
| R3 | **FIXED** | probe `R3-snapshot-failure-pause`: failed snapshot ⇒ `snapshotState:'snapshot_failed'` + real `externalHash`; 0 artifacts while faulted; accept/discard refused (`external_change_evidence_missing`); post-restore discard re-snapshots + proceeds **same call**; exactly **one** durable snapshot, content byte-identical |
| R4 | **FIXED** | probe `R4-failed-release-split-brain-gone` + repo two-process suite: `.thirdlight` dir-fsync fault during release ⇒ `write_failed{onDiskState:'new-undurable'}` (never a success ack); second identity claims the released record and mutates; **old backend's mutation refused** `project_unavailable{ownership_conflict, holder=<new owner>}`; disk holds only the new owner's write |
| R5 | **FIXED** | probes `R5a-accept-new-undurable-reconciles`, `R5b-release-new-undurable-reconciles`: operator envelope writes on `new-undurable` reconcile memory with disk and still report `write_failed` (no success ack); R5b: failed release **before the record write** leaves the project owned with the old session still the writer (§9 no-partial-release — a later mutation from the old backend is ok after from-disk re-verification), a re-issued original request re-executes fresh and fails `revision_conflict` (never a `duplicated:true` stale-map replay), and the retried release converges to ok |
| R6 | **FIXED** | probe `R6-close-failure-never-ok`: close fails every attempt ⇒ never ok, `failed{onDiskState:'previous'}`, target unchanged |
| R7 | **FIXED** | retained `.ts` probe now logs `SCAN_SYMLINK {…externalWritten:false}` (orphan note, not completed/modified); my `R7_OPEN_TIME_SYMLINK` probe: a **valid** external envelope behind a symlinked `scenes` child present at open ⇒ query refused (`project_not_found`), command refused, external bytes untouched, no ownership record written; `SCENES_SYMLINK` (post-open swap) remains within the documented §7.1 post-verification bypassing-actor bound (handoff §5 policy sentence), not a regression |
| R8 | **FIXED** | probes `R8a-unreadable-ownership-refused`, `R8b-proc-eaccess-unknown-live`: chmod-000 record ⇒ query AND takeover refused `ownership_conflict` (holder `null`), record byte-identical afterwards, original owner still open; procRoot seam EACCES ⇒ unknown⇒live, takeover refused `ownership_conflict`, real owner still alive (retained `.ts` probe logs the same) |
| R9 | **FIXED** | probes `R9a-seam-interleave-one-winner` (writeAll seam: B's full claim interleaved after A's O_EXCL, before A stamps ⇒ A ok, B refused `claim_inconsistent`, loser mutations refused end-to-end, on-disk record = winner, disk revision shows only the winner's write), `R9b-original-repro-refuted` (B interleaved at A's record-rename point — the pre-amendment double-claim window — ⇒ B refused), `R9c-post-completion-refused`; repo two-process suite `tests/ownership-claim-2026-09-18.test.ts` **6/6** (real `spawn` children: absent/released/stale races ⇒ exactly one winner; SIGKILL at the three claim crash points ⇒ orphan `claim_inconsistent` / proven-dead reclaim / absent-record reclaim). Amended §6.3 six-step primitive + self-reclaim + orphan-recovery + superseded-epoch cleanup verified step-for-step against `ownership.ts` (`claimOwnership`/`orphanRecovery`/`verifyClaimedFiles`/`unlinkOwnClaimFile`) |
| R10 | **FIXED** | probe `R10-deep-isolation`: caller mutation of a query result (`entity` fields) and of a success `history.undoDepth` never persists, never aliases (re-query + unrelated mutation + disk entity JSON all identical to the original); a re-issued recorded request replays the **recorded** result (original `undoDepth`, `duplicated:true`) after dispose + reopen |
| R11 | **FIXED** | probe `R11-noncanonicalizable-rejected` + `R11c`: `origin:undefined` and `BigInt` args rejected `invalid_request`/`field_type`; envelope stays byte-identical (no null-digest record); re-issue of the rejected request is a fresh rejection, not `request_id_reused` |
| R12 | **FIXED** | probe `R12-malformed-record-no-startup-throw`: corrupted persisted JSON ⇒ no startup throw; project blocked (mutation refused), bytes retained byte-identical (no auto-repair) |
| R13 | **FIXED** | probe `R13-strict-historical-payloads`: historical retry payloads strictly validated; a corrupted record blocks the project, replay refused, bytes retained |
| R14 | **FIXED** | probe `R14-scan-completes-all-101` + retained `.ts` probe `SCAN_CAP {total:101, reported:100, firstWritten:true, lastWritten:true}`: the scan completes every entry; only the report is capped at 100 |
| R15 | **FIXED** | probe `R15-idempotent-create-read-only`: live-foreign-owner create ⇒ idempotent no-op `{ok:true, created:false, revision:0}` with ownership + envelope bytes unchanged (no takeover/claim); released-record create ⇒ no re-claim (record byte-identical); invalid existing ⇒ no ownership write |
| R16 | **FIXED** | probe `R16-same-second-exemption`: 17 same-UTC-second foreigns (sha8-descending names) ⇒ after the 17th (pending) exactly 16 snapshots; the just-written (pending) snapshot is **retained**; pruned = the lexicographically-smallest **non-exempt** name per the §7.4 tie-break (retained `.ts` case re-run standalone: `count:16, newestRetained:true`) |
| R17 | **FIXED** | probe `R17-json-safe-errors`: query validation errors JSON-serializable (no `undefined`/BigInt leakage into error payloads) |
| O1 | **FIXED** | probe `O1a`/`O1b` against public `applyMutation` with the review's exact `JSON.parse('['.repeat(12000)+'0'+']'.repeat(12000))`: undo `args` ⇒ structured `invalid_request` at `/args`, `found` = the bounded marker `[truncated: exceeds bounded diagnostic traversal (depth <= 64, nodes <= 4096)]`; delete `args.entityId` ⇒ `field_type` at `/args/entityId` — **no RangeError escapes** |
| O2 | **FIXED** | probes `O2a`–`O2e`: undo `args:{'a/b':1}` ⇒ `/args/a~1b` (found carries the raw key); transform key `a~b` ⇒ `/args/transform/a~0b`; unknown top-level `x/y` ⇒ `/x~1y`; origin `p/q` ⇒ `/origin/p~1q`; combined `a/b~c` ⇒ `/args/a~1b~0c` (`~`→`~0` before `/`→`~1`). `pointerSegment` present at every commands dynamic-key pointer site (`validate-request.ts`) and in the workspace validators |
| L1 | **FIXED** | probes `L1-same-second-start-ambiguous-live` (owner started + claimed within the same wall-clock second ⇒ judged **live**, not dead), `L1-boundary-exactly-1s-live` (`startMs == openedAtMs + 1000` ⇒ ambiguous ⇒ live), `L1-conclusive-reuse-still-dead` (a conclusive reuse > 1 s later ⇒ dead). Code: `ownership.ts:249` `startMs > openedAtMs + 1000` — matches the orchestrator's mandated fix; record format unchanged |

BF closures (required checks): **BF-1** — the quaternion hint is byte-identical across `project-model.md` §12.5, `commands.md` §5.2, the generator literal, and the regenerated `scenarios/04-invalid-no-partial/messages.json` (my `grep -o | sort | uniq -c` extraction: one unique string ×4); `generate-fixtures.mjs --check` = "73 files byte-identical". **BF-2** — `workspace.md` §9 carries the "Scoped precisely" sentence (releasing backend's queries fail `workspace_closed` and never re-open; any backend's on-demand open claims at `lockEpoch`+1 with a new `openedAt`); the forbidden blanket phrase "any query reopens" occurs **0** times in workspace.md and 07.md. **BF-3** — §11 `takeoverWorkspace` row now lists `project_unavailable` (claim succeeded, §4.3 load failed ⇒ §7.5 block). **BF-4** — `dependencies.md` §3 lists `parseDocumentBytes`/`ByteParse` (export verified public at `project-model/src/index.ts:42`, consumed by 4 workspace files). **BF-5** — 05.md's public-type list matches the actual `export type` block exactly; 07.md interpretation #1 corrected per the wording rule.

---

## Evidence details

### Suite evidence
- `npm test` on the **real tree** (HEAD `10332ba`): **39 files / 551 tests passed**, exit 0 (twice).
- `npm test` on a **clean copy** of HEAD (`git archive` → `/tmp` + symlinked `node_modules`, copy since removed): **39 files / 551 tests passed** — no environment dependence in the suite result.
- `npx vitest run tests/ownership-claim-2026-09-18.test.ts` (isolated): **6/6** — T2 real two-process races (absent / released / stale-SIGKILL claim gates, single winner + superseded `claim-0` unlinked), T3 SIGKILL at the three claim crash points (empty orphan ⇒ `claim_inconsistent` + operator removal; proven-dead orphan ⇒ reclaim; absent record ⇒ normal path).
- `node fixtures/commands/tools/generate-fixtures.mjs --check`: "check OK: 73 files byte-identical".

### My probe runs (all on the repaired tree, disposable roots)
- `/tmp/tl-p07/probe-main.mjs` (esbuild bundle of the workspace public API): **23/23 passed** (log `main5.log`): R1, R2, R3, R4, R5a, R5b, R6, R10 (deep isolation + replay), R11, R12, R13, R14, R15, R16, R17, R8a, R8b, L1 ×3, R9a, R9b, R9c.
- `/tmp/tl-p07/probe-commands.mjs` (esbuild bundle of the commands public API): **8/8 passed**: O1a, O1b, O2a–O2e, R11c (BigInt arg ⇒ structured `field_type`, no throw).
- `/tmp/tl-p07/probe-retained.mjs`: re-ran the two retained `.ts` cases the crash prevented — `RECOVERY_NEWEST_PRUNED {error:'external_change_unresolved', count:16, newestRetained:true, discard ok}` and `QUERY_ECHO` (structured `invalid_request` at `/extra`); plus the `R7_OPEN_TIME_SYMLINK` case above.

### Retained review probes (re-run by me)
- `docs/reviews/2026-09-18-probes.mjs`: **aborts at its first assertion** (line 33, the R2 retry-overwrite case) — it pins the **pre-fix buggy** behavior (the retry acked while overwriting foreign bytes); the post-fix refusal is the correct behavior. As expected; not a failure.
- `docs/reviews/2026-09-18-path-ownership-probes.ts`: now runs six observation cases, all showing fixed behavior — `SCAN_SYMLINK` (orphan, `externalWritten:false`), `SCENES_SYMLINK` (see R7 bound note), `SCAN_CAP` (101/100, both ends written), `CREATE_LIVE_OWNER`/`CREATE_RELEASED_SELF`/`CREATE_RELEASED_OTHER` (`created:false`, `ownershipChanged:false`), `CREATE_INVALID_WRITES` (`project_exists_invalid`, `ownershipCreated:false`), `UNREADABLE_OWNER` (refused, owner unchanged, `firstStillOpen:true`), `PROC_EACCES` (query + takeover refused `ownership_conflict`, `realOwnerStillAlive:true`). It then **aborts at the `CLAIM_RACE` case**: that case pins the **pre-amendment** claim mechanism (a seam on the direct `renameFile` to `ownership.json`), which E1 replaced by design with the amended §6.3 claim-file primitive — the seam no longer fires, so the probe's `bResult` is undefined. Per the re-review brief, pre-amendment expectations there are **not** failures; my `R9a` probe covers the post-amendment interleave (winner/loser via the `writeAll` seam at the claim-file stamp point).

### Amended ownership mechanism conformance (check (a))
`claimOwnership` (`packages/workspace/src/ownership.ts`) matches amended `workspace.md` §6.3 step-for-step: (1) `open(claim-e, 'wx')` = `O_CREAT|O_EXCL` via `ops.openTempFile`; on any open failure — **no retry loop** — re-read the record and re-evaluate per the §6.2 rows (owned+live ⇒ `ownership_conflict`; owned+dead ⇒ `stale_ownership`; self-record at target epoch ⇒ self-reclaim row; released at e−1 ⇒ orphan-recovery; a non-ENOENT record read failure is **never** treated as absence — orphan-recovery rule or `claim_inconsistent`); (2) durable stamp (`writeAll` + `fsync` of `{backendId, pid, timestamp}`); (3) pre-record by-path verification of the claim file (foreign/unparseable/missing ⇒ `ownership_conflict`, **no record written**); (4) record `W` (state `owned`, `lockEpoch` e); (5) two-file verification re-read (`verifyClaimedFiles`: record identity+epoch AND claim-stamp identity+timestamp); (6) consistency (record epoch = e; mismatch ⇒ fail closed, claim file left on disk). Self-reclaim (record exactly ours at the target epoch ⇒ no O_EXCL against ourselves; claim-stamp re-verify; mismatch ⇒ refuse to serve) and superseded-epoch cleanup (successful claim at e unlinks `claim-(e−1)` best-effort **after** its own record W) both present. Orphan recovery: reclaim **only** on parseable content + proven-dead holder (live/unknown ⇒ refuse `claim_inconsistent` carrying `claimFile`, holder content, liveness outcome); reclaim rewrites the file with the claimant's own identity and then runs steps 4–6 unchanged. The release's own-claim-file unlink (`unlinkOwnClaimFile`, §9 step 1) verifies holder bytes by path before unlinking — foreign/empty/unparseable/unreadable files are never removed and never fatal. `stillHoldsOwnership` (R4 from-disk re-verification after a failed release) and `reReadOwnershipHolder` (E3 `holder:null` residual) present as documented.

### Delta-scope check (step 5)
`git diff --name-only be59d15..HEAD` = **50 files**, exactly the documented repair set: `packages/workspace` (17 src incl. the 11 `repair-2026-09-18-*.test.ts` + 1 tests), `packages/commands` (3: `errors.ts`, `validate-request.ts`, `request-validation.test.ts`), `tests/ownership-claim-2026-09-18.test.ts` + `tests/crash/ownership-child.ts`, `fixtures/commands` (6: generator + scenario-04 `messages.json` + scenario-08 `messages.json`/`scenario.md` + scenario-08/09 `disk-before/.thirdlight/claim-0` — all documented in handoff groups C/E1/BF), the three contracts, `docs/` (STATUS, 9 handoffs, orchestration, 3 reviews). The wider `87394e3..HEAD` delta additionally contains the pre-review packets 04–06 paths (`packages/project-model`, `tools/`, `fixtures/project-model`, `package.json`, `package-lock.json`, `tsconfig.base.json`) — all within the previously audited commits ≤ `be59d15`. **No unexpected path in the repair delta.** The repair touched **no** manifests or lockfile (`git diff 4493262..HEAD -- package.json package-lock.json` = empty).

### Working-state confirmation
HEAD `10332bacaefb…`, `git status --short` empty, `git rev-list --count origin/main..HEAD` = **88**. No push performed; nothing but this document committed.

---

## New findings

**None** (in the code/contracts under review). One environmental item, already recorded by the repair itself (so not a new finding per the brief's "unrecorded deviation" bar), is carried forward with my independent verification below.

## Carried forward

1. **Live-workspace `node_modules` corruption (environmental; recorded by the repair, independently re-verified).** `npm run build` / `npm run check-deps` **fail** on the live tree: `npm ls --depth=0` exits 1 with `missing: @thirdlight/{commands,project-model,workspace}` + `extraneous: node_modules@ /home/dadmin/projects/thirdlight/node_modules/node_modules` — an extraneous **nested** install tree (`node_modules/node_modules`, dir mtime 2026-09-18 12:22:35, i.e. between group E1's green `check-deps` and group E2's first documented failure; file mtimes inside were copied from the original 2026-09-17 install). **Repo consistency is proven:** on a clean `git archive` copy, `npm ci` (45 packages, lockfile consistent) + `npm run build` = **exit 0** (check-deps OK, check-boundaries OK — 66 source files / 286 specifiers, strict typecheck ×3) and `npm test` = 39/551 green. So the repair's characterization "pre-existing environmental failure at clean HEAD … not caused by this group/diff" is accurate in the sense it was verified (stashed-diff: the failure reproduces on clean HEAD; the repair delta touches no manifests/lockfile) — with the timing nuance that the corruption appeared *during* the repair session window, not before the review day. **Required pre-step before packet 08** (one operator command on the live workspace, out of scope for the repair): `rm -rf node_modules && npm ci`.
2. **project-model O1/O2-class observations (out of scope — separate package/gate; recorded in handoff §12).** Confirmed still present: `packages/project-model/src/validate.ts` interpolates dynamic unknown keys into `path` without RFC 6901 escaping (≈lines 395, 421, 426, 460, 479, 804) and `boundedFound` (lines 104–115) recurses over arrays without a depth/node budget (the per-level length cap is not a recursion bound). Not fixed by the repair, as recorded; must not be silently closed.
3. **Scan-time read through a symlinked child directory (non-gating observation).** With a **valid** external envelope behind a symlinked `scenes` child present at scan time, the startup scan's loadability probe reads the foreign bytes (report: `kind:'project', loadable:true, note:'complete (opens on demand)'`), but acts on nothing: no completion write, no claim, external bytes untouched; on-demand open is refused via `verifyChildDir` (my `R7_OPEN_TIME_SYMLINK` probe). Within the handoff §5 policy (containment verified before any content-acting read or write; the open/write/claim paths are gated) — candidate for a later hardening note, not a contract violation.
4. **Minor doc discrepancy (no contract impact).** Repair handoff §11 BF-5 verification prose says the project-model `export type` block is "exactly **12** type exports"; the list it enumerates and the actual block (`packages/project-model/src/index.ts:28–40`) both contain **11** names (`BoxComponent`, `BoxMaterial`, `CameraComponent`, `Entity`, `EntityComponents`, `Manifest`, `Quat`, `Scene`, `SceneRef`, `TransformComponent`, `Vec3`). 05.md's own list is correct (11).
5. **Retained probes now obsolete-by-design where they pin pre-fix/pre-amendment behavior** (documented above under "Retained review probes"): `probes.mjs` aborts at its first (pre-fix) assertion; `path-ownership-probes.ts` aborts at the pre-amendment `CLAIM_RACE` seam. They remain retained as historical evidence; post-fix/post-amendment behavior was verified instead, per the brief.

---

## Overall verdict

**ACCEPTED.** All findings R1–R17, O1, O2 and L1 are **FIXED** with independent probe/test evidence on the repaired tree (23/23 workspace probes, 8/8 commands probes, 6/6 real two-process claim suite, retained-probe re-runs showing the corrected behavior, R16/L1/R7/R8 boundary probes). The amended §6.3 claim primitive matches the amended contract step-for-step; BF-1…BF-5 closures are verified (requests (1) and (7) closed). The suite is green on the real tree **and** on clean copies (39 files / 551 tests; the `npm ci` copy additionally passes the full `npm run build`). The repair delta is limited to the documented file set; no manifests/lockfile touched. No new gating findings in the code/contracts. Carried-forward items are non-gating (one environmental pre-step before packet 08; the project-model observations remain a separate gate's work).

No approval by any reviewer is claimed; this document records only what was verified above. Nothing was pushed. Exactly one commit stages only this document; its hash is reported in the re-review's final report.