# Packet 39 — V3 game data, storage, migration and authoring contract (PROPOSED)

**Executed 2026-09-19 as parts 39-A and 39-B in one handoff. Nothing accepted,
nothing promoted, no commit.** All output is under the packet's write scope:
`docs/planning/m3-contracts/**`, `fixtures/m3/**`, this handoff and STATUS row 39.
`docs/contracts/**`, `packages/**`, `tests/**`, dependencies and the lockfile are
untouched.

## Outcome

39-A (`model.md`, `storage.md`, `diffs/{project-model,workspace}.md`) fixes scene
`schemaVersion` 3, envelope `storageVersion` 3, the legal combination table
(`manifest 1 + scene 3 + storage 3` added; all other new pairs refused
non-destructively; authoring `project.json` stays `schemaVersion` 1; the export
`manifest.json` `manifestVersion` 1→2 is cross-referenced to packet 42 only), the
§23 component set (`gameZone` with hazard/checkpoint/goal, `playerSpawn`,
`cameraFollow`, `light`, `surface`, `modelAnimation`), the audio `kind`
discriminator, the bounded `content.game` block, reference/deletion rules and the
v2→v3 copy-migration operator (§S5: new identity, revision 0, retry/history
reset, source byte-identical on success and refusal, four resumable marker
phases, no in-place upgrade, refused v1 mix).

39-B (`authoring.md`, `diffs/commands.md`) fixes the command surface for every new
value: `createEntity` gains optional `components`/`surfacePreset`; `setComponent`
owns all v3 components (add/edit/remove); new `applySurfacePreset` and
`setGameConfig` (create/partial-edit/remove); `publishAsset.kind`; query
additions; exact validation order, `change`/inverse/no-change/stale/conflict and
one-revision-one-history-entry semantics; prefab semantics kept copy-on-
instantiation with zones/controller/camera/light not capturable.

The three plan-review PR-1 values are owned with creation paths: the 320-char
`instructions` string (§23.4, `setGameConfig`), the checkpoint `safeSpawnId`
reference (§23.3.1) and the checkpoint `activation` appearance slot
(§23.3.1a). The authorability table (`authoring.md` §A8) has 21 rows; **every row
has a creation path**; no sample value is left to an implementation decision.

## Changed files (scoped diff; no commit)

New: `docs/planning/m3-contracts/{model,storage,authoring}.md`,
`docs/planning/m3-contracts/diffs/{project-model,commands,workspace}.md`,
`fixtures/m3/contracts/**` (36-indexed fixtures: 3 valid v3 envelopes, 19
one-rule-each invalid envelopes, audio catalog, 4 migration files, 3 scenario
files + no-change + failures, 2 source preimages; `index.json`, `README.md`,
`verification.md`, `tools/check-fixtures.mjs`), `docs/handoffs/39.md`.
Modified: `docs/STATUS.md` row 39 only. `dist/**` was rewritten by the required
`npm run build` (build artifacts only).

## Commands and results

| Command | Exit | Result |
|---|---|---|
| `node fixtures/m3/contracts/tools/check-fixtures.mjs` | 0 | 37 groups passed; all checks passed |
| same checker, byte-corrupted copy (`TL39_FIXTURE_ROOT=/tmp/m3-corrupt`) | 1 | `[digest]` sha256 mismatch + `[v3-valid]` `game_reference_missing /content/game/spawnId` |
| same checker, key-order-corrupted copy (`/tmp/m3-corrupt2`) | 1 | `[key-order] /content` wrong order + `[digest]` mismatch |
| `npm test` | 0 | green |
| `npm run typecheck` | 0 | green |
| `npm run check-deps` | 0 | green |
| `npm run check-boundaries` | 0 | green |
| `npm run build` | 0 | 4 built, 0 skipped |

No package, contract, fixture or lockfile in the accepted tree changed, so these
stay at their pre-packet results. Node 22.22.1.

## Acceptance

- **B01** (v1/v2 unchanged; explicit v2→v3 copy identity/reset; unknown versions
  refuse without rewrite): **PASS for the proposal** — combination table,
  non-destructive refusal rule, copy identity fixtures and interrupted-copy case.
  Runtime proof is packets 44/46.
- **B02/B03** (every value creatable/editable/removable; typed validation;
  inverse/no-change/redo/conflict): **PASS for the proposal** — authorability
  table (21/21 creatable), the `failures.json` reachable-failure set, the scenario replay.
- **B12** (audio import/cue assignment; malformed refused before publication):
  **PARTIAL/UNVERIFIED** — the `kind` discriminator, cue assignment and one real
  PCM-WAV preimage are fixed; the WAV profile, byte bounds and refusal cases are
  packet 41's.
- **B16/B17/B18** (settings reach both hosts; prefab copy workflow; one-envelope
  durability): **UNVERIFIED** — 39 defines only the data/command slots and keeps
  the accepted one-envelope/durability rules.

## Limitations

- The fixtures contain **packet-41 placeholders**: audio
  `importRecipe`/`metrics` (`profile: "pcm-wav"`, `recipeVersion: 0`, empty
  `toolchain`) and `modelAnimation` role bindings (`{"binding":
  "packet-41-placeholder"}`). They are marked in `index.json`/`README.md`; the
  checker validates the 39-owned container rules only. No WAV/GLB profile,
  decoded bound or animation-role field is claimed.
- The checker replays the command scenario from **recorded** change data
  (contract consistency), not the production `applyMutation`; it is not an
  implementation and proves no backend/workspace/command behaviour.
- `content.game`'s exact camera-follow math, zone-sweep semantics, game settings
  reachability, export manifest v2 and the CSP finding are later packets
  (40/41/42).
- No browser, network, performance, reviewer or owner approval is claimed.

## Contract-change requests (numbered; exact destination sections)

- **C39-1** `project-model.md` §§6, 8.1, 10 and §12.2/§12.3/§12.6/§17: add scene
  `schemaVersion` 3 (a new known version, not a same-version extension) and the
  seven appended v3 components. Supersedes nothing.
- **C39-2** `project-model.md` §18.1 rule 3: 39 cannot write the
  version-local clip-mapping exception; **packet 41 must supply the exact
  replacement text**, otherwise `modelAnimation` would silently redefine §18.1.
- **C39-3** `project-model.md` §18.2 content key set: add the required `game` key
  (v3 envelopes only); v2 keeps exactly five keys.
- **C39-4** `commands.md` §2 non-goals: the accepted clause "structural component
  add/remove for `box`/`camera`/`model`" must be reworded so it does not forbid
  the six v3 add-capable components; the `createEntity.kind` union stays closed.
- **C39-5** `workspace.md` §4.5: the accepted `| any | any | ≥3 or ≤0 |` and
  `| any | ≥3 | any |` rows are **superseded** by `≥4` because 3 becomes known.
  Explicit approval of this threshold move is requested.
- **C39-6** `project-model.md` §23.9 / `commands.md` §5.4: confirm the new
  document-level `game_config_invalid` wrapper (with `reason`) rather than
  reporting bare `field_*` codes for `content.game` structural failures, and the
  new command-level `game_reference_in_use` (the accepted `reference_in_use`
  carries `referencingEntityIds`, which game-config references cannot supply).
- **C39-7** Gate K: record which document owns `manifestVersion` (packet 42) and
  that 39's tables add only the authoring rows, so a reviewer does not read the
  v3 row as a delivery-manifest change. The packet-38 CSP finding remains
  packet 42's and was not touched.

## Split outcome and remainder

**39-A and 39-B both completed inside budget.** Remaining items (deliberately not
done here): promotion into `docs/contracts/**` (after Gate K), decision
`0003-m3-sample-game.md` (packet 43), packet 41's media fields, packet 42's
manifest v2/CSP, and packet 43's collision reconciliation of these diffs.

**Exact next step: packet 40 — Game flow, trigger, respawn and camera contract
(dependency 39 / Gate K). Do not start it.**
