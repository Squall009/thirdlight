# Packet 21 — Pure content and property commands

Owner pre-approval: **owner pre-approval (autonomous M2 build instruction,
2026-09-18); final manual review pending.** No commit made.

## Outcome

The non-prefab M2 ops share the existing pure history engine: `publishAsset`,
`publishBehavior` (declaration modes; `mode:"source"` refused with
`behavior_publication_unavailable` before any digest/declaration work),
`setBehaviorProperties`, `setComponent`, `setSettings`,
`acknowledgeBehaviorTrust`, the §8.3 step 2b `reference_in_use` check, and the
bounded `queryAssets`/`queryBehaviors`/`contentCounts`. Change/inverse data,
no-change over scene **and** content, one revision/history entry per success,
redo invalidation and stale-before-argument ordering all hold; every op runs the
§6.1 pipeline, with no workspace executor or alternate path.

## Scoped diff (no commit)

New: `content-ops.ts`, `properties.ts`, `queries.ts`,
`validate-content-args.ts`, `m2-content.test.ts`,
`fixtures/m2/commands/content-ops.messages.json` + `expected.json`,
`docs/acceptance/evidence-m2/21/**`. Modified: `apply.ts`, `errors.ts`,
`history.ts`, `index.ts`, `ops.ts`, `types.ts`, `validate-request.ts`,
`test-fixtures.ts`, one `api-surface.test.ts` assertion (both `fixtures/`
trees unchanged).

## Commands and actual results

`npm test` 68 files/916 passed · `typecheck`/`check-deps`/`build` exit 0 ·
`check-boundaries` 10 pkgs/161 files/571 specifiers exit 0 · `check-fixtures.mjs`
33/33 exit 0. Commands 195 tests (163 M1 + 32). Evidence:
`docs/acceptance/evidence-m2/21/manifest.md`.

## Acceptance

Passed: one revision/history entry per success; inverses restore the previous
content version; failures leave inputs byte-identical; M1 preserved (only
`ERROR_CODES`' assertion grew by the contract-mandated §5.4 rows); stale precedes
argument validation; redo invalidation; no-change; declaration replacement
compatibility; packet-16's M1/M2 pairs and 11 reachable failure cases replay
byte-exactly. Unverified: nothing packet-21-scoped.

## Limitations

No prefab ops/queries (22); no workspace I/O, blob/stage work or durable dedup
(23). `declaration_bytes` uses the canonical JSON convention (approximate).
Prefab-definition behavior uses check compatibility with `entityId`=localId;
`importedAt` value rules come from the model's content validation.

## Contract-change requests

- **C21-1:** three-block v2 validation needs the manifest; `CommandState` carries
  optional `content`/`manifest`, else validates scene+content plus the ops'
  explicit checks. Proposed: name those inputs in §6.1 step 5 (packet 23).
- **C21-2:** §3.1.1 omits the required `importedAt` (no clock in a pure layer) —
  implemented as a required validated arg. Proposed: add it, and state that
  reimport `displayName` replaces the name while create defaults it to the assetId.
- **C21-3:** §5.6 says `includeDeclaration` "adds the declaration"; packet 16's
  `queries.json` returns the full `BehaviorRecord`; the fixture shape was used.
- **C21-4:** F22/F23 error text contradicts the M1 pinned fixtures; M1 text kept.
- **C21-5:** F20's `setting_unknown` hint is pre-packet-17 wording, kept
  byte-identical. The helpers properties.md §12 proposed for the model were not
  delivered by packet 20, so they live in commands.

## Next

**Packet 22 (Pure prefab capture and instantiation) — not started.**
