# Packet 13 — M1 integrated acceptance and local deployment

Date: 2026-09-18 · Mode: single-session takeover (RESUME-2026-09-18).
**Gate C was accepted (bounded follow-ups CF-1…CF-4) before this packet
started; its prerequisite is satisfied.**

## Outcome

**COMPLETE — ready for Gate D review.** The normative 14-step M1 scenario
(`docs/planning/m1-acceptance.md` §1) was executed on the disposable project
`demo-m1` against the **real deployment bundle** (`node dist/backend/backend.mjs`
as separate processes, with restarts), with real files, a real stdio MCP
server, and real esbuild export builds:

- **14/14 steps PASS at the protocol/HTTP/WS/process level** — full detail,
  actual commands, and per-step evidence in `docs/acceptance/m1-report.md`
  (raw outputs in the disposable `/tmp/tl-m1/out/`).
- **Browser-visual portions UNVERIFIED** (no browser in this container —
  missing `libnspr4`/`libnss3`, no root): precise manual steps for a real
  browser are in the report §5 (this settles Gate C follow-up CF-1 as far as
  this environment allows).
- **Local deployment supplied + locally performed (disposable):**
  `docs/acceptance/deployment.md` — architecture, directories/mounts, the
  `THIRDLIGHT_*` config table, startup/shutdown/restart (stale-ownership
  takeover), backup/restore, harness MCP connection, serving exports. Clearly
  separated: supplied (config/docs/bundle) vs performed (disposable instance)
  vs NOT performed (no production instance left running; nothing deployed to
  the user's host; no public hosting).

Scenario highlights (evidence files): create → 201 r0 + on-disk manifest/
envelope (s1); attach full state (s2 stub log); box + transform commit (s3/s4);
undo/redo + redo truncation (s5); **restart: stale_ownership ⇒ explicit
takeover (lockEpoch 0→1) ⇒ reopen @ r5, envelope bytes md5-identical** (s6);
play cycle with frozen snapshot `demo-m1@r5`, authored state unchanged during
play (s7); stop lifecycle incl. `stopUnconfirmed` 5 s bound, no-browser 503
`session_unavailable`, unknown-id 404 (s8); **MCP edits (rev 6→7) delivered to
the connected session as `mutation.applied` with `origin {kind:"mcp",
clientId:"m1-harness"}` — no reload** (s9); stale write ⇒ 409
`revision_conflict {currentRevision:7}`, re-issue ⇒ r8 (s10); MCP screenshot
relay with `snapshotId`+`revision`, no-browser structured 503s (s11);
**export: independent static server with the backend STOPPED (all 4 files 200,
bundle scans 0 credentials/origins/node:/api, `fetch("./snapshot.json")`
exactly 1), reproducibility byte-identical except `meta.json:exportedAt`**
(s12); **external change: supported path (release ⇒ hand-edit ⇒ new-process
claim, history reset) and unexpected path (`external_change_unresolved` +
byte-exact recovery snapshot + paused writes + last-known-good queries +
discard resolution)** (s13); **disconnect/reconnect full resync + lost-ack
retry `duplicated: true`, revision unchanged** (s14).

## Changed files

- `tools/build.mjs` — backend deployment-bundle step (Node options; `ws`
  interop `createRequire` banner; **`esbuild` external** — see defect 3) +
  fixed-port bind support is backend-side.
- `packages/backend/src/index.ts` — `THIRDLIGHT_TOKENS` split on the **last**
  colon (defect 1) + docstring.
- `packages/backend/src/backend.ts` — `bindPort()`; `listen(bindPort, bindHost)`
  instead of hardcoded port 0 (defect 2; port 0 ⇒ ephemeral, test behavior).
- `packages/backend/src/static.test.ts` — new test: a second backend on the
  same configured port fails `EADDRINUSE` (the check that exercises the
  defect-2 failure).
- `packages/backend/src/ambient.d.ts` — removed the dead `node:url`
  declaration (no backend source imports it since the packet-12 `engineRoot`
  rework; keeps the ambient surface exact).
- `docs/acceptance/m1-report.md` (new), `docs/acceptance/deployment.md` (new).
- `docs/STATUS.md`, `docs/orchestration.md` (record).

## Commands run and results (summary)

- Scenario run: `node dist/backend/backend.mjs` (×6 processes, SIGTERM
  shutdowns), curl HTTP (all steps), headless WS stub (`ws-client.mjs`),
  `node mcp-client.mjs` (real SDK over stdio), `python3 -m http.server`
  (independent serving), `diff -r` (reproducibility), `cmp`/`md5sum`
  (envelope identity), sha256 (recovery snapshot). All outputs in
  `/tmp/tl-m1/out/`.
- One transient fault (honest record): a first export attempt in a fresh
  process failed `export_bundle_graph_forbidden` (esbuild
  `__filename is not defined`); clean restart of the same bundle exported;
  isolated probes clean; fails closed, no artifacts (report U-2).
- Toolchain (report time): `npm test` 66/841 · `npm run typecheck` 0 ·
  `check-deps` all exact · `check-boundaries` OK (10 pkgs, 150 files, 514
  specifiers) · `npm run build` 4 built, 0 skipped.

## Acceptance criteria (packet 13)

- **PASSED:** whole loop on a disposable project (all 14 steps, protocol/
  process level); external-change and disconnected-browser behavior; export +
  run-without-backend (static serving, independence scans, reproducibility);
  deployment config + docs (architecture, shared mounts, origins, auth,
  startup/shutdown, backup/restore, harness MCP); `docs/acceptance/m1-report.md`
  with actual commands/versions/status (no mocked substitution); bounded
  defects fixed (3, listed above); no opportunistic refactors; no deployment
  to the user's host; no daemon-socket mount; no public hosting; supplied vs
  performed distinguished; Gate D marked ready.
- **UNVERIFIED (environment):** all browser-visual evidence (report §5 manual
  steps); a real WebGL render of editor/preview/export pages.
- **NOT STARTED:** M2 (no automatic start — M1 is accepted only after its
  evidence is reviewed).

## Limitations

No browser/WebGL in this container (standing constraint, packets 08–13); the
scenario's pixel layer is unverified by design of this environment, not
skipped silently. The screenshot relay used a synthetic 1×1 PNG stand-in for
the preview capture (relay/bounds exercised; real capture unverified).
Process-marker caveat for liveness (report U-3).

## Contract-change requests (open, owner disposition)

1. sessions.md §13.7 — optional `engineRoot` config field + env var (from
   packet 12; implemented additive/optional, fail-closed).
2. export.md §5.4.1 — reference-entry interpretation under pinned esbuild
   (bare namespace entry elides to a 15-byte IIFE; implemented entry
   references the namespace; table counts unchanged and remain binding).

## Next packet

**Gate D review (M1 acceptance evidence)** — review `docs/acceptance/m1-report.md`
+ `docs/acceptance/deployment.md` from the committed tree, contracts, and
fresh probes; ≤ 2 repair rounds; then M1 acceptance (or bounded follow-ups).
Not automatically started.