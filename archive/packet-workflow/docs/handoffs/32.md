# Packet 32 — 2.5D platformer controller and diagnostic course

**Outcome.** New `@thirdlight/platformer` (`platformerSpec`,
`PLATFORMER_MODULE_ID`, `CONTROLLER_CONSTANTS`): the pure
`thirdlight.platformer:controller` module — `platformer.md` §7 A–K (single
jump, variable height, integer-step coyote/buffer windows, no air/wall jump, no
stair climbing), `physics.md` §8 grounding, and the `min_slope_slide_deg` slide
policy (promoted to `platformer.md` §7.4 by the Gate H repair, 2026-09-19) — over the injected input/physics ports; it traverses the frozen
packet-14 course in real Node runs through the real packet-31 adapter and
packet-30 mapping. **Selection per decision 0002 §1,
owner pre-approval (autonomous M2 build instruction, 2026-09-18); final manual
review pending.**

**Scoped diff (no commit).** New `packages/platformer/**` (exact
`@thirdlight/runtime@0.1.0`, `.` export; 2 files/26 tests); new
`fixtures/m2/course/**` (frozen course + copied packet-31 slope/snap courses,
tolerances, 24 input sequences, 28 cases, SHA-256 index, checker); new
`tests/m2-controller/**` (4 files/35 tests) and `tests/browser/m2-controller/**`
(manual host + procedure); evidence `docs/acceptance/evidence-m2/32/**`;
`tools/check-boundaries.mjs` (UNITS + platformer→runtime types-only edge); this
handoff + the STATUS row. **Lockfile delta: 2 hunks, +11 lines** — the
`@thirdlight/platformer` link/entry only (two unrelated stale `asset-pipeline`
normalizations reverted).

**Commands and actual results.** `npm test` 119 files / 1562 passed (baseline
113/1501; no M1 regression); `typecheck` clean (14 packages); `check-deps` OK;
`check-boundaries` OK 14/251/908 (disposable probe 4/4, removed); `build` 4
built; contracts checker 34/34; physics/input/runtime/course checkers green.

**Acceptance.** PASS (real adapter/Node; the full measured table with its
tolerances is in the evidence manifest): every A12/A13/A14 numerical case is
inside its frozen tolerance — settle/rest, flat run, 12-step accel and 8-step
decel, jump apex 1.2196625 vs 1.249 ± 0.05, variable-height release,
single/no-air jump, wall/ceiling/ledge, the 43°/47° ramps, the 29.9–45.1° slope
table with sliding at/above the 30° minimum, snap within/beyond, coyote and
buffer boundaries, Z locked, snapshot unchanged, stall/dropped wall time and
fail-stop. All 16 accepted traces × 178 rows replay exactly. CPU is
container/directional (BR-2): composed step p99 0.0606–0.1165 ms vs the 8.333 ms
tick budget (the committed `06-cpu.json` values). **UNVERIFIED: the actual browser course with a physical keyboard
and gamepad, real tab resume, WebGL rendering, real WASM init/CSP and all
browser CPU/GPU claims** (no browser/hardware;
`tests/browser/m2-controller/README.md`, packet 37).

**Contract requests.** C32-1 slide policy added to §7 (the contract defines
none; the adapter reports (0,1) at rest, so the steep-normal branch fires only
on a fresh contact). C32-2 §12.8's runtime constant exports are absent. C32-3
§7.1's coyote prose is off by one vs the fixture. C32-4 the 12 m/s wall
approach is runway-limited (8.67 m/s; the ledge run reaches 12.0). C32-5: the
packet-31 "19 free-fall steps" note is **consistent** with the measured snap
chain — the 0.50 m drop is descended over grounded + `snapped` steps and then
~20 ungrounded free-fall steps (landing 0.36) — so the packet-32 manifest row
was corrected and no packet-31 change is made (Gate H repair, 2026-09-19,
F6).

**Next packet:** **Gate H review** — not started.
