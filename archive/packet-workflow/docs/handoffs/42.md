# Packet 42 — game host, controls, immutable delivery and dependency contract

**PROPOSED, not accepted. No commit. No package/lockfile/accepted-contract
change.** Gate K material only; packets 43+ are not started.

## Outcome

The M3 delivery/control contract is drafted: manifest v2 with settings/game/media
identity and block digests; the C35-5 closure (resolved settings reach the
controller **and** the physics configuration, hash-bound); one browser-safe shared
`game-host` composition entry with the `platformer-game`/`game-host` dependency
rows and the editor-UI prohibition; frame/menu control separation with
fresh-release, exclusive injected mode and focus/visibility reset; a typed
observation relay/SDK tool with run identity and a closed failure set; and the
static closure: the C38-1 CSP one-token diff, the C38-2 `allow="gamepad"`
requirement, MIME/cache, non-root relative closure and the export's own policy.

Both packet-38 defects owned by this packet are proposed with exact diffs and
quoted raw evidence (`evidence-m3/38/raw/engine.json` blocked;
`engine-nocsp.json`/`engine-csp-wasm.json` allowed).

## Changed files (scoped diff; nothing committed)

- New proposal: `docs/planning/m3-contracts/delivery.md` (§§1–11).
- New diffs: `docs/planning/m3-contracts/diffs/{sessions.md,export.md,dependencies.md}`.
- Appended: `docs/planning/m3-contracts/diffs/runtime.md` §"Packet 42 additions"
  (R42-1…R42-4). **No** packet-42 addition was needed in `diffs/commands.md`
  (C40-8 confirmed: run-command failures stay runtime codes) or
  `diffs/{project-model,workspace}.md`.
- New fixtures/checker: `fixtures/m3/delivery/**` (23 fixtures + `index.json` +
  `README.md` + `verification.md` + `tools/check-fixtures.mjs`), including the
  digest preimages, the manifest v2 example/reproducibility/variant fixtures, the
  wire/relay/run/control fixtures, the CSP/MIME/graph/scan/closure rows and the
  dependency rows.
- `docs/handoffs/42.md` (this file) and the STATUS row 42.

## Commands run and results (2026-09-19)

| Command | Result |
|---|---|
| `node fixtures/m3/delivery/tools/check-fixtures.mjs` | `groups: 16, checks passed: 153, failed: 0`, exit **0** |
| `node fixtures/m3/delivery/tools/check-fixtures.mjs --corrupt-control` | digest corruption exit **1**, semantic corruption exit **1**, control exit **0** |
| packet-39 checker (`fixtures/m3/contracts/tools/check-fixtures.mjs`) | exit **0** (37 groups) |
| packet-40 checker (`fixtures/m3/gameplay/tools/check-fixtures.mjs`) | exit **0** (23 groups, 124 checks) |
| packet-41 checkers (`fixtures/m3/media/tools/check-fixtures.mjs`, `generate-fixtures.mjs --check`) | exit **0** (13 groups/85 checks; 40 files reproduce) |
| independent digest re-derivation (`node:crypto` / `python3 hashlib` / `sha256sum`) | three-way agreement on `buildId = c25ba8f8…c654` (`verification.md` §3) |
| `npm test` | exit **0** — 135 files / 1701 tests |
| `npm run typecheck` | exit **0** |
| `npm run check-deps` | exit **0** |
| `npm run check-boundaries` | exit **0** — 15 packages, 283 source files, 1068 specifiers, no violations |
| `npm run build` | exit **0** — 4 built, 0 skipped |

## Acceptance rows (proposal scope)

| Row | Status |
|---|---|
| B16 (non-default settings reach both hosts + physics, hash-bound, pinned run unchanged) | **PASS for the proposal** — mechanism fixed (delivery §3.3), numeric comparison and digests pinned; measurement is 58–60 |
| B19 (capture pins scene/game/settings/media at one revision; late edit/reimport and failed/cancelled build cannot mix bytes) | **PASS for the proposal** — §2.3/§2.6; implementation evidence is 58–60 |
| B20 (bounded credential-free session/run-specific controls/observations; no browser ⇒ unavailable) | **PASS for the proposal** — §5; implementation evidence is 48/59 |
| B21 (declared==emitted relative closure, no authoring/server/CDN dependency) | **PASS for the proposal** — §6.3/§6.4; implementation evidence is 58/60 |
| B23 (two-tree reproducibility, timestamp-normalized buildId, previous-output preservation) | **PASS for the proposal** — §2.5/§2.6; implementation evidence is 58/60 |
| B02/B03 (authoring paths) | not this packet (39/45/48/56) |
| CSP/gamepad **runtime** behaviour | **UNVERIFIED here** — the token and attribute are proposed and cite packet-38 raw evidence; no browser was run in this packet. Audible output, physical gamepad and hardware GPU remain UNVERIFIED (packet 38). |

## Limitations

- PROPOSED text only; Gate K accepts/rejects per diff, and promotion is separate.
- No browser/GPU/audio/gamepad claim is made from this packet. The checker's CSP
  group only re-reads the packet-38 evidence files and compares the fixture text
  with the accepted `sessions.md` §17.4 fence.
- The `settings` numeric expectations are arithmetic identities, not measurements.
- `game-host`/`platformer-game` rows are proposals for Gate K; the units do not
  exist and no package was created.

## Contract-change requests (C42-n; destination sections)

- **C42-1** `sessions.md` §§7/10.5/11.5/13.1/13.5/13.6/15/17.1.1/17.4/17.5/17.6 +
  new §20 — packet 42's sessions rows (S42-1…S42-12); packet 48 owns the wire
  validators and `tl_game_control`/`tl_game_observe`.
- **C42-2** `export.md` §§2/3/5.1/5.2/5.4.1/5.5/6/7 — E42-1…E42-9.
- **C42-3** `dependencies.md` §§2/3/4.1/4.2/4.3/5/9 — D42-1…D42-7 (the two new
  units, the editor-UI rule, the 58/60 re-measurement duty).
- **C42-4** `runtime.md` §3.1 (C35-5 closure, supersedes the deferral) and §12.5
  (menu channel) — R42-1…R42-3; §15 confirmed unchanged (R42-4).
- **C42-5** packets 55/59 must implement `game-host`/the preview wrapper with
  `allow="gamepad"` and the §6.1 CSP; packets 58/60 must re-measure
  `export.md` §5.4.1 and the `sessions.md` §17.5 fetch list.
- **Explicitly not mine:** C39-1…C39-6 (39), C40-1…C40-8/C40-11/C40-12 (40/41),
  C41-1…C41-4/C41-7/C41-8 (39/40/41/44/58); C40-9/C40-10/C41-5/C41-6 are carried
  here (delivery §10 routing table).

## Exact next step

**Packet 43 — Contract pack integration and fixture audit** (dependency 38–42 /
Gate K, then stop for Gate K review/repair/promotion). Not started.
