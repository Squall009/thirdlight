# Packet 14 — Browser baseline and bounded physics selection

Date 2026-09-18 (UTC) · runId `tl14-2026-09-18-a` · packet 14/14–37 (Gate E
prerequisite: plan review accepted 2026-09-18 — met). Executed in the
container host of the M1 acceptance run; no commit made (scoped diff
summary at the end). Stopped after packet 14; packet 15 not started.

## Outcome

**PROVISIONAL physics selection with complete container-side evidence.**
Proposed (not approved): `@dimforge/rapier2d-compat@0.20.0`
(integrity `sha512-FFYwGrfJ…prip4wdWQ==`, Apache-2.0, zero dependencies,
WASM base64-embedded in the ESM entry), its built-in kinematic character
controller, and the canonical M2 game-logic model (grounded ⇒ vy=0, jump
edge ⇒ vy=7, 120 Hz fixed step). The selection stays provisional until the
owner's desktop evidence (rendering M1 + the probe bundle in the intended
separate-origin topology, WASM init under real CSP, `getGamepads`
exposure) is recorded and Gate E accepts it — exactly the "missing real
evidence leaves selection provisional and blocks dependent approval" rule
of the packet. Alternatives and tradeoffs: decision 0002 draft §1.

Container results (all directional per BR-2; machine: i5-12600H, 6 cores,
16 GiB, Node v22.22.1 — `01-machine-facts.json`):

- Frozen course (64 static colliders; T1–T11 tolerances declared pre-run):
  **all 10 phases PASS, two byte-identical runs**; degenerate-input
  (downward-while-grounded) check clean.
- CPU: controller p50 ≈ 0.004 ms; total fixed-step p99 ≤ 0.031 ms vs the
  8.333 ms tick budget (3 runs × 3600 samples).
- Cold init (3 fresh processes): 73–77 ms total (import 8 ms + init ~50 ms).
- IIFE bundle under the exact export.md §5.3 pinned flag set (repo esbuild
  0.28.2, `nodePaths` into the disposable prefix): 2,169,354 bytes, no
  top-level await, executes standalone.
- Strict-TS (repo TS 5.9.3, strict base config): candidate `.d.ts` fails
  (33 × TS2550 `Symbol.dispose`); fixed by one additive per-package
  `lib: esnext.disposable` entry; negative control proves strictness intact.
- Planck 1.5.0 / cannon-es 0.20.0 bounded functional comparison: both pass
  through static walls with kinematic bodies, do not climb the 43° ramp,
  need custom grounding raycasts; cannon-es additionally traps on
  self-hit raycasts (fixed with collision-filter groups) and has no
  Capsule shape in 0.20.0. Choosing either ⇒ M2 re-implements the whole
  character-controller layer.
- Rapier 3D: eliminated by desk research (labeled; no benchmark row).
- M1 `npm run build` baseline green (4 built, 0 skipped); dist + eval
  assets serve standalone from independent origins (all 200, plain-HTTP
  style URLs).

## Changed files (scoped diff summary — no commit)

New (untracked; all packet-14 or prior-session planning artifacts):
- `tests/evaluations/m2-physics/` — `course-spec.json` (frozen course +
  pre-run tolerances), `probe-rapier2d.mjs` (T1–T12 + CPU + coldinit modes),
  `coldinit-sample.mjs`, `probe-planck.mjs`, `probe-cannon-es.mjs`,
  `ts-probe.ts`, `build-iife.mjs` (standalone by design — BR-1: never
  picked up by vitest/typecheck/boundary scans; candidates imported only
  from the disposable prefix `EVAL_PREFIX=/tmp/tl-m2-eval-14`).
- `docs/acceptance/evidence-m2/14/` — `manifest.md` (claims → artifacts,
  reproduction, failure cases, manual desktop procedure) + 14 sanitized
  artifacts (`01`–`12`, `10-probe-iife.js`, `course-spec.json`).
- `docs/decisions/0002-m2-content-and-behavior.md` — DRAFT, §1 proposed
  physics selection (provisional; other sections reserved for packets 15–19).
Modified:
- `docs/STATUS.md` — M2 header note + packet-14 row (only packet-14-relevant
  progress row touched).
- `docs/planning/m2-physics.md` — appended "Packet 14 results" addendum.

Unchanged: all contracts, all packages, the repo lockfile and
`node_modules` (BR-1 honored: candidates never installed into the repo),
and the pre-existing uncommitted planning docs from the M2 planning session.

## Commands run (key results)

- `npm --prefix /tmp/tl-m2-eval-14/<cand> install …` ×4 — exact identities
  recorded in `02-candidate-registry.json`.
- `node probe-rapier2d.mjs phases` ×2 → `05-phases-run1/2.json` (all PASS);
  full-output equality → `05-determinism.json`.
- `node probe-rapier2d.mjs cpu` → `06-cpu.json`; `node coldinit-sample.mjs`
  ×3 fresh processes → `07-coldinit.json`.
- `node probe-planck.mjs` → `08-planck.json`; `node probe-cannon-es.mjs`
  → `09-cannon-es.json`.
- `node build-iife.mjs /tmp/tl-m2-eval-14/out` (asserts esbuild 0.28.2)
  → `10-iife-build.json`; `node out/probe-iife.js` → marker OK, exit 0.
- `tsc 5.9.3 --noEmit` on `ts-probe.ts`, 3 configs (base / +esnext.disposable
  / negative control) → `11-tsc-strict.json`.
- `npm run build` (root) → green; two `python3 -m http.server` origins +
  curl checks → `12-m1-build-subpath-serving.json`.
- Root toolchain re-check after all edits: `npm run check-deps` OK,
  `check-boundaries` OK (10 packages / 150 files / 514 specifiers),
  `typecheck` OK, `npm test` → 66 files / 841 tests passed.

## Acceptance criteria (packet 14)

1. **Exact recommended pin/license and alternatives/tradeoffs
   documented** — **PASS** (decision 0002 draft §1: pin + integrity +
   license; planck/cannon-es/rapier2d/rapier3d tradeoffs; strict-TS
   accommodation; known limitations carried to packets 17/31/35).
   Note: documented as PROPOSED/PROVISIONAL — approval is Gate E's.
2. **Reference browser can render M1 and expose a physical gamepad in the
   intended separate-origin topology** — **UNVERIFIED in-container** (no
   browser/GPU/gamepad). Serving side verified (curl 200s, two origins);
   a step-by-step manual desktop evidence procedure is recorded in the
   manifest, and secure-context facts (`getGamepads` needs HTTPS or
   localhost; `gamepad` policy default `*`) are documented with the
   BR-3 fallback: if plain-HTTP LAN fails, the owner picks the
   localhost/TLS-on-LAN topology before packet 30.

## Limitations

- All CPU/init numbers are container/Node (BR-2); labeled, and the
  selection decision does not depend on them (p99 margin ≈ 270×), but the
  performance claim's acceptance does (desktop re-measurement available
  via the same probes).
- Browser execution of the bundle (WASM init under real CSP, rendering,
  gamepad) is unverified until the owner runs the desktop procedure.
- The IIFE run that proved the bundle executes was done in Node (no
  browser in the container); recorded as such.
- planck/cannon-es numbers are functional comparisons (what works
  out-of-the-box + required custom-controller inventory), not browser
  benchmarks — no fabricated benchmark column (m2-physics.md experiment 6).
- No GPU timing claims anywhere (packet rule).

## Contract-change requests

None. The strict-TS `lib: esnext.disposable` accommodation is proposed in
decision 0002 draft §1.2(4) (per-package additive, no repo-wide change).
Two candidate notes are carried to packet 17 (contract 17 drafting):
1-step horizontal stall jitter at existing floor contact, and the ~1.4 mm
first-step grounded-settle dip (declare a settle pre-roll). The
parentless-collider pattern goes to packet 31 (adapter).

## Exact next step

**Owner action:** record the desktop evidence per
`docs/acceptance/evidence-m2/14/manifest.md` (procedure §"Manual desktop
evidence procedure"; output → `13-desktop-evidence.json`), then approve or
amend decision 0002 §1 at **Gate E**. After that: **packet 15**
(content storage, asset identities and migration contract) — its
selection-dependent parts stay blocked while 14's evidence is absent, per
the packet's "Next" line. Not started.