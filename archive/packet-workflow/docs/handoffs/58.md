# Packet 58 — Manifest v2 core + M3 export pipeline (B16/B19/B21)

**Date:** 2026-09-21 · **Scope:** `packages/project-model/**` manifest v2
derivation, `packages/exporter/**` shared M3 closure builder + export writer/
scans, `packages/game-host/**` public composition (unchanged), the workspace
captured-view seam (additive v3 read), `tests/integration/m3-builds/**`,
approved exports/edges/build checks. No git commit (working tree only, HEAD
`5b746ee`).

## Outcome

The project-model now owns the pure **runtime-content manifest v2** derivation
and the exporter composes the complete **M3 standalone export** from ONE
captured authoring state (the single acknowledged envelope read — delivery.md
§2.6). Both the preview/play path (packet 59) and the export path will use the
same shared closure builder + the same single production composition
(`game-host`).

**`packages/project-model`** — `src/manifest-v2.ts` (new, pure):
`captureManifestV2` (the 22-key v2 document, self-identifying `buildId`),
`validateManifestV2` (strict reader incl. captured-state re-derivation:
`media_identity`, `asset_kind_mismatch`, `sceneDigest`),
`manifestBuildIdInputV2` (the `buildId` preimage), `captureContentViewV3`
(reachable kind-tagged assets + resolved six-key settings + frozen `game` +
`contentDigest`), `resolveMediaIdentityV3` (cue slots + animation rows),
`manifestVersionCompat` (v1→v2 rule), the block-digest primitives and the M3
engine pins / module table / recipe versions. All digests use
`sha256(JSON(value, 2)+"\n")` in the **owning contract's key order** (not
sorted). `src/capture.ts`: `collectAssetRefsV3` made public (was private —
consumed by the v2 view). `src/manifest-v2.test.ts` (23 pure tests).

**`packages/exporter`** — `src/content-closure.ts`: `buildContentClosureM3`
(the shared M3 closure: one captured v3 scene+content → v2 manifest + declared
kind-tagged artifact bytes; **fails closed on source-bearing behaviors** —
CC-55-3). `src/export-bootstrap-m3.ts` (new): the M3 page bootstrap — fetch
`manifest.json` (v2, WebCrypto `buildId` verify) + `scene.json` (digest
verify) + declared assets (generated switch), then `createGameHost` (the single
shared composition) with manifest `settings`/`game`, the Rapier port
(`solver.gravityY = settings.gravity_y`), the scene adapter and the audio
owner. `src/export-m3.ts` (new): the `exportProjectM3` pipeline (closure → M3
bundle → reference+Rapier probes → revision re-read → output target → graph
check → manifest self-identity → GLB/**WAV** container scans + §5.4 forbidden
patterns + relative closure → `meta.json` v2 → atomic publish). `src/graph.ts`:
`checkBundleGraphM3` (allow `game-host`/`platformer-game`; M3 exporter file =
`export-bootstrap-m3.ts` only). `src/export-bundle.ts`: `buildM3Bundle`
(artifact virtual module only). `src/export-content-scan.ts`: `scanWavContainer`
(RIFF/WAVE, PCM mono/48k/16-bit). `src/export.ts`: v3→M3 dispatcher branch.
**`export-bootstrap-m2.ts` is byte-stable (untouched).**

**`packages/workspace`** — additive **captured-view seam** `readCapturedV3`
(workspace.md §16): the single acknowledged envelope read's v3 `scene` +
`content` halves for the closure builder (pure read; a `storageVersion` 1/2
project is `version_combination_unsupported`). The v1 `captureContentView` view
is unchanged. **Interpretation (flag for Gate O re-review):** the packet-58
may-edit names the "workspace captured-view seam (only if named at K)". Gate K
did not name it explicitly; I treat delivery.md §1 ("the workspace owns the
single acknowledged envelope read") + the `captureContentView` public edge as
satisfying the naming — the seam is purely additive (no derivation duplicated
in the workspace) and the derivation stays in project-model.

## Green bar (fresh)

`npm test` **173 files / 2218 tests**; `typecheck` 17 pkgs clean; `check-deps`
OK (all exact); `check-boundaries` 17 pkgs / 333 files / 1305 specifiers / 0;
`build` 4/0; all M2+M3 fixture checkers green (delivery 153, media 119,
gameplay 127, contracts 39, storage 10, m2/contracts 34). New tests: 23
(project-model v2) + 16 (`tests/integration/m3-builds/manifest-v2.test.ts`,
byte-identical to committed `manifest-v2-example.json`) + 4 (B16) + 6 (B19) + 3
(B21).

## Acceptance criteria

- **B16** (settings reach both hosts + hash-bound + pinned run): **PASSED** —
  measured `run_speed` 4→6 (ratio 1.5×) through the real runtime+controller and
  `gravity_y` −19.62→−30 (a plain dynamic body free-falls `vy = g·N/SIM_HZ`,
  −30@120 / −60@240, no controller clamp) through the real Rapier world; changed
  `run_speed` changes `settingsDigest`/`contentDigest`/`buildId` while
  `sceneDigest` is stable; the captured closure pins revision 1 and a late edit
  is detectable.
- **B19** (capture/build pins at one revision): **PASSED** — the closure derives
  the v2 manifest from one capture; failure modes `blob_missing`,
  `asset_digest_mismatch`, `behaviors_unsupported` (CC-55-3 fail-closed) and
  invalid content are refused; the revision pin is verified.
- **B21** (complete declared==emitted relative closure): **PASSED** — the output
  tree (index.html, js/main.js, manifest.json, scene.json, the GLB + WAV
  content artifacts, meta.json v2 with the manifest block's
  `gameDigest`/`settingsDigest`/`mediaDigest`); declared==emitted; the emitted
  `scene.json` digest == `manifest.sceneDigest`; container scans (GLB+WAV) pass;
  the graph check flags `node:`/`backend` (negative control) and the clean M3
  graph passes; a late revision bump fails `export_snapshot_mismatch` leaving the
  previous output byte-untouched.
- **Browser/visual/audible:** **UNVERIFIED** (no browser/GPU/audio device in
  container — packet-38 baseline §1). The M3 page walkthrough (WebGL render, the
  audio owner, the DOM HUD) is packet 61/62 evidence.

## Limitations / disclosures

- **GLB model rendering gap (carried):** the shared three-adapter
  `createSceneAdapter` realizes boxes/lights/surfaces/camera only — model
  entities become empty Groups; there is no model-attach surface. The M3 closure
  still **declares + emits** the model bytes (B21 = tree completeness); the page
  reads only the audio cues it consumes. A visible player in the packet-61 sample
  must use a **box** player. Follow-up: a three-adapter model-attach surface (out
  of 58's may-edit — three-adapter is not on 58's list).
- **§5.4.1 count re-measurement deferred to the 58/60 re-measure:** the M3
  pipeline gates on the §5.4 **forbidden** patterns (a/b/c/e/g/i = 0) + relative
  closure + container scans; the exact per-needle counts against the current
  three + `game-host` install are re-measured at Gate O/P (58/60).
- **M3 export fails closed on source-bearing behaviors** (CC-55-3, open at Gate
  N): the shared composition has no behavior-linking channel; the sample (packet
  61) uses built-in modules only.

## Reopened section (Gate L) / contract-change requests

- **Reopened project-model §44 row (Gate L):** the manifest-v2 derivation
  (`manifest-v2.ts`) + the `collectAssetRefsV3` export (was private) reopen the
  packet-44-owned project-model section. **Bounded re-review:** additive v2
  surface only; the v1 `captureManifest`/`validateManifest` path is unchanged;
  the v2 core is tested (23 unit tests) and byte-identical to the committed
  `manifest-v2-example.json` (16 integration tests). Recorded here before Gate O
  acceptance.
- **Workspace captured-view seam interpretation** (above) — **flag for Gate O
  re-review** (no silent change; purely additive).
- No other contract changes.

## Next packet

**Packet 59 — Isolated M3 Play** (backend v3 play-start wiring: pass the
captured content block to the shared closure, the v3 snapshot with
`settings`+`game`, the preview wrapper → `createGameHost`; the relay/bridge;
CC-55-3 if behaviors are needed). Do not auto-start.