# Packet 43 — Contract pack integration and fixture audit

**Done 2026-09-19. Nothing accepted, nothing promoted, no commit.** Output is
inside the packet's write scope: `docs/planning/m3-contracts/**`,
`fixtures/m3/audit/**`, `docs/decisions/0003-m3-sample-game.md` (draft), this
handoff and STATUS row 43. `docs/contracts/**`, `packages/**`, `tests/**`,
dependencies and the lockfile were **not modified** (the accepted contracts and
packages show as modified only because HEAD is the M1 baseline with the
uncommitted M2 tree; this packet did not touch them). `dist/**` was rewritten by
the required `npm run build`.

## Outcome

The Gate K pack is consolidated and audited. Deliverables:

1. **`planning/m3-contracts/contract-diffs.md`** — the consolidated inventory:
   **104 diff rows** (PM1–PM19, W1–W9, C1–C14, CMD41-1…3, PM41-1…7, R40-1…16,
   R41-1…4, R42-1…4, S42-1…12, E42-1…9, D42-1…7) plus **3 packet-43
   reconciliation rows** (PM43-1…3) and **2 new-contract rows** (NC-1/NC-2) =
   **109 rows, every status `open`** (0 accept / 0 reject, Gate K's call). Each
   row has author packet, exact destination section, proving fixture(s),
   consumer packets, version effect, collision/supersession note and status.
   §3 routes **every C38/C39/C40/C41/C42 request exactly once**; §4 lists the
   open contradictions; §5 records the packet-43 supersessions; §6 promotion
   order; §7 the numbered Gate K decisions.
2. **`planning/m3-contracts/traceability.md`** — feature→command→UI/MCP→runtime
   →export→acceptance matrix: all 21 authorability rows with an explicit
   **creation** op, all B01–B24 with the acceptance owners, and the three PR-1
   values (instructions string, checkpoint `safeSpawnId`, checkpoint
   `activation`) called out with creation paths.
3. **`fixtures/m3/audit/**`** — integrated checker
   (`tools/check-audit.mjs`, plain Node, no dependency) + `index.json` +
   `README.md`. Runs the four packet checkers as one entry point and adds
   cross-pack checks: inventory closure, destination ownership, version-table
   agreement, one owner per new code/component/op/phase/module/artifact class,
   no-fixture-invents-a-rejection-code, traceability/creation coverage, and
   superseded-quote resolution.
4. **`docs/decisions/0003-m3-sample-game.md`** — appended a PROPOSED
   integrated-proposal map for §§2–§5 (status **not started**; no approval
   claimed).

## Changed files (scoped; no commit)

New: `docs/planning/m3-contracts/{contract-diffs,traceability}.md`;
`fixtures/m3/audit/{README.md,index.json,tools/check-audit.mjs}`;
`docs/handoffs/43.md`. Modified: `docs/decisions/0003-m3-sample-game.md`
(append-only), `docs/STATUS.md` row 43. Nothing under `docs/contracts/**` or
`packages/**`.

## Commands and results (exit codes)

| Command | Exit | Result |
|---|---|---|
| `node fixtures/m3/audit/tools/check-audit.mjs` | **0** | 8 groups passed, 0 failed |
| `node fixtures/m3/audit/tools/check-audit.mjs --corrupt-control` | **0** (control) | 3 corruptions each forced exit 1 with the expected check; all three controls PASS |
| `node fixtures/m3/contracts/tools/check-fixtures.mjs` | **0** | 37 groups |
| `node fixtures/m3/gameplay/tools/check-fixtures.mjs` | **0** | 23 groups, 124 checks |
| `node fixtures/m3/media/tools/check-fixtures.mjs` | **0** | 13 groups, 85 checks |
| `node fixtures/m3/delivery/tools/check-fixtures.mjs` | **0** | 16 groups, 153 checks |
| `npm test` | **0** | 135 files / 1701 tests |
| `npm run typecheck` | **0** | green |
| `npm run check-deps` | **0** | green (no dependency change) |
| `npm run check-boundaries` | **0** | 15 packages, 283 files, 1068 specifiers, no violations |
| `npm run build` | **0** | 4 built, 0 skipped |

Node 22.22.1. No install, no service, no commit.

## Audit findings (contradictions found and how resolved)

**Resolved by packet 43 (recorded, no other packet's substance rewritten):**
- Obsolete accepted M1/M2 non-goal wording superseded by three new rows
  `PM43-1…3` (`project-model.md` §1 and §14: "no lights" / "no materials beyond
  `box.material.color`"; `export.md` §8: "no GLB/glTF … no lights, no audio
  files"). Each only **scopes** the M1/M2 sentence to those versions; no
  requirement is weakened. Exact OLD quotes verified present in the accepted
  contracts by the audit checker.
- Coexistence notes added where two packets legitimately extend one table
  (`commands` §3.1.1 and §5.4; `runtime` §12.3 and §13), so "same destination"
  is explicit rather than ambiguous.

**Left open for Gate K (§4 items, Gate K decisions):**
1. **R40-16** is declared in the `diffs/runtime.md` summary (`runtime.md` §11
   bullets) but has no section body — incomplete diff.
2. **C41-1 `GameView.playerMotion`**: `presentation.md` §41.3.6 consumes it but
   `gameplay.md` §6 has no such field; the role selector's committed input is
   unnamed.
3. **`platformer-game` export name**: three readings — `sessionSpec`/`cameraSpec`
   (`gameplay.md` §11), `platformerGameSpec` (`delivery.md` §3.4, D42-2),
   `platformerGameSessionSpec` (`fixtures/m3/delivery/deps/dependency-rows.json`).
4. **`game_config_invalid` carries shape**: `path`+`reason` (`model.md` §23.9)
   vs `path` alone (`diffs/commands.md` C9).
5. **Manifest example versions** are illustrative (`@thirdlight/runtime` 0.3.0,
   `@thirdlight/three`) and do not match the repository packages (all 0.1.0) or
   the real npm name (`three`).
6. **Component count**: `model.md` §23.3 / PM5 say "seven" components and list
   **six** (the v2 registry is 8, the v3 registry 14).

**Checked and consistent:** the scene/storage combination tables in `model.md`
§23.2, `storage.md` §S2 and `diffs/workspace.md` W3; the `manifestVersion` 1→2
vs authoring `schemaVersion` 1 naming rule; one owning packet for all 37 new M3
error codes; one owning op for every v3 component; the phase order in
`gameplay.md` and `diffs/runtime.md`; every fixture rejection code registered.

## Acceptance

- **Proposal-scope PASS**: complete diff inventory with one owner per
  field/op/error/phase/module/artifact, a creation path for every sample value
  and B-row, and an executable audit checker with a passing corruption negative
  control — the packet's own acceptance condition ("all diff rows and owner
  decisions ready, not approved").
- **B02/B03** (authoring/creation coverage): **PASS for the proposal** only —
  `traceability.md` + `authoring.md` §A8.
- **B01–B24 implementation rows: UNVERIFIED.** No product, browser, filesystem,
  MCP or export behaviour was executed or claimed here.
- **Browser feasibility (packet 38, not re-measured):** a real headless
  Chrome-for-Testing/SwiftShader path exists (DOM/HUD, canvas `toDataURL`,
  console/network, resize, lifecycle, WAV decode, preview/export loading are
  executable and must be executed by their owners). **No physical gamepad, no
  audio device, no display, no hardware GPU**: B13 audibility, B15/B22 physical
  keyboard/gamepad and hardware-GPU claims stay **UNVERIFIED** with the owner
  manual annex. **No browser or hardware row is PASS in this packet.**

## Limitations

- The audit checker is a text/JSON consistency tool over committed proposals and
  fixtures; it runs no product code and proves no behaviour. It validates field
  ownership only for components (the v3 field containers with a registry);
  per-field shapes remain the packet checkers' scope.
- The four packet checkers are contract-consistency replays (unchanged here).
- No acceptance-plan constant required a Gate K fix, so `m3-acceptance.md` was
  **not edited**; no requirement was weakened. No acceptance row was re-scoped.
- No reviewer, owner, browser or hardware approval is claimed.

## Contract-change requests (numbered; exact destinations)

- **C43-1** `planning/m3-contracts/contract-diffs.md` — new Gate K inventory
  (109 rows, all `open`), routing table, open-conflict list, reconciliation
  section and promotion order. Consumed by Gate K and the docs-only promotion.
- **C43-2** `project-model.md` §§1/14 and `export.md` §8 (rows PM43-1…3) —
  explicit scoping of the obsolete M1/M2 non-goal sentences; promotion applies
  the §5 NEW text only if Gate K accepts each row.
- **C43-3** `diffs/runtime.md` §11 (row R40-16) — packet 40 must supply the
  missing change-rule bullets, or Gate K drops the row (decision K-1).
- **C43-4** `gameplay.md` §6 (decision K-2) — add `GameView.playerMotion` or name
  the selector input before packet 53.
- **C43-5** `gameplay.md` §11 / `delivery.md` §3.4 / `diffs/dependencies.md` §D
  and `fixtures/m3/delivery/deps/dependency-rows.json` (decision K-3) — one
  `platformer-game` export name.
- **C43-6** `diffs/commands.md` C9 vs `model.md` §23.9 (decision K-4) —
  `game_config_invalid` carries shape.
- **C43-7** `delivery.md` §2.2 / `manifest-v2-example.json` (decision K-5, and
  58's derivation duty) — illustrative identity values.
- **C43-8** `model.md` §23.3 / `diffs/project-model.md` PM5 (decision K-13) —
  "seven" → "six" components.
- **C43-9** `m3-acceptance.md` — no change requested; the plan's in-container
  rule and UNVERIFIED labels stand.
- **C43-10** Gate K — record the accept/reject per row and the K-1…K-13
  decisions; promotion is docs-only and separate.

## Exact next step

**Gate K review, repairs and docs-only promotion — stop. Do not start packet 44
or any K-dependent implementation.**
