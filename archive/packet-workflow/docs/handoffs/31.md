# Packet 31 — Rapier 2D adapter

**Outcome.** New `@thirdlight/physics-rapier`: cancellable async
`createPhysicsPort(config, signal?)` before runtime start, one `World` per game
with static box + convex-polygon colliders, the **parentless** kinematic capsule
with ground snap, XY-only grounding/support normals from collision results and
idempotent disposal. No second frame driver; `runtime` imports no Rapier.
**Selection per decision 0002 §1, owner pre-approval (autonomous M2 build
instruction, 2026-09-18); final manual review pending.**

**Scoped diff (no commit).** New `packages/physics-rapier/**` (exact
`@thirdlight/runtime@0.1.0` + `@dimforge/rapier2d-compat@0.20.0`; tsconfig with
the approved additive `lib: ["esnext.disposable", …]`; 4 test files/39 tests);
new `fixtures/m2/physics/**` (4 courses, 18 cases, 36 failure cases, Node
checker, bundle probe); new `tests/m2-physics/**` (3 files/61 tests);
`tools/check-boundaries.mjs` (UNITS + the new edge row) and `tools/check-deps.mjs`
(+PINS/PIN_CONSUMERS + its pin-table test); evidence
`docs/acceptance/evidence-m2/31/**`; this handoff + the packet-31 STATUS row.
**Lockfile delta: 3 hunks, +16 lines** — the pin entry (integrity
`sha512-FFYwGrfJov7d5kn4fxblG/ip5C+sYQL+Jl/hqLvDvu4BcMD/Mhrz7cTvnZTtuIRDXY8CLura
ObqB/prip4wdWQ==`, verified equal to lockfile/registry/decision 0002 §1) and the
`@thirdlight/physics-rapier` link/entry; nothing else changed.

**Commands and actual results.** `npm test` 113 files / 1501 passed; `typecheck`
clean (13 packages); `check-deps` OK; `check-boundaries` OK 13/246/895, 0
violations (negative probe 3/3 then removed); `build` 4 built / 0 skipped;
contracts checker 34/34; the physics/input/runtime fixture checkers green.

**Acceptance.** PASS (real library/Node, 39 + 61 tests): floor/wall/ceiling/box-ramp/
convex-ramp correction; slope normals + the 44.9/45.0/45.1° threshold; ground
snap; no Z; 100 create/dispose cycles with the library's own counts (62/61 per
cycle, stale-handle throw, JS heap flat); cancellation, invalid shape, forbidden
transform, stale handle, correction failure; the probe bundle builds under the
pinned flags (2,191,202 bytes). **UNVERIFIED: browser WASM init,
CSP, static packaging** (no browser; packet-14 procedure + packet 37).

**Limitations.** Rapier's WASM linear memory never shrinks after `World.free()`
(RSS +14–90 MB over 100 cycles, JS heap flat), so the resource evidence is
counted, not a leak proof. Selection stays PROVISIONAL until the desktop
evidence; the `StaticColliderSpec`-shaped gaps are above.

**Contract-change requests.** C31-1 §8's 45.1° cos is a typo (0.70587157, not
0.70608759). C31-2 the 0.20.0 one-time ground-offset push-out (11.3 mm) is absent
from `numComputedCollisions()` and exceeds the 1 mm non-`snapped` allowance:
`snapped` now covers any bounded ground-contact correction (≤ snap+skin), beyond
which the adapter throws `collision_correction_failed`; propose re-wording §8 item
5 / runtime.md §12.6. C31-3 snap chains across steps (a 0.2 m drop stays
grounded). C31-4 no autonomous sliding at the 30° minimum-slide angle (packet 32
owns that policy). C31-5 Rapier *does* report grounded on a 45.1° face (the §8
item 4 parenthetical is wrong). Two additive export/config notes are in the
manifest.

**Next packet:** 32 — 2.5D platformer controller and diagnostic course (not started).
