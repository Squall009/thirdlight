# Packet 62 — Integrated M3 acceptance and owner playthrough

**Outcome: COMPLETE in-container (2026-09-21).** The M3 acceptance record
(`docs/acceptance/m3-report.md`) is written with row-level B01–B24 verdicts
(22 PASS, 2 PARTIAL, 0 FAIL), and the PR-6 headless-browser path is **executed**
(not deferred) against the real Beacon Reach export. The subclaims the container
physically cannot execute (physical keyboard/gamepad, audible output, hardware
GPU, real display, rendered animated GLB poses, cross-host visual checklist) are
labelled UNVERIFIED and each names an owner procedure in the §6 annex. No engine
source edits this packet (all `packages/**` unchanged); the one sample defect the
probe found (the camera authored at z=0) is repaired in `samples/beacon-reach/`
+ `tests/integration/m3-sample/` (disclosed below).

## What was executed

- **PR-6 integrated-browser probe** (`tests/evaluations/m3-browser/beacon-reach.mts`,
  run `npx tsx …`, exit 0): builds the standalone M3 export of the captured sample
  by the real `exportProjectM3` pipeline over the 7 real generated asset bytes
  (fixed clock → deterministic tree), serves it at `http://127.0.0.1:<port>/games/
  beacon-reach/` (an independent static origin; the authoring origin is
  unreachable by construction), and loads it in the local headless Chrome 151
  (ANGLE/SwiftShader — software rasteriser, **never a hardware-GPU claim**) over
  the CDP harness. Evidence in `docs/acceptance/evidence-m3/62/` (`manifest.md` +
  `raw/{br-01-title,br-02-spawn,br-03-moved}.png, br-console-network.json,
  br-summary.json`):
  - **B04** title/HUD render ("Beacon Reach", objective+instructions as text),
    no movement pre-Start (two identical canvas frames), canvas focus, scripted
    Enter starts the run — **PASS**.
  - **B13** all 5 cues decode through a real `AudioContext` (mono, ~44.1 kHz,
    correct durations) — **PASS** (audibility itself UNVERIFIED).
  - **B21** browser network: 16 requests, **0 external origins** (the same-origin
    browser `favicon.ico` is not a closure dependency) — **PASS**.
  - **B10** resize executed (PR-6 requires it, not deferred): the canvas renders
    non-black at two desktop aspects (16:9 and ~21:9) and the view differs across
    the resize — **PASS**.
  - **console**: no `console.error` (only the same-origin `favicon.ico` 404) —
    **PASS**.
  - **B05** scripted `KeyD` reaches the focused canvas (keydown spy:
    `{code:"KeyD",repeat:false,isTrusted:true}`) and the rAF timestamp advances,
    but the rendered canvas is byte-identical across 8 rAF frames — the standalone
    export's simulation could not be confirmed to step the capsule in this
    headless/software environment, and the export does not expose the player
    position. **UNVERIFIED** (the Node step trace is the authoritative controller
    evidence).
- **M3 acceptance record** (`docs/acceptance/m3-report.md`): B01–B24 row-level
  verdicts (22 PASS / 2 PARTIAL / 0 FAIL), the integrated journey, the limitations,
  and the **owner manual annex (§6)** naming the exact procedure + artifact for
  every UNVERIFIED subclaim (O-B02/04/05/06/07/08/10/11/13/14/15/17/20/22/24).

## Sample defect found + fixed by the probe

The first probe run rendered a **black canvas**: the sample's baseline camera was
authored at `z = 0` (in the z=0 scene plane, looking away from the scene). The
camera's `position.z` is the fixed view depth (`CAMERA_Z = 12`, gameplay.md §7.1)
and is never written by the follow module. Fixed to `z = 12` in
`samples/beacon-reach/tools/capture-project.mts` and
`tests/integration/m3-sample/sample.test.ts`; the sample was re-captured (new
`sceneDigest d339acb1…`, content digest unchanged `e4d4906e…`), the sample tests
re-run (7/7), and the probe re-run — the canvas now renders the lit scene. This is
a sample-authoring correction (not an engine change), disclosed here per the
packet-61 disclosure that the sample is the packet-62 evidence input.

## Commands run and results

- `npx tsx tests/evaluations/m3-browser/beacon-reach.mts` → exit 0; rows recorded
  in `br-summary.json` (startedAt `2026-09-21T03:35:37.838Z`,
  `Chrome/151.0.7922.34`).
- `npx tsx samples/beacon-reach/tools/capture-project.mts` → exit 0;
  `sceneDigest d339acb1549eab49950e5bbef2c731bb80d781325f26e1ce044a1c21e07ce2bb`,
  `contentDigest e4d4906ea0e10f72f54719ce92d2d7a6db07afb96278d33094e39475acfb3c4f`.
- `npx vitest run tests/integration/m3-sample/` → 2 files / 7 tests passed.
- Full green bar: `npm test` **179 files / 2237 tests**; `npm run typecheck` (17
  pkgs), `check-deps`, `check-boundaries` (17 pkgs / 335 files / 1319 specifiers /
  0), `build` (5 built / 0 skipped) all exit 0; the binding M2 bundle-scan 1/1;
  M3-export determinism+bundle-scan 5/5.

## Acceptance criteria (B01–B24)

- **PASS (in-container, 22):** B01, B02 (command surface), B03, B04 (title/
  no-movement/keyboard start), B05 (controller obeys M2 + sample traversable, via
  the Node step trace), B06, B07, B08, B09, B10 (camera math), B11 (lighting logic
  + scene renders), B12, B13 (decode/lifecycle/voice-cap/denial), B14 (roles/
  blending logic), B15 (HUD + scripted keys), B16, B18, B19, B20 (isolation),
  B21, B23, B24.
- **PARTIAL (2):** **B17** (the sample authors the courier/beacon as direct model
  entities — it does not create two independent decoration prefab copies; the
  browser/SDK workflow images are not executed) and **B22** (the production-
  composition journey is verified by the Node step trace; the physical
  keyboard/gamepad playthroughs are not executed).
- **UNVERIFIED subclaims (owner annex §6):** physical keyboard/gamepad (B04/05/
  08/15/22), audible output (B13), hardware GPU / real display (all visual rows),
  rendered animated GLB poses (B14), cross-host visual look checklist (B11),
  preview rendered canvas (B20), two-aspect camera (B10), two independent prefab
  copies (B17).
- **FAIL: 0.** No unresolved gameplay/durability/security/export failure.

## Limitations

- The PR-6 path could not confirm the capsule stepping in the standalone export
  (canvas static; the export exposes no player position); the Node step trace is
  the authoritative controller evidence.
- The GLB courier/beacon are imported but not rendered as animated models (the
  three-adapter realizes boxes/lights/surfaces/camera) — the visible player is a
  box; the animated-pose subclaim is UNVERIFIED (O-B14).
- The browser/physical/audible subclaims are deferred to the owner (§6 annex);
  none is an implicit pass.

## Contract-change requests

None new. **CC-55-3** (the packet-55 HUD/status wording diff) remains open,
carried through Gates N/O, awaiting an explicit owner scope decision; it does not
block any gameplay/durability/export row. **CC-L-1** is resolved
(`repair-cc-l-1.md`).

## Changed files

`docs/acceptance/m3-report.md` (rewritten as the in-container acceptance record +
owner annex), `docs/acceptance/evidence-m3/62/{manifest.md,raw/**}`,
`tests/evaluations/m3-browser/beacon-reach.mts` (new probe),
`samples/beacon-reach/tools/capture-project.mts` + `samples/beacon-reach/captured/
project.json` + `samples/beacon-reach/recipe/commands.json` (camera z=12 repair,
re-captured), `tests/integration/m3-sample/sample.test.ts` (camera z=12 baseline),
`docs/handoffs/62.md`, `docs/STATUS.md` (row 62). No `packages/**` change.

## Exact next step

**Gate P** — the final in-session review of packet 62 (fresh toolchain + checkers
+ reviewer-side verification of the evidence), then the M3 acceptance record is
final. M4 receives a new planning request only when the owner asks for it.