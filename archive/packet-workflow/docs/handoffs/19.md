# Packet 19 handoff — delivery/protocol/export/dependency integration

2026-09-18, single session, autonomous M2 build. HEAD `5b746ee`; no commit, no
push, no install, no lockfile change. Exactly packet 19; Gate E, promotion, packet 20 not started.

## Outcome

PROPOSED boundary pack (pending Gate E):

- `planning/m2-contracts/delivery.md` — immutable runtime-content manifest
  (resolved asset/behavior versions + engine modules + recipe/toolchain + build
  identity; `snapshotId` ≠ `buildId` ≠ `outputDigest`; build failure preserves
  the previous output); authenticated project-scoped immutable-version
  asset-byte reads (renderer gets no token); read-only play-content locator
  (bearer capability, TTL 900 s + 60 s grace, no listing/traversal/cross-project,
  redaction, export exclusion, immutable cache, reload never newer); bridge v2
  keeping exact origin/source/nonce and no binary in WS state; bounded
  exclusive-test input relay (≤600 frames, step range + `snapshotId`/`buildId`,
  no browser ⇒ structured `session_unavailable`); format-aware GLB/WASM/JS/
  closure validation; export closure with allowlisted relative artifact reads
  (replacing M1's one-fetch rule only via the export diff); bounds; negative
  matrix.
- `planning/m2-contracts/contract-diffs.md` — every proposal ⇒ destination
  section ⇒ decision § ⇒ status ⇒ implementing packet, plus resolutions:
  `publishAsset`; container supersession; P17-A5/A6 `+`-headings; 15/16
  interface notes; naming (`behavior-build`; behavior types in `runtime`).
- `diffs/sessions.md` S19-1–S19-14 (snapping 0.25 m/15°/0.25, scale [0.01,100],
  1e-4 quantum, Shift disables one gesture, zero commands during drag, one on
  release, none on cancel, no persistent setting; locator; bridge v2; relay;
  `engineRoot`); packet-19 additions in `diffs/export.md` (E19-1–E19-10) and
  `diffs/dependencies.md` (D19-1–D19-10, D19-A).
- `decisions/0002` §§2–6 as DRAFT/PROPOSED (nothing approved);
  `m2-acceptance.md` refined with Gate-E-fixed constants/evidence names;
  README/verification updated; 6 `fixtures/m2/contracts/delivery/*.json` + 6
  checker groups `p19-*`; `docs/acceptance/evidence-m2/19/**`.

selection per decision 0002 §1, owner pre-approval

Pin `@dimforge/rapier2d-compat@0.20.0` (PROVISIONAL; packet-14 desktop evidence
+ Gate E open; lockfile only at packet 31). New units: `asset-pipeline`, `input`,
`physics-rapier`, `platformer`, `behavior-build`; no new GLB pin (pinned
`three@0.186.0` GLTFLoader subpath). U-4: ACCEPT both (`engineRoot`, export.md
§5.4.1 reference entry), fail-closed.

## Commands (actual)

- checker exit 0, 33/33; `npm test` exit 0, 841/841
- `typecheck`/`check-deps` exit 0; `check-boundaries` OK (10/150/514); `build`
  exit 0 (4 built)
- inventory check 6/6 exit 0; negative control exit 1 (9 failures)
Artifacts: `docs/acceptance/evidence-m2/19/{01..06}`.

## Criteria

Packet §19 criteria specified/verified at fixture/docs level. Unverified:
implementation, real bundle/locator/browser/network evidence (25/35/36/37;
A11/A18/A21/A22), Rapier approval.

## Limitations

No code exists; checks are shape re-derivations. Fence repairs in
packet-15–18 diff files were delimiter-only. U-4 needs Gate E confirmation.

## Next step

**Gate E review — not started.** Reviewer records accept/reject per
`contract-diffs.md` row + U-4/pins/trust; docs-only promotion then applies
accepted diffs before packet 20. Do not auto-start.
