# Packet 15 — Content storage, asset identities and migration contract

2026-09-18 (UTC) · packet 15 of 14–37, **Gate E**
(prereq: packet 14 report, met). Contract drafting only: no code, no
commit. Owner line in every draft: *owner pre-approval (autonomous M2 build
instruction, 2026-09-18); final manual review pending.*

## Outcome

**PROPOSED content persistence contract delivered; nothing accepted, no
implementation.** Storage v2 adds a bounded `content` block to the **same**
atomic envelope (`scene.revision` stays the sole revision; no second catalog
file). Authoritative bytes are immutable content-addressed blobs under
`sources/sha256/<digest>`; derived data lives under
`.thirdlight/derived/<sourceDigest>/<recipeDigest>/`. The harness staging area is
a **supported** edit path (BR-4: `.thirdlight/**` is outside the
external-change pause, with invariants + crash/retry fixtures). The authoritative
content command is stage-free, so dedup precedes every stage/blob lookup. No GC:
undo/history/snapshots/play can never be invalidated. Also: a version
matrix, backup classification, tamper handling, and an original-preserving
M1→M2 copy (marker + resume, reset-to-zero revision).

## Changed files (scoped diff — no commit)

New: `docs/planning/m2-contracts/{content-storage.md,assets.md}` and
`diffs/{project-model.md,workspace.md}`; `fixtures/m2/contracts/**` (42 indexed
fixtures + checker); `docs/acceptance/evidence-m2/15/**`; `docs/handoffs/15.md`.
Modified: `docs/STATUS.md` (packet-15 row + M2 header note only).
Unchanged: `docs/contracts/**`, decisions, packages, lockfile, node_modules.

## Commands run (actual)

- `node fixtures/m2/contracts/tools/check-fixtures.mjs` → `check OK: 13 check
  group(s) passed, 0 problem(s)`, exit 0.
- Negative control (one digest corrupted in a copy) → `check FAILED … 2
  problem(s)` (`digest-claims`, content-block cross-ref).
- `sha256sum` + independent Python recompute → all digests match.
- `npm test` → 66 files / **841 passed**; `typecheck` clean; `check-deps` pins
  exact; `check-boundaries` OK 10 / 150 / 514; `build` **4 built, 0 skipped**.
- `git status --porcelain docs/contracts package.json package-lock.json` → empty.

## Acceptance criteria

Byte-exact envelopes + migration examples, reimport/undo retention, no-GC,
explicit version compatibility, original-preserving migration, backup/tamper
rules: **PASS**. One-authoritative-commit and retry-replay-before-stage-lookup:
**PASS as contract text + declarative cases** (executable proof is Gate F's).
**Unverified:** GLB import profile (packet 24; effective extension allowlist
declared empty), all bounds (Gate E pins), v2 scene contents beyond the `model`
reference (packet 20).

## Limitations

Behavioral rows are specification + declarative fixtures, not executed tests;
`source-preimages/*.bin` are opaque placeholders, not GLBs; the quota cliff (no
blob deletion) is deliberate.

## Contract-change requests

None for the two diffs. Two interfaces are notes only: `dependencies.md` §3 rows
(packet 19) and the content mutation's op/args/inverse (packets 21/23). BR-4 is
resolved by the workspace diff.

## Next

**Packet 16 — Typed edits, prefab and property contracts.** Not started.
