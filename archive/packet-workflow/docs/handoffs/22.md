# Packet 22 — Pure prefab capture and instantiation

Owner pre-approval: **owner pre-approval (autonomous M2 build instruction,
2026-09-18); final manual review pending.** No commit made.

## Outcome

`createPrefab` captures a selected subtree into an immutable definition
(camera/nesting/external-reference/non-definition components rejected);
`instantiatePrefab` materializes one complete subtree in ONE transaction —
deterministic smallest-free IDs in definition document order, every local
entity/reference ID remapped exactly once, the exact mapping in the durable
result. One undo removes the whole subtree; redo restores exactly those IDs
without re-allocation. `queryPrefabs` is implemented. All ops run the accepted
§6.1 pipeline and the shared pure history engine.

## Scoped diff (no commit)

New: `packages/commands/src/prefab-ops.ts`, `validate-prefab-args.ts`,
`m2-prefab.test.ts`, `fixtures/m2/prefabs/{expected.json,independence.messages.json}`,
`docs/acceptance/evidence-m2/22/**`. Modified: `packages/commands/src/{types,errors,properties,validate-request,history,apply,queries,index}.ts`.
No `project-model` change; M1/packet-16/packet-21 fixtures and accepted
contracts byte-unchanged.

## Commands and actual results

`npm test` 69 files/938 passed · `typecheck`/`check-deps`/`build` exit 0 ·
`check-boundaries` 10 pkgs/164 files/587 specifiers exit 0 ·
`check-fixtures.mjs` 33/33 exit 0. Commands 214 tests (195 → 214).
Evidence: `docs/acceptance/evidence-m2/22/manifest.md`.

## Acceptance

Passed: two instances with distinct IDs and remapped refs; exact mapping in
results; one undo/redo identity; independent copies; atomic rejection (state
object byte-identical); limits/forbidden contents/overrides; packet-16 M1–M10
replays byte-exactly (M10 modulo `duplicated`) and 22 failure cases match.
Unverified: no visual/browser check (not in scope).

## Limitations

No workspace I/O, durable records or MCP surface (23). The `id_exhaustion`
branch is unreachable through a valid pipeline (1024-entity cap < 9999 ID
space); the allocator is unit-tested.

## Contract-change requests

- **C21-5 (deferred):** the accepted `project-model.md` §12.1 entry-point list
  does not name declaration/property helpers (only the superseded
  `properties.md` §12 proposal does), and the model/commands error surfaces
  differ (`ModelErrorV2` document paths vs `CommandError` op pointers/messages
  that packet-16 fixtures pin). Proposed additive diff: export
  `validateDeclaration(doc)` and
  `checkPropertyValue(prop, value, opts?) → {ok} | {ok:false, kind, type, expected}`
  from `project-model`, with commands mapping `kind` to its own codes/messages.
  No divergent duplicate was added: the value rules now live in one commands
  implementation shared by the field and override paths. No model edit made.
- **C22-1:** capture rejects `collider`/`controller` with
  `prefab_component_forbidden` (§20.2 defines the definition vocabulary; §5.4
  names only camera/prefab). Proposed: name collider/controller in the row.
- **C22-2:** C10's `id_exhaustion` is unreachable under the 1024-entity cap.
- **FD-22-1:** `prefab-failures.json` F21 addresses the Ramp `model-0006` as the
  Lantern; its payload contradicts the scenario's own M5 mapping. Re-derived
  with `model-0005`; committed fixture unmodified.

## Next

**Packet 23 (Workspace content publication and migration-copy workflow) — not
started.**
