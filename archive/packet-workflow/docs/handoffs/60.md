# Packet 60 — Standalone M3 export and production parity

**Outcome:** the M3 standalone export is proven a complete relative closure that
runs from static output with no editor services, and the **58/60 §5.4.1 + §17.5
re-measurement** is recorded against the real three + `game-host` install. The
packet is the measurement/parity/evidence packet over the already-built M3
export pipeline (packets 58/59) — **no package source changed**; the
deliverables are the binding re-measurement tests, the two-tree determinism +
preview/export parity tests, and the owner-run standalone procedure.

## Changed files (all NEW)

- `tests/integration/m3-export/bundle-scan.test.ts` (3 tests) — the §5.4.1
  re-measurement + production-parity graph.
- `tests/integration/m3-export/determinism.test.ts` (2 tests) — two-tree
  determinism + preview/export parity.
- `tests/browser/m3-export/README.md` — the owner-run standalone procedure
  (X1–X7).

## §5.4.1 re-measurement (recorded, real bundles)

**Reference full-core three** (pinned `three@0.186.0`, the §5.3 pinned esbuild
option set, entry `import * as THREE from 'three'; console.log(THREE.REVISION);`):
**d=3, f=3, h=26, j=3; a/b/c/e/g/i=0** — the recorded-exception table re-verifies
against the current install (binding 3). Measured 1,777,856 bytes (the table
records 1,777,857 — a 1-byte newline separator in the entry; the per-needle
counts are exact).

**The REAL M3 export bundle** (the production `buildM3Bundle` over a shared
closure with two declared assets): **a/b/c/e/g/i=0; d=8 = 3 (three core) + 1
(Rapier) + 1 (`./manifest.json`) + 1 (`./scene.json`) + 2 (one per unique
declared asset path); f=3, h=26, j=3** — i.e. exactly the recorded baseline +
the applicable exception rows + the counted engine call sites, with **0
contributed by `game-host`** to d/f/h/j and a/b/c/e/g/i. There is **no GLTFLoader
subpath in the M3 graph** (the export bootstrap imports only the root
`createSceneAdapter`, not the `./gltf-loader` port) and **no trust notice in the
M3 page**, so h and j stay at the three baseline. No absolute/remote `fetch`
literal.

**Verdict:** the packet-42 M3 addition holds **exactly** as recorded — `game-host`
adds 0 to a/b/c/e/g/i and 0 additional to d/f/h/j. The measured text matches the
recorded table + applicable rows, so **no bounded re-review is required and no
exception is widened** (the contract's deviation trigger did not fire).

## §17.5 fetch list (recorded)

The M3 export bundle's runtime fetch list is exactly: `./manifest.json`,
`./scene.json`, and **one read per unique declared asset path** — all relative
to the bundle. Zero absolute or remote fetches.

## Determinism + preview/export parity (Node-verified)

- **Two-tree determinism (B23):** two exports of the same captured state (a
  fixed wall clock) into two DIFFERENT output trees are **byte-identical** — the
  accepted timestamps (`capturedAt`/`exportedAt`) are the only time-dependent
  bytes, and the `buildId` is re-derived from the manifest content (stable for
  the same content). The existing `export_snapshot_mismatch` test (m3-builds)
  proves a failed re-export leaves the prior tree byte-untouched (hash-before-
  overwrite).
- **Preview/export parity (B22, Node half):** the M3 **EXPORT** manifest and the
  v3 **PLAY** manifest for the same capture are the **SAME build** — identical
  `buildId`/`sceneDigest`/`contentDigest`/`settingsDigest` — because both are
  derived by the SAME shared `buildContentClosureM3` from the SAME single
  captured read, and both wrap the SAME single `createGameHost`. The resolved
  six-key `settings` reach BOTH hosts (C35-5).
- **Production-parity graph:** the export bundle links the SAME shared
  `game-host` + `platformer-game` composition the preview uses; the §5.2 graph
  check passes (no forbidden module); **no** editor/backend/behavior source in
  the runtime bundle.

## Other binding surface (already pipeline-verified, re-cited)

- **Complete relative closure (B21):** `index.html` is a **static loader** (loads
  `./js/main.js` as a module + the HUD `div`s) — **no alternate sample logic**;
  `manifest.json`/`scene.json`/`meta.json` (versioned metadata + exact licenses/
  version hashes) + the `content/sha256/<digest>` assets are all relative and
  digest-addressed; declared==emitted at the closure level.
- **Closed failure modes:** missing/corrupt media and a rejected hostile source
  (CC-55-3 fail-closed `behaviors_unsupported`) preserve the previous output.
- **Negative authoring-token/capability/Node/URL scans:** run by the export
  pipeline over every emitted file, and re-asserted at the bundle level
  (a/b/c/e/g/i=0 + canary absent).

## Green bar (fresh run)

`npm test` **177 files / 2230 tests** exit 0 (+2 files, +5 tests over post-59);
typecheck 17 packages; check-deps all exact; check-boundaries 17 pkgs / 335
files / 1319 specifiers / 0 violations; build 5 built / 0 skipped. All fixture
checkers green: M2 contracts 34 groups; M3 delivery 16/153, media 14/119 +
control, gameplay 23/127, contracts 39, storage 9/10. M2 `preview-bootstrap.ts`
byte-stable (M2 bundle-scan test re-passes).

## Acceptance criteria

- **B21 (export closure):** PASS (Node) — complete relative closure + negative
  scans + failure preservation.
- **B22 (preview/export parity):** PASS (Node half) — same-build manifest
  identity + same shared composition. **UNVERIFIED: physical keyboard/gamepad**
  standalone play (owner-run).
- **B23 (two-tree reproducibility):** PASS (Node) — byte-identical trees, no new
  authoring value.
- **B19 (pin honored):** PASS (Node, from 59) — the export carries the frozen
  captured state; a fresh export adopts a new build, a pinned one serves the
  frozen build.
- **Standalone browser play (X1–X7):** **UNVERIFIED in-container** (no browser /
  GPU / audio device — packet-38 baseline §1).

## Limitations / carried

- GLB model rendering gap carries (the three-adapter realizes boxes/lights/
  surfaces/camera only) — packet 61's sample uses a box player.
- **CC-55-3 stays open** (no behavior-linking channel; fail-closed).
- Standalone owner-run evidence (network capture, keyboard/gamepad, audible)
  is the packet-61/62 evidence. No commit made.

## Next

**Gate O review (in-session, after 60)** — fresh full toolchain re-run + fixture
checkers + reviewer-side verification + `docs/handoffs/gate-o.md`; then
**packet 61** (Author Beacon Reach through supported workflows — box player).