# Packet 33 — Immutable behavior builds

Outcome: published trusted sources compile into a pinned browser artifact.
Owner pre-approval (autonomous M2 build instruction, 2026-09-18); final manual
review pending. No commit.

## Scoped diff (no commit)

New `@thirdlight/behavior-build` (deps `@thirdlight/project-model@0.1.0` + the
already-pinned `esbuild@0.28.2`): strict container parser, bounded textual
import/dynamic-code scanner, cycle/depth analysis, canonical digest-bound
`BehaviorManifest`, output scan, pure SHA-256/canonical-JSON helpers and
`compileBehavior` (async — C33-1) via an in-memory esbuild plugin
(`write: false`, `absWorkingDir: '/'`).

Wiring (smallest complete path): `commands`' source branch builds the
`BehaviorSourceRecord` only from prepared facts (new `PreparedBehaviorSourceFact`,
`behaviorPreparerRegistered`, `behavior_declaration_mismatch`); `workspace` gains
`prepareBehaviorSource` (stage/bytes → trust gate → injected compiler → immutable
blob → derived `prepared.json`), loads the prepared index at open and passes the
facts to the one `runCommand` pipeline; `backend` constructs/injects the compiler
and exposes the `publishBehaviorSource` facade on `/services`;
`tools/check-boundaries.mjs` gains the `behavior-build` row + injected edges.

Lockfile delta: the `@thirdlight/behavior-build` link/entry plus the two
`behavior-build` dependency declarations only (+13 lines; two unrelated stale
`asset-pipeline` normalizations from `npm install` were reverted).

Fixtures/tests/evidence: `fixtures/m2/behaviors/**` (2 valid + 31 hostile
containers, committed output artifact, `expected.json`, plain-Node checker);
`tests/m2-builds/**` (22 unit tests + cold harness);
`tests/integration/m2-builds/**` (6 publication + 2 cold-build);
`docs/acceptance/evidence-m2/33/**`. Two M2 expectations updated where the
placeholder became the real path (workspace source object; integration
`preparer_unavailable` → `preparation_missing`).

## Commands and actual results

`npm test` 123 files / 1594 passed (baseline 119/1562) exit 0 · `typecheck`
15 packages exit 0 · `check-deps` exit 0 · `check-boundaries` 261 files/959
specifiers, 0 violations exit 0 (four disposable negative probes each failed,
then removed) · `npm run build` 4 built/0 skipped exit 0 · packet-18 contracts
checker 34/34 · `node fixtures/m2/behaviors/tools/check.mjs` 5/5. Cold builds:
6/6 fresh processes, identical `outputDigest`/`manifestDigest`, 14–17 ms each,
0 failures; sentinel unexecuted. Deployed backend bundle 911 420 bytes
contains `thirdlight.behavior-compiler`, esbuild external.

## Acceptance

Passed: valid typed sample builds reproducibly (byte-identical 2-run); 31
hostile probes fail with the committed codes; source never executed; the old
publication stays byte-identical after every failure; cold builds measured
honestly. Unverified: browser execution of the artifact (packet 34), a larger
cold-build sample, no wire route yet.

## Contract-change requests (C33-1…C33-7)

async `compileBehavior` (esbuild plugins require `build`); `absWorkingDir`
determinism pin; `behavior_declaration_mismatch` missing from commands.md §5.4;
§5.5 scan letter table; trust-check order; `project-model` container exports
absent; declaration-bound limit names. Proposed diffs in the evidence manifest.

## Next

Packet 34 (Trusted behavior execution and publication UI) — not started.
