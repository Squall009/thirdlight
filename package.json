{
  "name": "thirdlight",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "Thirdlight — self-hosted browser game editor built on three.js. See docs/STATUS.md.",
  "engines": {
    "node": "22"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "node tools/check-deps.mjs && node tools/check-boundaries.mjs && node tools/typecheck.mjs && node tools/build.mjs",
    "typecheck": "node tools/typecheck.mjs",
    "test": "vitest run",
    "check-deps": "node tools/check-deps.mjs",
    "check-boundaries": "node tools/check-boundaries.mjs"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "typescript": "5.9.3",
    "vitest": "5.0.1"
  }
}
