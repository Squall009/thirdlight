"# fixtures/m3/delivery — packet 42 (PROPOSED)\n\nPROPOSED fixtures for `docs/planning/m3-contracts/delivery.md`. Not an\nimplementation: the checker re-derives every declared digest, canonical\nserialization, version rule, wire schema, control rule, closure/scan row and\ndependency row from the committed files. `manifest/*-preimage.json` are digest\npreimages, not full model-valid scenes (packet 39 owns full v3 validation).\n\nRun: `node fixtures/m3/delivery/tools/check-fixtures.mjs`\n"
